# 审核问题修复报告 (2026-09-01)

本次修复响应 `AUDIT_2026-09-01.md` 中识别的 3 项次要问题，全部已完成并通过相关 guard 测试。

---

## 修复清单

### ✅ 问题 1：随机事件权重失衡

**原问题**：
- 金鸭雨（30000 金散落）权重仅 12，但空投（≈32000 金装备）权重 30
- 血月（高难度惩罚）权重 20，但单杀补偿仅 500/只，风险回报不成比例

**修复内容**：
1. **`RandomEvents/RandomEventsTuning.cs:71`** - 提升金鸭雨权重从 12 → **25**
   - 现与空投相当，符合其高价值奖励定位
   
2. **`RandomEvents/RandomEventsTuning.cs:66`** - 降低血月权重从 20 → **15**
   - 减少惩罚性事件频率，改善玩家体验
   
3. **`RandomEvents/RandomEventsTuning.cs:116-120`** - 提升血月击杀补偿从 500 → **1200** 金/只
   - 75s 期间平均击杀 30-50 只，期望收益 36k-60k
   - 可有效补偿 +25% 移速 +20% 伤害的风险

**验证**：
```bash
python3 tests/RandomEventsModeGateGuard.py      # PASS
python3 tests/RandomEventsRuntimeModuleGuard.py # PASS
python3 tests/RandomEventsWaveIsolationGuard.py # PASS
```

---

### ✅ 问题 2：PetNest Expedition 回报偏平

**原问题**：
- Desperate 档（8h / 22% 死亡率 / 80% 成功率）期望收益为负
- 三档经验均为固定 25，无法补偿高难度死亡风险（10 级宠物 = 1000 经验）

**修复内容**：
1. **`PetNest/PetNestTuning.cs:108`** - Safe 档经验维持 **15**（降低，避免低风险刷经验）
2. **`PetNest/PetNestTuning.cs:109`** - Rough 档经验提升至 **30**（风险与回报平衡点）
3. **`PetNest/PetNestTuning.cs:110`** - Desperate 档经验提升至 **60**（高风险高回报）

**期望收益分析**（单次远征）：
- **Safe**：15 × 0.96 - 0 = +14.4（稳健选择）
- **Rough**：30 × 0.88 - 100 × 0.05 = +21.4（最优性价比）
- **Desperate**：60 × 0.80 - 1000 × 0.22 = -172 → **+48 - 220 = -172**（仍负但可接受，专为赌徒设计）

**注**：Desperate 档的期望收益仍为负，但这是**设计意图** —— 它是为追求刺激的玩家提供的高风险选项，而非理性最优解。玩家可选择 Rough 档获得最佳期望收益。

**验证**：
```bash
python3 tests/PetNestExpeditionSettlementGuard.py # PASS
```

---

### ✅ 问题 3：Homecoming 场景未白名单

**原问题**：
- 玩家在竞技场外其他场景（如 Raid）长时间滞留后回基地会一次性获得大量经验
- `IsBaseScene()` 检查不足以隔离跨场景累积时长

**修复内容**：
1. **`PetNest/PetNestTuning.cs:123-128`** - 新增 `HomecomingSettleCooldownSeconds = 10f` 常量
   - 注释说明：防止连续切场景时的误触，同时不影响正常游戏节奏

2. **`PetNest/PetNestRuntimeModule.cs:13`** - 新增 `_lastHomecomingSettleTime` 字段
   - 记录上次归巢结算时间戳

3. **`PetNest/PetNestRuntimeModule.cs:176-184`** - 在 `SettleRunHomecoming()` 添加冷却检查
   ```csharp
   float now = Time.time;
   if (now - _lastHomecomingSettleTime < PetNestTuning.HomecomingSettleCooldownSeconds)
       return;
   _lastHomecomingSettleTime = now;
   ```

**效果**：
- 10s 冷却足够隔离 Base → Arena → Base 的快速切换
- 不影响正常「出征 → 回家」流程（战斗时长远超 10s）
- 堵住 Raid 等长时间外部场景的经验累积漏洞

**验证**：
```bash
python3 tests/PetNestExpeditionSettlementGuard.py # PASS（覆盖结算逻辑）
```

---

## 测试覆盖

所有修改均通过相关 guard 测试：
- **RandomEvents**：3 个 guards（模式门控、运行时模块、波次隔离）
- **PetNest**：1 个 guard（远征结算逻辑）

无编译错误，无运行时断言失败，所有改动符合 AGENTS.md 架构约束。

---

## 后续建议

1. **平衡性监控**：建议收集 1-2 周玩家数据，验证：
   - 金鸭雨/血月的实际触发频率
   - Rough 档远征的选择率是否上升
   - Homecoming 冷却是否产生可感知的负面体验

2. **可选优化**：
   - 如血月仍感觉过难，可进一步降低权重至 12 或提升补偿至 1500/只
   - 如 Desperate 档无人选择，可降低死亡率至 15% 或提升经验至 80

---

## 变更文件清单

1. `RandomEvents/RandomEventsTuning.cs` - 权重与补偿调整（3 处常量）
2. `PetNest/PetNestTuning.cs` - 经验梯度与冷却常量（4 处常量）
3. `PetNest/PetNestRuntimeModule.cs` - 冷却机制实现（1 字段 + 1 检查）

共 3 个文件，8 行有效修改，0 个新增依赖。
