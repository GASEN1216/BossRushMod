# AUDIT 2026-09-03（进行中）— f9b83c0..HEAD 全面审核

> **状态：进行中。** 12 个子系统并行审查。第一节是**主会话逐条复核过的台账**，可据以决策；
> 第三节起是各子代理的原始报告，未去重、未编号，作为证据附件。
> 行数少于 60 的子报告说明该范围仍在重跑中。

生成时间：2026-09-04 11:32:25

## 审查范围

- 基线：`f9b83c0fa21a3ef03e8abc0941fb71beb3201ba2`（2026-08-27）
- HEAD：`c56a98f`，105 个提交，含未提交工作树改动
- 规模：546 文件 / 约 113000 行新增；`.cs` 改动 365 个
- 门禁实测（收尾复测）：编译 Build succeeded（零 error 零 warning）；守卫 519 脚本 PASS=519 / NEW-FAIL=0 / KNOWN-RED=0。审核初期曾测得 4 红，均在审核期间由并发会话修复，详见第〇节

## 〇、守卫基线的时间线更正（重要）

我在 22:20–22:45 首次测得 **517 脚本 / PASS=513 / NEW-FAIL=4**，并据此指出「未提交台账 7 处声称
516 全绿，与实测不符」。**该结论在测量时点成立，但现已过期**：本次审核进行期间，并发会话
把四个红项全部修掉了。我于收尾时复测：

```
guard runner: 共 519 个脚本，并发 8
汇总: PASS=519  NEW-FAIL=0  KNOWN-RED=0
```

逐个复验四项均已 PASS，且 `tests/empty_catch_budget.txt` **仍是 968、无任何改动**——
说明超出的 8 个空 catch 是被真删掉的，不是靠上调预算掩盖；两个 ModeH 守卫已按
AGENTS §4.10 同步（工作树 M 状态）。**故「台账与实测不符」这条不再作为 finding 提交**，
仅作为过程记录保留：它反映的是并发多会话下「台账先于修复写入」的时序风险，不是代码缺陷。

下表所有 finding 与此无关，均为独立的代码问题，且在复测全绿的当前树上**依然成立**。

## 〇之二、本会话已落地的修复（2026-09-04）

验证口径：Windows `compile_official.bat` → **Build succeeded，exit 0，零 error 零 warning**；
`python tools/run_guards.py` → **524 脚本 / PASS=523 / NEW-FAIL=1**，唯一红项
`ModBehaviourInstanceClassificationGuard`（Integration 计数 260 vs 基线 259）由**并行会话**
新增的 `ModBehaviour.Instance` 引起，与本批改动无关（本批 diff 新增该符号 0 处）。
**全部为静态验证，实机 smoke 待人工。**

| 项 | 改动文件 | 要点 |
| --- | --- | --- |
| **P0-A** 押品满仓永久丢失 | `ModeHWarehouseStakeJournal.cs`、新增 `ModeHWarehouseStakeJournalStorageBuffer.cs` | 三处内存滞留点（`ReturnEscrowItems` / `GrantPlannedRewards` / `RollbackDetached`）全部接官方 `PlayerStorage.Push(item, toBufferDirectly:true)`；`LoadPersisted` / `ResetStaticCaches` 清空前先 `DrainEscrowToStorageBuffer`。带归属后验（照 `ModeGRewardTransaction`）、`PlayerStorageBuffer.Instance` 门控、手动 `SaveBuffer()` |
| **P0-B** 阶段机三个死态 | `ModeHWarehouseStakeJournal.cs`、`ModeHRealStakeService.cs`、`ModeHRuntimeModule_CombatFlow.cs`、`ModeHLocalization.cs` | 新增 `TryCompleteFrozenSettlement`：按已冻结 kind 续做，`TryAbortReturn` 与 `TrySettleMatch` 共用；`CommitResult`/`CommitAbortReturn` 字段随 `TryAdvancePhase` 一起回滚；结算失败新增玩家可见文案 `Settle_Failed` |
| **P0-D** 乱入 Boss 顶掉本波身份 | `EnemySpawnCore.cs`、`DragonKingBoss.cs`、`PhantomWitchBoss.cs`、`DragonDescendantBoss.cs`、`RandomEventEffectsBridge_Spawn.cs` | 三个生成器补 `isNonWaveSpawn` 门控（照龙裔既有 `isChildProtectionSummon`），经 `EnemySpawnCoreOptions.SuppressWaveBossRegistration` 从随机事件桥透传 |
| **P1-J** 137 处提示静默 | `BossRushEagerReflectionCache.cs`、`UIAndSigns.cs` | 删掉恒 null 的 `ShowNext` 反射，改直调公有静态 `NotificationText.Push`（照同文件 `ShowBigBanner`） |
| **P1-L** 随机事件商人 key 零注入 | 新增 `Localization/RandomEventsLocalization.cs`、`BossRushIntegration_StartAndScene.cs` | 注入 `BossRush_RandomEvent_MerchantShop`，挂进 `InjectLocalization_Extra_Integration()` |
| **P1-H** 大兴兴血脉随从被反复销毁 | `ModBehaviour.cs` | `TryCleanNonBossRushDaXingXing` 补 `PetNestCompanionAgent.IsCompanionCharacter` 豁免，与另三条 Destroy 扫描对齐 |
| **P1-K** 空投宽限窗口不生效 | `RandomEventAirdropHold.cs` | 判据从 `InteractableBase.Interacting` 改为官方 `InteractableLootbox.OnStartLoot`/`OnStopLoot` 闩（幂等订阅 + 成对退订），`Interacting` 保留为次要信号 |

**守卫同步**（均为忠实同步，非放宽）：
`ModeHStakeJournalGuard` 与 `ModeHIsolationGuard` 的豁免名单收录 journal 的 partial 第二文件；
`compile_official.bat` 登记 2 个新增 `.cs`。
**没有上调 `empty_catch_budget.txt`**：新增的 4 个空 catch 全部补成有日志的（当前 967 ≤ 968）。


### 第二批（Phase 0 补审查出的项）

| 项 | 改动文件 | 要点 |
| --- | --- | --- |
| **P2** 落盘重试链被自身消费 | `CampaignSaveCoordinator.cs`、`CodexSaveCoordinator.cs` | `FlushPending()` 成功即消费 pending，`HasPendingWrite` 随之变 false；旧早返只看它，导致 `SaveFile` 失败后的重试与宿主销毁兜底**一起失效**，数据停在 SavesSystem 内存里永不落盘。新增独立的 `_saveFilePending`（欠一次 SaveFile），早返同时看它，只有 SaveFile 真正成功才清 |
| **P2** ModeF 补位重生泄漏 preset | `ModeFRespawn.cs` | 克隆 `characterPreset` 后补挂 `ModeECharacterPresetLease`，与 `ModeEBattle` 的准备期路径对齐。commit `1583da1` 为修 CR-2026-09-01-010 #2 删掉了本文件的 `Destroy(characterPreset)`，这条路径于是从「提前销毁」变成「永不销毁」 |


### 第三批（Phase 3 次要项 + Phase 4 收尾）

| 项 | 改动 |
| --- | --- |
| **P2** 九个新 TypeID 未进掉落黑名单 | `LootBlacklist.json` + `LootBlacklistRegistry` 硬编码 fallback 各补 9 条；同步 `LootBlacklistDataRegistryGuard` 的常量表 |
| **P2** `_storeFaulted` 切槽不复位 | 一次读回失败不再堵死本进程所有槽 |
| **P2** `RestoreForSlotChange` 早退 | 换槽丢弃旧 run；**刻意不走 `ShutdownRuntime`**——它的中止路径会把押品退还到已切换的新槽 |
| **P2** 图鉴面板销毁锁死输入 | `ResetStaticCaches` 改为先走 `Close()` 归还 `InputManager` 占用 |
| **P2** `MagicBlend` 短路表无界增长 | 并联进 `OnSceneUnloadAlwaysOnRuntime`，与方法自身注释一致 |
| **P2** SpawnCore 销毁失败角色不解绑掉落追踪 | 销毁前先 `ClearBossRandomLootTracking` |
| **P2** 展示柜加成开局等于零 | 抬上限后仅在「原本满血」时补到新满血，避免变成免费回血 |
| **P2** 随机事件商人/巡游鸭未解距离休眠 | 补 `SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep` |
| **P3** `PetNestUIPages.LastFailureText` 进程级残留 | 面板关闭时清 |
| **P3** 两个 `.bat` 的 fallback 恒返回失败 | `exit /b %ERRORLEVEL%` 在**解析期**展开；改 goto 分派 |
| **P3** `.gitignore` 漏放行 `docs/contracts.md` | `TypeIdLedgerGuard` 硬依赖它 |

**Phase 4**：
- CR 编号 `CR-2026-09-04-010 ~ -020` 已写入 `CODE_REVIEW_FINDINGS.md`（写入前复查最大值，
  与并行会话已占用的 001-009 无冲突），状态表已更新；`FIX_TRACKER.md` 补一条完整流水账。
- `AGENTS.md` §14 更正许愿台口径（见下）。
- repowiki 同步 5 篇（随机事件 / 鸭王征程 / 鸭皇图鉴 / 竞技场后山 / Mode H 内容文档）。

### 关于「AGENTS.md 两处文档错误」的更正

我先前报告说有两处。核实后只有**一处**成立：

- **成立**：§14 写「熔石带 Special tag，**不会**进星愿许愿台奖池（该池按 tag 排除 Special）」。
  实际底池确实排除 Special（`WishFountainRewardPoolBuild.cs:869`），但 `gift` 与 `healing`
  两个类别把 `Special` 列进了 **requireTags**（`:616`、`:617`）；日报签到池更宽，
  `requireTags = null`、**只**过掉落黑名单（`DailyReportRewards.cs:222`）。已按此更正。
- **不成立**：「英文缺 key 会显示 `*BossRush_*`」——这句**不在** AGENTS.md 里，
  是我在派单简报里自己写错的。文档第 90 行讲的是「完全不注入才显示星号」，那是对的。
  真实的未文档化行为是：Mod 从不订阅 `LocalizationManager.OnSetLanguage`，
  文案在启动时冻结语言，游戏内切语言不刷新。该项未修，记为交接。

### 最终门禁（2026-09-04 收尾）

- 编译：`Build succeeded`，exit 0，**零 error 零 warning**。
- 守卫：530 脚本 / **PASS=529 / NEW-FAIL=1**。唯一红项是并行会话的**未跟踪**新文件
  `DebugAndTools/PermanentDuckNpcDebug.cs`（`有 ResetStaticCaches 但未在 OnDestroy 路径上被调用`），
  与本批无关。

### 未修 / 交接项

- Mode H 伤病 `armor` 完全空操作（`IsArmorKitDisabled` 零读者）：正确修法要在 kit 装配时
  查该选手伤病，而装配发生在伤病绑定**之前**；该子系统正被并行会话重构（`ModeHEffectConditions`）。
- Mode H `finish` 口令恒不可选（认证探针 `ReadField` 不含点火型控制点）：同属该子系统。
- 全 Mod 文案不随游戏内切语言刷新（不订阅 `OnSetLanguage`）。
- 其余 P3（注释与实现口径不符、只写不读的诊断出口、F3 验收用例的若干项）见各子报告。

### 新增守卫（5 个，全部做过反向验证）

`ModeHStakeEscrowDurabilityGuard`、`NonWaveBossSpawnGuard`、`SaveCoordinatorRetryGuard`、
`PlayerFacingMessageGuard`、`CompanionCleanupExemptionGuard`。

**反向验证结果：9 条人为破坏 9 条被抓到，还原后 5 个守卫全绿。**
过程中反向验证自己抓出**我写的两条断言太弱**——用子串匹配时把标识符改名成 `xxx_REMOVED`
仍能命中，已改为正则词边界并要求字段真的声明。这正是「不做反向验证就不知道守卫是不是假绿」。

### 最终门禁状态（2026-09-04 10:19 我最后一次绿编译）

- 编译：`Build succeeded`，exit 0，**我改动的文件零 error 零 warning**。
- 守卫：528 PASS。

**此后工作树被并行会话改红**：它新增了 `Integration/NPCs/DuckNpc/Permanent/` 下两个文件
但尚未登记进 `compile_official.bat`，导致
`DuckNpcBlueprint.cs(124,16): error CS0246: 未能找到类型 PermanentDuckNpcData` 编译失败，
并连带 `OfficialCompileListFileExistenceGuard` 与 `StaticCacheLifecycleGuard` 两个红项。
**三项全部指向该会话的未完成文件，与本批改动无关**；我在 10:19 之后没有再改过任何 C# 内容。

### 与计划的两处偏差（都往好的方向）

1. **P0-B 最终没有动 §22.2 冻结转换表**。核对后确认
   `ResultCommitted → SettlementPending → Terminal` 与
   `AbortReturnCommitted → SettlementPending → RefundedTerminal` **本来就是合法路径**，
   缺的只是 `TryAbortReturn` 按阶段分派。因此不构成 `SCHEMA-`，
   `docs/contracts.md` 的冻结契约无需改动——owner 批准的那项授权未被使用。
2. **`ModeHWarehouseStakeJournal.cs` 触及 1200 行预算**，按 `ConfigModConfigKeys.cs` 的既有先例
   原样提取为同一 partial 类的第二个文件，行为逐字不变。

### 本会话未做（并行会话已接手，owner 2026-09-04 决定避让）

- **P0-C** 赔率页按钮出屏：并行会话已把 `CreateActions` 改为换行（`MaxSingleRowActions=7`），
  锁盘按钮已回到屏内，P0 的「困在时停页」已解除。**残留 P2**：押品格按钮仍进 `Actions`
  而非空着的 `ModeH_RealStakeSelector`，7 列宽 1824px 超出 1384 面板宽，按钮会压在面板外。
- **P1-D** 遗种蛋/熔石掉落 defer：并行会话已实现（含把
  `ShouldDeferBlueBossExtraDropToBossRushLootbox` 重命名为 `ShouldDeferExtraBossDropToModPath`）。
- **ModeH 口令适配层**（`ModeHCommandAdapters.cs`）：并行会话正在落地 CR-2026-09-03-020。

## 一、主会话已逐条复核的 finding 台账

下表只列**我已亲自读代码验证过证据**的条目。验证方式写在最后一列；「已核实」= 我独立读了
被引用的代码行与官方源码，确认失败链条成立，不是转述子代理结论。

尚未编号为 `CR-2026-09-03-0xx`——按 `CODE_REVIEW.md` §4，编号应在 owner 确认后统一分配。

| # | 严重级 | 一句话 | 关键位置 | 我的复核结论 |
| --- | --- | --- | --- | --- |
| A | **P0** | 仓库满时真实押品/奖励只留在内存 `List<Item>`，重启或切槽即永久丢失 | `ModeHWarehouseStakeJournal.cs:789`、`:1090`、`:30` | **已核实成立**。`ReturnEscrowItems` 无空位即 `return false` 且物品留在 `_escrowItems`（纯内存静态 List）；`LoadPersisted` 无条件 `_escrowItems.Clear()`。官方本有正解 `PlayerStorage.Push(item, toBufferDirectly:true)`（官方源码 `PlayerStorage.cs:162`，溢出进持久 `IncomingItemBuffer`，`:43`/`:207`），Mode H 全程未用——`grep PlayerStorage ModeH/` 只用到 `.Inventory` |
| B | **P0** | 押品阶段机 `ResultCommitted`/`AbortReturnCommitted`/`SettlementPending` 三态无返还出边，中止返还必然失败 | `ModeHWarehouseStakeJournal.cs:162-172`、`ModeHRealStakeService.cs:297-327` | **已核实成立**。冻结表里这三态的出边只有 `SettlementPending`/`Terminal`/`RefundedTerminal`；而 `TryAbortReturn` 对非 `Prepared`/`EscrowSnapshotDurable` 的一切阶段都走 `CommitAbortReturn`→`TryAdvancePhase(AbortReturnCommitted)`，必被判非法。恢复壳的「取回押品」按钮走同一函数，同样失败 |
| B2 | **P1** | 押品结算失败只写一行日志，比赛照常推进，玩家无感 | `ModeHRuntimeModule_CombatFlow.cs:809-817` | **已核实成立**。`TrySettleMatch` 返回 false 时只 `CriticalLog`。其注释写「交给恢复壳只读展示处置」，但按 B 恢复壳救不回来，属注释承诺与实现不符 |
| C | **P0** | 赔率页动作按钮单行平铺不换行，押品格 ≥5 时「锁盘」被推出屏幕，玩家困在时停模态页 | `ModeHUIPages.cs:398-424`、`ModeHRuntimeModule_MatchFlow.cs:938-958` | **已核实成立**。`CreateActions` 单行、步距 264px、无换行无滚动；按钮序为 下注档(3)+押品格(N)+锁盘(1)，锁盘排最后。N=5 时锁盘中心 x=1056 > 屏幕半宽 960。赔率页无关闭按钮，且模态层 `ClaimModalInput` 会把 `Time.timeScale` 置 0 并禁用输入 |
| D | **P1** | 遗种蛋直掉在标准竞技场/无间炼狱 100% 作废 | `PetNestDropService.cs:239-287`、`LootAndRewardsRandomBossLoot.cs:312`、`:520` | **已核实成立**。官方 `CharacterMainControl.cs:1297-1302` 先派发 `BeforeCharacterSpawnLootOnDead` 再 `if (dropBoxOnDead) CreateFromItem(characterItem)`；而主 handler 在 `:520` 把 `dropBoxOnDead=false` 并另建新箱。`LootAndRewards.cs:339` 的订阅序使 PetNest 先塞蛋、主 handler 后关箱。仓库已有规避机制 `ShouldDeferBlueBossExtraDropToBossRushLootbox`，`grep` 确认只有寒霜长矛与女巫镰刀接了，**遗种蛋与词缀熔石都没接** |
| E | **P1** | 赔率页 15 条分量标签本地化 key 双前缀，全部显示 `*BossRush_ModeH_BossRush_ModeH_Odds_xxx*` | `ModeHRuntimeModule_MatchFlow.cs:933` | **已核实成立**（我在基线阶段独立发现，子代理独立复现）。`ModeHOddsController.cs:600` 已写入完整 key，933 行又拼一次 `LocalizationKeyPrefix`。同文件 `:955` 写法正确可作对照 |
| H | **P1** | 大兴兴血脉的随从入场即被自家清理扫描销毁，并循环重生-被杀 | `ModBehaviour.cs:1375-1550`（`TryCleanNonBossRushDaXingXing`） | **已核实成立**。仓库有四条会 `Destroy` 角色的扫描，另三条（`ModBehaviour.cs:1158`、`WavesArenaEnemyMaintenance.cs:53/208`）都调 `PetNestCompanionAgent.IsCompanionCharacter` 跳过随从，**只有这条没有**——整个函数体 grep 无任何 PetNest/Companion 引用，第 1548 行直接 `Destroy(c.gameObject)`。随从 clone 保留官方 preset 身份（`NeutralizeClonePreset` 只改 team/exp/掉落，不改 nameKey），`DisplayName` 仍含「大兴兴」，正好命中该函数的匹配条件。经 `WavesArenaRuntimeHooks.cs:95` 周期调用 |
| I | **P0** | 随机事件乱入 Boss 若抽中三个自定义 Boss，会顶掉本波 Boss 身份，单 Boss 模式永久卡波 | `RandomEventCatalog.cs:639`、`EnemySpawnCore.cs:783/801/818`、`DragonKingBoss.cs:265`、`PhantomWitchBoss.cs:207` | **已核实成立**。乱入从 `GetFilteredEnemyPresets()` 取池，`grep` 确认 `RandomEvents/` 全目录**无** `IsManagedBossPreset` 排除；`EnemySpawnCore` 又把三个自定义 preset 路由到专用生成器，而生成器里 `currentBoss = character;` 是**无条件**的。`IsCurrentWaveBossMember`（`WavesArena.cs:481`）正是信这两个容器。这是 CR-2026-09-03-009 同类问题经**生成路径**的第二次发生（上次修的是掉落漏斗） |
| J | **P1** | `NotificationText` 反射绑定签名错误，137 处玩家提示在正式版全部静默 | `BossRushEagerReflectionCache.cs:91`、`UIAndSigns.cs:295-299` | **已核实成立**。绑定写的是 `GetMethod("ShowNext", Public|Static)`，而官方 `ShowNext()` 是**私有实例**零参方法（官方源码 `NotificationText.cs:50`），公有静态的是 `Push(string)`（`:15`）。绑定恒为 null → `if` 恒不进 → 只剩 `statusMessage` 赋值与 `DevLog`（release 被剥离）。本轮新增的两处「不再静默失败」分支也一并哑掉 |
| K | **P1** | 空投箱 3 秒宽限窗口几乎永不生效，箱子仍带物品到点消失 | 见子报告《随机事件+波次+掉落》F-3 | 子代理证据链完整（官方 `InteractableLootbox.OnUpdate` 会在开箱后一帧 `StopInteract()`，`Interacting` 随即为 false），**我尚未独立复核**。若成立则 CR-2026-09-03-010 的修复未达成目的 |
| L | **P1** | 随机事件商人的交互名 key 从未注入，玩家看到带星号的原始 key | `RandomEventEffectsBridge_Spawn.cs:571`、`:841` | **已核实成立**。key 由 `RandomEventsTuning.LocalizationPrefix + "MerchantShop"` 拼成 = `BossRush_RandomEvent_MerchantShop`，写进 `_overrideInteractNameKey`。全仓 grep 该前缀**只有 2 处命中**（常量定义本身 + 一个 Merchant ID 常量），`Localization/` 下无 `RandomEventsLocalization.cs`、无任何 RandomEvent 注入。该目录其余文案都走内联 `L10n.T(中,英)`，所以只有这一个 key 漏出来 |
| M | **P2** | 500059-500067 九个新 TypeID 全部不在掉落黑名单，可能混进许愿台/日报奖池 | `Assets/Data/LootBlacklist.json`、`Config/LootBlacklistRegistry.cs` | **已核实缺失**（逐个 grep 九个 ID，JSON 与硬编码 fallback 均无命中）。子代理进一步指出这些物品带 `Special` tag、品质 4-5，而许愿台底池与日报签到池只按该黑名单过滤——**最坏情况是刷出无血脉的遗种蛋，一个永远孵不出东西的死物**。奖池侧的可达性我尚未独立复核。**附带更正**：AGENTS.md §14 写「熔石带 Special tag 不会进许愿台（该池按 tag 排除 Special）」，子代理查证该池实际是对礼物/治疗类**要求** Special，文档这句是错的 |
| N | **P2** | 约 1150 条 `BossRush_*` 文案在游戏内切换语言后不刷新 | `LocalizationInjector.cs` 等 | 子代理证据：Mod 从不订阅 `LocalizationManager.OnSetLanguage`，全部文案冻结在 `Start_Integration` 时的语言。**我尚未独立复核**。附带澄清一条我在派单简报里写错的前提：`SetOverrideText` 是单值、语言无关的，所以不存在「英文缺 key 就显示 `*BossRush_*`」这回事，中英不一致的真实表现是切语言不生效 |
| O | **P2** | F3「出击餐」用例的 finally 用了不清登记的 `ClearForRun()`，残留餐食污染后续三个验收阶段 | 见子报告《调试工具+构建+守卫》F-2 | 子代理证据完整，**我未独立复核**。后果是测试档残留一份焚心椒，进竞技场被 `ApplyForRun` 消费给玩家挂上加成，而提示又被验收自己静音 |
| P | **P2** | `RunIsolatedCase` 的 try/catch 只包 `inner.MoveNext()`，子协程异常会让运行器永久卡死 | 见子报告《调试工具+构建+守卫》F-3 | 子代理证据完整，**我未独立复核**。`yield return` 出去的子协程由 Unity 驱动、不经该 catch，一旦触发 `RunSuite` 的 finally 不执行 |
| Q | **P2** | `run_guards.bat` / `verify_syntax.bat` 的 fallback 分支 `exit /b %ERRORLEVEL%` 在解析期展开 | 见子报告《调试工具+构建+守卫》F-7 | 子代理**已用等价 .bat 实测复现**（返回 9009）。无 `py` 启动器的机器上守卫全绿也会报失败 |
| F | **P1** | 焚心椒「换弹更利索」用了不存在的 stat key，效果完全不存在 | 见子报告《词缀锻造+后山》F-1 | 子代理证据完整，**我尚未独立复核**（属 AGENTS §14 已记载的 stat key 坑，先验概率高） |
| G | **P2×3 / P3×3** | 图鉴/日报/后山/持久化等次要项 | 见各子报告 | 子代理证据完整，**我尚未逐条独立复核** |

### 对我自己先前一处判断的更正

我在基线阶段写过「§17.4 局中重建在状态机层**结构性不可达**」，依据是 `Recovering` 没有通向
`MatchFighting/MatchSpawning/RelayPending` 的**直接**出边。子代理指出这是把「直接后继」当成了
「可达集」，我复查了冻结表，**它是对的**：

```
ModeHStateMachine.cs:209  map[Recovering]           ∋ ErrorRecoveryPending
ModeHStateMachine.cs:163  map[ErrorRecoveryPending] ∋ RelayPending
ModeHStateMachine.cs:170  map[RelayPending]         ∋ MatchFighting
```

三跳合法可达，且这正是 `ModeHStateMachine.cs:38-40` 类注释冻结的语义（「屏障完成后回
ErrorRecoveryPending，再由原 token 命令继续」）。

**这不改变删除本身的结论**——被删的六个符号确实零调用点，删前删后运行时行为逐字相同，删除仍然正确。
改变的是：源码里那段删除理由（「必须改冻结表」「属 AGENTS §10 owner 签字范围」）**写错了**，
且已固化进 `ModeHBattleSnapshot.cs`，会误导后来的维护者。这条应作为独立的 P2 记入台账。

---

# 二、主会话基线核实报告（门禁 / 守卫 / 台账一致性）


## 0. 并发写入警告（影响后续所有测量的可复现性）

本仓库当前有 6 个并行会话在写。`find -mmin -360` 显示 22:04–22:28 之间仍在改动：

| 时间 | 文件 | 归属 |
| --- | --- | --- |
| 22:04–22:16 | `Integration/NPCs/DuckNpc/*.cs`（5 个，未跟踪） | 另一会话在建捏脸 NPC 模块 |
| 22:14 | `DebugAndTools/F3DebugCheatMenu{Actions,Ui}.cs` | 同上（接 F3 入口） |
| 22:15 | `compile_official.bat` | 同上（登记 5 个 DuckNpc + 1 个 ErrorSwap 用例） |
| 22:15 | `Utilities/AlwaysOnRuntimeHooks.cs` | 未确定 |
| 21:15–21:23 | `ModeH/*.cs`（7 个） | 另一会话的 ModeH 收尾批 |

**结论：下面的 guard 结果是这个时点的快照，不代表任一提交的状态。**

## 1. 编译（AGENTS §4.2 / §8.1）

`compile_official.bat` on Windows：**Build succeeded，exit 0，零 error 零 warning**。
DLL 与全部 Assets（SpawnPoints / Data / ModeH / Campaign / Audio / BGM / 5 个 bundle / 图标）已部署。

## 2. 全量守卫（AGENTS §4.10 / §8.2）

`python tools/run_guards.py --verbose`：**517 脚本 / PASS=513 / NEW-FAIL=4 / KNOWN-RED=0**，退出码 1。
`tests/known_red_guards.txt` 为空（合规）。

### 4 个红项的归属

**(A) 2 个红项属并发会话的在建模块，不属本次审查范围**

- `EmptyCatchGuard`：total=976 > budget=968。我逐文件复算：`Integration/NPCs/DuckNpc/`
  贡献 8 个（`DuckNpcDebugProbe.cs` 2 + `DuckNpcFaceCatalog.cs` 6），**扣掉后恰为 968 = 预算持平**。
  即超支 100% 来自 22:04 之后新写的未跟踪模块。
  附带事实：预算自 base 起从 919 上调到 968（+49），是本轮新子系统带来的，属既有决策。
- `GameplayValidationCoverageGuard`：唯一报错 `生产模块缺少功能测试映射: Integration/NPCs/DuckNpc`，同源。

**(B) 2 个红项是 21:17 那次未收尾的编辑，是真问题**

21:17 有人从 `ModeH/ModeHBattleSnapshot.cs` 删除了 §17.4 局中重建链
（`ModeHSnapshotRebuildPlan` 类、`Validate`、`IsPositionUsable`、`TryRestoreHealth`），
并在 `ModeHCombatControl.cs:799` 删除 `RestoreFromSnapshot`。
删除本身论证充分且我已独立复核成立：

- 全仓库零调用点（`grep` 确认，唯一命中是注释）。
- `ModeHStateMachine` 冻结转换表中 `Recovering` 无任何通向 `MatchFighting/MatchSpawning/RelayPending`
  的出边，故重建在状态机层结构性不可达。
- 采集侧保留（`currentBattleSnapshot` 是 Season 落盘字段并参与 §20.2 canonical digest，
  摘掉是 `SCHEMA-`）。

但收尾缺三件事：

1. **`tests/ModeHBattleSnapshotGuard.py` 未同步 → 11 条断言全红**
   （`tests/ModeHBattleSnapshotGuard.py:113-117` 仍要求 `public static ModeHSnapshotRebuildPlan Validate(`、
   `public static bool TryRestoreHealth(`、`healthFraction * health.MaxHealth`、
   `health.SetHealth(target);`、`ReadHealthFraction`；`:45` 起仍要求 6 个 fail-closed reason id）。
   直接违反 AGENTS §4.10「改动被 guard 断言的结构时必须同步 guard」。
2. **删除注释承诺的 `ModeHSnapshotRebuildAbsenceGuard` 并不存在**
   （`ModeH/ModeHBattleSnapshot.cs:256` 写「由 ModeHSnapshotRebuildAbsenceGuard 守卫」，
   `ls tests/` 无此文件）。即「防止将来只接回一半」的保护措施是空承诺。
3. **`tests/ModeHStandInGuard.py:242` 断言 `error_swap_rebuild_profile_mismatch` 存在 → 红**。
   该字符串**从未出现在任何提交**（`git log -S` 全库无命中）也不在当前树里。
   而 13:23 加这条断言的同一批未提交 FIX_TRACKER 条目
   （`### 2026-09-03 可达性接线批`，行 161）明确写着修改内容包含
   「`ModeHCombatControl.cs`（…快照重建身份门…）」——**台账记录了一处并不存在的修复**。
   21:17 的删除注释又反过来说 §17.6.5 第 8 条「在当前语义下不成立」。
   两者未对齐，且都没落进台账。

## 3. 台账（`FIX_TRACKER.md` / `CODE_REVIEW_FINDINGS.md`）一致性

未提交改动新增 4 条 finding（CR-2026-09-03-012/013/014/015）+ 7 条 FIX_TRACKER 条目。

- 7 处声明「516 guard 全绿 / NEW-FAIL=0」。**当前实测 513/4 红**。其中 2 红可归因并发模块，
  另 2 红（§2-B）正是这批条目自己触及的文件。按 CODE_REVIEW §8「未 smoke 不得写成已验证」的
  同一口径，这些「全绿」声明在当前树上已不成立，需要重跑后回填真实数字。
- 21:17 的 §17.4 删除是一次结构性改动（删了两个文件的公开方法、动了 §17.4/§17.6.5 两节的语义），
  `FIX_TRACKER.md` 无对应条目。按 AGENTS §9 该记一条（分类至少 `SAFE`，因为静态证明行为不变；
  但涉及冻结设计节的语义收敛，建议 `Needs owner confirmation`）。

## 4. 已核实为合规、无需再查的门禁项

- `compile_official.bat` 清单：`F3GameplayValidationModeHErrorSwap.cs`（22:15 新增）与 5 个 DuckNpc 文件已登记；
  `OfficialCompileListFileExistenceGuard` PASS（双向）。
- `_errorSwapAttempted` 每场一次闩：`ModeHCombatControl.cs:512` 置位、`:147` 开局复位、
  `RestoreErrorSwap`（`:621`）不清除 —— 与 StandIn guard 的两条新断言一致，PASS。
- ERROR 互换输入让渡（CR-2026-09-03-013 的另一半）：`ModeHSpectatorLease.cs:378/396` 新增
  `YieldInputForErrorSwap` / `ReclaimInputAfterErrorSwap`，有真实调用点
  （`ModeHRuntimeModule_CombatFlow.cs:570/571`，另 `:1070` 在释放路径兜底收回），
  幂等且 `_inputDisabled` 语义未被破坏（Release 仍走同一恢复分支）。**这一半确实修好了。**
- 两个未跟踪守卫独立跑通：`ModeHDataStampGuard` PASS（7 个数据文件盖章新鲜）、
  `OfficialNameReferenceGuard` PASS（13 个官方名字）。
- `.gitignore` 未提交改动只加了三行注释（说明为何不纳管 `docs/官方本地化表/`），无实质规则变化。

## 7. repowiki 同步（AGENTS §4.13）—— 合规

- `RepowikiReferenceGuard` PASS：扫描 286 篇文档、11515 处 `file://` 引用、去重后 540 个目标**全部存在**，豁免条目 0 条。
- 八个新子系统全部登记进 `.qoder/repowiki/knowledge/zh/_index.yaml`：
  `campaign`、`back_mountain`、`codex`、`daily_report`、`affix_forge`、`random_events`、`pet_nest`、`mode_h`。
- 另有 9 个 repowiki 文件处于未提交状态（本轮同步的一部分）。

结论：§4.13「repowiki 内容过时视为未完成变更」这条在本轮**已满足**，不构成 finding。

---

# 三、子代理原始报告（证据附件）

| 子系统报告 | 行数 | 状态 |
| --- | ---: | --- |
| AffixForge_BackMountain_Reforge | 188 | 已完成 |
| Campaign_Audio_Common | 687 | 已完成 |
| Codex_DailyReport | 186 | 已完成 |
| DebugTools_Build_Guards_Tooling | 778 | 已完成 |
| Integration_core_ModBehaviour_Config_L10n | 689 | 已完成 |
| ModeH_combat_spawn_cert | 1204 | 已完成 |
| ModeH_content_ui_guards | 249 | 已完成 |
| ModeH_flow_persistence | 300 | 已完成 |
| ModesDEFG_Zombie_misc | 374 | 已完成 |
| PetNest_domain_persistence | 94 | 已完成 |
| PetNest_runtime_ui | 637 | 已完成 |
| RandomEvents_Waves_Loot_Utilities_Patches | 889 | 已完成 |

## ==== ModeH_flow_persistence ====

# Mode H 流程 / 状态机 / 持久化 / 押品 / 经济 审查报告

> 状态：进行中（边查边落盘）
> 范围：ModeH 状态机、run state、持久化、真实押品与 journal、经济结算、入口链、flush 协调、种子流、ModConfig、编译清单登记

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-01：仓库满时真实押品/奖励只留在内存 `List<Item>`，重启或切槽即永久丢失（官方溢出缓冲区未接）

> 与 F-04 合流后果加重：卡住的 journal 会把**七个旧模式入口全部锁死**，见 F-04「影响放大」。

- **严重级**：P0 Blocker（后果是玩家真实资产不可逆丢失 + 整个 Mod 的模式入口在该存档槽永久锁死）
- **兼容分类**：`BREAKING`（玩家真实资产）
- **位置**：
  - `ModeH/ModeHWarehouseStakeJournal.cs:789-794`（`ReturnEscrowItems` 无空位即 `return_no_empty_slot` 返回 false）
  - `ModeH/ModeHWarehouseStakeJournal.cs:840-846`（`GrantPlannedRewards` 无空位即 `reward_no_empty_slot`）
  - `ModeH/ModeHInventoryPersistenceBridge.cs:390-409`（`FindConfirmedEmptyPosition` 满仓返回 -1）
  - `ModeH/ModeHWarehouseStakeJournal.cs:30`（`_escrowItems` 是纯内存 `List<Item>`）
  - `ModeH/ModeHWarehouseStakeJournal.cs:1087-1092`（`LoadPersisted` 无条件 `_escrowItems.Clear()`）
- **证据**：

```csharp
// ModeHWarehouseStakeJournal.cs:789-794  ReturnEscrowItems
int position = ModeHInventoryPersistenceBridge.FindConfirmedEmptyPosition();
if (position < 0)
{
    failureReasonId = "return_no_empty_slot";
    return false;                 // ← 物品仍留在 _escrowItems（纯内存）
}
```

```csharp
// ModeHWarehouseStakeJournal.cs:1087-1092  LoadPersisted
public static void LoadPersisted(ModeHStakeJournalDto persisted)
{
    _active = persisted;
    _escrowItems.Clear();         // ← 重启 / 切槽 / 删档时把内存托管物无声抹掉
    RecomputeSlotConsistency(persisted);
}
```

  `LoadPersisted` 的调用点：`ModeHProfilePersistence.cs:137`（`HandleSetFile`）与 `:167`（`HandleSaveDeleted`）。
  `ResetStaticCaches`（`ModeHWarehouseStakeJournal.cs:1101-1108`）同样清空。
  全仓库 grep 确认：`_escrowItems` 除 `ReturnEscrowItems` / `RemoveEscrowByDigest` 外没有任何
  持久化或再实例化路径；journal DTO 只存 `ModeHItemTreeSnapshotDto`（语义摘要），
  没有任何函数能从摘要反向造回物品（`ModeHRewardItemPool.TryInstantiate` 只接受 `typeId`）。
- **官方本来有正确出口，Mode H 没用**：`鸭科夫源码/TeamSoda.Duckov.Core/PlayerStorage.cs:160-207`
  的 `PlayerStorage.Push(Item, bool toBufferDirectly)` 在仓库满时把物品序列化进
  `PlayerStorage.IncomingItemBuffer`（`PlayerStorageBuffer.Buffer`，**持久**），
  弹 `PlayerStorage_Notification_ItemAddedToBuffer` 提示，玩家之后在 `StorageDock`
  （`Duckov/UI/StorageDock.cs:114`）里取回。官方自己的奖励发放
  （`Duckov/Quests/RewardItem.cs:99`、`ItemUtilities.cs:218`）走的就是这条。
  Mode H 只用 `Inventory.AddAt`，满仓即失败。
- **失败场景**：
  1. 玩家押 3 件真实装备锁盘（3 格被腾空）；
  2. 比赛期间仓库那 3 格被别的来源填上（官方任务奖励 / 邮件 / 其他 Mod 系统 / 玩家自己在幕间回基地整理），
     或胜利时 `lockedOdds` 较大需要额外 N 格而仓库剩余空位不足；
  3. 结算走 `TrySettleMatch` → `ReturnEscrowItems` 命中 `return_no_empty_slot` 返回 false，
     journal 停在 `SettlementPending`，3 件真实装备只存在于 `_escrowItems`；
  4. 玩家退出游戏 / 切存档槽 / 删档 → `LoadPersisted` 清空 `_escrowItems` → **物品永久消失**。
     journal 里只剩摘要证据，`IsSlotConsistent=false` 把押品永久禁用，但没有任何路径能还回物品。
- **玩家影响**：真实仓库装备无声蒸发，且此后该槽的真实押品被永久锁死（`slot_active_journal:SettlementPending`）。
- **建议修复**：`ReturnEscrowItems` 与 `GrantPlannedRewards` 在 `FindConfirmedEmptyPosition() < 0` 时
  改走官方 `PlayerStorage.Push(item, true)`（直接入溢出缓冲区）并记 receipt，而不是返回 false；
  或至少在 `LoadPersisted` / `ResetStaticCaches` 清空 `_escrowItems` 前，把仍在托管中的物品
  强制 Push 进缓冲区。两处都要保证「物品先有归宿再清内存表」。
- **验证需求**：编译 + `ModeHStakeJournalGuard`；实机把仓库填满后押品胜利结算。

### F-04：押品阶段机的 `ResultCommitted` / `AbortReturnCommitted` / `SettlementPending` 是死态，中止返还救不回来

- **严重级**：P0 Blocker
- **兼容分类**：`BREAKING`（玩家真实资产）
- **位置**：
  - `ModeH/ModeHWarehouseStakeJournal.cs:139-172`（`IsLegalTransition` 冻结表）
  - `ModeH/ModeHRealStakeService.cs:297-327`（`TryAbortReturn` 只处理 Prepared / EscrowSnapshotDurable / EscrowRemovedDurable / MatchLocked）
  - `ModeH/ModeHRuntimeModule_CombatFlow.cs:809-817`（`TrySettleMatch` 失败只打 CriticalLog，继续走 Intermission）
  - `ModeH/ModeHRuntimeModule_SceneFlow.cs:674`、`ModeH/ModeHRuntimeModule.cs:298`、`ModeH/ModeHRuntimeModule_UiFlow.cs:330`（三条返还入口全部走 `TryAbortReturn`）
- **证据 —— 冻结表里这三个阶段没有通向"返还"的边**：

```csharp
// ModeHWarehouseStakeJournal.cs:162-168
case ModeHStakePhase.ResultCommitted:
    return to == ModeHStakePhase.SettlementPending;          // ← 没有 AbortReturnCommitted
case ModeHStakePhase.AbortReturnCommitted:
    return to == ModeHStakePhase.SettlementPending;
case ModeHStakePhase.SettlementPending:
    return to == ModeHStakePhase.Terminal || to == ModeHStakePhase.RefundedTerminal;
```

  而 `TryAbortReturn`（`ModeHRealStakeService.cs:308-326`）对这三个阶段一律走
  `CommitAbortReturn` → `TryAdvancePhase(AbortReturnCommitted, ...)`，必然被
  `journal_illegal_transition` 拒绝。**全仓库没有任何「续做未完成结算」的分支**
  （grep `EnterSettlementPending` / `Settle(` 的调用点，只有 `TrySettleMatch` 与
  `TryAbortReturn` 内部各一处，都是一次性直线执行，无重入口）。
- **失败场景 A（`EnterSettlementPending` 失败 → 卡在 `ResultCommitted`）**：
  1. 押品比赛结束，`TrySettleMatch` 依次 `CommitResult`（成功）→ `EnterSettlementPending`；
  2. 该步内含落盘，恰逢 `SavesSystem.IsSaving` → `FlushBatch` 返回 `flush_deferred_is_saving`
     → `TryAdvancePhase` 回滚阶段并返回 false；
  3. journal 停在 `ResultCommitted`，押品实物仍在内存 `_escrowItems`；
  4. `TrySettleMatch` 返回 false，但 `CombatFlow.cs:813-817` **只打一行 CriticalLog**，
     比赛照常进 Intermission，玩家毫无察觉；
  5. 离场时 `ShutdownRuntimeInternal:674` 的 `TryReturnRealStakeOnAbort` →
     `TryAbortReturn` → `ResultCommitted → AbortReturnCommitted` 非法 → 返还失败；
  6. 恢复壳的「取回押品」按钮（`_UiFlow.cs:315-333`）同样调 `TryAbortReturn`，**同样失败**；
  7. 进程退出 → `_escrowItems` 蒸发 → 真实装备永久消失。
- **失败场景 B（胜利返还遇满仓 → 卡在 `SettlementPending`）**：见 F-01 第 3-4 步，终点相同。
- **影响放大 —— 七个旧模式入口全部锁死**：卡住的非终态 journal 让
  `RecomputeSlotConsistency`（`ModeHWarehouseStakeJournal.cs:114-117`）
  置 `SetExternalAssetRiskBlocked(true, "slot_active_journal:" + phase)`，
  而 `ModeHRuntimeGates.IsLegacyModeEntryAllowed()`（`:385-388`）被下列入口消费：

```
ModeD/ModeD.cs:207                      ModeE/ModeEStartup.cs:836
ModeF/ModeFEntry.cs:76                  ModeG/ModeGEntry.cs:261
WavesArena/WavesArenaEntryAndTeleport.cs:37
ZombieMode/ZombieModeMapSelection.cs:13 与 :63
```

  即：一次结算落盘撞上自动存档，玩家在该存档槽**再也进不去 D / E / F / G / 无间炼狱 /
  丧尸模式中的任何一个**，且没有任何自愈路径（journal 不会自己终结）。
- **附带缺陷（同一处，独立成因）**：`CommitResult`（`:563-566`）与 `CommitAbortReturn`（`:595-600`）
  在调 `TryAdvancePhase` **之前**就写了 `_active.resultToken` / `_active.abortReturnToken`
  与父 operation，而 `TryAdvancePhase` 失败时**不回滚这些字段**（它只回滚
  `phase`/`phaseSequence`/`settlementKind`）。`CommitResult` 开头的
  `if (!string.IsNullOrEmpty(_active.resultToken)) return "commit_result_already_committed";`
  因此会让任何重试永久失败。
- **建议修复**：
  1. `TryAbortReturn` 增加「续做」分支：`ResultCommitted` / `AbortReturnCommitted` →
     先 `EnterSettlementPending` 再返还再 `Settle`；`SettlementPending` → 直接返还 + `Settle`
     （按已冻结的 `settlementKind` 走 Terminal 或 RefundedTerminal，不切换 kind）；
  2. `TrySettleMatch` 失败时给玩家可见反馈并把 lifecycle 送进恢复壳（`RequestSuspended`
     或至少 `OpenRecoveryShell`），不能只写日志；
  3. `CommitResult` / `CommitAbortReturn` 的字段写入与 `TryAdvancePhase` 一起回滚。
- **验证需求**：编译 + `ModeHStakeJournalGuard`（建议补一条断言：`TryAbortReturn`
  必须覆盖 `IsLegalTransition` 中所有非终态 phase）；实机需 Dev 强制
  `RequestStakeJournalWrite` 在 `EnterSettlementPending` 那一步返回 false。

### F-02：`ModeHProfilePersistence._storeFaulted` 切槽不复位，一次读回失败后整进程再也无法保存赛季

- **严重级**：P2 Minor
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHProfilePersistence.cs:117-146`（`HandleSetFile`）、`:148-175`（`HandleSaveDeleted`）、
  `:30`（`_storeFaulted` 声明）、`:276-280`（`StageWrite` 的拒绝分支）
- **证据**：`HandleSetFile` 与 `HandleSaveDeleted` 都清了 `_cache/_pending/_pendingDigest/_writeBarrier/_lastError`，
  **唯独没有清 `_storeFaulted`**：

```csharp
// ModeHProfilePersistence.cs:121-129
lock (_lock)
{
    _slotGeneration++;
    _cache = null;
    _pending = null;
    _pendingDigest = null;
    _writeBarrier = false;     // ← 清了
    _lastError = null;
}                              // ← _storeFaulted 没清
```

  `_storeFaulted` 的唯一复位点是 `ResetStaticCaches()`（`:383-394`），只在 Mod 卸载 / 宿主销毁时跑
  （`ModeHRuntimeModule.cs:365` 经 `ModeHSaveFlushCoordinator.ResetStaticCaches`）。
  同文件的兄弟类 `ModeHStakeJournalPersistence`/`ModeHHallOfFamePersistence` 需对照（见 §三）。
- **失败场景**：槽 A 发生一次 `season_readback_mismatch` 或 `season_flush_exception`
  （`:343`/`:365` 置位）→ 玩家回主菜单换到槽 B → `StageWrite` 恒返回 `season_store_faulted`
  → Mode H 在**本进程所有存档槽**里都再也写不进赛季。
- **与 `_writeBarrier` 的不对称**：`SetWriteBarrier`（`:244-253`）会同时 `SetRecoveryOnlyBlocked(true)`
  给玩家一个恢复壳出口；`_storeFaulted` 置位处（`:343`/`:350`/`:365`）**不设任何 gate**，
  玩家只看到"赛季推进按钮没反应"，符合本仓库第 14 节的「静默失败」形态。
- **建议修复**：若语义确为「store 级、跨槽」，则在置位处一并 `SetRecoveryOnlyBlocked(true, _lastError)`
  让玩家可见；若语义为「本槽」，则在两个 Handler 里一并复位。二选一需 owner 拍板，但当前
  「既不复位也不提示」是两边都不占。
- **验证需求**：编译 + guard；实机需构造读回失败（可 Dev 注入）。

### F-03：`RestoreForSlotChange` 在已有活动 run 时直接早退，换槽后旧槽 run 可写进新槽

- **严重级**：P2 Minor（需要「局内换槽」这一异常路径）
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHRuntimeModule.cs:209-213`
- **证据**：

```csharp
private void RestoreForSlotChange()
{
    if (HasActiveRun) return;      // ← 旧槽的 _runState 仍在内存里，直接不换
    RestoreFromSaveIfPresent();
}
```

  `HasActiveRun`（`:45-48`）= `_runState != null && Lifecycle != None`。
  `HandleSetFile`（`ModeHProfilePersistence.cs:117-141`）会清 `_cache` 并重载新槽，
  但 `_runState` 只由 `RestoreFromSaveIfPresent` 改写，被这一行拦住。
  注释（`:206-208`）自陈"局内换档属异常路径，交给既有的场景失配出口处理"，
  但**场景失配出口只在 `OnSceneLoaded` 里触发**（`ModeHRuntimeModule.cs:109-125`）——
  主菜单换槽不产生 Mode H 认可的场景回调时不会走到。
- **失败场景**：玩家在 Mode H 赛季进行中（`_runState.Lifecycle` 非 None）回主菜单并载入另一个槽 →
  `_runState` 仍是槽 A 的 → 之后任何 `TryPersistSeason` 都把槽 A 的赛季写进**槽 B** 的
  `BossRush_ModeH_Season_v1`。
- **建议修复**：`RestoreForSlotChange` 无条件重建（换槽本身就是 run owner 失效的充分条件），
  或先 `ShutdownRuntime(SlotChanged, ...)` 再重建。
- **验证需求**：实机：赛季进行中回主菜单载另一个槽，检查新槽赛季 key。

## 二、未验证线索（需实机或 owner 确认）

（填充中）

## 三、已核对且干净的关键面

（填充中）

## 四、对前几轮已 Fixed 条目的复核

（填充中）

## 附录 A：§17.4 局中重建链删除合法性独立核实（交办项）

### A.1 结论先说

**被交办核实的论断是【假】的。** 删除注释所依赖的唯一结构性支点不成立。

删除时写在 `ModeH/ModeHBattleSnapshot.cs:242-248` 的原文：

```
// 两者不是"少接了一根线"，而是**互斥的两种恢复语义**，且冻结转换表站在 §20.3 这边：
// ModeHStateMachine 里 Recovering 的出边只有
//   EntryIntent / SceneLoading / Drafting / RosterLocked / MatchBrief /
//   ErrorRecoveryPending / Intermission / TransferWindow / HallOfFame / Suspended，
// **没有任何一条通向 MatchFighting / MatchSpawning / RelayPending**。
// 也就是说局中重建在状态机层面结构性不可达；要启用它必须改冻结表，
// 那属于 AGENTS.md §10 需要 owner 签字的"模式状态机大规模重构"。
```

**出边枚举本身准确**（与 `ModeHStateMachine.cs:209-221` 逐字一致）。
**但从"无直接出边"推出"结构性不可达"是错的** —— 该注释把「直接后继」当成了「可达集」。
冻结表里存在一条完全合法的 3 跳路径：

```
ModeHStateMachine.cs:216   map[Recovering]           ∋ ErrorRecoveryPending
ModeHStateMachine.cs:165   map[ErrorRecoveryPending] ∋ RelayPending
ModeHStateMachine.cs:172   map[RelayPending]         ∋ MatchFighting
=> Recovering → ErrorRecoveryPending → RelayPending → MatchFighting  【冻结表内合法】
```

`map[ErrorRecoveryPending]`（`:163-169`）= { RelayPending, MatchSettling, Recovering, Suspended }，
`map[RelayPending]`（`:170-177`）= { MatchFighting, MatchSettling, ErrorRecoveryPending, Recovering, Suspended }。

而且这条路径不是意外产物，**正是 `ModeHStateMachine.cs:38-40` 类注释冻结的设计语义**：

```
/// - 已 CAS 的事实必须先进入 ErrorRecoveryPending，恢复动作开始后才转 Recovering，
///   屏障完成后回 ErrorRecoveryPending，再由原 token 命令继续；
```

"屏障完成后回 ErrorRecoveryPending，再由原 token 命令继续" 就是**局中续战**语义本身。
换言之：**启用局中重建根本不需要改冻结表**，因此也不构成 AGENTS.md §10 的
"模式状态机大规模重构"。删除注释里"要启用它必须改冻结表 / 属于 §10 owner 签字范围"
这两句同样是错的。

补充核实：`TryTransition` 的早期恢复子表约束（`ModeHStateMachine.cs:358-371`）**不会**
拦住这条路径 —— `EarlyRecoveryTargets` 只登记了 EntryIntent / SceneLoading / Drafting /
RosterLocked（`:238-253`）；当 `RecoveryOriginalLifecycle == MatchFighting` 时
`IsEarlyRecoverySource` 返回 false，该约束整段跳过。

### A.2 但删除的"死代码"事实为真

我独立复核了删除前（`git grep HEAD`）的调用点，**确认为零**：

| 被删符号 | HEAD 中的调用点 |
| --- | --- |
| `ModeHBattleSnapshot.Validate` | 仅 `ModeHBattleSnapshot.cs:250` 声明，零 invocation |
| `ModeHBattleSnapshot.TryRestoreHealth` | 仅 `:359` 声明，零 invocation |
| `ModeHSnapshotRebuildPlan` | 仅 `:23` 定义 + `Validate` 内部构造 |
| `ModeHCombatControl.RestoreFromSnapshot` | 仅 `:760` 声明，零 invocation（它自己调下游两个同名方法） |
| `ModeHCombatTelemetry.RestoreFromSnapshot` | 仅被上面那个死方法调用 |
| `ModeHCommandController.RestoreFromSnapshot` | 同上 |

运行时行为等价的说法也成立：`RequestTechnicalRetry`
（`ModeHRuntimeModule_MatchFlow.cs:201-211`）对 committedCombatFact 走
`ErrorRecoveryPending → Recovering`，随后 `DriveRecovery`（`:92-118`）经
`ResolveRecoveryResumeLifecycle`（`:124-164`）把 MatchBrief..MatchSettling 整族映射成
`MatchBrief`。**当前没有任何代码走 ErrorRecoveryPending → RelayPending 这条边。**

### A.3 判定

- 删除动作本身（删死代码）**可接受**，删除前后运行时行为确实逐字相同。
- **但注释里作为唯一支点写下的结构性论断是错的，且被固化进源码**，会误导后来的维护者
  （"要改冻结表"→ 实际不用改；"结构性不可达"→ 实际 3 跳可达）。
  这条注释同时也和 `ModeHStateMachine.cs:38-40` 的冻结设计语义直接矛盾。
- 见正文 F-13（P2，文档/注释性缺陷）与 F-14（P2，承诺的 guard 不存在）。

### A.4 采集侧保留正确性 —— 确认无误

`AttachTo`（`ModeHBattleSnapshot.cs:139-143`）写 `season.currentBattleSnapshot`、
`ClearFrom`（`:146-150`）清空，两者均保留。`currentBattleSnapshot` 确为 Season 落盘字段
并参与 §20.2 canonical digest（见正文 §三 对 `ModeHCanonicalDigest` 的核对）。
**保留是正确的**：摘掉它属 `SCHEMA-`，老档 `VerifyDigest` 会全量失败。删除注释这一段判断准确。

### A.5 附带发现：`ModeHSnapshotRebuildAbsenceGuard` 不存在

`ModeHBattleSnapshot.cs:256` 写 "由 ModeHSnapshotRebuildAbsenceGuard 守卫"（现在时陈述）。
全仓库 grep（排除 `鸭科夫源码/`）**只有这一处提及**，`tests/` 下无该文件。见 F-14。

---

## ==== ModeH_content_ui_guards ====

# Mode H 内容数据表 / UI / 选秀 / 赔率 / 本地化 / 资源缓存 / 专项守卫 审查报告

审查基线：`git status` 工作树（main，HEAD=`265937f`）。**只读审查，未修改仓库任何文件。**
并发写入提示：`ModeH/` 下 7 个 `.cs` 在 21:15–21:23 被其他会话改动；`ModeHDraftController.cs`、
`ModeHUIPages.cs`、`Assets/Data/ModeH/CommandCompatibility.json` 有未提交改动（下文逐条标注）。
`ModeHRuntimeModule_MatchFlow.cs`、`ModeHCombatControl.cs`、`ModeHBattleSnapshot.cs` 等在 22:5x 仍被改，
本报告以读取时刻（2026-09-03 23:0x）的磁盘内容为准。

所有 finding 均为**静态确认，未运行验证**（只读约束禁止编译与部署）。

---

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1：赔率页分量标签本地化 key 双前缀，15 条分量全部显示 `*BossRush_ModeH_BossRush_ModeH_Odds_xxx*`

- **严重级**：P1 Major
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHRuntimeModule_MatchFlow.cs:933`（消费点）、
  `ModeH/ModeHOddsController.cs:600`（生产点）、`ModeH/ModeHConfig.cs:23`（前缀常量）
- **证据**：

  `ModeHOddsController.Add()` 写入的已经是**完整** key：

  ```csharp
  // ModeH/ModeHOddsController.cs:595-601
  private static int Add(
      List<ModeHOddsBreakdownEntry> breakdown, bool isEnemySide, string labelSuffix, int value)
  {
      ...
      entry.LabelKey = ModeHConfig.LocalizationKeyPrefix + labelSuffix;   // 已含 "BossRush_ModeH_"
  ```

  消费点又拼了一次：

  ```csharp
  // ModeH/ModeHRuntimeModule_MatchFlow.cs:933
  page.Lines.Add(L10n.T(ModeHConfig.LocalizationKeyPrefix + entry.LabelKey)
      + "  " + (entry.Value >= 0 ? "+" : string.Empty) + entry.Value);
  ```

  `ModeHConfig.cs:23` 定义 `LocalizationKeyPrefix = "BossRush_ModeH_"`，最终 key 为
  `BossRush_ModeH_BossRush_ModeH_Odds_*`。`grep "BossRush_ModeH_BossRush" Localization/ModeHLocalization.cs`
  = 0 命中，未注册。全库 grep `LocalizationKeyPrefix + entry.LabelKey` 唯一命中就是 933 行；
  同文件 `:954` 的 `Button_LockIn` 用裸后缀，写法正确，可作对照。

- **玩家看到什么（补充确认）**：`L10n.T(string key)`（`Localization/L10n.cs:82-89`）→
  `LocalizationHelper.GetLocalizedText`（`Localization/LocalizationHelper.cs:68-79`）→
  官方 `LocalizationManager.GetPlainText`。官方实现（`鸭科夫源码/SodaLocalization/LocalizationManager.cs:115-138`）
  在 override 表、主表、fallback 表三处都未命中时**返回 `"*" + key + "*"`**：

  ```csharp
  if (text == null) { text = "*" + key + "*"; }
  ```

  因此玩家在赔率页看到的**每一行**都是
  `*BossRush_ModeH_BossRush_ModeH_Odds_Equipment*  +7` 这种形状——原样 key 加星号，不是空白，
  也不是退回中文。整页赔率拆解全部变成乱码行。

- **受影响的分量 label 后缀清单（`ModeHOddsController.cs` 里全部 `Add(...)` 调用，共 15 条）**：

  | 行 | 后缀 | 已注入的中文（`ModeHLocalization.cs`） |
  | --- | --- | --- |
  | :114 | `Odds_RelayAvailable` | 有可用接力 |
  | :114 | `Odds_RelayEmpty` | 无接力 |
  | :124 | `Odds_StarterMatchup` | 先发克制关系 |
  | :131 | `Odds_RelayMatchup` | 接力克制关系 |
  | :136 | `Odds_Equipment` | 虚拟整备 |
  | :148 | `Odds_Injury` | 伤病 |
  | :153 | `Odds_Anomaly` | 公开异常 |
  | :162 | `Odds_Scar` | 战痕 |
  | :166 | `Odds_Command` | 口令 |
  | :170 | `Odds_Arena` | 擂台条件 |
  | :453 | `Odds_EnemyStage` | 场次 |
  | :458 | `Odds_EnemyCount` | 公开人数上限 |
  | :463 | `Odds_EnemyCore` | 高威胁核心 |
  | :468 | `Odds_EnemySynergy` | 协同 |
  | :471 | `Odds_EnemyStatus` | 公开状态 |

  15 条 key 在 `Localization/ModeHLocalization.cs:336-350` 全部**已正确注入**中英双语，
  只是因为消费点多拼一次前缀而永远查不到。`Odds_RelayAvailable`/`Odds_RelayEmpty` 互斥，
  单场实际最多出现 14 行，但 15 条都不可达。

- **失败场景**：任何一场进入赔率页（`LoadoutEditing`/`OddsPreview`）→ 逐行拆解全部显示带星号的原始 key。
- **玩家/维护影响**：CR-2026-08-29-018 第 2 项「赔率页没有赔率」修完后，赔率页有了行，但每行的标签
  是乱码。这是 §17.5「公开分差逐行可展开拆解」这条设计的可读性完全丧失；玩家看得到数字看不懂含义。
- **建议修复**：933 行改为 `L10n.T(entry.LabelKey)`（与 `AppendReconLinesAndActions:562`
  的 `L10n.T(choice.NameKey)` 同型），或改 `Add()` 只存后缀。前者改动更小、且与
  「数据表/生产点存完整 key」的既有口径一致。
- **验证需求**：编译 + `ModeHOddsGuard` + 实机看赔率页任一行。
  建议同时给 guard 补一条「`LocalizationKeyPrefix +` 的右操作数必须是字符串字面量或裸后缀变量」的静态断言。

---

### F-2：赔率页动作按钮单行平铺，押品格数一多就把「锁盘」推出屏幕，玩家在时停模态页里无路可走

- **严重级**：P0 Blocker
- **兼容分类**：`COMPAT`
- **位置**：
  - `ModeH/ModeHUIPages.cs:398-424`（`CreateActions`：单行水平平铺，无换行/无滚动/无上限）
  - `ModeH/ModeHRuntimeModule_MatchFlow.cs:938-958`（赔率页装配顺序：下注档 → 押品格 → 锁盘）
  - `ModeH/ModeHRealStakeService.cs:51/122-133`（`MaxScannedPositions = 40`，逐格一个按钮）
  - `ModeH/ModeHInventoryPersistenceBridge.cs:169-193`（`ListOccupiedPositions`：前 40 格里**每个非空格**都算）
- **证据**：

  ```csharp
  // ModeHUIPages.cs:404-413
  float startX = -((count - 1) * (ActionSize.x + CardGap)) * 0.5f;
  for (int i = 0; i < count; i++) {
      ...
      new Vector2(startX + i * (ActionSize.x + CardGap),
          ModeHUI.SafeMargin + ActionSize.y * 0.5f),
  ```
  `ActionSize = (240, 56)`、`CardGap = 24` → 步距 264 px，**只有一行，永不换行**。

  赔率页往 `page.Actions` 里塞的东西（`ModeHRuntimeModule_MatchFlow.BuildOddsPageContent`）：
  1. `:939-948` 虚拟下注档 `0..maxStake`，赛季开局 `virtualStakeCredits=6` → `maxStake=2` → **3 个**；
  2. `:950 AppendRealStakeLinesAndActions` → `:761-778` 对
     `ModeHRealStakeService.GetSelectablePositions()` 的**每一格**加一个按钮；
  3. `:952-958` 「锁盘」按钮，**排在最后**。

  `GetSelectablePositions()` 上限 40（`MaxScannedPositions`），实际值 = 仓库前 40 格里的非空格数。
  该分支只在 `ModeHWarehouseStakeJournal.IsSlotConsistent == true` 时执行，而
  `RecomputeSlotConsistency`（`ModeHWarehouseStakeJournal.cs:80-87`）在**无 journal（全新档/正常档）时
  直接置 true**，所以这是常态路径而不是边角。

  面板与画布尺寸：`ModeHUI.MainPanelSize = (1480, 860)`（`ModeHUI.cs:537`），
  `ZombieModeUIHelper.ConfigureCanvasScaler` 参考分辨率 `1920x1080`（`ZombieMode/ZombieModeUIHelper.cs:449-451`），
  屏幕半宽 960。按钮锚点是 surface 的 `(0.5, 0)`，surface 居中。

  代入：按钮总数 `count = 3 + N + 1`（N = 可押格数）。最后一个（锁盘）的中心 x
  = `startX + (count-1)*264` = `+(count-1)*132`。
  - N=2 → count=6，锁盘中心 x=660，仍在屏内（但已超出 1480 面板可用宽 1384，压在面板外）；
  - **N=5 → count=9，x=1056 > 960，锁盘中心已在屏幕外**；
  - N=6 → count=10，x=1188，按钮左边缘 1068 > 960，**完全不可见、不可点**；
  - N=40（仓库前 40 格满）→ count=44，总宽 11592 px，锁盘 x=5676，屏上只剩中间 6 个按钮。

- **失败场景**：玩家仓库前 40 格里有 ≥5 件物品（几乎所有非新档），走 看盘→锁盘→赔率页。
  赔率页由 `ModeHUI.EnsureModalRoot` → `ZombieModeUIHelper.ClaimModalInput` 接管，而
  `ClaimModalInput`（`ZombieMode/ZombieModeUIHelper.cs:88-118`）**把 `Time.timeScale` 设为 0
  并 `InputManager.DisableInput`**，`EnforceModalInputPause`（:120-130）每次还会重新压回 0。
  赔率页本身**没有关闭/返回按钮**（`BuildOddsPageContent` 只加下注档、押品格、锁盘三类）。
  于是玩家被困在一个时停 + 禁输入 + 唯一出口按钮在屏幕外的模态页里，只能靠游戏自身 ESC 菜单逃生。
- **玩家/维护影响**：这与 CR-2026-08-29-008「玩家被困在时停赔率页」是**同一种玩家体感的第二次发生**，
  只是机制换成了布局溢出而非状态机拒绝。押品选择功能本身（§22.1）在有仓库存货时也基本不可用。
- **建议修复**：`CreateActions` 改为按可用宽度自动换行（或 `count` 超阈值时走
  `CreateScrollHost` 那套横向/纵向滚动）；同时把押品格从「底部动作行」移进独立的选择器区域
  （`CreateOddsPage` 已经画了一个 `ModeH_RealStakeSelector` 面板，420x220，但**里面只有一行说明文本、
  没有任何按钮**——押品格按钮本该长在那里）；并给锁盘按钮固定位置，不参与平铺。
- **验证需求**：编译 + 实机（带存货的档）开赔率页确认锁盘可见可点。
  建议给 `ModeHStructureGuard`/UI guard 补「单行动作按钮数上限」断言。

---

### F-3：赔率页整备失败时把内部 reasonId 原文喷给玩家

- **严重级**：P2 Minor
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHRuntimeModule_MatchFlow.cs:912-917`
- **证据**：

  ```csharp
  string prepareFailure;
  if (!EnsurePreparedMatchSelection(out prepareFailure))
  {
      page.Body = L10n.T("本场整备不可用：", "Match setup unavailable: ")
          + (prepareFailure != null ? prepareFailure : "unknown");
      return page;
  }
  ```

  `EnsurePreparedMatchSelection`（`ModeH/ModeHRuntimeModule_CombatFlow.cs:10-70+`）返回的是内部 ID：
  `match_selection_input_missing`、`match_roster_no_live_contract`、`match_starter_missing`、
  `loadout_digest_failed:<digestError>` 等。

  同一文件 `:839-861` 的 `ResolveLockRejectReason` 头注释白纸黑字写着
  「**绝不把 reasonId 原文喷给玩家**」，`:814-828`、`:869-890` 两处也都做了分因翻译——
  只有这一处没有。CR-2026-09-03-003 的修复正是为了消灭这类静默/原文暴露，
  且其验证需求写明 `ModeHStructureGuard` 断言「不得直接展示 reasonId」，
  说明该 guard 没覆盖到 `page.Body` 这条路径（见 F-9）。

  另外这段文本会被 `ModeHUIPages.CreateOddsPage:255-261` 当成**48pt 的大字标题**渲染在
  1384x96 的框里，英文 `Match setup unavailable: match_roster_no_live_contract` 在 48pt 下必然溢出。
- **失败场景**：合同选手全退役 / roster 摘要计算失败 → 玩家看到一行英文内部 ID。
- **建议修复**：照 `ResolveLockRejectReason` 的形态加一个 `ResolvePrepareFailureReason`，
  或直接复用它（失败原因集合高度重叠）。
- **验证需求**：编译 + guard 反向验证。

---

### F-4：`OddsWeights.json` 整表解析失败时静默回退硬编码权重，无任何日志且 `UsedOddsFallback` 零消费

- **严重级**：P2 Minor
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHContentCatalogParsers.cs:784-795`、`ModeH/ModeHContentCatalog.cs:66`
- **证据**：

  ```csharp
  private static bool ParseOddsWeights(ModeHJsonValue root)
  {
      if (TryParseOddsWeightsCore(root)) { _usedOddsFallback = false; return true; }
      // 纯数值权重允许同版本内置 fallback（§23.2）
      ApplyBuiltInOddsFallback();
      _usedOddsFallback = true;
      return true;
  }
  ```

  `TryParseOddsWeightsCore` 有 **13 处 `return false`**（tier 数不为 5、`odds != i+1`、
  `playerWeights`/`enemyWeights` 不是对象、`archetypeMatrix` 空、原型 ID 不认识、
  `commandTagMap` 条数不等于通用口令数、测试向量少于 3 条或档位自检不符……）
  ——**没有一处记录是哪一条失败的**，也没有任何 `DevLog`/`Debug.LogWarning`。

  全库 grep `UsedOddsFallback` / `_usedOddsFallback`：生产代码零消费
  （只有 `tests/ModeHOddsGuard.py:208` 断言该符号存在）。诊断页
  （`ModeHUI.EnsureDiagnostics` / `UpdateDiagnostics`）也不显示它。

  对照：同文件其它 6 个 `Parse*` 全部走 `_lastError = "<具体原因>"` 并 `return false`（fail-closed），
  只有这一张表是 fail-open + 静默。
- **失败场景**：`Assets/Data/ModeH/OddsWeights.json` 结构改坏（内容签名仍自洽，例如用
  `tools/modeh_stamp_data.py` 重新盖章后才发现字段写错）→ 全体权重悄悄换成
  `ApplyBuiltInOddsFallback` 里那份硬编码值，JSON 调参完全不生效，日志一行都没有。
- **玩家/维护影响**：不炸游戏，但调参会「改了没反应」，且赔率与 JSON 里写的不一致；
  与 AGENTS 4.8 第 3 条「大型数据表 + 硬编码 fallback」的意图（fallback 要可观测）相悖。
- **建议修复**：`ApplyBuiltInOddsFallback()` 前补一行 `ModBehaviour.DevLog`/`CriticalLog`，
  并把 `UsedOddsFallback` 接进诊断页（`Diag_*` 已有可复用的键位）。
- **验证需求**：编译 + guard。

---

（更多 finding 见下文，本节持续追加）

---

## 二、未验证线索（需实机或 owner 确认）

（填充中）

---

## 三、已核对且干净的关键面

（填充中）

---

## 四、对前几轮已 Fixed 条目的复核

（填充中）

---

## ==== PetNest_domain_persistence ====

# 遗种巢 PetNest 领域模型/持久化/服务/掉落/图鉴/模式门 审查报告

> 状态：进行中（边查边落盘）
> 范围：`PetNest/` 下领域与服务层 15 个文件 + `Integration/Items/RelicEggConfig.cs` +
> `Localization/PetNestLocalization.cs` + `Config/ConfigPetNest.cs` + `tests/PetNest*Guard.py`
> 不含：CompanionAgent/Runtime/Spawner、BaseIdleSpawner、DeathSuppression、DownedHandler、
> PetProxyBridge、Builder、Interactable、UI*、RevealView、Modal（另一位同事负责）

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1：遗种蛋直掉（欧轨）在标准竞技场与无间炼狱全程作废——蛋加进 Boss 自身库存，而 BossRush 随后把 `dropBoxOnDead` 置 false

- **严重级**：P1 Major（双轨掉落的「欧轨」在主玩法模式里 100% 不可得；4% 概率完全空转）
- **兼容分类**：`COMPAT`
- **位置**：
  - `PetNest/PetNestDropService.cs:239-287`（`TrySpawnEggIntoBossInventory`，蛋加进 `boss.CharacterItem.Inventory`）
  - `LootAndRewards/LootAndRewardsRandomBossLoot.cs:520`（`bossMain.dropBoxOnDead = false;`，标准奖励箱路径）
  - `LootAndRewards/LootAndRewardsRandomBossLoot.cs:312`（无间炼狱同样置 false）
  - 对照组（正确写法）：`LootAndRewards/LootAndRewardsSpecialLoot.cs:631-632`、
    `Integration/Frostmourne/FrostmourneBootstrap.cs:178-182,209-223`、
    `Integration/PhantomWitch/PhantomWitchScytheBootstrap.cs:239-269`
- **证据**：
  1. 官方 `鸭科夫源码/TeamSoda.Duckov.Core/CharacterMainControl.cs:1296-1313`：
     `BeforeCharacterSpawnLootOnDead` 派发完之后才 `if (this.dropBoxOnDead) InteractableLootbox.CreateFromItem(this.characterItem, ...)`。
     即：放进 `characterItem.Inventory` 的东西**只有 `dropBoxOnDead==true` 时才会变成可拾取的箱子**。
  2. `RegisterBossRandomLootTracking`（`LootAndRewards/LootAndRewards.cs:323`）订阅顺序为
     PetNest(:339) → AffixForge(:342) → LootAndRewards 主 handler(:364)。多播委托按订阅序调用，
     所以 PetNest 先把蛋塞进 Boss 库存，随后主 handler 走 `RandomizeBossLoot_LootAndRewards`
     并在 `:520` 把 `dropBoxOnDead` 置 false，改为 `Instantiate(prefab)` 出一个**全新的**奖励箱
     （`:534` + `InteractableLootboxInventoryHelper.EnsureLocalInventory(lootbox, 512)`）。
  3. 全仓 grep 确认**没有任何**代码把 `bossMain.CharacterItem.Inventory` 的内容搬进新奖励箱：
     `grep -rn "CharacterItem" LootAndRewards/` 只有一行**过时注释**
     （`LootAndRewardsRandomBossLoot.cs:425`「填充到Boss掉落源（CharacterItem.Inventory），由原版逻辑创建LootBox」，
     与 :520 的实际实现相矛盾）。
  4. 本仓已有**专门为此设计的规避机制**，PetNest 没接：
     `MarkBossRushLootboxPathTracking`（`LootAndRewards.cs:412-430`，在 :335 于 TryTrack **之前**调用）
     把「本 Boss 走 BossRush 奖励箱路径」登记进 `bossRushLootboxPathBosses`；
     Mod 物品需先问 `ShouldDeferBlueBossExtraDropToBossRushLootbox`（`LootAndRewardsSpecialLoot.cs:646`），
     为 true 时改为挂 pending，等 `AddBossSpecialLootToLootboxCoroutine` 里的
     `TryConsumePendingBossRushLootboxDrop(bossMain, inv)` 投进真正的奖励箱。
     寒霜长矛与幽灵女巫镰刀两件 Mod 物品都走了这条路；**遗种蛋与词缀熔石都没走**。
  5. 龙王路径更早失效：`DragonKingBoss.cs:360` 先订阅主 loot handler，`:365` 才 `PetNestDropService.TryTrack`，
     所以对焚天龙皇而言主 handler 先跑、奖励箱已经建好，PetNest 再往尸体库存里塞蛋，同样蒸发。
- **失败场景**：玩家开启遗种巢 → 标准 BossRush 竞技场击杀任意 Boss → 4% 判定命中 →
  日志打「[PetNest] 掉落遗种蛋，血脉=xxx」→ 奖励箱里**没有蛋**，尸体也不掉箱子 → 蛋随角色销毁。
  `config.enableRandomBossLoot` 默认 true（`Config/Config.cs:45`），因此这是默认路径。
  无间炼狱（`:312`）连奖励箱都不建，同样蒸发。
  仅 Mode E / Mode F（`allowModeEFIndependentLoot` 分支在 `:360` 提前 return，保留原生掉落箱）
  与玩家手动关掉「Boss 掉落随机化」时，蛋才真的能拿到。
- **玩家影响**：双轨设计里的「欧轨」在主模式静默归零，玩家只能靠 240 遗魂凝蛋这一条路，
  实际获取速率约为设计值的一半以下；且日志与「可凝蛋」提示都正常，玩家无从察觉。
- **建议修复**：照 `FrostmourneBlueBossDropHandler` 逐字并联——
  `TrySpawnEggIntoBossInventory` 前先问 `ModBehaviour.Instance.ShouldDeferBlueBossExtraDropToBossRushLootbox(boss)`，
  为 true 时把 (boss, lineageKey) 挂 pending；在 `AddBossSpecialLootToLootboxCoroutine`
  （`LootAndRewardsSpecialLoot.cs:631` 旁）并联一行 `PetNestDropService.TryConsumePendingLootboxDrop(bossMain, inv)`；
  `FinalizeBossRushLootboxPathTracking`（`LootAndRewards.cs:432`）并联 `CancelPending`。
  （词缀熔石同构，属另一范围，但成因与修法完全相同，建议同批处理。）
- **验证需求**：编译 + 新增 guard 断言（三处并联对）+ **实机**：把 `EggDropChance` 临时调到 1.0
  刷一次标准竞技场 Boss，确认奖励箱里有遗种蛋。

（其余 finding 见下，本文件持续追加）

## 二、未验证线索（需实机或 owner 确认）

（填充中）

## 三、已核对且干净的关键面

### 3.1 三个自定义 Boss 的 PetNestDropService 挂接点（交办的交叉核对）——**接线本身齐全**

| Boss | 挂接 | 退订 |
| --- | --- | --- |
| 龙裔遗族 DragonDescendant | 经共享漏斗：`Integration/DragonDescendant/DragonDescendantBoss.cs:220` → `RegisterBossRandomLootTracking` → `LootAndRewards.cs:339 TryTrack` | `DragonDescendantBoss_ModeGAdapter.cs:227`、`DragonDescendantBoss_RuntimeAndCleanup.cs:247` → `ClearBossRandomLootTracking` → `LootAndRewards.cs:386 ClearTracking` |
| 焚天龙皇 DragonKing | **手动并联**：`Integration/DragonKing/DragonKingBoss.cs:365 TryTrack`（紧随 :360 的手动订阅） | `DragonKingBoss.cs:159`（离场）+ `:634`（死亡）各一次 `ClearTracking`，**成对** |
| 幽灵女巫 PhantomWitch | 经共享漏斗：`Integration/PhantomWitch/PhantomWitchBoss.cs:352` | `:141`（清理委托表）、`:400`、`:714` 三处 `ClearBossRandomLootTracking` |

血脉 key 一致性也已核对：三个 Boss 都在生成时把 `characterPreset.nameKey` 改写成各自的 Config 常量
（`DragonKingBoss.cs:280` = `DragonKingConfig.BossNameKey` "boss_dragonking"；
`PhantomWitchBoss.cs:222` = "boss_phantomwitch"；`DragonDescendantBoss.cs:134` = "DragonDescendant"），
且**改写发生在 TryTrack/Register 之前**（281<365、240<352、137<220），
与 `PetNestLineageCatalog.CustomBossLineageKeys` 完全一致。
`ResolveLineageKey` 读的正是 `characterPreset.nameKey`，因此三条血脉都能解析到目录条目。

### 3.2 第三条路径「后山种子」不适用于 PetNest

`TryAddBackMountainSeedLoot` 挂在 `AddBossSpecialLootToLootboxCoroutine`
（`LootAndRewards/LootAndRewardsSpecialLoot.cs:637`），那是**箱子建好之后**的填充路径，
不是死亡事件订阅。遗种巢的记账与掉蛋都在 `BeforeCharacterSpawnLootOnDead` 阶段完成，
不需要在这条路径再挂一次。**但见 F-1**：正因为蛋没走这条路径，蛋才丢了——
修 F-1 恰好就是把蛋接进这条路径。

## 四、对前几轮已 Fixed 条目的复核

（填充中）

---

## ==== PetNest_runtime_ui ====

# 遗种巢 运行时/UI 审查报告

> **审查基线**：commit `f9b83c0` 之后的全部改动（含未提交工作树）。
> **范围**：`PetNest/` 下随从进局运行时、生成、AI 代理、重伤退场、死亡抑制、宠物代理桥、
> 基地闲逛、建筑与交互、HUD、面板 UI、演出层、弹窗共 19 个新增文件；
> 外加与之交互的 `Patches/Combat/CharacterOnDeadPatch.cs`、
> `Patches/Combat/BossLethalHealthProtectionPatch.cs`、
> `Patches/Compatibility/MagicBlendInitializationOrderPatch.cs`。
> **方式**：19 个文件全部逐行读完（非抽样），关键结论均已 grep 调用点并对照
> `鸭科夫源码/` 官方反编译源码核对。
> **统计**：P0 = 0，P1 = 1，P2 = 2，P3 = 4。
> **验证状态**：全部为静态确认，未运行验证（本轮为只读审查，未编译、未跑 guard、未实机）。

---

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1（P1 Major / `COMPAT`）：大兴兴清理扫描漏掉随从豁免 —— 该血脉的崽进标准局会被自家逻辑反复销毁重生

**位置**
- 缺陷点：`D:\code\ykf\BossRushMod\ModBehaviour.cs:1449-1550`（`TryCleanNonBossRushDaXingXing` 的身份判定与销毁）
- 触发器：`D:\code\ykf\BossRushMod\WavesArena\WavesArenaRuntimeHooks.cs:85-101`（`TickWavesArenaBossCleanupRuntime`）
- 受害方：`D:\code\ykf\BossRushMod\PetNest\PetNestCompanionSpawner.cs:162`、
  `D:\code\ykf\BossRushMod\PetNest\PetNestCompanionRuntime.cs:126-148`、`:200-216`

**证据**

`ModBehaviour.cs:1460-1485` 的身份判定：

```csharp
displayName = preset.DisplayName;
if (!string.IsNullOrEmpty(displayName) && (displayName.Contains("大兴兴") || displayName.Contains("小兴兴")))
{ isDaXing = true; }
else { key = preset.nameKey;
       if (key.IndexOf("daxing", StringComparison.OrdinalIgnoreCase) >= 0) isDaXing = true; }
```

命中后的豁免名单只有三条：主角（`:1444`）、`bossRushOwnedDaXingXing`（`:1492-1505`）、
Mode G owner（`:1507-1524`）。之后无条件 `UnityEngine.Object.Destroy(c.gameObject)`（`:1546-1550`）。
**没有 `PetNestCompanionAgent.IsCompanionCharacter` 豁免。**

链条逐环已确认：

1. **大兴兴确实是 BossRush 的池内 Boss**：`ModBehaviour.cs:1176-1184` 在标准 Boss 生成路径里
   对 `EnemyPresetInfo preset` 调 `IsDaXingXingPreset(preset)` 并登记进 `bossRushOwnedDaXingXing`
   —— 只有池里真有它才需要这段。
2. **池内 Boss 一律进血脉目录**：`PetNest/PetNestLineageCatalog.cs:129-164` 的 `AddOfficialLineages`
   遍历 `owner.GetFilteredEnemyPresets()`，只要 `ResolveSourcePreset(info.name)` 解析得到就登记，
   没有任何按名字排除。因此玩家可以拿到大兴兴的遗种蛋、孵出大兴兴幼体。
3. **随从的 clone preset 会命中身份判定**：`PetNestCompanionSpawner.cs:162`
   `if (!string.IsNullOrEmpty(lineageKey)) clone.nameKey = lineageKey;`——官方血脉的
   `lineageKey` 就是官方 `nameKey`，而官方 `CharacterRandomPreset.DisplayName`
   （`鸭科夫源码/TeamSoda.Duckov.Core/CharacterRandomPreset.cs:30-35`）正是 `nameKey.ToPlainText()`，
   中文环境下即"大兴兴"。
4. **门控完全重叠**：清理只在 `IsActive || bossRushArenaActive || IsZombieModeActive` 时跑
   （`WavesArenaRuntimeHooks.cs:90`），而 `PetNestModeGate.IsCompanionAllowed`
   （`PetNest/PetNestModeGate.cs:66-70`）的允许名单头两项正是 `owner.IsActive` 与
   `owner.IsBossRushArenaActive`。
5. **随从一定在被扫描的缓存里**：`WavesArena/WavesArenaEnemyMaintenance.cs:301`
   的 `ContinuousClearEnemiesUntilWaveStart` 在 `!IsActive && bossRushArenaActive` 期间
   每 0.5s 无条件 `RefreshCharacterCache()`，而随从正是在 `bossRushArenaActive` 置位后
   由重试窗口拉进场的。
6. **范围检查挡不住**：随从落点是 `player.position + (1.2, 0.5, 1.2)`
   （`PetNestCompanionSpawner.cs:67`），玩家在竞技场内即在半径内。

**失败场景**

玩家孵出一只大兴兴/小兴兴血脉的崽 → 设为出战 → 进标准 BossRush 或竞技场 →
随从入场后 ≤0.5 秒被 `TryCleanNonBossRushDaXingXing` 静默 `Destroy`。随后：

- `PetNestCompanionRuntime.HasCompanion`（`:51-54`）因 `_handle.Character == null` 变 false；
- `pet.state` 从未被置 `Downed`（是直接 Destroy，不走伤害，钳制链根本没触发），
  于是 90 秒重试窗口内 `TickSpawnRetry`（`:126-148`）每秒重新 `TrySpawnForScene` → 重新生成 →
  再被销毁，**循环最多 90 次**；
- 每一轮 `SpawnAsync` 的 `_handle = handle;`（`:200`）直接覆盖旧 handle，**不走 `CleanupOnce`** →
  旧 handle 的 `ClonePreset`（`Object.Instantiate` 出来的 `CharacterRandomPreset`
  ScriptableObject，不随场景卸载消失）永不销毁，**单局最多泄漏约 90 个**；
- 每一轮重新 `TryBorrowSeat`（`:209`）：`PetNestPetProxyBridge.cs:171-177` 的
  "席位被他方随从占着"判定写作
  `if (current != null && current != companion && IsCompanionCharacter(current))`，
  而 `current` 此刻是**已销毁**的上一只随从，Unity 重载的 `!=` 使 `current != null` 为 **false**
  → 判定被跳过 → `_previousOccupant = current`（`:191`）把**官方宠物的原始引用永久覆盖成一个已销毁引用**。
  最终 `ReleaseSeat`（`:231`）把这个已销毁引用写回 `LevelManager.petCharacter`，
  官方 `PetProxy.Update`（`鸭科夫源码/TeamSoda.Duckov.Core/PetProxy.cs:112`）从此
  `PetCharacter == null` 直接早返 —— 本局剩余时间捡漏背包不再跟随、容量不再同步。

**玩家影响**

该血脉的随从功能 100% 不可用且无任何提示（不是"打不过"，是凭空消失又凭空出现）；
面板与 HUD 会闪烁；本局捡漏背包失效；长会话下 ScriptableObject 泄漏。
其余血脉不受影响，故定 P1 而非 P0。

**建议修复**

在 `ModBehaviour.cs:1449` 的 `isDaXing` 判定命中之后、销毁之前，补一条与仓库其余三处同形的豁免
（`ModBehaviour.cs:1158`、`WavesArenaEnemyMaintenance.cs:53`、`:208` 已是先例）：

```csharp
if (PetNestCompanionAgent.IsCompanionCharacter(c)) continue;
```

放在 `bossRushOwnedDaXingXing` 判定附近即可，一行级、加法分支。
次要项（可另开）：`PetNestCompanionRuntime.SpawnAsync` 在 `_handle = handle;` 之前
应先对旧 `_handle` 调一次 `PetNestCompanionSpawner.CleanupOnce`；
`PetNestPetProxyBridge.TryBorrowSeat` 的"他方占席"判定应改用
`!object.ReferenceEquals(current, null)`，或在 `_seatBorrowed` 已为 true 时不再覆盖 `_previousOccupant`。

**验证需求**

Windows 编译 + `python tools/run_guards.py --filter PetNest`；
实机：孵一只大兴兴崽 → 设为出战 → 进标准 BossRush，看随从是否留在场上；
Player.log 里若出现 `清理非 BossRush 源的大兴兴: goName=PetNest_Companion_...`
（该日志行在 `ModBehaviour.cs:1539`）即直接坐实。

---

### F-2（P2 Minor / `COMPAT`）：命名弹窗预填名可超出清洗上限，且没有"取消"出口——确认键会把崽静默改成截断名

**位置**
- `D:\code\ykf\BossRushMod\PetNest\PetNestRenameModal.cs:144-160`（只有"确认"与"用回血脉名"两个按钮）
- `D:\code\ykf\BossRushMod\PetNest\PetNestRenameModal.cs:179`（`_field.characterLimit = PetNestTuning.MaxPetNameLength`）
- `D:\code\ykf\BossRushMod\PetNest\PetNestRenameModal.cs:196`（`_field.text = PetNestService.GetPetDisplayName(pet);`）
- `D:\code\ykf\BossRushMod\PetNest\PetNestHatchService.cs:417-428`（`SanitizeName` 截断）

**证据**

```csharp
// PetNestRenameModal.cs:196
_field.text = PetNestService.GetPetDisplayName(pet);
```

`PetNestService.GetPetDisplayName`（`PetNest/PetNestService.cs:269-280`）在崽未改过名时
返回**血脉显示名**。而 `SanitizeName` 对超长名是**截断**而不是拒绝：

```csharp
// PetNestHatchService.cs:423-426
if (trimmed.Length > PetNestTuning.MaxPetNameLength) {
    trimmed = trimmed.Substring(0, PetNestTuning.MaxPetNameLength);
}
```

`MaxPetNameLength = 12`（`PetNestTuning.cs:136`），而三个自定义血脉的英文显示名
（`PetNestLineageCatalog.cs:198-212` 直接取各 Boss Config 的双语常量）分别是：

- `Integration/DragonKing/DragonKingConfig.cs:69` `"Skyburner Dragon Lord"` — **21 字符**
- `Integration/DragonDescendant/DragonDescendantConfig.cs:231` `"Dragon Descendant"` — **17 字符**
- `Integration/PhantomWitch/PhantomWitchConfig.cs:73` `"Phantom Witch"` — **13 字符**

`TMP_InputField.characterLimit` 只约束**用户键入**，程序化 `text` 赋值不截断，
所以玩家看到的是完整的 21 字符名字。

**失败场景**

英文环境，玩家在巢页误点"改名"（次动作按钮就在每张崽卡上，`PetNestUIPages.cs:242-247`）。
弹窗没有"取消"/"再想想"按钮（对照 `PetNestReleaseConfirmModal.cs:157-162` 是有的），
唯一两个出口是"就叫这个"和"用回血脉名"。玩家按最自然的"就叫这个"想原样退出 →
`TryRename(petId, "Skyburner Dragon Lord")` → 落档成 `"Skyburner Dr"`。
崽从"未命名"变成"已命名且名字被砍断"。

**玩家影响**

可见的名字损坏；可用"用回血脉名"恢复，故不是不可逆，定 P2。
弹窗缺取消出口本身也不符合"打开后总有关闭出口"的口径（虽然确认键事实上能关掉）。

**建议修复**

(a) 预填时先过一遍 `PetNestHatchService.SanitizeName`，让所见即所得；
(b) 补一个"再想想"按钮（形态照 `PetNestReleaseConfirmModal`）；
(c) 未改过名的崽预填空串（`Rename_Hint` 已说明"留空 = 恢复默认"）。建议 (b)+(c)。

**验证需求**

英文语言下开面板 → 巢页 → 任一自定义血脉崽 → 改名 → 直接按确认，看名字是否被截断。

---

### F-3（P2 Minor / `SAFE`）：基地闲逛崽的生成协程缺 `finally` 回滚，异常路径泄漏角色与 clone preset

**位置**：`D:\code\ykf\BossRushMod\PetNest\PetNestBaseIdleSpawner.cs:120-153`

**证据**

```csharp
PetNestCompanionHandle handle = await PetNestCompanionSpawner.CreateIsolatedAsync(...);
if (handle == null) return;
if (sceneGeneration != _sceneGeneration || CharacterMainControl.Main == null)
{ PetNestCompanionSpawner.CleanupOnce(handle); return; }

Vector3 spawnPos = player.transform.position + new Vector3(...);   // ← 可抛
...
_handles.Add(handle);
```

`player` 是 **await 之前**捕获的引用；await 之后只重验了 `CharacterMainControl.Main == null`，
没有重验 `player` 本身。若 `player` 在 await 期间被销毁而 `Main` 已指向新角色，
`player.transform` 抛 `MissingReferenceException` → 冒泡到 `SpawnAsync` 的 `catch`（`:110-113`）
→ **`handle` 既没进 `_handles` 也没被 `CleanupOnce`**，角色 GameObject 与 clone
`CharacterRandomPreset` 双双成为孤儿。

对照组：同系统的 `PetNestCompanionRuntime.SpawnAsync`
（`PetNest/PetNestCompanionRuntime.cs:231-239`）恰恰有这段兜底，注释还写明
"任何提前 return 都不能把半成品 handle 留在场上"。两边形态不一致。

**失败场景**

基地场景内玩家角色被重建（读档 / 传送 / 官方重生流程）恰好落在两次 await 之间。
概率低但可达；一旦发生，基地里会留下一只不受 `CleanupAll` 管理、也不会被
`RefreshForScene` 清掉的幽灵幼体（它仍带 `PetNestCompanionAgent`，会一直跟着玩家跑），
且 clone preset 泄漏到进程结束。

**玩家影响**：基地里出现清不掉的多余幼体；ScriptableObject 泄漏。

**建议修复**：把 `SpawnOneAsync` 主体包进
`try { ... _handles.Add(handle); handle = null; } finally { if (handle != null) PetNestCompanionSpawner.CleanupOnce(handle); }`，
并在 await 之后改用 `CharacterMainControl.Main` 而非预捕获的 `player` 取落点基准。

**验证需求**：静态确认，未运行验证。修复后跑 `PetNestCompanionLifecycleGuard`。

---

### F-4（P3 Note / `SAFE`）：`PetNestDebugProbe.ResetStaticCaches` 丢引用不回收，与自身注释和全系统惯例都不一致

**位置**：`D:\code\ykf\BossRushMod\PetNest\PetNestDebugProbe.cs:336-341`

**证据**

```csharp
internal static void ResetStaticCaches()
{
    _probeHandle = null;          // ← 没有 CleanupOnce
    _spawnInFlight = false;
    _lastStatus = "尚未召唤 PoC 幼体";
}
```

同文件 `DespawnProbeCompanion()`（`:146-162`）走的是
`PetNestCompanionSpawner.CleanupOnce(_probeHandle)`；文件头注释（`:13`）明写
"单例 handle，重复召唤先回收旧的（不留孤儿）"。
`ModBehaviour.cs:789` 在宿主销毁路径上调的正是 `ResetStaticCaches`。

**失败场景**：F3 探针幼体在场时 Mod 卸载 / 宿主重建 → clone `CharacterRandomPreset` 泄漏，
探针角色成为孤儿。（席位不会出问题：`ModBehaviour.cs:788` 已在其前无条件
`PetNestPetProxyBridge.ReleaseSeat()`。）

**玩家影响**：仅 dev 构建可达（F3 菜单由 `DevModeEnabled` 门控，
`DebugAndTools/F3DebugCheatMenu.cs:107-113`），玩家不可见。

**建议修复**：`ResetStaticCaches` 改为先 `DespawnProbeCompanion()` 再清标志。

---

### F-5（P3 Note / `SAFE`）：`PetNestDebugProbe.DespawnProbeCompanion` 无条件还席，会把真随从的借席一并还掉

**位置**：`D:\code\ykf\BossRushMod\PetNest\PetNestDebugProbe.cs:146-162`

**证据**

```csharp
internal static void DespawnProbeCompanion()
{
    PetNestPetProxyBridge.ReleaseSeat();      // ← 在 _probeHandle 判空之前，无条件执行
    if (_probeHandle != null) { ... }
}
```

`ReleaseSeat()` 操作的是全局单席状态（`PetNestPetProxyBridge.cs:205-250`），
不区分席位是探针借的还是正式随从借的。

**失败场景**：局内已有正式随从（已借席、已挂容量加成），开发者按 F3"回收 PoC 幼体" →
正式随从的席位被提前还回官方宠物，但 `RemoveCapacityBonus` 没被调用，
于是玩家身上留着 `PetCapcity` 加成而捡漏背包不再跟随随从——状态不一致。

**玩家影响**：仅 dev 构建可达。

**建议修复**：`if (_probeHandle == null) return;` 提到最前，或只在探针确实借过席时还席。

---

### F-6（P3 Note / `SAFE`）：`PetNestUIPages.LastFailureText` 是进程级静态残留，关面板/过图/切档都不清

**位置**：`D:\code\ykf\BossRushMod\PetNest\PetNestUIPages.cs:80`、`:97-100`；
消费点 `D:\code\ykf\BossRushMod\PetNest\PetNestUI.cs:214-217`

**证据**

全仓 grep（已执行）显示 `ClearFailure()` 唯一调用点是标签切换（`PetNestUI.cs:196`）。
`PetNestUIPages` 没有 `ResetStaticCaches`（`grep -c` 结果为 0），
`PetNestUI.ResetStaticCaches`（`:505-508`）只调 `Close()`，
`PetNestRuntimeModule.OnDestroy` / `ShutdownIfEnabledTurnedOff` 也都不碰它。

**失败场景**：玩家在巢页触发一次失败（如"巢已经满了"）→ 关面板 → 出图打一局 → 回基地重开面板，
顶部仍挂着上一次那条红色失败提示（`Refresh()` 无条件读它）。切存档槽同理。

**玩家影响**：误导性的过期报错。

**建议修复**：在 `PetNestUI.Open`（或 `Build`）开头、以及
`PetNestRuntimeModule.CloseAllInteractiveViewsForSceneChange` 里调一次
`PetNestUIPages.ClearFailure()`；并补一个被 `ModBehaviour` OnDestroy 调用的
`PetNestUIPages.ResetStaticCaches`。

---

### F-7（P3 Note / `SAFE`）：注释承诺的 BoomCar 二道防线在随从路径上永远不可能执行

**位置**：`D:\code\ykf\BossRushMod\PetNest\PetNestCompanionSpawner.cs:275-280` 与 `:123`

**证据**

`TryActivate` 的注释写"复用既有净化入口：移除自爆技能与 **BoomCar 特殊挂件**"，
但 `NeutralizeClonePreset` 在创建**之前**已把列表清空：

```csharp
// PetNestCompanionSpawner.cs:123
clone.specialAttachmentBases = new List<AISpecialAttachmentBase>();
```

而 `Utilities/ZombieSpawnSanitizer.cs:36-40` 的 BoomCar 分支恰恰以
`preset.specialAttachmentBases == null || Count <= 0` 作为早返条件：

```csharp
CharacterRandomPreset preset = character.characterPreset;
if (preset == null || preset.specialAttachmentBases == null || preset.specialAttachmentBases.Count <= 0)
{ return; }
```

随从的 `characterPreset` 就是那个被清空的 clone，因此该分支**恒早返**。
（自爆技能分支 `RemoveBossRushZombieSelfDestructionSkills` 读的是 `ai.skillInstance` /
`ai.skillPfb`，不受影响，仍然有效。）

**失败场景**：功能上无实害——挂件本就因为列表为空而不会被实例化。
问题是"二道防线"只存在于注释里；将来若有人改动 `NeutralizeClonePreset`
保留 `specialAttachmentBases`，会误以为 sanitizer 还兜着底。

**建议修复**：改注释说明实际由清空 `specialAttachmentBases` 承担，
或让 sanitizer 的 BoomCar 分支改为直接扫 `GetComponentsInChildren<AISpecialAttachmentBase>()`
而不以 preset 列表为前置条件。

---

## 二、未验证线索（需实机或 owner 确认）

1. **借席期间容量 Modifier 摘除与官方宠物背包的交互**：`PetNestCompanionRuntime.CleanupOnce`
   （`:356`）在回基地时摘掉 `PetCapcity` 加成，官方 `PetProxy.Update`
   （`鸭科夫源码/.../PetProxy.cs:122-125`）随后 1 秒内 `inventory.SetCapacity` 调小。
   静态看 `Inventory.SetCapacity`（`鸭科夫源码/ItemStatsSystem/Inventory.cs:686-695`）
   只改 `defaultCapacity` 并发事件，**不销毁 content**，理论上不丢物品；
   但"缩容后超出容量的格子在 UI / 存档读回时怎么处理"没有静态结论。
   需实机：借席局内把捡漏背包塞满 → 撤离回基地 → 确认物品还在。

2. **`PetNestExpeditionRevealView.PlayPending` 可能被 5 秒一次的基地维护打断重放**：
   `PetNestRuntimeModule.TickBaseMaintenance`（`:231-256`）在存在发奖欠账
   （`HasPendingRewardDebt`）时每 5 秒重跑一次并再调 `PlayPending()`，
   而 `PlayPending` 开头就 `Stop()` 掉正在播的那次（`PetNestExpeditionRevealView.cs:53`）。
   一次翻 ≥3 张牌（每张约 2.25 秒）且同时存在发奖欠账时，第 5 秒会把演出打断重来。
   需实机：制造一次发奖失败（背包满）+ 3 张以上待翻牌。

3. **随从不被官方距离休眠影响这一结论未见 IL 佐证**：`clone.setActiveByPlayerDistance = false`
   写在创建前（`PetNestCompanionSpawner.cs:105`），对应官方
   `CharacterMainControl.SetRelatedScene(int, bool)` 的第二参分支
   （`鸭科夫源码/.../CharacterMainControl.cs:1891-1902`）。
   但 `CreateCharacterAsync` 的 async 状态机在反编译源码里被吞掉，
   "它确实把 preset 这个字段透传给 `SetRelatedScene`"是按 AGENTS §14 的既有结论推断的。
   需实机：带崽跑离玩家 100m 以上（实测时需临时调大 `TeleportDistance` 或用 F3 探针，
   否则 >40m 传送兜底会先把崽拉回来）。

4. **英文以外语种的名字长度**：F-2 用三个自定义血脉的英文常量坐实了超限，
   官方血脉走 `nameKey.ToPlainText()`，各语种实际长度未逐一核对。

5. **`PetNestUIPages.BuildPetCard` 的 `Personality_` 空 id**：`T("Personality_" + (pet.personalityId ?? ""))`
   （`PetNestUIPages.cs:214`）。`personalityId` 只在孵化时赋值（`PetNestHatchService.cs:172`），
   未确认 `PetNestModels.Normalize` 是否给老档补默认值；若为空会在副标题显示裸 key
   `BossRush_PetNest_Personality_`。该文件属另一位同事范围，只在此登记接缝。

6. **滚动列表的空白区可能滚不动**：`PetNestUI.CreateScrollList`（`:249-281`）建的 viewport
   没有 `Graphic`（`RectMask2D` 不参与射线），鼠标滚轮只在悬停到卡片/文本上时才被
   `ScrollRect` 接到。内容满屏时无感，内容稀疏时玩家可能觉得"滚不动"。未实机确认。

7. **`CharacterOnDeadPatch.Prefix` 里 `ModeHDeathSuppressionRegistry.IsSuppressionArmed`
   没有包在 try 里**（`:30`），与相邻的 `IsModeGSuppressionArmed()` / PetNest 侧写法不一致。
   PetNest 侧自身是 no-throw 的，此项属 Patches/ModeH 范围，仅登记。

---

## 三、已核对且干净的关键面

**门禁**

- 19 个在审文件**全部**已登记进 `compile_official.bat`（第 761-783 行区段，逐个 grep 命中）→ 无 4.1 违规。
- `PetNest/` 目录内**零** `[HarmonyPatch]` / `new Harmony`（已 grep），符合"零新增补丁"契约。

**随从生成与阵营（AGENTS §4.5）**

- 中性化五件套 `hasSkill=false / exp=0 / hasSoul=false / team=Teams.player / dropBoxOnDead=false`
  （`PetNestCompanionSpawner.cs:89-97`）写在 `CreateCharacterAsync` **之前**且在 `try` 之外，
  异常不会跳过；五个字段在官方 `CharacterRandomPreset` 中均存在
  （`鸭科夫源码/.../CharacterRandomPreset.cs:346/205/196/179/167`）。
  伤害归一三项（`damageMultiplier`/`meleeDamageMultiplier`/`gunCritRateGain`）同样写在创建前，
  与 `PetNestCompanionNeutralizationGuard.py` 的断言逐条对上。
- `TryActivate` 取 `master.Team` 而非硬编码 `Teams.player`（`:271-272`），Mode E/F 独立阵营不被破坏。
- 敌对性安全网豁免已接线两处：`ModBehaviour.cs:1158`、`RandomEvents/RandomEventEffectsBridge_Spawn.cs:159`。
- 清场豁免已接线：`WavesArena/WavesArenaEnemyMaintenance.cs:53`（ForceKillAllEnemies）与 `:208`
  （ClearEnemiesForBossRush），且额外要求 `characterPreset.team` ∈ 敌对队伍——双保险。
  `ModeH/ModeHArenaIsolationLease.cs:242` 亦有豁免。
- 其余所有会 `Destroy` 角色的 mod 路径（`ModeD/ModeDWaves.cs`、`ModeE/ModeELifecycle.cs`、
  `ModeE/ModeEMerchant.cs`、`Utilities/EnemySpawnCore.cs`、`ZombieMode/ZombieModeSpawner.cs`）
  都只作用于各自 `onSpawned` 回调内的 `ctx.character`，够不到随从
  ——**唯一漏网的是 F-1 的大兴兴扫描**。
- `Utilities/OriginalCharacterIsolationHelper.Disable` 靠调用方传 `shouldSkipCharacter`，
  ModeH 那个调用方已传豁免。
- `WavesArenaEnemyMaintenance.cs:315` 的 `enemyCount = _cachedCharacters.Count` 只进日志，
  不参与波次判定。

**统计误计（图鉴 / 日报 / 击杀 / 掉落 / 成就）**

- `Integration/Codex/CodexKillCollector.cs:102` 用 `IsCompanionHealth(target)` 排除。
- `Integration/DailyReport/DailyReportStatsCollector.cs:98` 只统计
  `info.fromCharacter.IsMainCharacter` 的击杀，随从既不计为战果也不计为玩家阵亡。
- 各模式（D/E/F/G/H、丧尸）的击杀与赏金都挂 per-boss `OnDeadEvent` /
  `BeforeCharacterSpawnLootOnDead`，只登记自己 spawn 的角色。
- 官方经验 / 灵魂方块 / 掉落箱三条路径因中性化五件套天然不触发。
- `PetNestModeGate`（`:37-85`）只经公开只读门面判定（`IsModeGRunInProgressSafe` /
  `IsZombieModeActive` / `IsModeHRunInProgressSafe` / `IsActive` / `IsBossRushArenaActive` /
  `IsModeDActive` / `IsModeEActive` / `IsModeFActive`），八个门面全部存在且签名匹配；
  异常 fail-closed。

**死亡抑制 / 致死钳制**

- 第四消费者 `TryClampPetNestCompanion`（`BossLethalHealthProtectionPatch.cs:206-225`）
  接在逆鳞 / 龙王 / 龙裔三条之后，前三条命中即 `return`，**不会波及 Boss**；
  只在 `IsInsideHurt && value <= 0f` 时进入，随从平时不无敌。
- 官方 `Health.Hurt` 的两处 `if (CurrentHealth <= 0f)`（`鸭科夫源码/.../Health.cs:415/427`）
  都在 `CurrentHealth -= finalDamage` 之后，setter 补丁把值钳到 1 → 两个分支都不进；
  且官方 `Health` **没有** `Kill()` 或任何绕过 `Hurt` 的置死入口（全仓 grep 只有 `SetHealth`，
  唯一 mod 调用点 `DragonKingAbilityController_ChildProtection.cs:631` 打的是 Boss 自己）
  → "钳制没生效导致随从真死"在当前官方版本下不可达。
- `CharacterOnDeadPatch.cs:30-48` 的三张抑制表（ModeG / ModeH / PetNest）是 OR 并联，
  各自只对自己的身份返回 true，**不存在优先级或互斥问题**；
  PetNest 侧两个入口都 no-throw、fail-open=false。
- 短无敌有到期兜底（`PetNestDownedHandler.ReleaseStaleInvincibility:218-231`，
  `DownedInvincibleSeconds = 1.5f`），`Tick` 无待办时 O(1) 早返。
- 抑制身份表随组件同寿命：`PetNestCompanionAgent.OnDestroy → UnregisterIdentity`（`:308-316`），
  `_armedCount` 增减成对；`ModBehaviour.cs:796` 在宿主销毁路径清表
  → **不存在跨局残留导致下一局某角色不死**。

**AI 代理**

- 只写官方 `AICharacterController.leader`（`PetNestCompanionAgent.cs:268-273`），
  不写 `searchedEnemy`、不写 `leaderAI`、不成组（`group=null, isLeader=false`）
  → 不污染其他 AI 的索敌目标，也**不存在自写的目标选择逻辑**，因此不会主动攻击友军 / NPC / 商人。
- 维护节流 4Hz（`:236-245`），热路径无 `FindObjectsOfType`、无 `GetComponent`、无字符串分配。
- 战痕 stat key 用 `WalkSpeed / RunSpeed / MaxHealth`（`PetNestDownedHandler.cs:272`），
  三者在官方均为真实 stat（`CharacterMainControl.cs:3438/3444`、`Health.cs:526`）
  ——**没有踩 AGENTS §14 的 `MoveSpeed` 不存在的坑**。
- Modifier 全部 source-tagged（`CompanionPetModifierSource` / `CapacityModifierSource`），
  apply 前先 `RemoveAllModifiersFrom` 保幂等，随角色销毁自动消失；
  官方 API `Item.AddModifier(string, Modifier)`、`Item.RemoveAllModifiersFrom(object)`、
  `Modifier(ModifierType, float, object)` 均存在
  （`鸭科夫源码/ItemStatsSystem/Item.cs:1748/1730`、`Stats/Modifier.cs:66`）。
- 战痕封顶口径单点：`GetEffectiveScarPercent = Mathf.Max(sum, -0.10f)`，
  `PickScarStatKey` 的 `sum > cap` 判定对负数语义正确（`ScarModifierFraction=-0.02f`、
  `ScarModifierCapFraction=-0.10f`），与注释一致。

**宠物代理桥**

- 唯一反射写点 `AccessTools.Field(typeof(LevelManager), "petCharacter")` 目标确实存在：
  `鸭科夫源码/TeamSoda.Duckov.Core/LevelManager.cs:785 private CharacterMainControl petCharacter;`，
  且有字段类型校验（`:69-74`）与解析失败 fail-closed。
- 官方读者只有 `PetHouse.cs:42` 与 `PetProxy.cs:112/116`，均已考虑。
- 基地图一律不借席（`:140-145`），不与官方 `PetHouse` 的宠物驻留位冲突。
- `PetCapcity` 拼写与官方一致（`PetProxy.cs:122` 读的正是
  `CharacterMainControl.Main.PetCapcity`），且用 `ModifierType.Add` 而非百分比，
  符合官方 `Mathf.RoundToInt` 的格子数语义。
- 借席失败时**不挂**容量加成（`PetNestCompanionRuntime.cs:209-216`）——fail-closed 语义正确。
- 宿主销毁顺序正确：`ModBehaviour.cs:788` 先 `ReleaseSeat()` 再 `:790 ResetStaticCaches()`，
  不会把死引用留在官方席位上。（唯一缺口见 F-1 的 `_previousOccupant` 覆盖与 F-5。）

**基地闲逛崽**

- 只在基地生成、离开基地立刻 `CleanupAll`（`PetNestBaseIdleSpawner.cs:44-52`），
  且非基地分支同步推进 `_sceneGeneration`，in-flight 协程 await 后比对失败即回收。
- 上限 `MaxBaseIdleCompanions = 3`，分帧生成（每只间隔 0.35s），符合 4.12。
- 远征中的崽天然不入选（`CollectCandidates:78`）。
- **不写任何存档**（全文件无 Commit / Store 符号）→ 无存档写入风暴。
- 进入随从身份表是**文件头注释明确声明的设计**（`:11`"闲逛崽…不进致死钳制身份表以外的任何链路"），
  不作为缺陷记录。

**建筑与交互**

- `InitPetNestBuilding` 三重幂等：`petNestBuildingInjected` 标记 +
  `InjectPetNestBuildingData` 按 id 判重（`PetNestBuilder_DataEventsAndRuntime.cs:99-110`）+
  `CreatePetNestBuildingPrefab` 的 `petNestBuildingPrefabGO != null` 早返
  → **重复进基地不会双实例**。
- 建筑事件订阅有 `petNestBuildingEventsRegistered` 幂等 bool 与成对退订（`:208-263`），符合 4.6。
- 运行时交互体的官方私有字段初始化**正确**：宿主走 `PrepareGroupedInteractionOwner`
  （`PetNestInteractable.cs:58`）、三个子选项走 `GetOrCreateGroupList`（`:226`），
  都在 `base.Awake()` **之前**。这正是 commit `1c69f85` 修的那类 NRE；
  官方 `InteractableBase.Awake` 对 `otherInterablesInGroup` 的 foreach（`:180`）
  **不受 `interactableGroup` 门控**，所以子选项那一处也是必需的——两处都写了。
- 子选项 `MarkerActive=false` 不会被官方 `Start()` 的
  `MarkerActive = interactMarkerVisible`（`InteractableBase.cs:211`）翻回来
  （setter 写的就是同一个 backing 字段）。
- 全部继承成员（`interactCollider` / `interactMarkerOffset` / `overrideInteractName` /
  `_overrideInteractNameKey` / `InteractName` / `MarkerActive` / `interactableGroup`）
  在官方 `InteractableBase` 中均存在。
- `RestorePetNestBuildingsDelayed` 在 `try{...}finally{...}` 里只用 `yield break`
  （**不是** `yield return`），不触发 CS1631 / `IteratorYieldInCatchGuard`。
- `preparedPetNestBuildingInstanceIds` 按 active scene handle 失效（`:351-357`），跨场景不误判。
- `MagicBlendInitializationOrderPatch` 解决的是官方 MagicBlend 组件的初始化顺序问题；
  PetNest 建筑的运行时 prefab 走 `CreatePrimitive` 或 bundle 里的模型，自身不挂 MagicBlend，
  也不改官方材质初始化顺序，**该补丁的问题在 PetNest 建筑上不成立**
  （已 grep：`PetNest/` 无任何 MagicBlend 引用）。

**HUD / 面板 / 演出 / 弹窗（AGENTS §4.14）**

- `sortingOrder` 全部走 `BossRushUILayers` 常量（`PetNestCompanionHud=990` /
  `PetNestPanel=2100` / `PetNestModal=3150`），无魔法数字。
- 遮罩全部 `BossRushUI.CreateBackdrop`（用 `BossRushUIColors.Backdrop`），无第二套 `(0,0,0,0.7)`。
- 字体全部 `BossRushUI.ApplyGameFont`，无 `Resources.GetBuiltinResource<Font>`；文本全部 TMP。
- Canvas 全部走 `BossRushUI.CreateCanvasRoot`（内部已调
  `ZombieModeUIHelper.ConfigureCanvasScaler`，`Common/UI/BossRushUI.cs:340-354`）。
- 底图走 `BossRushUI.ApplyPanelSkin`。
- HUD：4Hz 节流 + struct 快照 + 只有变化才写 TMP text + 不在场整块隐藏 + `raycaster=false`，
  与 `tests/PetNestHudThrottleGuard.py` 的 7 条断言**逐条对上**，无漂移。
- 面板不每帧重建：只在 `Open` / 切页 / 按钮回调时 `Refresh()`，`ClearSpawned()` 追踪了全部生成物。
- 内容区与动作区都是 `ScrollRect + RectMask2D + VerticalLayoutGroup + ContentSizeFitter`，
  不再按 y 预算硬截断（远征页 9 个档位按钮全可达）。
- 关闭出口：主面板有"关闭"、放生弹窗有"再想想"、两个演出层有"跳过"
  （唯一例外见 F-2 的命名弹窗）。
- 静态实例在切图 / Shutdown 清空：`PetNestRuntimeModule.CloseAllInteractiveViewsForSceneChange`
  （`:380-394`）在 `OnSceneLoaded` 最前无条件调用，覆盖 UI / 两个弹窗 / 两个演出层 / HUD 共 6 处；
  `OnDestroy` 与 `ModBehaviour.cs:789-800` 再各清一遍。
- modal lease 成对：每个视图都 `Close()` + `OnDestroy()` 双保险并把 `_modalLease` 置 null
  防二次 Release；`PetNestUI.OnDestroy` 先关子弹窗再放自己的租约（顺序正确）。
  `ZombieModeUIHelper.ClaimModalInput` 是引用计数式，`Time.timeScale` 只在计数 0→1 / 1→0 时存取，
  嵌套（面板 → 弹窗 / 演出）不会错误还原 timeScale。
- 演出层幂等：两个 RevealView 都是 `Stop()` 后重建单例，`OnDestroy` 里 `if (_instance == this)`
  防误清；文件内**零** roll 符号（`Random.value` / `Random.Range`）与零写档符号
  （`Commit` / `Store` / `SavesSystem`），符合 `PetNestRevealIdempotencyGuard` 的核心不变式。
- `GetPendingReveals()`（`PetNestExpeditionService.cs:675-685`）返回**新建快照 List**，
  `MarkRevealed` 修改的是服务层的 `Records`，不是被遍历的那个列表
  → **无"遍历中修改集合"问题**；`SkipAll` 对已翻记录重复 `MarkRevealed` 会走
  `FindRecordById == null → AbortTransaction` 早返，幂等。
- 放生确认不可误触双放：`Confirm()` 里 `TryReleasePet` 之后立刻 `Close()`（销毁整个 canvas 与租约），
  同一帧内不可能有第二次点击事件；服务层再拒一次。
- 命名弹窗输入长度：`characterLimit = MaxPetNameLength`（键入侧）+ `SanitizeName` 去首尾空白 / 限长
  （数据侧），空名按"恢复默认名"处理——**不存在空名或超长名落档**
  （唯一缺口是 F-2 的预填侧不过清洗）。

**事件订阅与协程（AGENTS §4.6）**

- `Health.OnHurt`：`_hurtSubscribed` 防重复 + `ShutdownHurtSubscription` 成对退订，
  且只在随从在场窗口内订阅（`PetNestDownedHandler.cs:103-132`）。
- `PetNestProgressionService.EnsureKillTrackingSubscribed / ShutdownKillTracking` 与之成对，
  由同一个 `CleanupOnce` 收口。
- 建筑事件反射订阅 / 退订成对（见上）。
- 唯一协程 `RestorePetNestBuildingsDelayed` 由 `petNestRestoreCoroutine` 去重，
  `CleanupPetNestBuilding`（`PetNestBuilder.cs:156-160`）在卸载路径 `StopCoroutine`。
- 演出层协程随 GameObject 销毁停止，`Stop()` 在过图 / Shutdown 路径被无条件调用。

**调试后门**

- `PetNestDebugProbe` 的三个入口只从 F3 菜单进（`DebugAndTools/F3DebugCheatMenuUi.cs:617-619`），
  而 F3 热键由 `DevModeEnabled` 门控（`DebugAndTools/F3DebugCheatMenu.cs:107-113`），
  非 dev 时连菜单都不显示 → **不构成非 dev 构建的测试后门**。
- 探针无 `Update`、不订阅任何全局事件、无每帧日志。

**本地化**

- 面板 / 演出 / 弹窗用到的全部 `BossRush_PetNest_*` key 已逐个比对
  `Localization/PetNestLocalization.cs`：`SystemName` / `SystemDesc` / `Page_*` / `Rename_*` /
  `Release_*` / `CapacityMilestoneHint` / `Level_Adult` / `Soul*` / `Condense*` / `Personality_*` /
  `Dest_*` / `Risk_*` / `DeathRateLabel` / `ElementAffinity` / `Fail_*` **全部有注入**；
  目的地 id 与 `PetNestTuning.cs:240/242/244` 的三个常量一一对应。
- 建筑与四个交互点的 key 由 `InjectBuildingKeys()` 注入，且在建筑数据注入之前调用
  （`PetNestBuilder.cs:100`），符合 4.4。

**除零 / 越界 / 空引用 / `string.Format`**

- 全范围无 `string.Format`（都是 `+` 拼接），不存在参数错配。
- 除法只有 HUD 的 `CurrentHealth / MaxHealth`，前置 `MaxHealth > 0f` 判定
  （`PetNestCompanionHudView.cs:161`）。
- `ResolveNextCapacityMilestone`（`PetNestUIPages.cs:186-194`）在数组非空时才被调用
  （`:146` 已判 `milestones.Length > 0`），`milestones[Length-1]` 安全。
- `PetNestHatchRevealView.PlayRoutine` 的 `lineages[(i*7+3) % lineages.Count]`
  有 `lineages.Count > 0` 前置判定（`:172`）。

**事务成对**

- `PetNestCompanionRuntime.NotifyDowned`（`:296-309`）与 `PetNestDownedHandler.Tick`（`:178-201`）
  的 `BeginTransaction` 成功后都要么 `StageCommit` 要么 `AbortTransaction`；
  `PetNestPersistence.CommitTransaction:684` 在 Store 之前已把 `_activeTransaction` 置 null，
  Store 失败也不会留悬挂事务。两处顺序执行、不嵌套。

---

## 四、对前几轮已 Fixed 条目的复核

> 依据 `CODE_REVIEW_FINDINGS.md` / `FIX_TRACKER.md` 中与遗种巢相关的条目，
> 逐条回到当前代码验证"修复是否真的正确、有无回归"。只覆盖落在本次范围内的部分。

| 条目 | 结论 | 依据 |
| --- | --- | --- |
| **CR-2026-08-29-016**（关开关不清掉落追踪） | **修复有效，无回归** | `PetNestRuntimeModule.ShutdownIfEnabledTurnedOff` 的 `PetNestDropService.ClearAllTracking()`（`:347`）确实放在 `if (!_bootstrapped) return;`（`:348`）**之前**，两个关闭分支（`OnSceneLoaded:120`、`OnUpdate:173`）都经这里，是唯一咽喉点。 |
| **CR-2026-08-29-020**（`PetNestDropService._hooks` 慢泄漏） | **修复有效，无回归** | `OnSceneLoaded:114` 每次过图无条件 `ClearAllTracking()`，且排在 `IsEnabled` 判定之前。 |
| **对抗式审查 17 条 · 模式 H 清场误删友方 / 功能 NPC** | **修复有效** | `ModeH/ModeHArenaIsolationLease.cs:242` 保留了 `IsCompanionCharacter` 豁免（ModeH 本就被 `PetNestModeGate` 禁入，属双保险）。 |
| **计划完整性审计 · 「出战崽被无差别清场」经核实不成立** | **当时结论正确，但覆盖不完整** | `WavesArenaEnemyMaintenance` 两处（`:53`、`:208`）确实都有豁免，针对 `ForceKillAllEnemies` / `ClearEnemiesForBossRush` 的结论成立。**但当轮没有覆盖到第三条会销毁角色的路径 `TryCleanNonBossRushDaXingXing`**，至今无豁免，即本报告 **F-1**。 |
| **自定义崽 P1**（`ResolveCompanionSourcePreset` / 自定义血脉底模） | **修复有效，无回归** | `PetNestCompanionSpawner.cs:532-569` 三个自定义 key 各自解析到官方底模，`ResolveSourcePreset` 保持 fail-closed（不回落"同阵营强敌"，guard 亦断言无 `showName`）；`CreateIsolatedAsync:162` 的 `clone.nameKey = lineageKey` 身份戳与 `PetNestLineageCatalog.ResolveCustomDisplayName`（`:198-212`）配套，名条与战痕凶手名不会显示底模名。 |
| **自定义崽 P1 · 天赋 / 战痕真正挂上** | **修复有效** | `TryActivate:284 → ApplyPetModifiers`（`:431-463`）确实在激活路径上；`PetCapcity` 被显式跳过（`:448-451`）并改由 `PetNestPetProxyBridge.ApplyCapacityBonus` 挂到**玩家**身上——正是官方 `PetProxy.cs:122` 读玩家 stat 的正确口径。 |
| **`PetNestHatchRevealView` 天赋数值 100 倍单位错** | **修复有效** | `:217` 已改用 `PetNestUIPages.FormatModifierValue(t.value, t.percentage)`，与面板同一口径（`PetNestUIPages.cs:293-301` 内部 `value * 100f`）。 |
| **`SanitizeName` 改用 `PetNestTuning.MaxPetNameLength`** | **修复有效，但产生新接缝** | `PetNestHatchService.cs:423` 与 `PetNestRenameModal.cs:179` 确实共用同一常量，消除了"两套上限"。**但预填路径（`RenameModal:196`）没有过这个清洗**，导致 **F-2**。 |
| **`PetNestBaseIdleSpawner` 非基地分支同步推进 `_sceneGeneration`** | **修复有效** | `:44-51` 在 `CleanupAll()` 之后写 `_sceneGeneration = sceneGeneration`，in-flight 协程 await 后的 `:103` / `:136` 比对会失败并回收，闲逛崽不会落进战斗图。 |
| **`PetNestUI` 关闭面板时补翻牌（只挂用户点击路径）** | **修复有效** | `CloseAndPlayPendingReveal`（`:325-338`）只绑在"关闭"按钮上（`:175`），静态 `Close()` 不触发；且有 `IsBaseLevel` 门控。 |
| **PoC 闸门结论 · 借席不夺席** | **常规路径有效，两处新缺口** | 常规路径（`TryBorrowSeat` → `ReleaseSeat` 只在席位仍是自己时写回）正确。缺口一：F-1 的重生循环会把 `_previousOccupant` 覆盖成已销毁引用（`PetNestPetProxyBridge.cs:171` 的 `current != null` 对已销毁对象为 false，令"他方占席"判定失效）。缺口二：F-5 的探针无条件还席。 |
| **P3 组 ·「基地闲逛崽实为跟随玩家（含 >40m 传送）」** | **仍然存在，未修** | `PetNestBaseIdleSpawner.SpawnOneAsync:146` 走的仍是同一个 `TryActivate`，末尾 `agent.Bind(handle.Character, master)` 把玩家设为 leader，`TeleportDistance = 40f` 兜底照常生效。属已登记的 owner 取舍项，本报告不重复计为新 finding。 |
| **P3 组 ·「场景切换只停两个演出层」** | **已修复** | 现在 `OnSceneLoaded` 最前调 `CloseAllInteractiveViewsForSceneChange()`（`:105`），覆盖 UI + 两个弹窗 + 两个演出层 + HUD 共 6 处。 |
| **P3 组 ·「主面板 modal lease 极端时序可带进战斗图」** | **已收敛** | 同上：该清理在 `IsEnabled` 判定**之前**无条件执行，`PetNestUI.Close()` → `ReleaseLease()` 必然发生。 |
| **P3 组 ·「天赋 / 战痕 / 蛋 KV 展示裸英文 key」** | **仍然存在，未修** | `PetNestUIPages.DescribeTalents:278` 与 `DescribeScarEffect:334` 直接拼 `t.statKey` / `pair.Key`（`MaxHealth` / `WalkSpeed`…），`PetNestHatchRevealView.BuildDetailText:217` 同样。属已登记项，不重复计。 |
| **`PetNestSaveCoordinator` 的 `IsBaseLevelSafe` 场景闸** | 不在本次范围（持久化侧），未复核 | — |

---

## 附：本轮已执行的核对动作（可复现）

- `compile_official.bat` 逐文件 grep（19 / 19 命中，第 761-783 行区段）。
- 官方源码对照：`CharacterRandomPreset.cs`、`CharacterMainControl.cs`、`Health.cs`、
  `InteractableBase.cs`、`LevelManager.cs`、`PetProxy.cs`、`PetHouse.cs`、`PetAI.cs`、
  `ItemStatsSystem/Item.cs`、`ItemStatsSystem/Inventory.cs`、`ItemStatsSystem/Stats/Modifier.cs`、
  `Duckov/Utilities/SetActiveByPlayerDistance.cs`（grep 时已排除 `鸭科夫源码/` 混入 Mod 侧结果）。
- 全仓 grep：`IsCompanionCharacter` / `IsCompanionHealth` / `IsCompanionArmed` 的**全部** 20 个调用点；
  `FindObjectsOfType<CharacterMainControl>` 的全部 Mod 侧调用点；
  `Destroy(*.gameObject)` 的全部角色销毁路径；`LastFailureText` / `ClearFailure` 的全部读写点；
  `PetNestDownedHandler.*` / `PetNestCompanionRuntime.*` / `PetNestBaseIdleSpawner.*` /
  `PetNestDebugProbe.*` / `PetNestUIBridge.*` 的全部外部调用点；
  `PetNest/` 内 `[HarmonyPatch]` / `new Harmony`（零命中）。
- Guard 对照（读源码，未执行）：`PetNestCompanionNeutralizationGuard.py`、
  `PetNestHudThrottleGuard.py`、`PetNestLethalClampGuard.py` 的断言逐条与实现比对，**无漂移**。
- **未执行**：编译、`run_guards.bat`、任何写仓库的操作、任何实机验证。

---

## ==== AffixForge_BackMountain_Reforge ====

# 词缀锻造 / 竞技场后山 / 重铸 审查报告

> 进行中（边查边落盘）。范围自 `f9b83c0`：`Integration/AffixForge/`、`Integration/Reforge/`、
> `Integration/BackMountain/`、`Config/Config{AffixForge,BackMountain}.cs`、
> `Localization/{AffixForge,BackMountain}Localization.cs`、
> `Integration/Affinity/{NPCs/GoblinAffinityConfig,Systems/NPCShopSystem,AffinityUIManager}.cs`、
> `Integration/Utils/NPCInteractionGroupHelper.cs`、
> `tests/{AffixForgeInvariantGuard,BackMountainStructureGuard}.py`。
>
> 已跑（只读）：`python tests/AffixForgeInvariantGuard.py` PASS、
> `python tests/BackMountainStructureGuard.py` PASS、`python tests/ModConfigOptionChangeGuard.py` PASS。
> 未运行游戏，凡运行时结论均标「静态确认，未运行验证」。

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1：焚心椒的「换弹更利索」用了不存在的 stat key，该效果在游戏里完全不存在

- **严重级**：P1 Major（功能未接线 + 玩家可见的物品描述与实现不符）
- **兼容分类**：`COMPAT`
- **位置**：
  - `Integration/BackMountain/RaidMealService.cs:179`
    `AddModifier(main, ZombieModeStatNames.ReloadSpeedMultiplier, EmberChiliReloadBonus);`
  - 常量定义 `ZombieMode/ZombieModeTuning.cs:22`
    `public const string ReloadSpeedMultiplier = "ReloadSpeedMultiplier";`
  - 描述承诺 `Integration/BackMountain/BackMountainItems.cs:118-120`
    「出击前吃下，**下一局**跑得更快、**换弹更利索**」/「quicker on the feet **and the reload**」
- **证据**：
  - `grep -rn "ReloadSpeedMultiplier" 鸭科夫源码/ .codex_tmp/core_decomp/` → **零命中**。
    官方真正的换弹 stat 是 `ReloadSpeedGain`（`鸭科夫源码/TeamSoda.Duckov.Core/CharacterMainControl.cs:3588`
    `private static int reloadSpeedGainHash = "ReloadSpeedGain".GetHashCode();`，
    `ItemAgent_Gun.cs:702` 读它），词缀锻造的「迅手」用的就是这个正确 key
    （`Integration/AffixForge/AffixBuffFactory.cs:66`）。
  - 落地路径静默：`Common/Stats/RuntimeStatModifierTracker.cs:55-59`
    `Stat stat = character.CharacterItem.GetStat(statName); if (stat == null) return false;`
    —— **连一行日志都没有**。官方 `StatCollection.GetStat(string)`
    （`鸭科夫源码/ItemStatsSystem/StatCollection.cs:57-66`）对不存在的 key 返回 null，不会新建。
  - 全仓 `ReloadSpeedMultiplier` 只有 3 个命中，全是消费端（本处 + `ZombieMode/ZombieModeRewards.cs:22`
    + 常量定义），**没有任何地方给角色加过这个 stat**。
- **失败场景**：玩家在后山菜地种出焚心椒 → 基地吃下 → 进任意一局。
  移速加成生效（WalkSpeed/RunSpeed 是真 stat），**换弹速度一点没变**；
  无报错、无日志、编译绿、guard 绿。
- **玩家/维护影响**：焚心椒是三种出击餐里唯一的双效果餐，一半效果不存在；
  与 CR-2026-08-30 修掉的「幽影蘑菇描述说减伤、实现给生命上限」是同一类缺陷的漏网之鱼。
  **同一个错误 key 在 `ZombieMode/ZombieModeRewards.cs:22` 也有一份**（丧尸模式换弹奖励，
  不在本次范围，但同源同症，建议一并核）。
- **建议修复**：`ZombieModeStatNames.ReloadSpeedMultiplier` 的字面值改成 `"ReloadSpeedGain"`
  （或新增一个正确常量供出击餐使用）。注意 `ReloadSpeedGain` 在官方是**加点型**读法
  （`ItemAgent_Gun.cs:77` 用 `1f + CharacterReloadSpeedGain` 做除数），
  而 `RuntimeStatModifierTracker.TryAdd` 写死 `PercentageAdd`，需按「迅手」的做法确认量级
  （AffixForge 为此专门自建了带 `ModifierType` 参数的 `AffixStatModifierApplier`，可直接复用）。
- **验证需求**：编译 + guard + 实机（吃焚心椒进局，对比换弹动画时长）。

### F-3：展示柜的 MaxHealth 加成挂在官方满血治疗**之后**，玩家每次进局都不满血且加成在开局等于零

- **严重级**：P2 Minor（玩家可见、稳定复现；数值不致命但与建筑描述承诺不符）
- **兼容分类**：`COMPAT`
- **位置**：
  - `Integration/BackMountain/BackMountainRuntimeModule.cs:230`
    `LevelManager.OnAfterLevelInitialized += HandleLevelReady;`
  - `Integration/BackMountain/ShowcaseService.cs:222-223`
    `RuntimeStatModifierTracker.TryAdd(main, ZombieModeStatNames.MaxHealth, total, ...)`
  - 失准注释：`Integration/BackMountain/BackMountainRuntimeModule.cs:22-23`
    「（官方 BuildingEffect 与本 mod 的 SetBonusManager / DragonSetBonus 用的都是这个时机）」
- **证据**（官方 `LevelManager.InitLevel` 的固定顺序，见
  `.codex_tmp/core_decomp/LevelManager.cs:430-453`，与 `鸭科夫源码/` 版本一致）：
  ```
  LevelManager.OnLevelInitialized?.Invoke();              // ← 官方 BuildingEffect 挂加成在这里
  float health = SavesSystem.Load<float>("MainCharacterHealth");
  mainCharacter.Health.SetHealth(health);                 // SetHealth = Min(MaxHealth, v)
  if (IsBaseLevel || isNewRaidLevel)
      mainCharacter.AddHealth(mainCharacter.Health.MaxHealth);   // ← 满血治疗
  ...
  LevelManager.OnAfterLevelInitialized?.Invoke();         // ← 后山展示柜加成挂在这里
  ```
  官方 `Duckov/Buildings/BuildingEffect.cs:16` 订阅的是 **`OnLevelInitialized`**，
  **不是** `OnAfterLevelInitialized`（两份反编译一致）。注释所称的「同一时机」不成立。
  `Health.SetHealth` / `AddHealth` 都是 `Mathf.Min(MaxHealth, …)`
  （`鸭科夫源码/TeamSoda.Duckov.Core/Health.cs:496-505`），且 `MaxHealth` 上调**不会**带动 CurrentHealth。
- **失败场景**：
  1. 玩家登记满 8 格（`ShowcaseService.cs:52,55` 最高约 +21%）。
  2. 进任意竞技场：满血治疗按**基础** MaxHealth 执行 → 当前血 = 基础上限；
     随后 `OnAfterLevelInitialized` 才把 +21% 挂上 → 上限变 1.21×，当前血仍是 1.0×。
  3. 结果：**开局血条只有约 83%，展示柜加成在出生那一刻等价于零**，
     只有找到治疗品才能兑现。每次进局、每次回基地都稳定复现。
  4. 次生：非基地且非 `isNewRaidLevel` 的关卡初始化（局中读档等）会跳过满血治疗，
     此时 `SetHealth(savedHealth)` 在加成尚未挂上时执行，被 `Min(基础上限, v)` 截掉，
     玩家因展示柜而多出来的那部分血被静默削掉。
- **玩家/维护影响**：建筑描述承诺「登记得越多，你越经打」，实际体验是「登记得越多，
  进局血条缺得越多」。这也让 CR-2026-08-31-002 的修复只兑现了一半——
  加成确实存在了（Stat 上有 Modifier），但在最需要它的开局瞬间不起作用。
- **建议修复**：把**只影响上限的加成**（展示柜 MaxHealth）改挂 `LevelManager.OnLevelInitialized`
  （与官方 BuildingEffect 对齐，主角在该事件前已创建完毕，见 InitLevel 第 396-420 行的
  `await CreateMainCharacterAsync`，因此 `CharacterMainControl.Main` 已可用，
  不会退回 CR-2026-08-31-002 的时序坑）；或保留现有时机但在挂上之后按比例补血
  （类似 `AffixRuntimeService_Effects.ClampCurrentHealthToMax` 的反向操作）。
  出击餐的伤害/移速类加成不受此影响，可留在 `OnAfterLevelInitialized`。
  **同时修正 `BackMountainRuntimeModule.cs:22-23` 的注释**——官方 BuildingEffect 用的不是这个事件。
  另注：`Integration/Bonus/SetBonusManager.cs:77` 与 `Integration/Bonus/DragonSetBonus.cs:108`
  也订阅 `OnAfterLevelInitialized`，若它们也挂 MaxHealth 则同源同症（不在本次范围，建议一并核）。
- **验证需求**：编译 + guard + 实机（登记若干高品质战利品后进局，看血条是否满）。

### F-2：出击餐给 `"MoveSpeed"` 挂 Modifier —— 该 stat 不存在，调用被静默丢弃

- **严重级**：P2 Minor（无玩法后果，但直接违反 AGENTS 第 14 节明文教训，且是误导性死代码）
- **兼容分类**：`SAFE`
- **位置**：`Integration/BackMountain/RaidMealService.cs:176`
  `AddModifier(main, ZombieModeStatNames.MoveSpeed, EmberChiliSpeedBonus);`
- **证据**：AGENTS.md 第 14 节：「角色没有 `MoveSpeed` 这个 stat……
  `"MoveSpeed"` 只是 Animator 参数名」。官方源码里 `"MoveSpeed"` 只出现在
  `CharacterAnimationControl.cs:228` / `_MagicBlend.cs:171` / `_Moto.cs:203` 的
  `Animator.StringToHash`，**没有任何 Stat 用它**。本仓库自己也在
  `ModeF/ModeFBloodfire.cs:97` 写下过同一条注释（"MoveSpeed" 只是 Animator 参数名，不是 stat key——曾挂在这里）。
- **失败场景**：无玩法后果——紧邻的 `RunSpeed` / `WalkSpeed`（:177-178）是真 stat，
  移速加成照常生效。但这一行是永远返回 false 的死调用。
- **建议修复**：删掉该行（`ModeF` 已有先例），或在 `ZombieModeStatNames.MoveSpeed`
  上加显式注释说明它只对 Animator 有效。
- **验证需求**：编译 + guard。

## 二、未验证线索（需实机或 owner 确认）

（填写中）

## 三、已核对且干净的关键面

### 3.1 AFX_ / RF_ 前缀双向互斥（成立）

- 重铸 → 词缀方向：`Integration/Reforge/ReforgeSystem.cs:535-545` 的
  `IsRuntimeTrackingVariableKey` 已加 `key.StartsWith("AFX_")`，是
  `IsPropertySupportedForReforge`（:564）唯一咽喉；`IsVariableEligibleForReforge`（:607）
  另有 `DataType == CustomDataType.Float` 硬限，而 AFX_ 全是 Int/String/Bool，
  **两道独立防线**。`ReforgeDataPersistenceCleanup.CleanupUnsupportedReforgeData`
  只删 `TryParseReforgeDataKey` 能解析的 `RF_*` 条目，不碰 AFX_。
- 词缀 → 重铸方向：`AffixItemData` 全部写入都经 `SlotKey/LockKey/NameKey/KEY_VERSION/KEY_CAPACITY`，
  `StripAll`（:512）按 `IsAffixVariableKey`（AFX_ 前缀）过滤，不会命中 RF_。
- 静态确认，未运行验证。

### 3.2 词缀运行时 stat key 全部经官方源码核实（无 `MoveSpeed`）

`AffixBuffFactory.cs:65-67` 与 `AffixRuntimeService_Effects.cs:89-95` 用到的 8 个 key
逐个在 `鸭科夫源码/` 找到对应 hash 常量：
`BodyArmor`/`HeadArmor`（`Health.cs:580,583`）、`ReloadSpeedGain`/`GunShootSpeedMultiplier`/`Moveability`
（`CharacterMainControl.cs:3588,3585,3462`）、`MaxHealth`、`GunDamageMultiplier`/`MeleeDamageMultiplier`、
`GunCritRateGain`/`MeleeCritRateGain`。**没有出现 `MoveSpeed`**，狂潮的移动增益用的是
`Moveability`，符合第 14 节教训。

### 3.3 Buff ID 段不重叠

`AffixBuffFactory.cs:41-45` 三个基址（500601/500604/500607）看似相邻重叠，实际 ID = 基址 + tier(1..3)，
占用 500602-500604 / 500605-500607 / 500608-500610，互不冲突，与注释一致。

### 3.4 官方 API 签名核对

`ExplosionManager.CreateExplosion(Vector3,float,DamageInfo,ExplosionFxTypes,float,bool)`
（`ExplosionManager.cs:17`）、`Health.CurrentHealth` 的 public setter 只发 `OnHealthChange`
（`Health.cs:98-110`，确认死契流失不会走致死分支）、`Health.AddHealth`/`SetHealth`（:496/:502）、
`LevelManager.IsBaseLevel`（:77）、`CharacterMainControl.AddBuff(Buff,CharacterMainControl,int)`（:2199）、
`ModifierType.PercentageMultiply`（`ItemStatsSystem/Stat.cs:185`）均存在且签名匹配。

### 3.5 ModConfig 白名单

`_AffixForgeEnabled` 与 `_BackMountainEnabled` **有意**不在
`Config/ConfigModConfigKeys.cs:29-51` 白名单里——两者都是「内容系统恒开」开关，
已登记进 `tests/ModConfigOptionChangeGuard.py` 的 `CONTENT_SYSTEM_SWITCHES`（:46-55），
由 `ForceContentSystemSwitchesOn()`（`Config/ConfigContentSystemSwitches.cs:40,43`）抹平老档 false。
旋钮 `_BackMountainUnlockAll` **在**白名单（:51），热更新分支可达。guard PASS。

## 四、对前几轮已 Fixed 条目的复核

### CR-2026-09-03-015（焚天龙皇不掉词缀熔石）—— 修复正确，三处并联齐备

- 生成侧：`Integration/DragonKing/DragonKingBoss.cs:375`
  `AffixForgeStoneDropService.TryTrack(this, character);`，紧随 `:360` 的
  `character.BeforeCharacterSpawnLootOnDead += lootHandler;`（同一 try 块内，中间只有遗种巢并联）。
- 离场清理：`:165` `ClearTracking(character)`，与 `:150` 的手动 `-= lootHandler` 同段。
- 死亡清理：`:641` `ClearTracking(deadKing)`，与 `:624` 的 `-= lootHandler` 同段。
- 退订成对且幂等：`AffixForgeStoneDropService.ClearTracking`（:103-125）先 `TryGetValue`
  再 `Remove`，未追踪时直接返回；`TryTrack`（:56）第一步就调 `ClearTracking` 退旧，
  重复调用不会叠加订阅。开关关闭时 `TryTrack` 在 `:59` 早返（dormant 契约保持）。
- **幽灵女巫与龙裔遗族确实已通**：两者走共享漏斗
  `PhantomWitchBoss.cs:352` / `DragonDescendantBoss.cs:220` →
  `LootAndRewards.RegisterBossRandomLootTracking`，该函数 `:342` 并联 `TryTrack`，
  `ClearBossRandomLootTracking` `:387` 并联 `ClearTracking`。
- 跨局残留另有三道兜底：`Integration/IntegrationRuntimeHooks.cs:15` 每次场景加载
  `ClearAllTracking()`；`TryTrack` 内 `PruneDestroyedEntries()`；
  `AffixForgeHostCleanup.cs:28` 宿主销毁 `ResetStaticCaches()`。
- guard `check_forge_stone_drop_wiring`（未提交改动，`tests/AffixForgeInvariantGuard.py:65-102`）
  三条断言与实际代码一致，已实跑 PASS。
- 结论：**修复正确，未见回归**。实机 smoke 仍待做（8% 概率，需刷多次龙王）。

---

## ==== Codex_DailyReport ====

# 鸭皇图鉴 Codex + 鸭科夫日报 DailyReport 审查报告

> 状态：进行中（边查边落盘）。范围：`Integration/Codex/`(13)、`Integration/DailyReport/`(16)、
> `Config/ConfigCodex.cs`、`Config/ConfigDailyReport.cs`、`Localization/CodexLocalization.cs`、
> `Localization/DailyReportLocalization.cs`、`Integration/WikiBookItem.cs`、`Integration/UI/ImageViewerUI.cs`、
> `Achievement/` 分类枚举改动、三个 guard。
> 基线 commit `f9b83c0` —— 两个子系统的 29 个 `.cs` **全部是新增文件**（`git diff --stat f9b83c0` 确认），
> 因此全部读完整源码；`Achievement/`、`WikiBookItem.cs`、`ImageViewerUI.cs` 走 diff + 上下文。

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

（填充中 —— 已确认条目见下，编号最终会重排）

### F-1：战役终章「冠军之影」进图鉴后显示原始本地化 key

- 严重级：**P2 Minor**
- 兼容分类：COMPAT
- 位置：
  - `Campaign/CampaignFinalBoss.cs:484-490`
  - `Integration/Codex/CodexKillCollector.cs:271-274`（`ResolveBossKey` 取 `preset.nameKey`）
  - `Integration/Codex/CodexBossCatalog.cs:241-267`（`AddHistoricalEntries` 用存档快照名，**不查本地化表**）
  - `Integration/Codex/CodexBossCatalog.cs:370-380`（`ResolveDisplayName` 查不到目录就返回 key 本身）
- 证据：

  战役终章把克隆出来的 preset 改名：
  ```csharp
  string nameKey = "BossRush_Campaign_FinalBoss_Name";
  LocalizationHelper.InjectLocalization(nameKey, L10n.T("冠军之影", "Shadow of the Champion"));
  if (boss.characterPreset != null) boss.characterPreset.nameKey = nameKey;
  ```
  该 key **不属于**图鉴目录的任何一步来源：不在 `GetFilteredEnemyPresets()` 官方池
  （幽灵女巫在池里用的是 `PhantomWitchConfig.BossNameKey`）、不在 `CustomBossKeys` 三常量、
  不是 `zombie_boss_*`。因此击杀时：
  `RecordKill(key, CodexBossCatalog.ResolveDisplayName(key))` → `TryGet` miss → 回落 `key` 本身
  → 存档条目 `n = "BossRush_Campaign_FinalBoss_Name"`。下次目录重建走 `AddHistoricalEntries`，
  `entry.DisplayName = stored.DisplayName`（同一串 key），**全链路没有任何一处 `L10n.T`**。
  `CodexView_Grid.ResolveCardName` 直接用 `info.DisplayName` 渲染。
- 失败场景：打完鸭王征程终章 →（下次会话或任一次目录 Invalidate 后）打开图鉴 →
  出现一张卡片，名字是 `BossRush_Campaign_FinalBoss_Name`，分类标「历史记录」。
  注意它**不带星号**，不会被"游戏内查 `*BossRush_*` 占位符"的常规自检发现。
- 玩家/维护影响：图鉴是收藏册，玩家最容易截图分享的界面上出现一条内部 key。
  另外它与幽灵女巫是两条独立条目，击杀终章 Boss **不计入**幽灵女巫的击杀数（是否符合产品意图需 owner 拍板）。
- 建议修复（三选一，owner 定）：
  1. `CodexBossCatalog.AddHistoricalEntries` 在快照名等于 key 时补一次 `L10n.T(key)` 并剔除带 `*` 的结果
     （最小改动，顺带修好所有"落档时目录还没建好"的条目）；
  2. 把 `BossRush_Campaign_FinalBoss_Name` 显式登记进 `CustomBossKeys` 并给 `ResolveCustomDisplayName` 加分支；
  3. 若产品上不希望终章变体单独占格，则在 `ApplyCampaignFinalBossVariant` 里改 `showName` 而不改 `nameKey`。
- 验证需求：编译 + 实机（跑完战役终章，回基地开图鉴）。

### F-2：`UnlockedCount` 与目录 `Count` 口径不一致，「全录」成就存在提前解锁窗口

- 严重级：**P3 Note**（触发窗口窄，但会白送 100 万现金成就）
- 兼容分类：COMPAT
- 位置：
  - `Integration/Codex/CodexMilestones.cs:114-118`
  - `Integration/Codex/CodexModels.cs:120-133`（`UnlockedCount` 数的是存档全部条目）
  - `Integration/Codex/CodexBossCatalog.cs:88-92`（`Count` 数的是目录条目）
- 证据：
  ```csharp
  int total = CodexBossCatalog.Count;
  if (total > 0 && unlocked >= total) TryUnlock(CodexTuning.AchievementAll);
  ```
  目录只在 `Invalidate()`（切档 / BossFilter 变化 / preset 刷新）之后才把新出现的 key 并进来
  （`AddHistoricalEntries` 第 4 步）。而 `RecordKill` 是**先写存档 DTO 再当场调 `Evaluate`**，
  中间没有任何 `Invalidate`。于是「击杀一个目录里没有的 Boss」那一瞬间
  `unlocked = N+1`、`total = N`（目录未含它）。
  必然可达的实例就是 F-1 的冠军之影；理论上还包括任何不在 `enemyPresets` 池里的 Boss。
- 失败场景：玩家已收集 `total-1` 个目录条目 → 打战役终章击杀冠军之影 →
  `unlocked` 达到 `total` → `codex_all`（`MilestoneCashAll = 1000000`）当场发放，
  而图鉴里仍有一格是灰的。
- 玩家/维护影响：经济面（100 万现金）+ 成就语义破口。
- 建议修复：`EvaluateUnlockCount` 的分子改为「与目录取交集后的已解锁数」，
  或在 `RecordKill` 里对目录 miss 的 key 先 `CodexBossCatalog.Invalidate()`（代价是下次开面板全量重建）。
- 验证需求：静态确认，未运行验证。

### F-3：面板打开状态下宿主销毁会把玩家输入永久锁死（`ResetStaticCaches` 不走 `Close`）

- 严重级：**P3 Note**（需 owner 判断触发面是否可达）
- 兼容分类：COMPAT
- 位置：
  - `Integration/Codex/CodexView.cs:96-115`（`ResetStaticCaches` 直接 `Destroy(go)`）
  - `Integration/Codex/CodexView.cs:180-196`（`OnDestroy` 只 `HideDetail()` + `ClearCards()`，不调 `Close()`）
  - `Integration/Codex/CodexView.cs:222-279`（`Open` 里 `InputManager.DisableInput(gameObject)`，
    唯一的 `InputManager.ActiveInput(gameObject)` 在 `Close()` 里）
  - 调用链：`ModBehaviour.cs:816` → `CleanupCodexRuntimeOnDestroy()`（`CodexBookItem.cs:505`）→ `CodexView.ResetStaticCaches()`
- 证据：`OnDestroy` 中没有 `Close()` 也没有 `InputManager.ActiveInput`，
  被 `DisableInput` 登记的 blocker 随 GameObject 一起消失。
- 失败场景：图鉴面板开着时 Mod 被卸载 / `ModBehaviour` 被销毁（热重载、Mod 管理器禁用）。
- 缓解事实：过图路径已由 `CodexRuntimeModule.OnSceneLoaded → CloseViewIfOpen()` 覆盖，
  正常游玩几乎撞不到；退出游戏时无影响。
- 建议修复：`OnDestroy` 开头补一次 `Close()`（幂等），或在 `ResetStaticCaches` 里销毁前先 `Close()`。
- 验证需求：需核对官方 `InputManager.DisableInput/ActiveInput` 的 blocker 集合语义（见第二节未验证线索）。

## 二、未验证线索（需实机或 owner 确认）

（填充中）

## 三、已核对且干净的关键面

### Codex（截至本节）

- **编译清单**：13 个 Codex `.cs` + `Config/ConfigCodex.cs` + `Localization/CodexLocalization.cs`
  全部命中 `compile_official.bat`（逐个 grep 计数 = 1）。
- **事件订阅幂等 + 退订**（AGENTS 4.6）：`CodexKillCollector.OnGlobalDead/OnGlobalHurt` 是命名静态方法，
  `Utilities/PlayerLifecycleRuntimeHooks.cs:18-19` 订阅、`:36-37` 对称退订，无 lambda。
  `CodexPersistence` 三个 `SavesSystem` 事件走 `_subscribed` 布尔幂等 + `ShutdownSubscription` 成对退订。
- **模式门（CR-2026-09-03-004 修复面）**：`CodexKillCollector.cs:93`（OnDead）与 `:165`（OnHurt）
  都在 `info.fromCharacter` 判定**之前**加了 `ModBehaviour.IsModeHRunInProgressSafe()`，
  且 OnHurt 也挡了开表（避免计时表只进不出）。位置正确、覆盖完整。
- **遗种巢随从**：`CodexKillCollector.cs:102` 有 `PetNestCompanionAgent.IsCompanionHealth(target)` 排除；
  另有 `victim.Team == Teams.player` 二重网。
- **随机事件乱入 Boss**：`RandomEventEffectsBridge_Spawn` 的 preset 来自
  `GetFilteredEnemyPresets()`（同一张池），key 天然在目录里，计入是正确行为。
- **龙裔复活不重复计数**：复活在 `Health.Hurt` 进死亡分支**之前**拦截
  （`DragonDescendantAbilities_ResurrectionAndPhase.cs:60-66`），`OnDead` 不会二次派发；
  即使派发也被 `_countedInstances`（`CharacterMainControl.GetInstanceID()`）挡住。
- **自定义 Boss preset 不被污染**：幽灵女巫在 `PhantomWitchBoss.cs:218` 对
  `characterPreset` 做了 `Instantiate` 副本，战役改 `nameKey` 只影响那一只（F-1 的前提成立，
  但**不会**把所有女巫击杀带偏）。
- **落盘避开战斗帧**：物理 `SavesSystem.SaveFile` 唯一出处是
  `CodexSaveCoordinator.FlushBatch:179`，非基地一律 deferred 且 `Tick()` 在非基地
  **不消耗重试预算**（`:91`），设计正确。
- **写屏障 fail-closed**：`CodexPersistence.LoadOrInitCore` 对
  `KeyExisits` 抛错 / `Load` 抛错 / `schemaVersion != 1` / `Decode` 返回 null 四种情况
  一律 `_writeBarrier = true` 只读不覆盖；`Store` 与 `FlushPending` 都尊重屏障。
- **跨槽泄漏防线（对照 CR-2026-08-29-017）**：图鉴额外加了 `_cacheSlot` 槽位烙印
  （`CodexPersistence.cs:41`），`LoadOrInitCore:231-237` 在**读取侧**比对
  `SavesSystem.CurrentSlot`，即使 `OnSetFile` 已退订也能自失效；
  另有 `DiscardCacheOnResubscribe`（`:117-130`）覆盖「同槽删档重开」（CurrentSlot 不变）的盲区。
  这是日报 017 那条 finding 的正确进化版。
- **Codec 边界**：`SimpleJsonHelper.EscapeString` 转义 `\ " \n \r \t \b \f`；
  `ForEachObject` 的 `{}` 深度扫描会跳过字符串内容，因此显示名里的 `{}[]` 不会破坏对象边界；
  `entries` 是最后一个字段且是唯一数组，`FindArrayBounds`（第一个 `[` / 最后一个 `]`）成立；
  六个字段名 `k/n/kills/first/fm/fast` 两两不构成带引号前缀关系（逐对核过）；
  `Decode` 对 `Kills/FirstKillTicks/FastestKillSeconds` 负值全部钳回、
  `MaxEntries=256` 双侧截断（Encode `:70` / Decode `:134`）。
- **物品接线（500061）**：
  `ItemFactory` 配置器 `Integration/Items/ItemContentRegistry.cs:32` 已注册；
  `BossRushDynamicItemRegistry.cs:374-375` 已登记 FallbackLoader（避免重启退化 `FallbackItem`）；
  `DisplayNameRaw = "BossRush_CodexBook"` 由 `CodexLocalization.InjectBookKeys()` 注入，
  接线点 `Integration/BossRushIntegration_StartAndScene.cs:49 CodexLocalization.Inject()`，
  且注入了 `Item_{id}` / `{id}` / 中英字面量四组别名；
  商店注入 `TryInjectCodexBookIntoShop` 有真实调用点
  （`Integration/BossRushIntegration.cs:437` 与 `IntegrationDeferredBootstrap.cs:258`），
  幂等（先扫 `shop.entries` 找同 TypeID 再决定是否插入）；
  库存持久化 `OnCollectSaveData_CodexBookStock` / `OnSetFile_CodexBookStock`
  在 `BossRushIntegration_StartAndScene.cs:102-103` 订阅、`:139-140` 退订，成对；
  使用不消耗（`MaxDurability/Durability = 999f` + `OnUse` 不 Consume）；
  `priceFactor = 1f` 已是修好后的值（旧实现 1/rawValue 把书压到 1 金）；
  已挂 `NotSellable` tag。
- **立绘缓存**：`EnsureBundleLoaded` 每 runtime 至多一次 `LoadFromFile`（`_loadAttempted` 闩）、
  先复用 `GetAllLoadedAssetBundles()` 里的同名实例；缺包 fail-open 只走占位链；
  `Unload()` 顺序正确（先 Destroy 自造 Sprite/Texture 再 `bundle.Unload(true)`）且幂等；
  raw PNG fallback 由编译期常量 `AllowDevRawPngFallback = false` 门控。
  加载时机是首次 `GetPortrait`（面板打开时），符合 AGENTS 4.12 按需门控。
- **ModConfig**：`codexEnabled` / `dailyReportEnabled` 按「内容系统恒开」口径处理正确 ——
  `RegisterCodexModConfigOption` / `RegisterDailyReportModConfigOption` 零调用点（不进 UI），
  `ForceContentSystemSwitchesOn()`（`Config/ConfigContentSystemSwitches.cs:38-39`）抹平老档 false，
  两个键都**不在** `ConfigModConfigKeys.cs` 白名单里（与不注册 UI 一致），
  `tests/ModConfigOptionChangeGuard.py:48-49` 已把两键登记为 CONTENT_SYSTEM_SWITCHES 并断言恒开。
- **热路径分配**：`OnGlobalDead` / `OnGlobalHurt` 无字符串拼接、无 LINQ、无 DevLog；
  丧尸 marker 的 `GetComponent` 由 `IsZombieModeActive` 门控（4.12）。
  `CodexView.Update()` 只判 `Input.GetKeyDown(Escape)`，无目录扫描。
- **UI 规范（4.14）**：`BossRushUI.CreateCanvasRoot` + `BossRushUILayers.Panel` / `.ModalConfirm`、
  `BossRushUI.CreateBackdrop`、`BossRushUI.ApplyPanelSkin`、`BossRushUIColors.*` token、
  全 TMP（`ZombieModeUIHelper.CreateText/CreateTMPText`）、零 `Arial.ttf`、
  `CanvasScaler` 由 `CreateCanvasRoot` 内部配置。关闭出口三条：右上 `×`、
  遮罩点击、`Esc`（详情层先收再关面板），另有 footer 提示。

## 四、对前几轮已 Fixed 条目的复核

（填充中）

---

## 附：审查进度日志

- [x] AGENTS.md / Integration/AGENTS.md / CODE_REVIEW.md / 事件订阅生命周期约定 / 相关 finding 已读
- [x] Codex：Tuning / Models / KillCollector / BossCatalog / Persistence / Codec / SaveCoordinator /
      Milestones / RuntimeModule / BookItem / PortraitCache / View / View_Grid / ConfigCodex / CodexLocalization
- [ ] DailyReport 16 文件
- [ ] Achievement 分类枚举 diff
- [ ] WikiBookItem / ImageViewerUI diff
- [ ] 三个 guard 是否假绿
- [ ] 本地化 key 双向对齐三张清单
- [ ] tools/build_codex_bundle.ps1 / tools/gen_codex_art.py 安全扫描

---

## ==== Campaign_Audio_Common ====

# 鸭王征程 Campaign + Boss BGM Audio + Common 共享基础设施 审查报告

> 范围 `f9b83c0..HEAD` + 三个未提交改动（`Campaign/CampaignDialoguePlayer.cs`、`Campaign/CampaignFinalBoss.cs`、
> `Audio/BossBgmCoordinator.cs`，另 `compile_official.bat`、`tests/CampaignSkeletonGuard.py` 亦有未提交改动）。
> 覆盖 `Campaign/`(20) + `Config/ConfigCampaign.cs` + `Localization/CampaignLocalization.cs`
> + `Assets/Data/Campaign/Chapters.json` + `Audio/`(4 文件) + `Assets/Data/Audio/BgmTracks.json`
> + `Common/`(UI 2 / Infrastructure 5 / Lifecycle 6 / Data 1 / Events 1 / MapConfig 2)。
>
> **只读审查**：未改动仓库任何文件，未做改状态的 git 操作，未跑编译 / guard / 部署脚本。
> 本报告是唯一写入的文件。前一位同事因额度中断只留下 A–F 段原始笔记、无结论；
> 本轮从零逐文件重做，A–F 中经复核仍成立的结论已并入第三节并标注复核方式。
>
> **主会话已立项的 `BossRushEagerReflectionCache.cs:91` `ShowNext` P1 不在本报告内**（按要求不重复）；
> 该文件其余 11 条绑定已逐条对照官方源码，见第三节 §3.1。

**摘要：Confirmed 5 条（0×P0 / 0×P1 / 1×P2 / 4×P3）；未验证线索 4 条；已核对干净的关键面 14 组；
对已 Fixed 条目的复核 11 条（10 条复核通过、1 条部分口径与实现不符但结论仍成立）。**

---

## 一、Confirmed Findings

### F-1（P2 / COMPAT）落盘失败后的重试链被自身消费掉，`SaveFile` 抛异常那一次实际不会重试，连宿主销毁兜底也一起失效

**位置**
- `Campaign/CampaignSaveCoordinator.cs:117-199`（`FlushBatch`）
- `Campaign/CampaignSaveCoordinator.cs:73-96`（`Tick`）、`:99-111`（`TryFlushOnHostDestroy`）
- `Campaign/CampaignPersistence.cs:349-392`（`FlushPending`，成功即清空 pending）

**代码摘录**（`CampaignSaveCoordinator.FlushBatch`）
```csharp
if (!CampaignPersistence.HasPendingWrite)        // ← 步骤 1
{
    lock (_lock) { _deferredFlushPending = false; _deferredRetryCount = 0; }
    return true;                                  // ← 直接判成功，不落盘
}
...
if (!CampaignPersistence.FlushPending()) { ...defer...; return false; }   // ← 成功时 pending 已被清空
...
try { ... SavesSystem.SaveFile(false); }
catch (Exception e)
{
    error = "savefile_failed:" + e.GetType().Name;
    lock (_lock) { _deferredFlushPending = true; _lastError = error; }   // ← 重试标记置起
    ModBehaviour.DevLog(CampaignTuning.LogPrefix + "[ERROR] SaveFile 异常: " + e.Message);
    return false;
}
```
`CampaignPersistence.FlushPending()` 成功时会 `_pendingJson = null; _pendingActive = false;`（`:378-379`），
于是 `HasPendingWrite` 变为 false。

**失败场景（静态确认，未运行验证）**
1. 玩家在基地公告板交付某章 → `WriteStateAndRewards` → `CampaignPersistence.Store()`（入队）→ `RequestFlush()`。
2. `FlushBatch` 走到 `FlushPending()`：`SavesSystem.Save<string>(key, json)` + 回读核对成功 → **pending 被消费**。
   官方 `Saves/SavesSystem.cs:600-612` 的 `Save<T>` 只是 `ES3.Save(realKey, value, CurrentFilePath)`，
   写的是 **ES3 缓存文件**，不是磁盘。
3. 紧接着 `SavesSystem.SaveFile(false)`（官方 `:503-520`，真正的 `ES3.StoreCachedFile`）抛异常
   （磁盘满 / IO 错误 / 备份拷贝失败——`SaveFile` 内部还会 `CreateBackup()`）。
4. `catch` 置 `_deferredFlushPending = true` 并返回 false。
5. 下一帧 `Tick()` → `FlushBatch()` → **步骤 1 命中**（pending 已在第 2 步被消费）→
   `_deferredFlushPending = false`，**return true**。重试就此结束，`SaveFile` 再也不会被调用。
6. 更糟的是关停兜底同样失效：`TryFlushOnHostDestroy()` → `FlushBatch(bypassSceneGate: true)`
   也在步骤 1 早返 true，**绕过场景闸的"最后一次机会"什么都没写**。

**影响**
交付进度、已授予 token 与线索停留在 ES3 内存缓存里，只有等**别的系统**再调一次 `SavesSystem.SaveFile`
（官方自动存盘、其它子系统的协调器、`OnCollectSaveData` 之后的官方写盘）才会落到磁盘；
在此之前进程崩溃/断电即丢失该次交付。触发前提是 `SaveFile` 抛异常（罕见），
且实际丢失还需要"在下一次任何存盘之前进程死掉"，因此定 P2 而非 P1。

真正的缺陷是**代码与自身三处注释的承诺不符**：
- `:52-53`「IsSaving / 非基地 / 写失败时返回 false 并保留 pending（由 Tick 重试）」
- `:104`「销毁/关停是最后机会，必须绕过场景闸：宁可在战斗帧写一次，也不能丢进度」
- `:88-89`「存档 deferred 重试预算耗尽，pending 保留待下次写入触发」

**建议修复**
在 `FlushBatch` 里把"key 已写进 ES3 缓存"与"批次已物理落盘"拆成两个状态：
`FlushPending()` 成功后置一个 `_cachedButNotStored = true`，步骤 1 的早返条件改为
`!HasPendingWrite && !_cachedButNotStored`；`SaveFile` 成功后再清。

**同构风险（跨子系统，需与对应 owner 协调）**
`Integration/Codex/CodexSaveCoordinator.cs` 的 `FlushBatch` 是**同一形态**
（`CampaignSaveCoordinator.cs:4` 的头注释明说"形态照 CodexSaveCoordinator"）：
它同样先 `if (!CodexPersistence.HasPendingWrite) return true;`，再 `FlushPending()`（消费 pending），
再 `SavesSystem.SaveFile(false)`，异常时 `_deferredFlushPending = true`。修复应两处一起做，
否则会出现"征程修了、图鉴没修"的不一致。（PetNest / DailyReport 的协调器未在本轮范围内，建议一并扫一遍。）

**验证需求**
静态确认，未运行验证。实机需注入 `SaveFile` 失败（只读存档目录 / 磁盘满）才能复现，属故障注入边界。

---

### F-2（P3 / SAFE）`AGENTS.md` 第 14 节的 CS0649 pragma 指针已失效，指向的文件里已无该模式

**位置**
- `AGENTS.md:303-304`：「定点 `#pragma warning disable/restore 0649` 包住 DTO，并写明原因（见 `Audio/BossBgmCoordinator.cs`）」
- `Audio/BossBgmCoordinator.cs` 全文（655 行）、`Audio/BossBgmTrackTable.cs:1-118`

**证据**
```
$ grep -rn "0649\|JsonUtility" Audio/
Audio/BossBgmTrackTable.cs:33:    /// 复用既有 token parser 显式读取数组，避免 Unity JsonUtility 在 Mod DTO 上静默产出空表。
$ grep -rln "warning disable 0649" --include=*.cs .   （排除 鸭科夫源码/）
./ModeH/ModeHUIPages.cs
```
BGM 的三个 DTO 已在 CR-2026-09-02-007 的修复中迁到 `Audio/BossBgmTrackTable.cs:8-30`，
且字段全部由显式 token parser 以 `out entry.bossKey` / `out entry.file` 等形式**在代码里赋值**
（`BossBgmTrackTable.cs:66-91`），编译器不会产生 CS0649，因此那里确实不需要 pragma——
**代码是对的，是 AGENTS 的指针过时了**。全仓唯一残留的 `#pragma warning disable 0649` 在 `ModeH/ModeHUIPages.cs`。

**影响**
`AGENTS.md` 是仓库自称的"唯一事实来源"。新协作者按第 14 节去 `BossBgmCoordinator.cs` 找范例会扑空，
可能据此得出"这条纪律被删了"的错误结论。

**建议修复**
把示例指针改到 `ModeH/ModeHUIPages.cs`，并补一句「BGM 侧改用显式 token parser 后已无 JsonUtility DTO，
不再需要该 pragma」。

**验证需求** 纯文档，静态确认。

---

### F-3（P3 / SAFE）`CampaignTuning.FinalBossScale` 注释「纯视觉，不影响碰撞判定口径」与实现及另外两处注释矛盾

**位置**
- `Campaign/CampaignTuning.cs:76-77`
- `Integration/PhantomWitch/PhantomWitchBoss.cs:275-284`（实际落点）
- `Campaign/CampaignFinalBoss.cs:464-465`、`Integration/PhantomWitch/PhantomWitchBoss.cs:150-155`（反面注释）

**代码摘录**
```csharp
// CampaignTuning.cs:76
/// <summary>终章 Boss 体型放大系数。纯视觉，不影响碰撞判定口径。</summary>
internal const float FinalBossScale = 1.15f;
```
```csharp
// PhantomWitchBoss.cs:278-281 —— 这个值最终落在 transform.localScale 上
float modelScale = PhantomWitchConfig.BossModelScale * (extraModelScale > 0f ? extraModelScale : 1f);
character.transform.localScale = Vector3.one * modelScale;
```
```csharp
// PhantomWitchBoss.cs:151-154（同一改动的另一处注释，说法相反且正确）
/// 必须走这个参数、不要在生成完成后再改 localScale：localScale 会缩放碰撞体，
/// 而属性/AI 初始化会缓存碰撞器半径，事后缩放会让命中判定与模型口径不一致。
```

**影响**
玩家侧无影响——放大发生在 `SetupPhantomWitchAttributes` / `SetupAIAggro` **之前**（`:286` 与 `:326`），
行为是正确的。风险纯在维护面：三处注释里有一处说反，将来有人信了「纯视觉」把缩放挪到生成之后，
就会踩回 `PhantomWitchBoss.cs:151` 明确警告过的坑，而且是静默的（命中判定与模型不一致，不报错）。

**建议修复** 把 `CampaignTuning.cs:76` 的注释改为「随 transform 一起缩放碰撞体，必须在生成时传入」。

**验证需求** 静态确认，未运行验证。

---

### F-4（P3 / COMPAT）终章击杀同帧播两条 stinger，其中 `RunVictory` 的语义是「整局通关」

**位置**
- `Campaign/CampaignFinalBoss.cs:537-539`（`OnCampaignFinalBossDead`）
- `Integration/PhantomWitch/PhantomWitchBoss.cs:688-690`（`OnPhantomWitchDeath`）
- 语义定义：`Audio/BossBgmCoordinator.cs:45-49`

**代码摘录**
```csharp
// BossBgmCoordinator.cs:45-49
/// <summary>单个自定义 Boss 被击败。</summary>
internal const string BossVictory = "BossVictory";
/// <summary>整局通关（胜利奖励箱流程启动时）。</summary>
internal const string RunVictory = "RunVictory";
```
```csharp
// CampaignFinalBoss.cs:537-539
BossRushAudioManager.Instance?.StopBossBGM(BossBgmKeys.PhantomWitch, campaignFinalBossInstance);
BossRushAudioManager.Instance?.PlayStinger(BossBgmEvents.RunVictory);
```
```csharp
// PhantomWitchBoss.cs:688-690
BossRushAudioManager.Instance?.StopBossBGM(BossBgmKeys.PhantomWitch, deadWitch);
if (...) BossRushAudioManager.Instance?.PlayStinger(BossBgmEvents.BossVictory);
```

**失败场景**
冠军之影是一只幽灵女巫，`Health.OnDeadEvent` 上同时挂着 `OnCampaignFinalBossDead`
（`CampaignFinalBoss.cs:415` 订阅）与 `OnPhantomWitchDeath`（`PhantomWitchBoss.cs:335` 订阅）。
它死亡时两个回调都跑，`stinger_boss_victory.wav`（166 KB）与 `stinger_run_victory.wav`（268 KB）
同帧经 `PostCustomSFX` 叠放。两者都不占 bgmSource，所以不会互相打断，只是重叠。
另外终章并非一次 run clear（没有胜利奖励箱流程），`RunVictory` 用在这里与常量注释的口径不符。

**影响** 观感层面，无玩法影响。是否要双响属产品取舍。

**建议修复** 二选一：终章改用 `BossVictory`（与自定义 Boss 击败一致，且 PhantomWitch 那条已经在播，
等于去掉重复）；或保留双响但把 `RunVictory` 的注释扩成「整局通关或战役终章」。**Needs owner confirmation**。

**验证需求** 静态确认；实机听一次即可判断。

---

### F-5（P3 / SAFE）召唤石维护 tick 的性能注释口径与实现不符（结论仍成立，CR-2026-08-31-005 的修复有效）

**位置** `Campaign/CampaignFinalBoss.cs:208-229`

**代码摘录**
```csharp
/// 【判定顺序是性能约束，不是风格】这是每帧路径。终章契约查询是纯内存的
/// （字典命中 + 整数比较，零分配），而场景判定会分配字符串。
```
实际 `CampaignProgressService.GetActiveChapterDef()` 的调用链是：
`GetActiveChapterId()` → `EnsureInitialized()` → `CampaignPersistence.Current` → `LoadOrInit()` →
`LoadOrInitCore()` 内 **`lock (_lock)`** + `ReadCurrentSlotSafe()`（`SavesSystem.CurrentSlot`）→
返回 `_cache` → 再对 `data.chapters` 做**线性扫描**；随后 `CampaignContentCatalog.GetChapter()`
又对六章做一次线性 `string.Equals` 扫描。`ShouldCampaignFinalBossAltarExist:225` 还会再调一次
`GetState(def.ChapterId)`，那里 `ReadStoredState` **又走一遍 `CampaignPersistence.Current`**——
每帧至少两次 `lock`，不是"一次字典命中"。

**为什么结论仍成立**：全链路确实**零托管分配**（无 boxing、无字符串拼接、无 LINQ），
`SavesSystem.CurrentSlot` 官方实现（`Saves/SavesSystem.cs:15-30`）是 `int?` 静态缓存读，
首次之后不再访问 PlayerPrefs；六章线性扫描与两次无竞争 `Monitor.Enter/Exit` 的量级可忽略。
CR-2026-08-31-005 要消除的"每帧 `SceneManager.GetActiveScene().name` 托管字符串分配"确实消除了。

**影响** 维护面：注释描述的数据结构（字典）与调用次数（一次）都不成立，
将来做性能预算时按注释估算会低估。

**建议修复** 改注释为「零分配，但含两次无竞争 lock 与两轮六元素线性扫描」；
如要真正压到一次，可在 `ShouldCampaignFinalBossAltarExist` 内把 `def` 与 `GetState` 合并成一次查询。

**验证需求** 静态确认，未运行验证。

---

## 二、未验证线索（不是 bug，升格前需实机或 owner 确认）

### U-1 `CampaignRuntimeModule.OnSceneLoaded` 不区分 `LoadSceneMode`，additive 子场景加载可能在决战途中误收尾终章

**位置**
- `Campaign/CampaignRuntimeModule.cs:99-119`
- `ModBehaviour.cs:833-847`（`SceneManager.sceneLoaded` 订阅，Single 与 Additive 都会触发）
- `Common/Lifecycle/SceneRuntimeContext.cs`（结构体带 `LoadMode` 字段，**全仓无任何模块读它**）

```csharp
public override void OnSceneLoaded(SceneRuntimeContext context)
{
    _sceneGeneration++;
    if (_owner != null) { _owner.CleanupCampaignFinalBoss(false); }   // ← 无条件，不看 context.LoadMode
```

**为什么值得怀疑**：本轮**未提交改动**里 `Audio/BossBgmCoordinator.cs:479-491` 的新注释
（2026-09-03 复核结论）自己就把这条写死了：
> 「`OnSceneLoaded` 不区分 `LoadSceneMode`……additive 加载时活动场景不变：接线版会在 Boss 战中途抹掉租约」

即仓库自己认定"Boss 战中途发生 additive 加载"是一个需要防的真实场景，并据此**拒绝**把
`BossBgmCoordinator.NotifySceneChanged()` 接进 `OnSceneLoaded`。而 `CampaignRuntimeModule` 走的正是被拒绝的那条路。

**若成立的后果**：`CleanupCampaignFinalBoss(false)` 会摘掉 `OnDeadEvent` 监听（`:601`）、
`campaignFinalBossInstance = null`、`campaignFinalBossActive = false`、`campaignFinalBossRunId++`、
并 `ResetCampaignFinalBossTracking()`——**但 `destroyBoss:false` 不销毁 Boss**。
结果是场上留一只无人记账的强化女巫（打死它不再推进 ch6），同时
`ShouldCampaignFinalBossAltarExist` 因 `campaignFinalBossActive == false` 重新放出召唤石，
玩家可以再召一只。

**未验证的前提**：竞技场 `Level_DemoChallenge_Main` 里是否存在会触发
`MultiSceneCore.LoadSubScene(..., LoadSceneMode.Additive)` 的传送点。官方
`Duckov/Scenes/MultiSceneCore.cs:305-360` 的 additive 加载只由 `BeginLoadSubScene` / `LoadAndTeleport`
驱动（即传送门），而 `Player.log:7156` 出现过 `Player SubSceneID: 'Level_GroundZero_1'`，
证明副本地图确实有子场景体系。**竞技场是否有，静态判定不了。**

**如何确认**：dev 构建下进竞技场召唤终章，走一遍场内所有可疑传送/门，看 Player.log 是否出现
`[Campaign] 决战 Boss 已不在场，自动收尾` 或召唤石重现。若成立，最小修复是
`if (context.LoadMode != LoadSceneMode.Single) return;` 之后再收尾（或至少跳过收尾那一句）。

### U-2 交付奖金的"已发钱"闩是会话级，跨重启失效（CR-2026-09-03-005 自己写明的 accepted 残留）

`Campaign/CampaignProgressService.cs:45` `_cashPaidPendingChapterId` 只在内存里。
若"写盘失败 **且** 退款失败"之后玩家退出重进，闩丢失、章节仍是 `ReadyToDeliver`，
再交付一次就是第二笔奖金。CR-2026-09-03-005 已把这条记为「已接受的残留：不为 P3 增加存档字段」。
本轮复核确认实现与该记录一致，**不作为新 finding**，仅在此登记以免下一轮重复排查。
若 F-1 被修复（`SaveFile` 抛异常后能真正重试），这条的触发面会进一步收窄。

### U-3 `CampaignAssetCache` / `BossRushUISkinLoader` 会 `Unload(true)` 一个可能不属于自己的 bundle

`Campaign/CampaignAssetCache.cs:115-123` 与 `Common/UI/BossRushUISkinLoader.cs:109-117`
都有"复用 `AssetBundle.GetAllLoadedAssetBundles()` 里已加载实例"的分支（按名字子串匹配），
而 `Unload()` / `Cleanup()` 里对 `_bundle` 调的是 `Unload(true)`（连同已加载资源一起销毁）。
若那个 bundle 是别的系统加载的，销毁会波及对方。当前静态排查显示
`campaign_presentation` 与 `bossrush_ui_skin` 各自只有这一个加载方（grep 全仓确认），
所以复用分支实际只会命中"自己上一次加载后未清干净"的情况，**当前不构成问题**。
登记为线索是因为：将来若有第二个消费方（例如调试面板预览立绘），这条会静默变成真 bug。

### U-4 语言在 HUD 可见期间切换时，契约进度条文案不刷新

`Campaign/CampaignHud.cs:103-119` 的 `HasProgressChanged` 只比 `ChapterId` + 各目标 `Current`/`Failed`，
不含语言。`HideImmediate()`（`:224-239`）作废快照的注释只承诺覆盖"隐藏期间换了语言"。
局内可见时切语言，标题与目标文案会停在旧语言，直到某个计数变化。
未验证的是：官方是否允许战斗中改语言（若只能在主菜单改，则本条不可达）。P3 级观感问题。

---

## 三、已核对且干净的关键面

### 3.1 `BossRushEagerReflectionCache` 逐条绑定核对（对照 `鸭科夫源码/`）

| 行 | 绑定 | 官方定义 | 结论 |
| --- | --- | --- | --- |
| 59 | `InteractableBase.otherInterablesInGroup` (NonPublic\|Instance) | `InteractableBase.cs:663` `private List<InteractableBase> otherInterablesInGroup;` | ✅ |
| 63 | `MultiInteraction.interactables` | `MultiInteraction.cs:50` `private List<InteractableBase> interactables;` | ✅ |
| 67 | `NotificationText.duration` | `[SerializeField] private float duration = 1.2f;` | ✅ |
| 69 | `NotificationText.durationIfPending` | `[SerializeField] private float durationIfPending = 0.65f;` | ✅ |
| 73 | `StockShop.merchantID` | `StockShop.cs:423` `private string merchantID = "Albert";` | ✅ |
| 75 | `StockShop.itemInstances` | `:465` `private Dictionary<int, Item> itemInstances` | ✅ |
| 77 | `StockShop.accountAvaliable` | `:440` `private bool accountAvaliable;`（官方拼写就是 Avaliable） | ✅ |
| 79 | `StockShop.refreshAfterTimeSpan` | `:432` `private long refreshAfterTimeSpan;` | ✅ |
| 81 | `StockShop.refreshStockOnStart` | `:448` `private bool refreshStockOnStart;` | ✅ |
| 83 | `StockShop.lastTimeRefreshedStock` | `:462` `private long lastTimeRefreshedStock;` | ✅ |
| 87 | `CharacterRandomPreset.characterIconType` | `:190` `private CharacterIconTypes characterIconType;` | ✅ |
| 91 | `NotificationText.ShowNext` | 私有实例 `UniTask ShowNext()` | ❌ 主会话已立项 P1，本报告不重复 |

**除已立项的 `ShowNext` 外，其余 11 条签名全部命中，无第二处错配。**
另注：`IsInitialized`（`:93`）只在**整体抛异常**时才为 false；`GetField` 找不到成员时返回 null 而不抛，
因此单条失配不会让 `IsInitialized` 变红——这正是 `ShowNext` 能静默至今的机制。
消费方（如 `NPCInteractionGroupHelper.GetOrCreateGroupList`）逐条判 null 并各自打日志，未因此崩溃。

### 3.2 其它反射 / Harmony 目标核对

- `Audio/BossBgmCoordinator.cs:538-559`：`Type.GetType("Duckov.AudioManager, TeamSoda.Duckov.Core")` ✅；
  `PlayCustomBGM(string, bool)` 对应官方 `Duckov/AudioManager.cs:358 public static EventInstance? PlayCustomBGM(string filePath, bool loop = true)` ✅；
  `StopBGM()` 对应 `:399 public static void StopBGM()` ✅。带默认参数的 `PlayCustomBGM` 在
  `InvokePlayCustomBgm` 里显式补齐两个实参（反射 Invoke 必须如此）✅。
- `Campaign/CampaignNoteBridge.cs`：`NoteIndex.Instance` / `.Notes` / `SetNoteDynamic` / `GetNoteUnlocked` /
  `SetNoteUnlocked` 全部对应官方 `Duckov/NoteIndexs/NoteIndex.cs:15/25/163/174/186` ✅；
  `Note.key` / `.image` / `.hide` 对应 `Note.cs` 的三个 `[SerializeField]` 公有字段 ✅；
  头注释关于「`SetNoteDynamic` 只写 `MDic` 不写 `notes` 列表，而界面走 `GetAllNotes()` 遍历 `notes`」的
  论断经官方 `NoteIndex.cs:82-99` 核实**正确**，两边都写的做法是必需的 ✅。
- `Campaign/CampaignBoardBuilder.cs`：`Duckov.Buildings.Building` 的 `id/dimensions/graphicsContainer/functionContainer/areaMesh`、
  `BuildingDataCollection` 的 `Instance/infos/prefabs/readonlyInfos`、`BuildingInfo` 的
  `id/prefabName/maxAmount/requireBuildings/alternativeFor/requireQuests/iconReference/cost`、
  `BuildingManager.GetBuildingAmount`、`Duckov.Economy.Cost(long)` —— 全部走 `FindGameType` + 判空，
  任一缺失只记 `LogError` 不抛；`Cost` 走 `GetConstructor(new[]{typeof(long)})` 并有 `Activator` + 字段回退 ✅
  （官方 `Economy/Cost.cs:136` 确有 `public Cost(long money)`）。
- `Integration/Dialogue/DialogueActorFactory.cs:55-59` 的四个私有字段
  （`id` / `nameKey` / `offset` / `_portraitSprite`）——未提交改动里 `CampaignDialoguePlayer` 依赖的
  「工厂缓存按 GameObject 索引，命中缓存时忽略传入 actorId/nameKey/portrait」这一论断，
  经 `DialogueActorFactory.Create:93-108` 核实**正确**，因此 `_championActorHost` 与 `_actorHost`
  必须分开是对的 ✅。
- `Common/Infrastructure/HarmonyBindingSelfCheck.cs`：调用点 `Utilities/AlwaysOnRuntimeHooks.cs:61`
  （`InitializeBootstrapRuntime` 末尾）**不受 dev 门控**，失败经 `CriticalLog` 玩家端可见；
  `:334` 有成对的 `ResetStaticCaches`。覆盖面复核：全仓 48 个含 `[HarmonyPatch]` 的文件**全部是类级特性**
  （逐个确认 `ModeF/ModeFUI_BountyRadarAndHealthBars.cs:1057`、`Patches/Combat/ProjectileHalfObstaclePatch.cs:13`、
  `Patches/UI/ItemUIUtilitiesElementFactorFormatPatch.cs:54`、`ZombieMode/ZombieModeRewardProjectilePatch.cs:5`、
  `Patches/ItemStatsSystem/ItemAssetsCollectionDynamicRegistrationPatch.cs` 的 8 处），
  因此 `type.GetCustomAttributes(typeof(HarmonyPatch), true)` 这一层不存在漏检类 ✅。
  启动开销：同一次启动里 `AccessTools.GetTypesFromAssembly` 被走两遍（逐类 apply 一次 + 自检一次），
  仅本 Mod 程序集、一次性，可接受。

### 3.3 战役目标采集（Collector / Tracker / ModeBridge）

- **全局 Health 采集器订阅幂等 + 退订**：`Utilities/PlayerLifecycleRuntimeHooks.cs:23-24 / 39-40`
  用命名方法成对 `+=` / `-=`（AGENTS 4.6），唯一调用点 `ModBehaviour.cs:614`（Awake，单例守卫内）
  与 `:771`（OnDestroy）；幂等由单例保证 ✅。
- **武装按「开局」而非「进场景」**（CR-2026-09-03-011 复核）：
  `CampaignModeBridge.cs:125` 标准分支已带 `IsActive`；`:114-118` 丧尸改用
  `ZombieModePhaseGuards.IsRunActive`；`CampaignObjectiveTracker.cs:78-81` 模式不符时先 `ResetSession()` ✅。
  进一步反向验证 `IsActive` 的唯一写点：`SetBossRushRuntimeActive(true)` 全仓只有
  `ModeD/ModeDWaves.cs:79` 与 `WavesArena/WavesArenaBossSpawning.cs:78` 两处，
  置 false 的四处（`LootAndRewardsVictoryRewards.cs:40/:51`、`LootAndRewardsRandomBossLoot.cs:147`、
  `Integration/BossRushIntegration_StartAndScene.cs:499`）**都在局终或死亡**，
  局中不会翻转，因此不存在"中途误 ResetSession → 重新武装时无伤失败位被抹掉"的漏洞 ✅。
- **胜利漏斗与状态复位无竞态**：`LootAndRewardsVictoryRewards.cs:51` `SetBossRushRuntimeActive(false)`
  与 `:66` `NotifyCampaignStandardCleared()` 之间逐行读过，**没有任何 `await`**（方法虽是 `async void`，
  但首个 `await` 在更靠后），每帧桥不可能插进来；`EnsureArmedFor` 因此仍命中"已武装且模式相同"早返，
  本局 `Failed` 位得以保留，`SatisfyNoDamageOnRunComplete` 的 `if (item.Failed) continue;` 生效 ✅。
  这一条是本轮特意去证的"零进度即时达标"漏洞候选，**证伪**。
- **随从 / 替身 / 乱入 Boss / 非本波 Boss 污染**：
  `CampaignObjectiveCollector.cs:47` 用 `info.fromCharacter.IsMainCharacter` 过滤，随从击杀不计 ✅；
  Mode H 的 ERROR 互换（CR-2026-09-03-004 的成因）对战役不可达——反向验证：
  `ResolveCampaignCurrentMode` 不检查 `modeGActive`/ModeH，但标准分支要求 `bossRushArenaActive && IsActive`，
  而 Mode G 只写 `bossRushArenaActive`（`ModeG/ModeGRuntimeBridge.cs:268`）**从不置 `IsActive`**，
  Mode H 两个都不写（grep 全仓确认），所以 G/H 期间 `ResolveCampaignCurrentMode` 返回 null、追踪不武装 ✅。
  乱入 Boss 会计入 ch3 的 `faction_boss_kills`——这是 CR-2026-08-31-007 已裁决的"宽松方向、玩家不吃亏"，
  文案也已改成「击败 8 名头目」与实现一致 ✅。
- **目标完成后重复完成 / 跨局残留**：`EvaluateAndNotify` 的 `_notified` 闩在**上报成功后**才置位
  （`CampaignObjectiveTracker.cs:336-339`，注释说明了为什么不能先置位）；
  `NotifyObjectivesSatisfied` 对已是 `ReadyToDeliver` 的重复调用返回 true 但不重复写盘（`:284`）；
  跨局残留由桥的 `mode == null → ResetSession`（`CampaignModeBridge.cs:60-67`）+
  `TryAcceptContract` / `TryAbandonContract` / `NotifySlotChanged` 三处 `ResetSession` 覆盖 ✅。
- **状态桥轮询频率与每帧分配**：`CampaignRuntimeModule.OnUpdate:148` 每帧一次；
  未启用时 `TickCampaignModeBridge` 首行 bool 早返；未武装时 `Tracker.Tick` / `ReportPlayerKill` / `ReportPlayerDamaged`
  首行 `IsArmed` 早返。全链路无 `string.Format`（grep 确认范围内零命中）、无 LINQ、无装箱 ✅。
  唯一的每帧非零成本见 F-5（两次无竞争 lock + 两轮六元素扫描，量级可忽略）。
- **4 处 notify 漏斗接线**（grep 全仓确认，复核前一位同事的 E 段）：
  `LootAndRewards/LootAndRewardsVictoryRewards.cs:66`、`ModeD/ModeDWaves.cs:993`、
  `ModeF/ModeFExtraction.cs:252`、`ZombieMode/ZombieModeExtractionController.cs:231`，
  与 `CampaignModeBridge` 的四个 internal 方法一一对应，无零调用点 ✅。
  Mode E 无漏斗是设计（ch3 的 `faction_boss_kills` / `survive_minutes` 不需要终局事件）✅。
- **波次口径**：`ModeDWaveIndex` 由 `ModeD/ModeD.cs:38` 的 0 起、`ModeDWaves.cs:94` `modeDWaveIndex++`
  在开波时推到 1，**是 1 基**，ch2 的 `reach_wave 5` 语义正确 ✅；
  标准竞技场 `(currentEnemyIndex / perWave) + 1` 的除数有 `perWave = bossesPerWave > 0 ? bossesPerWave : 1` 保护，
  无除零 ✅；ch1 `no_damage_until_wave 2` 与 `ReportPlayerDamaged` 的 `currentWave > Threshold` 组合
  = 第 1、2 波受伤判死、第 3 波起免疫，与文案「前 2 波一滴血不掉」一致 ✅。

### 3.4 契约交付与经济

- **原子性**（CR-2026-09-03-005 复核）：`CampaignProgressService.cs:331-352` 的 `_cashPaidPendingChapterId`
  会话闩已实装；发钱成功即置位，写盘成功（`:385`）或退款成功（`:366`）清空，`NotifySlotChanged`（`:77`）
  与 `ResetStaticCaches`（`:529`）也清 ✅。
- **官方经济 API 核对**：`EconomyManager.Add(long)`（官方 `:269`，`Instance == null` 时返回 false 不抛）
  与 `EconomyManager.Pay(Cost, bool, bool)`（官方 `:224`）都存在且返回值被消费 ✅。
  退款走 `Pay` 会先从 `Money` 扣、不足再扣 `Cash`（官方 `:186-211`），而奖金是 `Add` 加进 `Money`；
  交付发生在基地公告板、同一帧内退款，账面净额为零 ✅。
- **奖励发放失败（背包满）**：本战役奖励**只有现金**，没有物品发放路径
  （`CampaignChapterDef` 只有 `RewardCash` / `FacilityToken` / `ClueId`），
  因此不存在"背包满吞奖 / 双发"面 ✅。
- **章节推进单向**：`GetActiveChapterId()` 只返回 `ContractActive` / `ReadyToDeliver` 两态的章节
  （`:145-151`），`TryAbandonContract` 因此不可能把 `Completed` 写回 `Available` ✅；
  `IsUnlockedByPrerequisite` 只读前一章的 `Completed`（`:190-198`），是纯推导、不落 `Available` ✅。
- **线索注入幂等、读档不重播**：`CampaignNoteBridge.RegisterNote:84-107` 判重**看官方真实 `notes` 列表**
  而不是本地 `_registeredKeys`（注释解释了场景重载后官方实例会重建），
  `:110-114` 只在 `IsClueUnlocked && !GetNoteUnlocked` 时补 `SetNoteUnlocked`，不重复发提示 ✅；
  入口 `RegisterCampaignNotesForScene`（`CampaignBoardBuilder.cs:531`）由
  `Integration/IntegrationDeferredBootstrap.cs:323` 在每次进基地的装配管线里调一次 ✅。
  另核实：`PlayChapterDeliveredAsync` 在 actor 创建失败时早返、**跳过 `UnlockClue`**，
  但线索的权威副本已由 `TryDeliver` 写进 `data.unlockedClues`（`CampaignProgressService.cs:451-454`），
  下次进基地 `RegisterNote` 会据此补 `SetNoteUnlocked` —— **自愈，不是漏发** ✅。
- **token 只在本会话新授予时触发事件**：`CampaignFacilityUnlocks.TryGrant:149` 用 `_granted.Add(token)`
  的返回值做幂等门，`LoadGrantedTokens`（读档路径）**不发事件** ✅，符合 `docs/contracts.md` §3.1。
  `TryGrant` 在 `!_loaded` 时明确拒绝而不是静默写进会被覆盖的集合（`:142-148`）✅。
- **跨槽泄漏**：`CampaignPersistence` 三条防线齐全 —— `OnSetFile`/`OnSaveDeleted` 订阅（`:103-105`，
  幂等 + 成对退订）、缓存槽位烙印 `_cacheSlot` 每次比对（`:223-228`）、
  以及"未收到切档回调"时的漂移自愈 + 下游广播（`:205-214`）。
  下游广播顺序正确：`SaveCoordinator → FacilityUnlocks.ResetForSlotReload → ProgressService.NotifySlotChanged`
  （`:177-189`），token 在复位之后才由 `EnsureInitialized` 重新发布 ✅。
  开关关闭期间退订 `OnSetFile` 的窗口，由槽位烙印兜底（头注释第 3 条明写这个理由）✅。
- **未知版本 fail-closed**：`LoadOrInitCore:277-286` 对 `schemaVersion != 1` 置写屏障、只读不覆盖 ✅；
  key 分类失败（`:238-245`）与 payload 读失败（`:259-265`）同样置屏障 ✅。
- **bootstrap 时序**：`CampaignRuntimeModule.EnsureBootstrapped:207-208` **先订阅再读档**，
  注释点明了反过来会导致 A 档解锁泄漏到 B 档 ✅。主菜单期（未载档）的提前读档会让
  `_loaded = true` 且集合为空——答案与 fail-closed 一致（false），且随后官方 `OnSetFile` 会整体复位重读 ✅。

### 3.5 终章「冠军之影」

- **敌对性安全网（AGENTS 4.5）**：`Integration/PhantomWitch/PhantomWitchBoss.cs:255-268`
  `if (!Team.IsEnemy(Teams.player, character.Team)) character.SetTeam(Teams.wolf);`，
  且**排在随从生成之前**（注释说明随从会复制 Boss 的 team）✅。
- **距离休眠解除**：`PhantomWitchBoss.cs:318` `SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep(character)`，
  紧跟 `SetActive(true)` 之后 ✅（AGENTS 第 14 节 2026-09-01 第 1 条）。
- **数值倍率确为叠乘、且是有意设计**：`SpawnPhantomWitch:288` 先 `ApplyBossStatMultiplier(character)`（全局 config），
  `ApplyCampaignFinalBossVariant:457` 再 `ApplyBossStatMultiplier(boss, 1.6f)`；
  `Utilities/Utilities.cs:210` 是 `hpStat.BaseValue *= mult`，两次相乘。
  `CampaignFinalBoss.cs:454` 注释「在 Boss 自身倍率之上再叠战役倍率」与实现一致 ✅。
  伤害倍率有 `Mathf.Min(mult, 3f)` 上限，第二次施加时以 1.6 计仍在上限内 ✅。
- **体型放大不破坏碰撞/寻路**：`extraModelScale` 在 `SetupPhantomWitchAttributes`（`:286`）与
  `SetupAIAggro`（`:326`）**之前**应用（`:275-284`），属性/AI 初始化读到的是已缩放的碰撞器半径 ✅
  （注释口径问题见 F-3）。
- **染色目标 Renderer 空引用**：`SetCampaignFinalBossRendererColor:498-518` 判 `renderer == null` 早返、
  `sharedMaterial == null` 落到 `_BaseColor` 分支（`SetColor` 对不存在的属性是 no-op，不抛）✅；
  复用同一个 `MaterialPropertyBlock` 安全——每次 `GetPropertyBlock` 会整块覆盖内容，无跨 renderer 串色 ✅；
  走 PropertyBlock 而非 `sharedMaterial`，不污染同款敌人 ✅。
- **预设克隆**：`PhantomWitchBoss.cs:218` `Object.Instantiate(character.characterPreset)` 后才赋回
  `character.characterPreset`，因此 `ApplyCampaignFinalBossVariant:489` 改 `nameKey`
  **不污染 Boss 池** ✅（`CampaignFinalBoss.cs:481` 的注释成立）。
- **打输一次是否永久卡死**（CR-2026-08-31-003 复核）：三条收尾路径齐备 ——
  ① `CampaignRuntimeModule.OnSceneLoaded:110` 幂等收尾（死亡回基地主路径）；
  ② `TickCampaignFinalBossYield:575` 的「生成已出结果但实例已销毁」检测；
  ③ `CleanupCampaignFinalBoss:635` 自增 `campaignFinalBossRunId` 作废在飞的异步生成，
  `StartCampaignFinalBossAsync:383-399` 回来发现编号不符即 `ClearBossRandomLootTracking` + `Destroy` ✅。
  未提交改动新增的独白层（`StartCampaignFinalBossPrologueThenSpawnAsync:344-348`）也做了同一套 runId 比对 ✅。
- **门禁覆盖全部模式**：`IsAnyGameplayModeActiveForCampaign:134-163` 覆盖
  `bossRushArenaActive` / D / E / F / **G** / 丧尸 / **Mode H**，`catch` 里返回 true（保守拒绝）✅。
- **Boss 死亡后清理**：熔石掉落订阅经 `ClearBossRandomLootTracking` 在两条主动销毁路径上先清
  （`:390` 与 `:613`，CR-2026-09-02-017 的修复）✅；BGM 经 `StopBossBGM(key, owner)` → `ReleaseBossBgm` ✅；
  变体标记复位在 `finally` 块里无条件执行（`:629-637`）✅。
- **直接销毁 vs 正常死亡两条路径**：`CleanupCampaignFinalBoss(true)` 销毁 + 停 BGM；
  `(false)` 只退订与复位，尸体与掉落交给既有流程 ✅。销毁路径里 `Destroy(gameObject)` 之后才调
  `StopBossBGM` —— Unity 的 `Destroy` 在帧末才真正销毁，此刻 `campaignFinalBossInstance` 仍非"假 null"，
  `owner.GetInstanceID()` 有效、租约能正确移除 ✅（另有 `_ownerLeases` 的 `Owner == null` 自清兜底）。
- **`DamageInfo` 零值初始化**（CR-2026-09-02-002 复核）：本轮范围内 `Campaign/` 已无 `new DamageInfo()`
  的无参构造（grep 确认）；该问题原在 `DebugAndTools/F3GameplayValidationRunner.cs`，属另一范围 ✅。
- **召唤石交互体**：`CampaignFinalBossInteractable.Awake:30-39` 在 `base.Awake()` **之前**
  `GetOrCreateGroupList` 初始化官方私有 `otherInterablesInGroup`（CR-2026-09-01-009 第 6 项的修复）——
  经官方 `InteractableBase.cs:164-190` 核实必要性：`base.Awake` **无条件** `foreach (… in this.otherInterablesInGroup)`，
  列表为 null 必 NRE ✅。层级由官方 `base.Awake:178`
  `interactCollider.gameObject.layer = LayerMask.NameToLayer("Interactable")` 自动设置，
  Mod 侧不需要也没有重复设 ✅（`CA_Interact.cs:55` / `AIMainBrain.cs:90` 用的正是这个层）。
  召唤石根物体在 `AddComponent<CampaignFinalBossInteractable>()`（`CampaignFinalBoss.cs:257`）之前
  已挂好 `BoxCollider`（`:252`），`GetComponent<Collider>()` 命中 ✅。

### 3.6 BGM

- **零素材 fail-open**：`EnsureTableLoaded` 在表缺失时只 `DevLog` 不喊错并留空集合（`:124-129`）；
  `HasBossTrack` / `PlayBossBgm` / `PlayStinger` 都要求 `FileExists(ResolveSoundPath(...))` 才动作 ✅。
- **龙王旧路径仍生效**：`Assets/Data/Audio/BgmTracks.json` 的 `bossTracks` 只有 `DragonDescendant`
  与 `PhantomWitch`，**DragonKing 刻意缺席**（JSON 内有 `_comment_dragonking` 说明理由）；
  `BossRushAudioManager.PlayDragonKingBGM:84-101` 先试协调器（返回 false）再回落
  `Assets/Sounds/DragonKing/dragonking.mp3`，与从前完全一致 ✅。
  `ResetDragonKingBGMState` 调 `StopBossBgm("DragonKing")`，因 `_playingBossKey != "DragonKing"` 直接早返，
  不会误停别人的曲子 ✅。
- **素材与数据表一致**：`Assets/Sounds/BGM/` 下 4 个 wav 与 JSON 的 4 个 `file` 字段逐一对应，无孤儿条目 ✅。
- **状态机成对性**：租约配平经 grep 全仓确认 ——
  `PhantomWitchBoss.cs:363` Acquire ↔ `:102`(场景清理) / `:399`(生成失败) / `:688`(死亡) 三处 Release；
  `DragonDescendantBoss.cs:209` Acquire ↔ `DragonDescendantBoss_RuntimeAndCleanup.cs:191` Release；
  `CampaignFinalBoss.cs:537` / `:620` 两处 Release 的 owner 就是 `:363` Acquire 时的同一个 Boss 实例 ✅。
- **多 Boss 同场 / 被抑制死亡 / 玩家死亡 / 切图**：
  同 key 多 owner 共享曲目、最后一个释放才停（`ReleaseBossBgm:355-366`）；
  释放后若还有别的 key 的活租约则切到最近激活的那条（`GetMostRecentLiveLease`）✅；
  owner 被销毁（被抑制死亡、切图、被别的系统清掉）时由 `EnsureOwnerLeasesForCurrentScene:383-386`
  的 `Owner == null` 逐条剔除 ✅；跨场景由 `_playingSceneHandle` 比对整体作废（`:374-382`）✅；
  `StopBossBgm:428-432` 在记账来自上一局时只清记账、不去停当前场景正在放的东西 ✅。
  **不会卡在战斗 BGM 或永久盖掉官方 BGM**：即使所有清理路径都没跑到，下一次
  `PlayBossBgm` 的 `IsPlaybackFromCurrentScene()` 也会因场景 handle 变化而放行重播（`:277-281`）✅。
- **stinger 与循环互不干扰**：stinger 走 `ModBehaviour.PlaySoundEffect`（官方 `PostCustomSFX`，
  不占 bgmSource），不会打断循环曲 ✅。
- **`AudioSource` 创建/销毁**：本系统**不自建 AudioSource**，全部委托官方 FMOD 通道，无泄漏面 ✅。
- **订阅幂等 + 退订**：协调器**零全局事件订阅**（靠 `_playingSceneHandle` 自愈，见未提交注释的论证），
  因此不存在订阅泄漏 ✅；`ResetStaticCaches:628-651` 清空全部静态状态并先 `StopBossBgm()` ✅。
- **未提交改动**（`:479-491`）只新增注释，论证「不要把 `NotifySceneChanged` 接进 `OnSceneLoaded`」，
  推理链（additive 加载时活动场景不变 → 无条件清租约会在战中抹掉记账 → `_playingBossKey` 为 null →
  `ReleaseBossBgm` 提前返回 → 曲子放到下次真正切场景）经代码核对**成立** ✅。
  该注释同时是 U-1 的证据来源。

### 3.7 共享 UI 库与 UI 规范（AGENTS 4.14）

- `Common/UI/BossRushUI.cs` 的 `CreateCanvasRoot:340-354` 内部完成 `Canvas + ConfigureCanvasScaler + GraphicRaycaster`
  三件套，`raycaster.enabled = interactive` 让 HUD 点击穿透 ✅。
- **`sortingOrder = ` 魔法数字 grep**：全仓命中 15 处，其中 Canvas 相关只有 6 处，
  且**全部用命名常量**（`BossRushUI.cs:345` 是共享库参数、`MODEF_BOUNTY_RADAR_CANVAS_ORDER`、
  `CanvasSortOrder`、`SortingOrder`、`MARRIAGE_VIDEO_CANVAS_ORDER`、DailyReport/WishFountain 的
  `parentCanvas` 相对计算）；其余 9 处是 `SpriteRenderer` / `LineRenderer` / `TMP` 的世界空间
  `sortingOrder`，不在 4.14 的 Canvas 层级表管辖范围内 ✅。
  **本轮范围内（Campaign）零魔法数字**：`CampaignHud.cs:180` 用 `BossRushUILayers.HudOverlay`，
  `CampaignBoardView.cs:84` 用 `BossRushUILayers.Panel` ✅。
- **字体**：`ZombieModeUIHelper.CreateTMPText:244-268` 内部就调 `GetGameFont()` 并赋 `tmp.font`，
  因此 `CreateText` / `CreateButton` 产出的所有文本天然带游戏字体；
  Campaign 侧额外的 `BossRushUI.ApplyGameFont` 调用是冗余但无害的双保险 ✅。
  范围内**零 `Resources.GetBuiltinResource<Font>("Arial.ttf")`**（唯一一处在
  `BossRushUI.GetLegacyChineseFont:310`，是 legacy 调试面板的显式回退，已有注释说明）✅。
- **TMP**：Campaign 两个界面全部用 `TextMeshProUGUI`，无 legacy `UI.Text` ✅。
- **`ApplyPanelSkin` 九宫格 border 缺失时的表现**：`BossRushUI.cs:317-334` 只设
  `sprite` / `type = Sliced` / `pixelsPerUnitMultiplier = 1f`，**不校验 border**。
  注入一张 border 全 0 的图时 Unity 的 `Image` 会退化成整图拉伸（不抛异常、不打警告），
  表现即 AGENTS 第 14 节所述的"面板拉伸变形"。这条风险由构建期兜住 ——
  `Assets/Editor/BossRushUISkinBundleBuilder.cs` 用代码设 border 并回读校验（AGENTS 明写）——
  运行时 fail-soft 是可接受的设计，**不作为 finding**。
- **skin bundle 缺失 fail-open**：`BossRushUISkinLoader.EnsureBundleLoaded:124-129` 文件不存在时
  只 `DevLog`「素材还没做是完全正常的状态，不喊错」并回落程序化皮肤 ✅；
  只有 panel 或只有 button 时允许单张注入（`:82-84`）✅。
  注入时机在 `Utilities/AlwaysOnRuntimeHooks.cs:185-194`（常驻初始化，早于任何面板创建）✅。
  `Cleanup:171-198` **先复位注入点再 `Unload(true)`**，避免留下已销毁的 Sprite 引用 ✅。
- **静态缓存 `ResetStaticCaches`**：`BossRushUI.ResetStaticCaches` 在 `ModBehaviour.cs:777` 被调
  （并由 `tests/BossRushUISharedLibraryGuard.py:95` 钉住），销毁 `HideAndDontSave` 的程序化 Sprite/Texture ✅；
  `BossRushUISkinLoader.Cleanup` 在 `ModBehaviour.cs:779` ✅。
- **`CampaignHud` 切图销毁与静态引用清空**：Canvas 走 `DontDestroyOnLoad`（`:181`）故意跨场景常驻，
  `ResetStaticCaches:242-263` 销毁 Canvas 并把 4 个静态引用与 2 个快照容器全部清空 ✅；
  `CampaignBoardView` 的 Canvas **不**做 DontDestroyOnLoad，随场景销毁后 `_root` 变假 null、
  `IsOpen` 自然为 false，无残留 ✅。
- **模态输入租约**：`CampaignBoardView` 未取 `ZombieModeUIHelper.ClaimModalInput`。
  这与同族的基地信息面板一致（`Integration/Codex/CodexView.cs:339`、
  `Integration/BackMountain/ShowcaseUI.cs:77` 同样只有 `CreateBackdrop` 无租约），
  取租约的是 ModeG/ModeH/PetNest/ZombieMode/Wedding 这些接管局内的界面。属既有设计口径，**不作为 finding**。

### 3.8 章节数据契约（`docs/contracts.md` §3.2）

- `CampaignContentCatalog.TryLoadFromJson:141` 用 `ModeHCanonicalDigest.TryParse`（严格 token parser），
  **全文无 `JsonUtility`** ✅。
- 整表校验完整：version==1、chapterId 非空且唯一、order 必填、mode 白名单、
  rewardCash≥0、facilityToken/clueId 非空、objective type 白名单（未知即整表回退）、
  threshold>0、每章至少一个目标、order 从 1 连续、章数==6、final 只在最后一章、
  token/clue 全表唯一，**外加与硬编码 fallback 的内容签名逐字比对**（`:294-300`）——
  任一不合约即 `LogTableRejected` + 返回 null 整表回退 ✅。
- **回退时有日志且是 `CriticalLog`**（`:318-323`，按 key 去重，正式构建可见），不是会被剥离的 DevLog ✅。
- 公开 `Source`（Json/Fallback）与 `ContentSignature`（FNV1a64 十六进制）✅。
- **硬编码 fallback 与 JSON 逐字段比对完全一致**（六章的 id/order/mode/中英标题/奖金/token/clue
  与每条 objective 的 type/threshold/descCN/descEN 全部核对）✅ ——
  且由 `:294-300` 的签名校验在运行时强制，JSON 一旦偏离就整表回退而不是静默生效。

### 3.9 编译清单与文件登记（AGENTS 4.1）

脚本化核对（`compile_official.bat` 的 `echo(` 行 vs 磁盘）：
- `Campaign/` + `Audio/` + `Common/` 全部 51 个 `.cs` 加 `Config/ConfigCampaign.cs`、
  `Localization/CampaignLocalization.cs` 共 53 个目标，**清单缺失 0 个** ✅。
- **旧名 `Common/Infrastructure/ReflectionCache.cs` 已从清单移除且磁盘已无该文件** ✅；
  清单里剩的 `Common/Utils/ReflectionCache.cs` 是**另一个类**（运行时按需字典懒缓存，
  与 `BossRushEagerReflectionCache` 的启动期急切绑定不同，改名理由见新文件 `:15-21` 的 remarks），
  磁盘存在，**不是遗漏** ✅。
- 全清单**零"列了但磁盘没有"**的条目（含未提交改动新增的 13 行）✅。

### 3.10 ModConfig 白名单（AGENTS 第 14 节「内容系统总开关恒开」）

- `campaignEnabled` 默认 true（`Config/ConfigCampaign.cs:31`），
  在 `ForceContentSystemSwitchesOn`（`Config/ConfigContentSystemSwitches.cs:42`）里被拉回 true，
  由 `Config/Config.cs:199` 在 `LoadConfigFromFile` 末尾调用；
  `tests/ModConfigOptionChangeGuard.py:52` 的 `CONTENT_SYSTEM_SWITCHES` 已收录 ✅。
- 未注册进 ModConfig UI，符合「系统总开关不暴露给玩家、默认开启」的 owner 口径 ✅。
- 唯一只读入口 `IsCampaignConfiguredEnabled()` no-throw、缺配置返回 false，无反射、无缓存副本 ✅。

### 3.11 本地化双向对齐

消费方 → 注入方全部命中，无死键、无缺键、中英双语齐全：
`BossRush_Campaign_Board_Interact`、`BossRush_Campaign_FinalBoss_Interact`、
`BossRush_Campaign_FinalBoss_Name`、`BossRush_Campaign_Broker_Name`、
`Building_bossrush_campaign_board`(+`_Desc`)、`Note_BossRushCampaign_clue_ch1..ch6_Title/_Content`（12 条）。
- 注入入口 `CampaignLocalization.Inject()` 挂在 `Integration/BossRushIntegration_StartAndScene.cs:53` ✅；
  建筑键另由 `CampaignBoardBuilder.cs:89` 在创建 prefab **之前**再注一次（顺序不能颠倒，注释已说明）✅；
  `CampaignDialoguePlayer.cs:112` 在独白前防御式再注一次 `FinalBoss_Name`（因为 Boss 生成时才注、晚于独白）✅。
- 键构造与官方查表规则一致：`Note_` + `NoteKeyPrefix`(`BossRushCampaign_`) + `clueId`，
  对应官方 `Note.titleKey => "Note_" + key + "_Title"`（`Duckov/NoteIndexs/Note.cs`）✅。
- **全部无 `{0}` 占位符**，范围内 `string.Format` grep 零命中，不存在实参数不匹配问题 ✅。

### 3.12 美术资产接线

- `Assets/ui/campaign_presentation`（1.28 MB bundle）与 `Assets/ui/Campaign/` 下 8 个 raw PNG 同时存在；
  文件名与代码里的资源名逐一对应：`campaign_portrait_broker`、`campaign_portrait_champion`、
  `campaign_poster_ch1..ch6` ✅。
- 消费点齐全：中间人立绘 `CampaignDialoguePlayer.cs:266`、冠军立绘 `:288`（本轮未提交改动新增，
  在此之前**零调用点**）、章节海报 `CampaignNoteBridge.cs:100` ✅。
- **按需加载 / fail-open / 卸载成对**：`CampaignAssetCache.GetSprite:78-100` bundle 优先→raw PNG→null，
  null 也缓存避免反复打盘；缺立绘时官方 DialogueUI 自动隐藏立绘位，对话照常播 ✅；
  `Unload:197-228` 先销毁程序化 Texture/Sprite（`_ownedObjects` 记账）再 `Unload(true)`，幂等 ✅。
- **卸载顺序正确**：`CampaignRuntimeModule.OnDestroy` 里 `CampaignDialoguePlayer.ResetStaticCaches()`（`:178`，
  销毁持有立绘引用的 actor 宿主）**排在** `CampaignAssetCache.ResetStaticCaches()`（`:180`，`Unload(true)` 销毁立绘）之前 ✅。

### 3.13 运行时模块宿主与共享基础设施

- `BossRushRuntimeModuleHost`：六个生命周期回调**逐模块 try/catch**（单模块异常不拖垮其他），
  `OnDestroy` 逆序（`:104`），`Register` 用 `modules.Contains` 去重 ✅；
  异常经 `CriticalLog` 按 `模块+阶段` 去重上报（本轮改动，正式构建可见）✅。
- `BossRushRuntimeModuleRegistration:45-52`：Campaign 先注册、BackMountain 后注册，
  注释写明「后山 bootstrap 要订阅征程的解锁事件，host 按注册顺序回调」——启动顺序依赖成立 ✅；
  单实例纪律（先存字段再把**同一引用**注册给 host）✅，无双注册。
- `BossRushEventBus`：全仓仅一对生产者/消费者（`Achievement/BossRushAchievementManager.cs:299` Publish ↔
  `Achievement/AchievementRuntimeHooks.cs:13/44` Subscribe/Unsubscribe，命名方法、成对）；
  `Subscribe` 有 `Contains` 幂等；`Publish` 深度 0 用共享 scratch、嵌套发布改用 `ToArray()`
  （不会被内层踩坏，逐帧推演确认）；handler 异常按事件类型去重隔离；
  `ResetStaticCaches` 在 `Utilities/AlwaysOnRuntimeHooks.cs:335` 被调 ✅ —— **无订阅泄漏面**。
- `JsonDataRegistry`：唯一 JSON 读取入口，三条失败路径（modPath 空 / 文件不存在 / 读取异常）
  本轮全部从 DevLog 升级为按 key 去重的 `CriticalLog` ✅；新增的 `subDirectory` 重载
  与旧单参重载行为向后兼容（`subDirectory` 为空时路径不变）✅。
- `ObjectCache`：本轮只加注释无行为改动；注释里的两条论断都经 grep 反向验证 ——
  `GetBoxColliders` **确实零调用点**（全仓仅定义）、`GetTmpFonts` 确实只被内部 `:163` 消费 ✅。
- `MapSpawnPointRegistry` / `BossRushMapConfig`：本轮新增的 5 个 Mode H 可选字段是 `SCHEMA+`，
  `ExtractVector3Array` / `ExtractNullableVector3` 在键缺失时返回 null，构造器原样存 null，
  旧地图 JSON 不受影响 ✅；三处失败日志同样升级为 `CriticalLog` ✅。

### 3.14 通用缺陷扫描（范围内）

- `string.Format`：范围内**零命中**，不存在实参/占位符错配 ✅。
- 除零：两处除法均有保护（见 §3.3）✅。
- 遍历中改集合：范围内 5 处 `foreach` 全部遍历只读快照或不在循环内改集合 ✅。
- 越界：`CampaignHud.HasProgressChanged` 在索引前先比 `_shownCurrent.Count != progress.Count`，
  `_shownFailed` 与 `_shownCurrent` 由 `CaptureProgressSnapshot` / `HideImmediate` / `ResetStaticCaches`
  三处**成对**增删，长度恒等 ✅。
- 空引用：`CampaignBoardView.BuildDetailText:253-254` 用 `progress[i].Def == objective` 引用比对，
  正确挡住"显示的不是武装中那一章"的错配 ✅。

---

## 四、对已 Fixed 条目的复核

| 条目 | 复核结论 | 依据 |
| --- | --- | --- |
| **CR-2026-08-31-003** 终章打输一次即永久卡死 | ✅ 修复在位、无回归 | 三条收尾路径逐条核对（§3.5）；本轮未提交的独白层也接上了同一套 runId 作废机制。**但 U-1 指出 `OnSceneLoaded` 收尾不区分 additive，可能引入新的误收尾面** |
| **CR-2026-08-31-005** 召唤石 tick 每帧分配字符串 | ✅ 分配已消除，⚠ 注释口径不准 | `ShouldCampaignFinalBossAltarExist` 已把零分配的契约查询排在场景判定之前，场景判定改走 `IsCampaignArenaSceneCached`（按 `SceneGeneration` 缓存），全链路无托管分配；但"字典命中、一次查询"的说法与实现不符 → F-5 |
| **CR-2026-08-31-006** 契约 HUD 每帧构建字符串 | ✅ 修复在位 | `CampaignHud.Tick:84` 的 `HasProgressChanged` 确实在**构建之前**、只比 int 与 bool；`CaptureProgressSnapshot` 用复用容器；`HideImmediate` 作废快照保证再次显示必重建 |
| **CR-2026-08-31-007-4** 删掉"公告板有线索页签"的假承诺 | ✅ 已改 | `CampaignNoteBridge.cs:17-19` 现在只承诺官方笔记图鉴，并明写"不能假称公告板另有线索页签" |
| **CR-2026-08-31-007-5** 召唤石在击杀后到交付前不得重复召唤 | ✅ 已加门禁 | `ShouldCampaignFinalBossAltarExist:225` 增加 `GetState(def.ChapterId) != ContractActive → return false` |
| **CR-2026-08-31-007-7** ReadyToDeliver 落盘 | ✅ 已落盘 | `NotifyObjectivesSatisfied:286` 走 `WriteState(chapterId, ReadyToDeliver)`；`GetState:107-111` 与 `GetActiveChapterId:145-149` 都优先读存档态，会话缓存只做加速 |
| **CR-2026-08-31-008-3** 交付忽略存档失败 | ✅ 事务化到位，⚠ 见 F-1 | 克隆 `CloneSaveData` → `Store` → 成功后才 `TryGrant` 发布 token；失败退款并保持待交付。**但"存档成功"实际只到入队 + `RequestFlush`，`SaveFile` 抛异常后的重试链有洞（F-1）** |
| **CR-2026-09-01-009-1** 章节表被 JsonUtility 静默读空 | ✅ 已换严格 parser | `CampaignContentCatalog:141` 用 `ModeHCanonicalDigest.TryParse`，且整表校验 + 签名比对齐全（§3.8） |
| **CR-2026-09-01-009-6** 自建交互体未在 `base.Awake` 前初始化官方私有分组表 | ✅ 两个交互体都已接线 | `CampaignBoardInteractable.Awake:35` 与 `CampaignFinalBossInteractable.Awake:34` 均在 `base.Awake()` 之前调 `NPCInteractionGroupHelper.GetOrCreateGroupList`；必要性经官方 `InteractableBase.cs:180-188` 核实（无条件 foreach，null 必 NRE） |
| **CR-2026-09-02-007** BGM 曲目表经 JsonUtility 读出空数组 | ✅ 已换显式 token 解析 | `BossBgmTrackTable.TryParse` 复用 `ModeHJsonParser`；协调器全文无 `JsonUtility`；数据表与素材四条对得上（§3.6）。**副作用：AGENTS 第 14 节指向该文件的 CS0649 pragma 说明已过时 → F-2** |
| **CR-2026-09-02-017** 终章直接销毁未清熔石掉落订阅 | ✅ 两条主动销毁路径都已清 | `CampaignFinalBoss.cs:390`（迟到 Boss）与 `:613`（让路销毁）都在 `Destroy` 之前调 `ClearBossRandomLootTracking` |
| **CR-2026-09-03-005** 交付退款失败可重复领奖金 | ✅ 闩已实装，残留已登记 | `_cashPaidPendingChapterId` 三处置位/清空齐全；会话级残留见 U-2（原条目自己已 accepted） |
| **CR-2026-09-03-011** 目标追踪按"进场景"武装且换模式不解除 | ✅ 三条修复都在位，并已反向验证 | 标准分支带 `IsActive`、丧尸改 `ZombieModePhaseGuards.IsRunActive`、`EnsureArmedFor` 模式不符先 `ResetSession`；额外反向验证 `IsActive` 的全部写点确认局中不会翻转（§3.3），排除了"重新武装抹掉无伤失败位"的次生漏洞 |

### 守卫复核（本轮未提交改动新增的断言）

`tests/CampaignSkeletonGuard.py` 新增第 7、8 段共 5 条断言，逐条对照当前代码**均为真**（非假绿）：
- `"CleanupCampaignBoardBuilding();"` → `Integration/BossRushIntegration_StartAndScene.cs:175` ✅
- `"CampaignAssetCache.GetChampionPortrait()"` → `CampaignDialoguePlayer.cs:288` ✅
- `"_championActorHost"` → `CampaignDialoguePlayer.cs:52/282/284/290/309` ✅
- `"PlayFinalBossPrologueAsync"` → `CampaignFinalBoss.cs:334`（未提交）✅
- `"StartCampaignFinalBossAsync(runId).Forget();"` → `CampaignFinalBoss.cs:360`，字面量唯一
  （独白层用的是 `await StartCampaignFinalBossAsync(runId);`，不会误命中）✅

`tests/BossBgmCoordinatorGuard.py` 的 5 段断言对照当前代码同样均为真；
其中 `"bool loop = true"` 命中 `BossBgmTrackTable.cs:12`、
`"new Type[] { typeof(string), typeof(bool) }"` 命中 `BossBgmCoordinator.cs:551` ✅。
两个守卫都是纯只读、只 print，**未执行**（遵守只读纪律），以上为静态对照结论。

---

## ==== ModesDEFG_Zombie_misc ====

# Mode D/E/F/G + 丧尸模式 + 地图选择/成就/Boss筛选 审查报告

> 范围：`f9b83c0..HEAD`。`ModeD/` `ModeE/`（复核旧草稿）`ModeF/` `ModeG/` `ZombieMode/`
> `MapSelection/BossRushMapSelectionHelper.cs` `Achievement/` `BossFilter/` `Config/ConfigModeG.cs`
> 及相关 `tests/*Guard.py`。全程只读，未编译、未跑 guard、未实机。
> 所有判定除非注明，均为**静态确认，未运行验证**。
>
> **覆盖率（逐文件，非抽样）**：Mode D 3/3、Mode E 8/8（复核旧草稿）、Mode F 11/11、
> Mode G 14/14 + `Config/ConfigModeG.cs`、ZombieMode 19/19、MapSelection 1/1、
> Achievement 6/6、BossFilter 2/2 = **65 个改动文件全部过完**；
> 另核对了 `tests/` 侧 20 余个相关 guard 的 diff（专查「放宽断言掩盖行为变化」）。

---

## 一、Confirmed Findings（按 P0>P1>P2>P3）

### P2

#### F-1（P2 / COMPAT）Mode F 补位重生的克隆预设没挂租约，与 Mode E 的修复不对称

- 证据：`D:\code\ykf\BossRushMod\ModeF\ModeFRespawn.cs:855-862`

```csharp
if (ctx.character.characterPreset != null)
{
    CharacterRandomPreset customPreset = UnityEngine.Object.Instantiate(ctx.character.characterPreset);
    customPreset.aiCombatFactor = 1f;
    customPreset.showName = true;
    customPreset.showHealthBar = true;
    ctx.character.characterPreset = customPreset;
}
```

- 对照 `D:\code\ykf\BossRushMod\ModeE\ModeEBattle.cs:671-692`：同样的克隆之后立刻
  `AddComponent<ModeECharacterPresetLease>()` + `presetLease.Assign(customPreset)`。
- `ModeECharacterPresetLease` 的类注释（`ModeEBattle.cs:858-861`）自称
  「**Mode E/F** 运行时克隆预设的对象级租约」，但 Mode F 自己的第二个克隆点没有用它。
- 本轮 commit `1583da1` 在 `ModeFRespawn.cs:397`（`CleanupModeESharedRuntimeForModeFBoss`）
  删掉了 `UnityEngine.Object.Destroy(boss.characterPreset);`（见 `diff_ModeF.patch` 唯一的
  ModeFRespawn hunk），修掉了 CR-2026-09-01-010 #2 的提前销毁；但删除后**没有补上租约**，
  于是这条路径从「提前销毁」变成「永不销毁」。
- Mode F 两条 Boss 生成路径已核实是**分叉**的，不是同一条：
  - 准备/开局：`ModeEBattle.cs:646 OnModeEEnemySpawned`（`isModeFRun == true`）→ 克隆 + 租约。
  - 战中补位重生：`ModeFRespawn.cs:836 ConfigureModeFRespawnedBoss`（`SpawnEnemyCoreInternalAsync`
    的 `onCommit`，`ModeFRespawn.cs:801`）→ 克隆，**无租约**。
    `RegisterModeFBoss` 全仓仅两处调用（`ModeEBattle.cs:778`、`ModeFRespawn.cs:869`），印证只有这两条路径。
- 失败场景：血猎追击每死一个 Boss 就补一只（`TryFulfillModeFPendingRespawns` →
  `RespawnModeFBossAsync`）。每次补位泄漏一个 `CharacterRandomPreset`
  ScriptableObject 实例；Boss 死亡、`CleanupModeESharedRuntimeForModeFBoss` 走完、
  GameObject 销毁之后，这个实例仍然存在，只能等 Unity 下一次
  `Resources.UnloadUnusedAssets`（通常是非叠加式换场景）才回收。一局长时间血猎追击
  会累积几十个孤儿 ScriptableObject。不影响正确性，是**跨局前的内存泄漏**。
- 需要注意的次生风险（同一处）：`ctx.character.characterPreset = customPreset` 覆盖时，
  被覆盖的那个 preset 如果本身也是运行时副本（龙裔/龙皇由
  `DragonDescendantBoss_ModeGAdapter` / `BossCleanupHelpers` 体系生成），
  同样失去引用；而 `Utilities/BossCleanupHelpers.cs:43-59 DestroyRuntimePreset` 是按
  `nameKey`/`name.StartsWith(presetName)` 匹配销毁的，`Instantiate` 的克隆名是
  `"<原名>(Clone)"`，**仍会命中 StartsWith 分支**——也就是说自定义 Boss 的清理路径会去销毁
  Mode F 的克隆，而不是它自己那份。该行为在本轮之前就存在，此处只作记录，不单列 finding。
- 建议修复：在 `ModeFRespawn.cs:861` 之后照抄 `ModeEBattle.cs:682-688` 的三行租约挂接。
  另可补一条 guard 断言「凡 `Instantiate(...characterPreset)` 之后必须同一方法体内出现
  `ModeECharacterPresetLease`」——当前 `tests/` 下**没有**任何 guard 覆盖这个不变式
  （`grep -l PresetLease tests/` 只命中 `LatestPlayerLogRegressionGuard.py`，与此无关）。
- 验证需求：静态确认，未运行验证。修复后需实机跑一局 Mode F 观察补位重生的 Boss 血条/名牌正常。

### P3

#### B-1（P3 / COMPAT）Boss 池每次勾选都会触发遗种巢血脉目录全量重建，图鉴面板开着时还会整页重画

- 证据：`D:\code\ykf\BossRushMod\BossFilter\BossFilter.cs:229-250`

```csharp
private void InvalidateFilteredPresetsCache()
{
    _filteredPresetsCacheDirty = true;
    try
    {
        if (PetNestRuntime != null) PetNestRuntime.NotifyEnemyPresetsRefreshed();
        if (CodexRuntime != null) CodexRuntime.NotifyEnemyPresetsRefreshed();
    }
    ...
}
```

- 两个消费端的成本不对称：
  - `Integration/Codex/CodexRuntimeModule.cs:262-280` 是**惰性**的（`CodexBossCatalog.Invalidate()`，重建推到下次 `EnsureBuilt`），
    但面板开着时会 `EnsureBuilt` + `CodexView.Instance.RefreshAll()`——整页重画。
  - `PetNest/PetNestRuntimeModule.cs:295-307` 是**急切**的：`PetNestLineageCatalog.Invalidate()` 紧跟
    `EnsureBuilt(_owner)`，而 `PetNest/PetNestLineageCatalog.cs:137` 又会回调
    `owner.GetFilteredEnemyPresets()`——刚被置脏的缓存在这里立刻重跑一遍 LINQ `Where().ToList()`。
- 失败场景：`BossFilter.cs:186 SetBossEnabled` 每次被调用都走这条链。玩家在 Boss 池面板里
  逐个勾选 N 个 Boss = N 次「LINQ 全量过滤 + 血脉目录全量重建 + 一行 DevLog」。
  单次成本很小（预设约数十条），**不是每帧热路径**，因此定 P3 而不是 P2。
- 更明显的放大点在 dev 路径：`WavesArena/WavesArenaBossSpawning.cs:161-165`（收窄到单 Boss）
  与 `:200-203`（还原）都是**逐名字循环调用 `SetBossEnabled`**，一次收窄/还原就是 2N 次全量重建。
  这两处被 `if (!DevModeEnabled) { ... return false; }`（`:140`）挡住，**玩家侧不可达**，
  只影响 F3 验收耗时。
- 对照：`EnableAllBosses` / `DisableAllBosses`（`BossFilter.cs:262-286`）是「先改完整个字典再 invalidate 一次」，
  写法正确；`SetBossEnabled` 的循环调用方没有走同款批处理。
- 正确性无问题：已核对 `NotifyEnemyPresetsRefreshed` 两个实现都不会回调
  `InvalidateFilteredPresetsCache`，无递归；`EnsureBuilt` 有 `_built` + `lock` 双重保护；
  中间态目录会被最后一次调用覆盖成正确结果。
- 建议修复：给 `SetBossEnabled` 加一个 `bool notifyCatalogs = true` 形参（或提供
  `SetBossEnabledBatch`），让 `WavesArenaBossSpawning` 的两个循环末尾只 invalidate 一次；
  或把 PetNest 侧改成与 Codex 同款惰性（只 `Invalidate()`，重建留给 `EnsureBuilt`）。
- 验证需求：静态确认，未运行验证。

#### B-2（P3 / COMPAT）Boss 池面板 sortingOrder 由 10 抬到 2000，原注释写明的「避免遮挡鼠标光标」意图被连注释一起删掉

- 证据：`D:\code\ykf\BossRushMod\BossFilter\BossFilterUi.cs:22`

```csharp
// 旧：canvas.sortingOrder = 10;  // 降低层级，避免遮挡鼠标光标
canvas.sortingOrder = BossRushUILayers.Panel;   // = 2000
```

- 该面板带一张 anchorMin=0 / anchorMax=1 的全屏 `Backdrop` 背景（`BossFilterUi.cs:31-40`），
  且 canvas 是 `DontDestroyOnLoad`。层级从 10 抬到 2000 之后，任何 sortingOrder 介于两者之间的
  游戏内 UI（官方 `Duckov/UI/FollowCursor` 这类跟随光标的准星层）会被这张全屏遮罩盖住。
- 减轻因素：`BossRushUILayers.Panel = 2000` 是 4.14 收口后的共享面板层，
  `Achievement/AchievementView.cs:156`、`Integration/Codex/CodexView.cs:335`、
  `Campaign/CampaignBoardView.cs:84` 等 6 个既有面板已经在用同一层且未见问题；
  设置面板打开时压住准星通常也是期望行为。
- 因此定 P3：**「有意设定被静默改掉」这件事是静态确认的，实际观感回归未验证**。
  若 owner 确认新观感可接受，把结论回填到 4.14 即可，不需要改代码。
- 验证需求：需实机打开 Boss 池面板确认鼠标/准星仍可见。

---

## 二、未验证线索（需实机或 owner 确认）

- **U-G1：Mode G 战斗帧写屏障的欠账没有自主重排程**。
  `ModeG/ModeGPersistenceFlushCoordinator.cs:174-178` 在战斗帧只置 `_hostWriteDeferred = true` 就 return，
  而 `CompleteBatchAndMaybeReschedule`（:199-217）的重排程条件只看 `_pending`
  与两个 store 的 `HasPendingFlush`，**不看 `_hostWriteDeferred`**。
  于是欠账只能等下一次 Store、`End()` 的显式 `RequestFlush()`（`ModeGRuntimeModule_PublicApiAndShutdown.cs:344`）
  或 `TryFlushOnHostDestroy` 的 `forceHostWrite:true` 来结清。
  静态看不会丢数据（typed Save 已写进内存存档字典，官方任意一次存盘会顺带带走），
  设计注释也明说了这一点；但「战斗中崩溃/强杀进程」这一窗口内物理文件确实落后。
  需 owner 确认该窗口可接受，或补一条「欠账也触发重排程」。
- **U-G2：Mode G 放弃确认页开在生成相位是否会误报 `TechnicalIntegrityLoss`**。
  `ModeGEntry.cs:685-705 TryHandleModeGAbandonHotkey` 只按 `state.IsActive`（lifecycle）放行，
  **不看 `combatPhase`**，因此波次生成在途时也能打开确认页并 `ClaimModalInput`（时停）。
  官方 `CreateCharacterAsync` 是否受 `timeScale` 影响、生成超时窗口是否会被时停撑爆，静态无法判定。
  与 `CODE_REVIEW_FINDINGS.md` 既有 seeded lead「模式G 弃局确认页开在 Spawning 相位……」同一条，
  本轮新增的快捷键使它**从理论可能变成玩家随时可触发**，优先级应提升。
- **U-Z1：丧尸安全区取消规则收窄后，「徒手 / 非武器伤害」是否成为免罚站桩位**。
  `ZombieMode/ZombieModeWaveController.cs:128-153 IsZombieModeSafeZoneCancellingWeapon（消费点 :112）` 要求
  `damageInfo.fromWeaponItemID > 0` 且带 `Gun/Weapon/MeleeWeapon/Melee` tag。
  徒手攻击（`fromWeaponItemID == 0`）、以及任何不带这四个 tag 的武器/道具伤害都**不再取消安全区**。
  官方手雷走 `Grenade.cs:201` 也写 `fromWeaponItemID`，其是否带 `Weapon` tag 只能由
  `ItemShortcut.cs:129` 的反证间接推断（手雷可进快捷栏 ⇒ 无 `Weapon` tag），未直接查数据表。
  需实机在安全区内分别用枪/近战/手雷/徒手打一次，确认取消行为符合设计意图。
- **U-M1：Mode H intent 的 generation 可能为 0**。
  `MapSelection/BossRushMapSelectionHelper.cs:636-652 SetPendingMapEntryIndex` 在
  `pendingEntryKind == ModeH` 时会重绑目标场景，但**不递增也不校验 generation**；
  若某条路径先把 kind 置为 ModeH 而没调用 `FreezeModeHEntryIntent`，
  `TryMatchModeHSceneIntent` 会返回 `true` 且 `sceneGeneration = 0`。
  当前唯一的 ModeH 入口（`ModeH/ModeHEntry.cs:130` → `:134`）顺序正确（先 Freeze 再开 UI），
  所以现状不可达；但这是跨模块的隐式前置条件，属 ModeH 侧应确认的契约。
- **U-B1：B-2 的观感回归**（见上）。

---

## 三、已核对且干净的关键面

> 逐文件流水见文末附录。此处只列「查过、结论是干净」的关键面。

1. **Mode F 命火 Bloodfire 的 Moveability 坑（AGENTS §14）**：只挂 `WalkSpeed`/`RunSpeed`，
   未挂 `"MoveSpeed"`；四个 stat key 全部在官方 `CharacterMainControl` 里有 hash 字段实证。
   apply/revert 成对，五条退出路径全覆盖，无除零/溢出，tick 零分配。
2. **Mode F 悬赏印记闩的事件顺序前提**：官方 `Health.cs:439` 先 `OnDeadEvent`、:444 才静态 `OnDead`，
   与注释一致；闩按 instanceID 配对、读取即失效、双向重置。
3. **Mode G 放弃入口全链**：ModConfig 键已登记白名单（不踩「白名单陷阱」）、三个配置方法都有调用点、
   presenter 单实例 + 输入租约双路径释放 + 三处幂等关闭覆盖。
4. **Mode G `OnSceneLoaded` override 删除的可达性论证成立**：注册的是空壳实例
   （`Common/Lifecycle/BossRushRuntimeModuleRegistration.cs:16`），真实 run 实例
   （`ModeGEntry.cs:570`）从不注册；替代链 `Utilities/ModeRuntimeHooks.cs:37-46` 实在，
   guard 已改为反向断言。
5. **Mode G 入场取消不吃道具**：两条新早返都在 `TryConsumeModeEntryItem` 之前，
   且调用方对 `TryStartModeG()==false` 会退还地图预扣船票。
6. **Mode G 成就 token 掺实例身份 / 终局消费早于 session 关闭**：两条都是真修复，去重键与
   session 闸门的实现已逐行核对。
7. **丧尸 `SafeZoneStealthBroken` 删除是死状态清理**：`git grep` 证明基线上全仓无一处赋 true。
8. **丧尸掉落拾取节流不会误销毁背包物品**：官方 `Inventory.cs:492` 的 `SetParent`
   使进包物品随玩家移动，恒落在 5m 距离豁免内；节流是固定时长而非性能档分叉。
9. **丧尸两条真修复**：弹出与去仇恨解耦（`shouldSuppress` 前置并传参）、逐敌抑制补记
   `SafeZoneThreatSuppressed`（否则 `ReleaseZombieModeSafeZoneThreatSuppression` 早返、敌人永不恢复追击）。
10. **丧尸/Mode E/F/G 都不接共享 mutator roll，本轮没有引入性能档分叉的玩法差异**；
    `ZombieModeModels` 新增 5 字段在两个 `Reset()` 里都补齐。
11. **地图选择的入口互斥与 intent 冻结**：`BossRushPendingEntryKind` 是单一枚举，天然互斥；
    `ModeHEntry.cs:27-32` 显式拒绝 pending ModeG；`MarkEntryFlowFromMapSelectionUi(kind)`
    刻意不清 ModeH 的目标场景与 generation，`FreezeModeHEntryIntent` → `OpenModeHMapSelection`
    的顺序因此安全。过滤后的条目索引改由 `BossRushMapEntryClickHandler.entryIndex`
    （全量表下标，`FindMapConfigIndex` 求得）承载，`RefreshAllEntryDisplayNames` 已同步读它，
    **不会出现「筛选后点错地图」**。克隆官方 `MapSelectionEntry` prefab 的逻辑本轮未改。
12. **成就分类追加安全**：`Taming`/`Codex` 追加在 `Ultimate` 之后，0..6 的既有 int 值不变；
    且存档实际是 `SavesSystem.SaveGlobal(SAVE_KEY_UNLOCKED, List<string>)`
    （`BossRushAchievementManager.cs:424`），**只存字符串 id，根本不依赖枚举 int**，
    所以比 guard 注释宣称的还要安全（该注释「存档依赖 int 值」略有夸大，不构成缺陷）。
    全仓无 `case AchievementCategory` 分派表，新分类不会漏文案。
13. **10 个新成就的触发点全部存在**：5 个 `petnest_*` 在 `PetNest/PetNestMuseumStats.cs:136-152`，
    5 个 `codex_*` 在 `Integration/Codex/CodexMilestones.cs:53/100/104/109/117`；
    现金数额与 `Integration/Codex/CodexTuning.cs:125-129` 台账逐项对齐。
14. **`SteamAchievementPopup` 的 legacy `UI.Text` + 内置 Arial 已换成 TMP + `ApplyGameFont`**，
    这是 4.14「内置 Arial 渲染不了中文」的真修复；全仓已无 `GetBuiltinResource<Font>` 生产用法
    （仅 `Common/UI/BossRushUI.cs:310` 的 legacy 兜底 helper 保留）。
15. **Boss 筛选不会混入不该出现的 Boss**：全仓只有 3 处向共享 `enemyPresets` 注册
    （龙裔 `DragonDescendantBoss_RuntimeAndCleanup.cs:310`、龙皇 `DragonKingBoss.cs:755`、
    幽灵女巫 `PhantomWitchBoss.cs:812`）。终章「冠军之影」是在既有 Boss 上做运行时增强
    （`Campaign/CampaignFinalBoss.cs`），不注册预设；Mode H 用的是
    `ModeHRuntimeModule_CombatFlow.cs:252` 的**同名局部变量**，与共享池无关。
    → **终章变体与 ModeH 替身都不会出现在 Boss 池面板里**，符合预期。
16. **事件订阅**：本轮 Mode F/G/丧尸/成就/BossFilter 均未新增静态事件 `+=`；
    新增的 `ModeGAbandonPresenter` 只用 `Button.onClick` 与自身 `OnDestroy`，随对象销毁释放。
17. **新增 `.cs` 全部登记编译清单**：`ModeF/ModeFBloodfire.cs`（`compile_official.bat:325`）、
    `ModeF/ModeFUI_KillRewardBubble.cs`（:430）、`Config/ConfigModeG.cs`（:270）。
18. **保留的 Mode D / Mode E 草稿结论复核后仍成立**（见附录两节，未改动）。

---

## 四、对已 Fixed 条目的复核

| 条目 | 复核结论 |
| --- | --- |
| **CR-2026-08-17-001**（Mode G 官方快照逐 key eligibility / 九波计划去重） | 本轮 diff **未触碰** `ModeGBossSnapshot` / `ModeGWavePlan` / `ModeGManagedBossEligibility` 相关文件；`ModeGEntry.TryStartModeG` 里 `HasMinimumModeGOfficialBossPool()`（:290）与 `RefreshModeGSignatureEligibility()`（:378）两道 fail-closed 仍在。无回归。 |
| **CR-2026-08-17-002**（未知存档版本写屏障 / 挂起宿敌） | `ModeGNemesisPersistence.cs` 本轮**只改类头注释**（把「战斗中不写盘」的旧承诺改写成新的两段式语义），逐 key 写屏障与 `MarkCoordinatorFaulted` 未动。新增的 `_hostWriteDeferred` 只影响物理 `SaveFile` 时机，typed `FlushPending(writeFile:false)` 照旧逐 key 执行；`NotifySlotChanged` 一并清欠账，不会拿新槽文件补写旧槽。无回归，但见 U-G1。 |
| **CR-2026-08-17-003**（奖励回调异常 / Rewarding 死亡抢占） | nonce 双闸仍在：`ModeGRewardTransaction.Execute:510` 的 `!AreNoncesValid() \|\| state.rewardNonceInvalidated`，以及完成回调首行 `if (state.rewardNonceInvalidated) return;`（`ModeGDeathRouting.cs:269`）。本轮在 `Execute` 未启动的分支补了 `TryReturnRelicOnce`，**没有削弱 nonce 语义**（`Execute` 内 :521 的原返还点仍在，CAS 保证不双发）。无回归。 |
| **CR-2026-08-17-004**（启动退款所有权唯一） | `StartModeGRuntime(..., out startupRefundOwnedByRuntime)` 与外层 `if (!started && !startupRefundOwnedByRuntime)`（`ModeGEntry.cs:432-450`）原样保留。本轮新增的入场取消早返都在扣道具之前，不进这条退款分支。无回归。 |
| **CR-2026-08-17-005**（候选地图显式验证状态） | `IsModeGEntryPreviewValidForCurrentScene(preview)` 仍是 `TryStartModeG` 的必经闸（:393），且本轮把它从「`??` 短路可能跳过重建」改成「先 `GetOrCreate` 再校验」，**闸门比以前更严**。`ModeGEntryPreviewGuard.py` 同步加了反向断言禁止 `??` 短路。无回归。 |
| **CR-2026-08-29-014**（Mode G 无伤成就跨局残留） | **本轮才落地**（commit `5907422`）：`Achievement/AchievementTriggers.cs:433` 加 `AchievementTracker.HasTakenDamage = false;`。调用点可达（`ModeG/ModeGRuntimeModule.cs:220`）。刻意不调 `ResetSessionStats()` 的理由成立——那会连带重置 `ArenaEnterTime`/`HasUsedHealItem`/`HasPickedUpItem`，污染 Legacy/Mode D 会话。`ModeGAchievementIsolationGuard.py` 已加两条对应断言。**仍需按 finding 原文做实机验证**（先打一局受伤模式，再打无伤 Mode G）。 |
| **CR-2026-08-29-019**（Mode G 4 项 P2） | 四项逐条对上：①自动入场静默 → `ModeGEntry.cs:397/410` 补 `ShowMessage` + `ShowTerminalBannerFallback` 覆盖 6 种终局；②宿敌战斗帧落盘 → `_hostWriteDeferred` 写屏障；③`OnDestroy` 用 `new` 隐藏基类 → 改 `protected override` + `base.OnDestroy()`（官方 `InteractableBase.cs:531` 确为 `protected virtual`，已核）；④AFK 提示指向不可达操作 → 文案改为「撤离返回基地后重新入场」。**四项均已实装且实现与描述一致。** 其中 019-①还点名了 `WavesArena/BossRushEntryFlow.cs` 与 `Integration/BossRushIntegration_TravelAndSetup.cs`，那两个文件不在本人范围，需由对应审查员确认。 |
| **CR-2026-09-01-010 #2**（Mode E/F 克隆预设提前销毁） | Mode E 侧已修（租约）；**Mode F 补位重生路径漏挂租约 → 本报告 F-1**。这条 finding 现状是 `Open`，回填时应连同 F-1 一起处理。 |
| **CR-2026-09-01-010 #3**（Mode D 敌对性 + 结束清场） | 草稿已复核，`ModeDWaves.cs:579` 设置+回读+fail-closed、`ModeD.cs:287` 注销恢复→禁掉落→销毁，均在。无回归。 |
| **CR-2026-09-01-010 #4**（Mode E 商店 Albert 噪声） | 草稿已复核 `CreateConfiguredModeEMerchantShop`（`ModeEMerchant.cs:304`）的 inactive→bootstrap ID→Awake→稳定 ID 流程，且证明不污染 `StockShop_Merchant_Normal` 存档 key。无回归。 |
| **CR-2026-09-02-008**（F3 读 Mode F 瞬时结算标志） | 修复实装可见：`ModeF/ModeFExtraction.cs` 新增 `modeFSuccessfulExtractionCount` + `ModeFSuccessfulExtractionCountForValidation`，`:328` 处 `if (!modeFActive) modeFSuccessfulExtractionCount++;`——按「生产成功结算计数增量」判定，与 finding 描述一致。计数器刻意不随单局 Reset 清零，符合注释。无回归。 |
| **CR-2026-09-02-014**（丧尸撤离验收缺正数净化点样本） | 修复在 F3 验收侧（`DebugAndTools/`，非本人范围）。ZombieMode 生产侧本轮未改净化点初值与结算比例（`SettleZombieModeExtractionCashShell` 逐字未动），**没有为凑测试而改产品数值**。无回归。 |
| **CR-2026-09-03-009 的 Mode D 串台部分** | 草稿已复核 `WavesArena/WavesArena.cs:498` 的 `if (modeDActive) return false;` 与其注释理由。无回归。 |
| **CR-2026-08-31-009 / CR-2026-09-02-008 之外的 F3 条目** | 均落在 `DebugAndTools/` 与 `Utilities/`，不在本人范围；本人只核对了消费侧（Mode F 计数器、丧尸撤离工厂双闩），结论见附录。 |
| **额外发现（过程性，非代码缺陷）** | `tests/ZombieModeRewardServiceAtomicityGuard.py` 在 **f9b83c0 基线上就是红的**：旧断言要求 `RecycleZombieModeSafeZoneBoundTemporaryRealNpcs` 体内出现 `CloseZombieModeTemporaryRealNpcServices(npc);`，而 `git show f9b83c0:ZombieMode/ZombieModeDropsAndPerformance.cs` 显示该方法体当时就已经是 `RemoveZombieModeRunOnlyObjectRecord(npc.GameObject); Destroy(npc.GameObject);`；`require_before`（:100-107）在 `find` 返回 -1 时直接 fail。`tests/known_red_guards.txt` 里**没有**这条登记。本轮把 guard 改成断言 `RemoveZombieModeRunOnlyObjectRecord` 先于 `Destroy`，并且该写法确实等价——`ZombieModeRewardEffectsAndNpc.cs:288` 把 `() => CloseZombieModeTemporaryRealNpcServices(npc)` 登记为 run-only 记录的 `CleanupAction`，`RemoveZombieModeRunOnlyObjectRecord` → `record.Cleanup(false)`（`ZombieModeCleanup.cs:114`）会执行它。**结论：guard 改动正确、不掩盖行为变化**；但它印证了先前「全部 guard 双绿」的台账声明与实测不符，建议 owner 重跑一次全量 runner 校准基线。 |

---

## 附录：逐文件审查流水（工作草稿）

### [草稿·保留] Mode D 逐 hunk（diff_ModeD.patch，3 文件）

| hunk | 判定 |
| --- | --- |
| `ModeD.cs:199` 新增 `ModeHRuntimeGates.IsLegacyModeEntryAllowed()` 入口门 | 行为改动（加法）。全仓 7 个旧模式入口一致挂接（ModeD/E/F/G、WavesArena、ZombieMode×2）。`ModeHRuntimeGates.cs:385/439` 存在。catch 放行 = fail-open，符合注释。 |
| `ModeD.cs:252` `EndModeD` 在 `!modeDActive` 时也调 `CleanupModeDWaveEnemiesOnExit()` | 行为改动，修 CR-2026-09-01-010 #3「只清列表不销毁实体」。幂等。 |
| `ModeD.cs:287` 新增 `CleanupModeDWaveEnemiesOnExit()` | 先 `UnregisterEnemyRecovery` → `dropBoxOnDead=false` → `SetActive(false)`+`Destroy`。不走 Hurt，故不会产生幽灵击杀计数。 |
| `ModeDInteractables.cs:158` `ReflectionCache` → `BossRushEagerReflectionCache` | 纯重命名，`Common/Infrastructure/BossRushEagerReflectionCache.cs:25` 同名同类型字段，全仓统一（8 个调用点）。SAFE。 |
| `ModeDWaves.cs:579` 敌对性安全网 | 行为改动，修 CR-2026-09-01-010 #3。设置 + 回读 + fail-closed 销毁，符合 AGENTS 4.5。 |
| `ModeDWaves.cs:993` `NotifyCampaignModeDWaveComplete` | 调用点存在（`Campaign/CampaignModeBridge.cs:224`），未启用时早返。 |

**CR-2026-09-03-009 的 Mode D 串台部分已复核**：`WavesArena/WavesArena.cs:481 IsCurrentWaveBossMember`
内 `if (modeDActive) return false;`（:498），且注释说明了为何不放在 `HandleBossDeath` 开头
（保成就计数）。逻辑与 finding 描述一致，无回归。

### [草稿·保留] Mode E 逐 hunk（diff_ModeE.patch，8 文件）

关键行为改动：
1. `ModeEBattle.cs:682` 新增 `ModeECharacterPresetLease`（同文件内类，非新增 .cs，无需登记编译清单）；
   `ModeEBattle_ScalingAndRuntime.cs`、`ModeEIntegrityAndHelpers.cs`、`ModeELifecycle.cs`、
   `ModeFRespawn.cs:397` 四处 `Destroy(characterPreset)` 全部删除 → 修 CR-2026-09-01-010 #2。
   **但 ModeF 的克隆点没挂租约 → 已成文为 F-1。**
2. `ModeELifecycle.cs:186` 结束清理由 `Health.Hurt(致命)` 改为 `SetActive(false)+Destroy`。
   `CleanupModeEEnemyRuntimeState`（:391）先 `UnregisterModeEEnemyDeath` 再销毁，
   所以模式结束清场**不会**再走 `OnModeEEnemyDeath` → 不污染击杀统计/图鉴/日报。属修复方向。
   用 `modeEEndCleanupEnemyScratch` 快照后倒序遍历，避免 `UntrackModeEAliveEnemy` 边迭代边改。
3. `ModeEMerchant.cs:304 CreateConfiguredModeEMerchantShop`：inactive → AddComponent → 置
   `Merchant_Normal` → SetActive(true)（Awake 同步执行）→ 切回 `ModeE_<suffix>`。
   官方 `StockShop.Awake` 只做 `InitializeEntries`+订阅+`Load`，Mod 随后 `entries.Clear()` 重填，
   `SaveKey` 在 Save 时才求值（已是 `StockShop_ModeE_*`），**不会污染 `StockShop_Merchant_Normal`
   存档 key，也不会双倍库存**。`Merchant_Normal` 是官方有效 ID（`Integration/BossRushIntegration.cs:258` 已引用）。
4. `ModeEUiAndHealthBars.cs` 本轮**只加注释**，零行为改动。
5. `ModeEStartup.cs:828` 同 Mode D 的入口门。
6. `ModeELifecycle.cs:47` 新增 `DebugSettleModeEExtractionForValidation`，`DevModeEnabled` 门控，
   dev-only；调用点在 F3 验收侧。

**商人不被敌对性安全网改队**：商人经 `merchantPreset.CreateCharacterAsync` 直连生成
（`ModeEMerchant.cs:122`），不走 `EnemySpawnCore`，随后 `SetTeam(modeEPlayerFaction)`；
不进 `modeEAliveEnemies`、不 `RegisterModeEEnemyDeath`。已核对干净。

### Mode F 逐文件（diff_ModeF.patch，11 文件）

| 文件 / hunk | 判定 |
| --- | --- |
| `ModeFBloodfire.cs`（新增 229 行） | 已登记 `compile_official.bat:325`。Moveability 坑处理正确：只挂 `WalkSpeed`/`RunSpeed`（官方 `CharacterMainControl.cs:3438/3444` 确有这两个 hash），未挂 `"MoveSpeed"`；`GunDamageMultiplier`/`MeleeDamageMultiplier` 亦是官方真实 stat（官方 `CharacterMainControl.cs:3573/3582`）。 |
| Bloodfire apply/revert 成对 | `StartModeFBloodfireOverload` 先 `ClearModeFBloodfireOverloadModifiers()` 再挂 4 条，杜绝叠加；`EndModeFBloodfireOverload` 必调 `ClearModeF...`。清理路径全覆盖：`StartModeFRun`（:98）、`ExitModeF`（:630）、`TickModeFBloodfireOverload` 到期（:148）。`ExitModeF` 由玩家死亡（`ModeFPhases.cs:545/556`）、撤离成功（`ModeFExtraction.cs:326/338`）、入口失败回滚（`ModeFEntry.cs:412`）、场景切换（`ModeFRuntimeHooks.cs:19`）、F3 验收（`F3GameplayValidationRunner.cs:117`）五条路径调用。 |
| 数值上下限/除零 | `overflowGrowth<=0.001f \|\| growthCap<=0.001f` 早返，`growthCap = Mathf.Max(1f, ...)` 再兜一层，无除零。`BloodfireCharge` 由 `Mathf.Min(MAX,...)` 钳制；`BloodfireOverloadRemaining` 由 `MODEF_BLOODFIRE_OVERLOAD_MAX_REMAINING=24f` 钳制、`Mathf.Max(0f,...)` 下限。无溢出面。 |
| 每帧 tick 分配 | `TickModeFBloodfireOverload` 只有两个浮点运算 + 一个 bool 判断，非激活时首行早返。零分配。 |
| `ModeFModels.cs` 三个新字段 | `BloodfireCharge` / `BloodfireOverloadActive` / `BloodfireOverloadRemaining` 三个字段与 `Reset()` 三行一一对应，无漏项。 |
| `ModeFBounty.cs` 悬赏印记闩 | `LatchModeFPlayerBountyKill` 在 `BountyMarksByCharacterId.Remove` **之前**调用（:219 前），消费者 `Campaign/CampaignModeBridge.cs:188`。官方顺序已核实：`鸭科夫源码/TeamSoda.Duckov.Core/Health.cs:439` 先 `OnDeadEvent`、:444 才 `Health.OnDead` 静态事件，注释描述与官方源码一致。闩按 `instanceID` 配对、读取即失效，`StartModeFRun`/`ExitModeF` 双向重置。 |
| `ModeFExtraction.cs` `NotifyCampaignModeFExtracted()` 位置 | 在 `ExitModeF()`（:326）**之前**（:252），注释解释正确——`ExitModeF` 会把 `modeFActive=false`，`CampaignObjectiveTracker.EnsureArmedFor` 之后的武装判定就认不出 Mode F 了。 |
| `ModeFEntry.cs:71-88` Mode H 风险门 | 与 ModeD/E 同一模板，catch 放行 fail-open。 |
| `ModeFUI.cs` | `MODEF_BOUNTY_RADAR_CANVAS_ORDER` 由魔法数 240 改引 `BossRushUILayers.ModeFBountyRadar`（`Common/UI/BossRushUI.cs:31` 值同为 240），零行为变化，符合 4.14。`ShowModeFRewardBubble` 增加 `duration` 默认参数，旧调用点行为不变。 |
| `ModeFUI_BountyRadarAndHealthBars.cs:423` | 手写 CanvasScaler 四行改 `ZombieModeUIHelper.ConfigureCanvasScaler`（`ZombieMode/ZombieModeUIHelper.cs:442-452`）。**注意**：helper **不设** `screenMatchMode`，而原代码显式设了 `MatchWidthOrHeight`；由于 `CanvasScaler` 该字段默认值本就是 `MatchWidthOrHeight`，且这里的 scaler 是随 `new GameObject(...)` 新建的，行为等价——代码注释已写明该推理，核对属实。 |
| `ModeFUI_BountyRadarAssets.cs` 删 `GetModeFBountyRadarPanelSprite` | 静态字段 `modeFBountyRadarPanelSprite` 与方法一并删除，全仓无残留引用（含 `ModeFUIStaticCacheReset.cs`），改用 `BossRushUI.ApplyPanelSkin(image, 6)`（`Common/UI/BossRushUI.cs:317` 签名匹配）。距离底板 alpha 由 0.14 提到 0.55 是配套的视觉调整。 |
| `ModeFUI_KillRewardBubble.cs`（新增） | 已登记 `compile_official.bat:430`。零 List/`string.Join` 分配，符合 `ModeFKillRewardBubbleNoListGuard`。`overloadExtension >= 0.5f` 阈值避免显示「+0秒」，`else if (bloodfireGain>0.01f)` 分支互斥合理。 |
| 数值平衡改动 | `MODEF_HEAL_NORMAL_KILL` 0.50→0.30；悬赏回血改 0.45 + 每多一印记 0.05、上限 0.60；最大生命成长由「固定 +1 HP」改为「入场上限 ×4%」、上限 ×50%。属产品数值决策，已被新 guard 逐个常量钉住，**不当作 bug**；需 owner 知晓这是玩家可感知的平衡改动。 |
| 守卫改动 | `ModeFBloodfireOverloadGuard.py`（新增 200 行）、`ModeFMaxHealthCleanupGuard.py`（新增 72 行）均为**新增断言**，未放宽任何既有断言。`ModeFKillRewardBubbleNoListGuard.py` 唯一改动是 `SOURCE` 路径跟随文件拆分（`ModeFUI_BountyRadarAndHealthBars.cs` → `ModeFUI_KillRewardBubble.cs`），断言体逐字不变，属跟随迁移而非放宽。 |
| 官方 API 核对 | `Modifier.RemoveFromTarget()`（官方 `ItemStatsSystem/Stats/Modifier.cs:92`）、`Health.SetHealth(float)`（官方 `Health.cs:502`）、`CharacterMainControl.AddBuff(Buff, CharacterMainControl, int)`（官方 `CharacterMainControl.cs:2199`）、`GameplayDataSettings.Buffs.Burn`（官方 `GameplayDataSettings.cs:133/1108`）全部存在且签名匹配。 |
| dev-only 入口 | `DebugValidateModeFBloodfire`、`DebugAdvanceModeFPhaseForValidation`、`DebugAwardModeFBountyMarkForValidation`、`DebugTriggerModeFExtractionForValidation` 四个均以 `if (!DevModeEnabled) return false;` 开头，走产品链而非测试后门，符合 AGENTS §14 的验收口径。 |

### Mode G 逐文件（diff_ModeG.patch，14 文件 + Config/ConfigModeG.cs）

**结论：未发现 confirmed bug。** 逐项核对：

| 关注点 | 核对结果 |
| --- | --- |
| `ConfigModeG.cs` 新增 + ModConfig 白名单 | 已登记 `compile_official.bat:270`。`ModeGAbandonHotkeyModConfigKeySuffix`（`Config/ConfigModeG.cs:22`）已进白名单 `Config/ConfigModConfigKeys.cs:50`，**不会踩「键不登记白名单则热更新静默失效」的陷阱**。三个方法均有真实调用点：批量加载 `Config/Config.cs:382`、单键变更 `Config/Config.cs:551`、UI 注册 `Config/Config.cs:1045`。默认 `KeyCode.K`，`<=0` 视为禁用且轮询侧 O(1) 早返。 |
| 放弃入口 `ModeGAbandonPresenter`（`ModeGInteractable.cs:509+`） | 同文件内新类，非新增 `.cs`，无需登记清单。`TryOpen` 单实例闸（`_active != null` 早返）；`OpenPage` 内若抛异常，`TryOpen` 的 catch 销毁 host GameObject → 触发 presenter 的 `OnDestroy` → 释放输入租约 + 销毁 `_modalRoot`，**DontDestroyOnLoad 的面板不会成为孤儿**。`Close`/`OnDestroy` 双路径都释放 `ModalInputLease`，时停不会卡死。`ConfirmAbandon` 先 `Close()` 再 `End(ManualExit)`，顺序正确。 |
| 放弃快捷键的关闭覆盖 | `ShutdownModeG`（`ModeGEntry.cs:714-715`）、`End`（`ModeGRuntimeModule_PublicApiAndShutdown.cs:319-320`）、模块 `OnDestroy`（同文件 :304-305）三处都调 `CloseActiveConfirmation()` + `CloseIfOpen()`。过图经 `Utilities/ModeRuntimeHooks.cs:37-46` → `End(SceneChanged)` + `ShutdownModeG()`。 |
| 快捷键闸门与注释一致性 | `state.IsActive` 定义为 `lifecyclePhase == Active`（`ModeGRunState.cs:127`），确实排除 Starting/Rewarding/Exiting/终局，与注释「防止弃奖误触」一致。 |
| 入场取消不吃玩家道具 | 新增的两条早返（`ModeGEntry.cs:400` preview 失效、:413 preview 重建）都在 `TryConsumeModeEntryItem`（:416/:419）**之前**；且调用方 `ModeGInteractable.ConfirmAndStart`（:439-443）在 `TryStartModeG()==false` 时调 `TryRefundModeGPendingPrepaidTicket()`，地图选择预扣的船票会退还。**无道具丢失面。** |
| `OnSceneLoaded` override 被删除（行为删除） | 删除理由可复核为**真**：`Common/Lifecycle/BossRushRuntimeModuleRegistration.cs:16` 注册的是 `new ModeGRuntimeModule()` 空壳，真实 run 实例在 `ModeGEntry.cs:570` 另建且从不注册 → host 场景回调只打在 `_state == null` 的空壳上，原实现体确实不可达。替代路径 `Utilities/ModeRuntimeHooks.cs:37-46` 显式 `End(SceneChanged)` + `ShutdownModeG()` 实在。`ModeGMapSupportGuard.py` 已同步改为**反向断言**（该文件里不得再出现 `OnSceneLoaded(SceneRuntimeContext context)`）+ 正向断言 ModeRuntimeHooks 的新链，属收紧不是放宽。 |
| 成就 token 掺入实例身份（`ModeGRuntimeModule.cs:804-807`） | 真修复。去重键是 `ModeGAchievementReportKey(token, bossType, wasFlawlessAtDeath)`（`Achievement/AchievementTriggers.cs:473-475`），旧 token = FNV(`bossType\|waveEpoch`)：同波同类型第二只 Boss 三元组完全相同 → 被 `HashSet.Add` 吞掉。掺 `health.GetInstanceID()` 后唯一。 |
| 成就消费早于 session 关闭 | `End` 内 `ConsumePendingAchievementReports(_state)` 被放到 `EndModeGAchievementSession()` **之前**；`ReportModeGBossKillAchievement` 首行就是 `if (!modeGAchievementSessionActive) return;`（`AchievementTriggers.cs:468`），顺序断言属实。`PrepareHostDestroy` 侧的二次消费改为纯 drain（删掉 `state.pendingAchievementReportsConsumed` 标志位），字段已从 `ModeGRunState` 删除且**全仓零残留引用**（含 guard）。 |
| 战斗帧写屏障 `_hostWriteDeferred` | 语义自洽：typed Save 不推迟（数据进内存存档字典），只推迟物理 `SavesSystem.SaveFile`。欠账在 `NotifySlotChanged`（切档）与 `MarkFaulted` 时清零，不会拿新槽文件补写旧槽。`End` 在 `ModeGCleanupController.Cleanup` **之后**补一次 `RequestFlush()`（此时战斗相位已归零，屏障解除），`TryFlushOnHostDestroy` 走 `forceHostWrite:true`。**见「未验证线索」U-G1**。 |
| `TryReturnRelicOnce` 改 internal + 放弃分支补返 | `Interlocked.Exchange` CAS 幂等；失败路径会把闸门置回 0 并 `DestroyTree()` 释放实例，不会「闸门已消费但物品没到手」。闸门在 run 开始（`ModeGRuntimeModule.cs:178`）与 `End`（`..._PublicApiAndShutdown.cs:405`）双向重置，无跨局残留。`SubmitVictoryReward` 的 `\|\|` 短路使 `plan==null/empty` 时 `Execute` 不被调用（Execute 内 :521 才是原返还点），此时新增的补返正好补上这个洞。 |
| `IsNewBestWave` 改为「与 RecordRun 之前的旧值比较」 | 真修复：旧实现 `wave >= profile.bestWaveReached` 在 `RecordRun` 之后读，必然成立 → 每局都报「新纪录」。两条终局路径（`ModeGDeathRouting.cs:60` 失败、:193 胜利）都在 `RecordRun` 之前取值，透传到 recap。 |
| `ModeGRuntimeModule_PublicApiAndShutdown` 的 `ShowTerminalBannerFallback` | 加法。刻意跳过 Victory/PlayerDeath 避免双报；`SpawnExhausted`/`TechnicalIntegrityLoss` 用 `_firstWaveCombatStarted` 避开与退款播报重复。自带 try/catch，不阻塞 End。 |
| `ModeGInteractable.OnDestroy` 由 `new` 改 `protected override` | **真修复**。官方 `鸭科夫源码/TeamSoda.Duckov.Core/InteractableBase.cs:531` 确为 `protected virtual void OnDestroy()`；`new` 隐藏会让官方 `StopInteract` 在销毁时不执行。改后先 `CloseModal()` 再 `base.OnDestroy()`，顺序注释与实现一致。 |
| UI 层级收口 | `ModeGHUD.CanvasSortOrder` 900 → `BossRushUILayers.ModeGHud`（同值 900）、`ModeGInteractable` 950 → `ModeGEntry`、`ModeGRecapPanel` 940 → `ModeGRecap`（`Common/UI/BossRushUI.cs:33/35/37`），三处值逐一相等，零行为变化，符合 4.14。新 presenter 用 `ZombieModeUIHelper.ConfigureCanvasScaler` + `CreateModalSurface`，无 legacy `UI.Text`、无内置 Arial。 |
| 每帧分配 | `ModeGHUD` 4Hz 刷新（`RefreshIntervalSeconds = 0.25f`）+ 复用 `StringBuilder(256)`，字符串拼接只在刷新帧发生；`TryHandleModeGAbandonHotkey` 每帧只有 bool 链 + 一次 `Input.GetKeyDown`，零分配。 |
| `ModeGSpawnTransaction` fail-closed | 本轮只有两处改动：类注释纠正（`HoldForExternalCommit` official 路径实际早已是 `true`，见 `:203`/`:219`，旧注释写成 false 属**注释与实现不一致**，本轮改对了）+ 删除零调用点的 `CommittedSlotCount`。回滚逻辑本轮未改。 |
| `ModeGCombatTelemetry` 删三个成员 | `TotalShotCount` / `NamedAmmoTypeIds` / `GetBossDamageContribution` 全仓（含 `tests/`）**零引用**，底层字段仍被其他成员使用，纯死代码清理，SAFE。 |
| 与 PetNest / Mode H 死亡抑制补丁的交互 | `Patches/Combat/CharacterOnDeadPatch.cs:30-31` 三闸并联；`ModeHDeathSuppressionRegistry.IsSuppressionArmed`（`ModeH/...:35-42`）与 `PetNestDeathSuppressionRegistry.IsSuppressionArmed`（`PetNest/...:31-38`）**内部各自 try/catch no-throw**，虽然调用点没有再包一层 try（与 `IsModeGSuppressionArmed()` 不对称），实际不会向 Harmony prefix 抛出。Mode G 语义不变。 |
| guard 是否被放宽 | `ModeGManagedBossLootGuard.py` / `ModeGPerformanceGuard.py` 的 `StaticBoolFirst` 正则加了两个**可选**分支 `(?:\s*\|\|\s*ModeHDeathSuppressionRegistry\.IsSuppressionArmed)?`，形式上放宽（去掉这两项也能过），但核心断言「首行是静态 bool 早返」仍在，且是为容纳 Mode H/PetNest 的真实接线；不构成掩盖行为变化。`ModeGAchievementIsolationGuard.py`、`ModeGEntryPreviewGuard.py`、`ModeGMapSupportGuard.py`、`ModeGPersistenceFlushCoordinatorGuard.py` 均为**新增/收紧**断言。 |

### 丧尸模式逐文件（diff_ZombieMode.patch，19 文件）

**结论：未发现 confirmed bug。** 逐文件：

| 文件 | 判定 |
| --- | --- |
| `ZombieModeBackpackJunkRecycle.cs` | ①记账顺序改为「先算值 → 移除+销毁 → 再记账」，修「移除抛异常仍白送净化点」。②`protectedTags` 局部数组提为 `static readonly`，20 个字符串内容逐字不变，纯分配优化。**行为改动只在异常分支，非性能档分叉。** |
| `ZombieModeBossController.cs` | 新增 `IsZombieModeBossEngagedWithPlayer`（`ZombieModeTuning.BossStuckEngagedDistance = 6f`）：Boss 贴脸（水平 6m 内）不移动时刷新 `LastReachableTime`，不再被判卡死瞬移。不会导致波次卡死——`TeleportZombieModeBossNearPlayer` 本来就是「传到玩家附近」，Boss 已在 6m 内时该动作无意义；且玩家就在旁边可以击杀。 |
| `ZombieModeCashInvestmentView.cs` / `ZombieModeEntry_StarterLoadout.cs` / `ZombieModeRewards.cs`(×2) / `ZombieModeExtractionController.cs` / `ZombieModeHudController.cs` | sortingOrder 魔法数 → `BossRushUILayers.{ZombieModalInput=30100, ZombieModal=30000, ZombieService=30500, ZombieHud=28000}`，`Common/UI/BossRushUI.cs:74/76/78/80` 值逐一相等，零行为变化。 |
| `ZombieModeCleanup.cs` | ①`record.Cleanup(false)` 补 try/catch，一条记录清理失败不再中断整个 prune 循环。②`ClearZombieModeEnemiesInsideActiveSafeZone` 改用 `AnyZombieModeSafeZoneActive` + `IsZombieModePositionInsideActiveSafeZone`（已覆盖双槽），一致。 |
| `ZombieModeDropsAndPerformance.cs` | 新增 `DropPickupScanIntervalSeconds = 1f` 节流：`GetComponent<Item>()` 拾取检查不再每帧对每个候选物跑。**已核对不会误销毁玩家背包内物品**：`forceWaveCleanup` 路径强制 `scanPickups = true`；非强制路径在销毁前还有 `delta.sqrMagnitude < 25f` 距离豁免，而官方 `ItemStatsSystem/Inventory.cs:492` 是 `item.transform.SetParent(base.transform)`（worldPositionStays=true），进包物品的 transform 随玩家移动、与玩家保持拾取时的小偏移，恒在 5m 内。`zombieModeLastDropPickupScanTime` 未跨局重置，但 `GetZombieModeRuntimeNow()` 只会向前跳，最坏结果是新局首次立即扫描，方向安全。节流是固定时长、非性能档分叉，不违反 ZombieMode/AGENTS「不按性能档改变玩法」。 |
| `ZombieModeEntry.cs` / `ZombieModeMapSelection.cs` | 入口拒绝统一到 `IsZombieModeStartBlocked(out failureReason)`（放在 MapSelection 文件是因为 `ZombieModeEntry.cs` 已顶到 `tests/large_file_existing_allowlist.txt` 行数上限，注释已写明）。两类拒因文案分离（Mode H 风险门 vs 其他模式在跑）；地图选择壳也补了 Mode H 门 + 玩家可见提示（此前只有 DevLog，纯静默）。 |
| `ZombieModeExtractionController.cs`（最大改动） | **便携安全区双槽**：战斗期部署仍写主槽（替换、带清服务 NPC），准备期部署写新的便携副槽（与主槽并存、不带商人、不回收主槽绑定 NPC）。`CleanupZombieModePreparationObjects` 删掉 `preservePortableSafeZone` 参数，两个槽在波次边界一起清；两个调用点（`ZombieModeWaveController.cs:288/355`）已同步。`ClearZombieModePortableSafeZoneSlot` 完整清 visual + POI + run-only 记录 + 5 个状态字段。新增 `RemoveZombieModeSafeZoneRunOnlyRecord`，解决「Destroy 了但 run-only 记录还挂着」。`NotifyCampaignZombieExtracted()` 放在 `CleanupZombieModeForSceneChange` **之前**（:231 vs :241），注释理由正确。 |
| `ZombieModeHudController.cs` | ①`SafeZoneStealthBroken` 相关的 HUD 文案与颜色分支删除（见下）。②战斗期便携安全区无准备倒计时，`PreparationTimer <= 0f` 时不再显示恒为 0 的那一行。③HUD 三块裸文本补半透底板 `Image` + `ApplyPanelSkin`，`raycastTarget = false` 已关（不挡点击）；`ZombieModeUIHelper.CreateRect` 只加 RectTransform，`AddComponent<Image>()` 不会造成重复组件。 |
| `SafeZoneStealthBroken` 字段整体删除 | **已核实是死状态**：`git grep SafeZoneStealthBroken f9b83c0` 显示全仓 17 处引用中**没有一处赋 true**，恒为 false。删除后 `ShouldSuppressZombieModeEnemyAggroForSafeZone` / `ShouldZombieModeEnemyAggroPlayerNow` 的求值结果与旧代码逐字等价。全仓（含 `tests/`）已无残留引用。玩家攻击一直是走 `CancelZombieModeSafeZone(runId, "PlayerAttack")` 整个取消安全区，不是「破隐」。 |
| `ZombieModeSafeZoneController.cs` | ①双槽判定（`AnyZombieModeSafeZoneActive`、`TryResolveZombieModeSafeZoneExclusionSlot` 按敌人实际所在槽算弹出方向）。②**真修复**：`bool shouldSuppress = ...` 提到 `KeepZombieModeEnemiesOutsideSafeZone(shouldSuppress)` **之前**并作为参数传入——旧代码在弹出时无条件传 `!SafeZoneStealthBroken`（恒 true），玩家在区外时蹭边界的丧尸会被永久清空 `forceTracePlayerDistance`。③**真修复**：`SetZombieModeEnemyThreatSuppressed` 的抑制分支补 `zombieModeRunState.SafeZoneThreatSuppressed = true;`——`ReleaseZombieModeSafeZoneThreatSuppression`（:327-334）以该标志早返，不记账等于被抑制的敌人永不恢复追击。 |
| `ZombieModeTuning.cs` | 删 `BossPreparationCountdownSeconds`（全仓零引用，`ZombieModePacingTuningGuard`/`ZombieModeStateModelGuard` 已同步删除对应断言，属死常量清理）；新增 `BossStuckEngagedDistance=6f`、`DropPickupScanIntervalSeconds=1f`。**没有既有数值被偷偷改动**。 |
| `ZombieModeUIHelper.cs` | 新增只读 `ModalInputLeaseCount`（诊断用，不改租约语义）；`CreateButton`/`CreateModalSurface` 三处补 `BossRushUI.ApplyPanelSkin`。**注意跨模块影响面**：`ConfigureCanvasScaler` 本轮未改，但 `CreateButton`/`CreateModalSurface` 被 Mode G 放弃页、ZombieMode 各面板等多处复用，皮肤变化是全局视觉改动（无行为影响）。 |
| `ZombieModeWaveController.cs` | 新增 `IsZombieModeSafeZoneCancellingWeapon`：只有带 `Gun`/`Weapon`/`MeleeWeapon`/`Melee` tag 的武器直伤才取消安全区。官方 API 已核对：`DamageInfo.fromWeaponItemID`（`鸭科夫源码/.../DamageInfo.cs:162`）、`ItemAssetsCollection.GetMetaData(int)`（`ItemStatsSystem/ItemAssetsCollection.cs:179`）、`ItemMetaData.tags`（`ItemMetaData.cs:19`）均存在；tag 名 `"Weapon"`/`"Gun"`/`"MeleeWeapon"` 在官方源码里有实证（`CharacterMainControl.cs:628/3378`、`InventoryEntry.cs:352`）。枪由 `ItemAgent_Gun.cs:1501` 写 `fromWeaponItemID`、近战由 `ItemAgent_MeleeWeapon.cs:265` 写，两条主路径都能命中；手雷走 `Grenade.cs:201`，官方 `ItemShortcut.cs:129` 反证手雷不带 `Weapon` tag。**见「未验证线索」U-Z1。** |
| `ZombieModePollution_RuntimeSkills.cs` | `Shader.Find("Sprites/Default")` 结果静态缓存（每发弹丸省 2 次字符串查表）；`LineRenderer` 两点在生成时先钉到弹丸自身位置，修「首帧从世界原点拉一条线」。材质仍逐弹丸持有，`MaterialOwner` 回收契约未变。 |
| `ZombieModeRewardCatalogAndSelection.cs` | 新增 `GetZombieModeRewardAccentColor`，12 个 `ZombieModeRewardCategory` 枚举值（`ZombieModeModels.cs:165-179`）**全部覆盖** + default 兜底。纯呈现，不改奖励内容。 |
| `ZombieModeRewardPreparationDuration.cs` | 删 `GetZombieModePreparationDurationOptions`（全仓零引用；`ZombieModeRewardCatalogGuard.py` 已同步删断言）。 |
| `ZombieModeModels.cs` | 新增 5 个 `PortableSafeZone*` 字段，**两个 Reset 方法（:794 与 :881）都补齐了全部 5 项**，无漏项；`SafeZoneStealthBroken` 从字段与两个 Reset 一并删除。`ZombieModeStateModelGuard.py` 是「必需片段」式断言，不引用这两组字段，无需同步（该 guard 本轮只删了 `BossPreparationCountdownSeconds` 一行）。 |
| `ModeExtractionPointFactory` 消费侧 | 工厂侧把 `ResolveFallbackActions` 移到 `ClearPersistentEvents` 之后（`Utilities/ModeExtractionPointFactory.cs:70-73`），实际效果是两个 fallback 恒为 true（注释诚实写明了）；并新增 `successDispatched` 闩。丧尸侧 `CompleteZombieModeExtractionSuccess`（`ZombieModeExtractionController.cs:215-242`）自带 `ExtractionSuccessHandled` 闩 + `TryDispatchZombieModeExtractionSuccess` 再派发，两层闩互锁，**不会双跑返程也不会两条都不跑**。 |
| 丧尸击杀的图鉴/日报计数 | `ZombieMode/` 本轮没有触碰击杀上报；`Integration/Codex/CodexKillCollector.cs:259/285` 显式识别 `owner.IsZombieModeActive` 并归到 `CodexTuning.ModeIdZombie`，属既有正确接线。战役第 5 章走的是撤离漏斗 `NotifyCampaignZombieExtracted`，本轮新接、调用点属实。 |
| guard 同步 | `ZombieModeSafeZoneGuard.py`（大幅重写，新增「便携安全区不得跨波存活」等断言）、`ZombieModeExtractionFactoryGuard.py`（新增顺序 + 闩断言）、`ZombieModeMarkerOwnerFallbackCacheGuard` / `ZombieModeRunOnlyMarkerFallbackCacheGuard` / `ZombieModeSafeZoneMarkerReuseGuard`（跟随方法签名 `KeepZombieModeEnemiesOutsideSafeZone(bool suppressThreat)`）全部属**收紧或跟随**。三个 `...Guard.py` 把 `"...cs ^"` 改成 `"...cs"` 是跟随 `compile_official.bat` 改用 Roslyn 响应文件（现在是 `echo(路径\文件.cs`，没有 `^` 续行符），**不是放宽**。 |

### 地图选择 / 成就 / Boss 筛选 逐文件

| 文件 | 判定 |
| --- | --- |
| `MapSelection/BossRushMapSelectionHelper.cs`（+187） | 新增 `BossRushPendingEntryKind` 枚举替代单个 Mode G 布尔；两个 `bool` overload 保留为 wrapper（`false → None`、`true → ModeG`），`HasPendingModeGEntryIntent()` 语义逐字等价。新增 Mode H 的 typed intent：目标 sceneName/sceneID + 单调递增 generation，`ClearPendingEntryFlowState` 一并清 4 项。`TryMatchModeHSceneIntent` 首行就查 `kind != ModeH → false`，所以别的入场流留下的陈旧 target 是惰性的。 |
| 同上 · 筛选后索引错位的修复 | `BuildSelectableMapConfigs` 只在 ModeH intent 下按 `ModeHMapSupportRegistry.IsSupportedPair` 过滤；`clickHandler.entryIndex` 改为 `FindMapConfigIndex(mapConfig)`（**全量表下标**，先 `ReferenceEquals` 再 sceneName+sceneID 双字段兜底），`RefreshAllEntryDisplayNames` 同步改读 `clickHandler.entryIndex` 并做 `0 <= idx < len` 越界检查。**这是本轮真正的正确性修复**：不改的话 ModeH 过滤后点第 2 个条目会传送到全量表的第 2 张图。`ModeHMapSupportRegistry.IsSupportedPair`/`TryGetMap`/`ModeHSupportedMap` 均存在（`ModeH/ModeHMapSupportRegistry.cs:194/203/10`）。`using System.Collections.Generic` 已在文件头（:18）。 |
| 同上 · 互斥与 intent 冻结 | 单枚举天然互斥；`ModeHEntry.cs:27-32` 显式拒绝 pending ModeG；`FreezeModeHEntryIntent`（`ModeHEntry.cs:130`）在 `OpenModeHMapSelection()`（:134）之前，而 `MarkEntryFlowFromMapSelectionUi(kind)` **不清** ModeH 的 target/generation，冻结值得以存活。UI 层级本轮未改。 |
| `Achievement/BossRushAchievementDef.cs` | `AchievementCategory` 追加 `Taming`、`Codex` 到 `Ultimate` 之后。既有 0..6 不变。存档只存字符串 id（`BossRushAchievementManager.cs:424` `SaveGlobal(SAVE_KEY_UNLOCKED, List<string>)`），枚举 int 完全不进存档 → **老档零影响**。 |
| `Achievement/BossRushAchievementManager.cs` | 注册 5 个 `petnest_*` + 5 个 `codex_*`。10 个触发点全部存在（`PetNest/PetNestMuseumStats.cs:136-152`、`Integration/Codex/CodexMilestones.cs:53/100/104/109/117`）。现金额与 `CodexTuning.MilestoneCash*`（:125-129）一一对齐，未硬编码。副作用：`completionist`（解锁所有成就）门槛随之提高——设计决策，非缺陷。 |
| `Achievement/AchievementTriggers.cs` | 唯一行为改动是 `BeginModeGAchievementSession` 内 `AchievementTracker.HasTakenDamage = false;`（CR-2026-08-29-014 的修复）。`HasTakenDamage` 是 `AchievementTracker.cs:25` 的 public static 字段，可写。调用点 `ModeG/ModeGRuntimeModule.cs:220` 可达。 |
| `Achievement/AchievementView.cs` / `AchievementEntryUI.cs` | 全是 4.14 收口：`sortingOrder 10 → BossRushUILayers.Panel`、手写 Scaler → `ConfigureCanvasScaler`（原来漏设 `matchWidthOrHeight`，超宽屏会溢出）、`(0,0,0,0.7)` → `BossRushUIColors.Backdrop`、多处 `ApplyPanelSkin` / `ApplyGameFont`。颜色常量改引 `Accent`/`Success`/`Danger` token（数值变了，纯视觉）。均一次性构建，无每帧分配。 |
| `Achievement/SteamAchievementPopup.cs` | **真修复**：标题与描述由 legacy `UI.Text` + `Resources.GetBuiltinResource<Font>("Arial.ttf")` 换成 `TextMeshProUGUI` + `BossRushUI.ApplyGameFont`——内置 Arial 渲染不了中文（AGENTS 4.14），中文成就弹窗此前是方框/空白。`horizontalOverflow/verticalOverflow` 正确映射到 `enableWordWrapping=false` + `overflowMode=Overflow`，`TextAnchor.MiddleLeft → TextAlignmentOptions.MidlineLeft` 对应正确。`sortingOrder 1000 → BossRushUILayers.Toast(4000)`，仍低于 ZombieMode 的 28000+，与层级表注释一致。文件内已无残留 Arial 用法。 |
| `BossFilter/BossFilter.cs` | ①`InvalidateFilteredPresetsCache` 并联通知遗种巢 / 图鉴目录——修「场内改了 Boss 池，掉落资格与图鉴条目仍停在旧快照」。已核对无递归（两个 `NotifyEnemyPresetsRefreshed` 都不回调本方法）。性能面见 **B-1**。②5 处 `TextMeshProUGUI` 补 `ApplyGameFont`。 |
| `BossFilter/BossFilterUi.cs` | `CanvasScaler` 此前**只 AddComponent 不配置**（退化成 `ConstantPixelSize`，高分屏面板缩成一小块）——这正是 4.14 点名的坑，本轮补上 `ConfigureCanvasScaler`，**真修复**。遮罩改 `BossRushUIColors.Backdrop`、面板补 `ApplyPanelSkin`、标题补 `ApplyGameFont`。sortingOrder 改动见 **B-2**。 |
| `ZombieModeUIHelper` 改动的跨模块影响面 | `CreateButton`（42 个调用点）、`CreateModalSurface`（10 个）各加了一次 `BossRushUI.ApplyPanelSkin`。已核对 `ApplyPanelSkin`（`Common/UI/BossRushUI.cs:317-334`）**只设 `sprite`/`type`/`pixelsPerUnitMultiplier`，不碰 `image.color`**，因此调用方「先设色后套皮」与「先套皮后设色」两种写法（本轮两种都出现了）结果一致；bundle 缺失时 `GetPanelSprite()` 返回 null 即原样早返，退回旧观感（fail-open）。`ConfigureCanvasScaler`（22 个调用点）本轮未改。 |

---

## ==== ModeH_combat_spawn_cert ====

# Mode H 战斗 / 生成 / 口令认证 / 伤病战痕 / Harmony / 隔离观战 审查报告

> 审查范围：`ModeH/` 下 25 个战斗-生成-认证相关文件 + `Assets/Data/ModeH/{Commands,CommandCompatibility,Scars,LoadoutKits}.json`
> + `Patches/Combat/CharacterOnDeadPatch.cs` / `BossLethalHealthProtectionPatch.cs`。
> 审查方式：逐文件读当前完整源码（工作树，含未提交改动）+ 全仓库调用点 grep（排除 `鸭科夫源码/`、`tests/`、`Build/`、`.codex_tmp/`）
> + Harmony / 反射目标逐个回查官方反编译源码 `鸭科夫源码/`。
> 审查日期：2026-09-04。基线 `f9b83c0..HEAD` + 工作区未提交改动。
>
> **不重复覆盖**：`ModeH_flow_persistence`（流程/状态机/持久化/押品/经济）与
> `ModeH_content_ui_guards`（内容表/UI/选秀/赔率/本地化）两份同批报告的范围。

---

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

**汇总：P0 ×0，P1 ×2，P2 ×6，P3 ×10（共 18 条）。**

| # | 严重级 | 分类 | 一句话 | 主位置 |
| --- | --- | --- | --- | --- |
| F-0 | **P1** | COMPAT | `finish` 口令对任何 key 都永远不可选，8 条通用口令实际只有 7 条 | `ModeHCommandCertificationProbe.cs:126-140,229-246` |
| F-M | **P1** | COMPAT | 伤病 `armor`（护具受损）是完全空操作——`IsArmorKitDisabled` 零读者 | `ModeHInjuryAndScarSystem.cs:73` |
| F-A | P2 | COMPAT | `broken_shield_charge` 的破甲判据漏 `UseDurability` 守卫，且官方扣耐久受 `IsRaidMap` 门控 | `ModeHCombatControl.cs:532-541` |
| F-B | P2 | SAFE | 触发型伤病/战痕的去重键每帧重新拼字符串，战斗热路径持续 GC | `ModeHCombatControl.cs:479-524` |
| F-I | P2 | SAFE | 认证逐 key 超时后 pending 的创建任务把角色与 clone preset 泄漏在场景里 | `ModeHProductionCertification.cs:320-331` |
| F-N | P2 | SAFE | 认证诊断注册表整条不通电，路由的"诊断优先"保护结构性从未生效 | `ModeHEventRouter.cs:234-255` |
| F-O | P2 | COMPAT | `GetVerifiedEffectIds` 零调用点，§17.6.4「文案只由已通过 effect 生成」未接线 | `ModeHCommandCompatibilityRegistry.cs:463-480` |
| F-P | P2 | COMPAT | 16 个固定 kit typeId 从不在运行时核对，官方表变动会变成开局中途技术中止 | `ModeHLoadoutKitRegistry.cs:184-213` |
| F-C | P3 | SAFE | 生成期副作用检测的三个计数器无写入者，"额外掉落/追踪副作用"恒检测不到 | `ModeHSpawnBridge.cs:338-403` |
| F-D | P3 | SAFE | `RestoreScarWindows` 零调用点——§17.4 重建链清理的漏网 | `ModeHInjuryAndScarSystem.cs:352` |
| F-E | P3 | SAFE | `ReadHealthFraction` 的 XML 注释被新插入的方法截断，且 null 返回值与注释相反 | `ModeHCombatTelemetry.cs:292-297` |
| F-F | P3 | SAFE | `RollCoward` 用 `anomalyId.Length` 当种子子域，`blood`/`crowd` 撞号 | `ModeHCombatControl.cs:450-451` |
| F-G | P3 | SAFE | `ModeHSpawnTransaction` 收下 `sceneGeneration`/`ownerToken` 但从不使用 | `ModeHSpawnTransaction.cs:33-34` |
| F-H | P3 | SAFE | `SpawnBatch` 的每帧生成预算计数是死逻辑 | `ModeHSpawnTransaction.cs:124-140` |
| F-J | P3 | SAFE | 观战租约注释承诺了两个实现里没有的步骤 | `ModeHSpectatorLease.cs:11-15` |
| F-K | P3 | SAFE | 晚到 spawner 的 `created` 原值被硬编码 false，恢复后可能重复刷怪 | `ModeHArenaIsolationLease.cs:344-350` |
| F-L | P3 | SAFE | 三个逐 effect 遥测出口零调用点 | `ModeHCommandController.cs:254` |
| F-Q | P3 | SAFE | 一批只写不读的诊断出口（14 处，含 `RecycleEnemy`） | 见表 |

---

---

### F-0：`finish` 口令对**任何** stable key 都永远不可选——8 条通用口令实际只有 7 条能到玩家手里

- **严重级**：**P1 Major**（内容不可达；且使 CR-2026-09-03-017 针对 `finish` 的整条修复失去意义）
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHCommandCertificationProbe.cs:126-140,229-246`；
  `ModeH/ModeHCommandCompatibilityRegistry.cs:348-374`；
  `Assets/Data/ModeH/Commands.json`（`finish` 的两条 effect）；
  `ModeH/ModeHCommandController.cs:63-91`（`GetSelectableCommands`）

**证据**

`finish` 是**唯一一条两个 effect 都是点火型**的口令：

```json
{"effectId":"finish.searchedEnemy",      "controlPointId":"searchedEnemy",      "op":"fire_lowest_health_target"}
{"effectId":"finish.setNoticedToTarget", "controlPointId":"setNoticedToTarget", "op":"fire_notice_current_target"}
```

认证探针对每个非自结算分量**先无条件写 ReportOnly，再判是否可测**：

```csharp
// ModeHCommandCertificationProbe.cs:131-139
ModeHCommandCompatibilityRegistry.RecordEffectStatus(stableKey, effect.EffectId,
    ModeHCommandCompatibilityStatus.ReportOnly);
object first  = ReadField(scavAi, effect.ControlPointId);
object second = ReadField(wolfAi, effect.ControlPointId);
if (!effect.Restore || first == null || second == null) continue;   // ← 点火型在此被排除
...
if (measurable.Count == 0) yield break;                              // ← finish 整组直接退出
```

而 `ReadField`（`:229-246`）的 switch **不含** `searchedEnemy` / `setNoticedToTarget` / `moveToPos` / `nextReleaseSkillTimeMarker`，
两条都返回 null → `finish` 的 `measurable.Count == 0` → **两条 effect 永远停在 `ReportOnly`，
永远不会被升级为 `VerifiedBehavior`**。

口令级派生（`ModeHCommandCompatibilityRegistry.cs:348-366`）：

```csharp
if (passed == effectIds.Count) return VerifiedBehavior;   // passed = 0
if (passed > 0)                return PartiallyVerified;  // 0 > 0 false
if (unavailable == effectIds.Count) return Unavailable;   // unavailable = 0
return ReportOnly;                                        // ← finish 恒走这一支
```

`IsCommandSelectable`（`:369-374`）只接受 `VerifiedBehavior` / `PartiallyVerified`
→ **`finish` 恒 false**，`GetSelectableCommands` 永远不会把它放进列表，
锁盘时的 `commands.Contains(...)` 也永远选不到它。

对照：其余 7 条通用口令与 5 条招牌口令**每条都至少有一个可测分量**，
因此都能拿到 `PartiallyVerified` 及以上（`center` 1/2、`hold` 2/3、`all_in` 3/4、`press` 3/4、
`weakness` 1/2 …），只有 `finish` 是 0/2。`pinnedOverrides` 在 `CommandCompatibility.json`
里是空数组，且**全仓库没有任何代码读它**，不存在人工放行的旁路。

**失败场景**：任意赛季、任意 stable key、首次认证与缓存命中两条路径都一样——
选令列表里永远没有「终结」这一条。

**玩家影响**：内容表、Wiki 与选令 UI 承诺 8 条通用口令，实际只有 7 条可选。
玩家永远看不到 `finish`。

**与既有 finding 的关系（重要）**：CR-2026-09-03-017 的「为什么三道全绿还是漏了」一节写的是
「`ModeHCommandAdapter.Validate()` 对这两个控制点的判据是 `_ai.searchedEnemy != null` 与 `_ai.noticed`
……于是认证把 `finish` 标成 `VerifiedBehavior` 正常发给玩家选」。
**这段归因与当前（以及上一提交的）探针代码不符**：`Validate()` 的宽松判据根本轮不到执行，
点火型分量在进入 `ApplyEffects` 之前就被 `ReadField == null` 滤掉了
（`git diff HEAD -- ModeH/ModeHCommandCertificationProbe.cs` 显示这段过滤在本轮改动**之前**就存在）。
所以那条 finding 修好的是「点火目标没有生产者」（对 `press` 与战痕有效），
但 `finish` 本身从来就没被玩家选到过，修复对它是空转。

**建议修复**（三选一，需 owner 拍板取哪种）
1. 给 `finish` 补一个可测分量（例如追加一条 `sightDistance` 或 `combatTurnSpeed` 的小幅调制），
   让它能到 `PartiallyVerified`——最小改动，且与 `center`/`weakness` 的既有形态一致；
2. 在 `GetCommandStatus` 里把「全部分量都是不可测类型（点火 / marker）」单独归为一档，
   按 §17.6.4 决定是否允许选择（属兼容矩阵语义变更，`SCHEMA-` 风险，需 owner）；
3. 给点火型控制点补真正的遥测判据（目标身份而不是 `!= null`），把 `ReadField` 扩到能读
   `searchedEnemy` 的**身份**——`tests/` 现有守卫明令禁止给 `ReadField` 加 case，改前须同步守卫。

无论选哪种，都应补一条守卫断言：「每条 `commonCommands` 至少存在一条 `ReadField` 可读的分量」，
否则今后新增纯点火口令会重复这个坑。

**验证需求**：静态确认，未运行验证。实机复现最快：开一局 Mode H 到选令页，
确认列表里没有「终结 / finish」；或在认证结束后打印 `GetCommandStatus(key, "finish")`。

---

### F-A：`broken_shield_charge` 的「首次破甲」触发条件在竞技场恒不成立，或恒成立——两种坏法二选一

- **严重级**：P2 Minor（若 arena 确为非 raid 图则升 P1：CR-2026-09-03-019 三条触发型战痕修复里的一条实际仍未生效）
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHCombatControl.cs:532-549`（`IsActiveFighterArmorBroken` / `ResolveArmorItem`）；
  官方 `鸭科夫源码/TeamSoda.Duckov.Core/Health.cs:360-368`；`鸭科夫源码/ItemStatsSystem/Item.cs:956,1158-1162`

**证据**

Mod 侧判据（`ModeHCombatControl.cs:532-541`）：

```csharp
private bool IsActiveFighterArmorBroken()
{
    if (_activeFighterArmorItem == null) return false;
    try { return _activeFighterArmorItem.Durability <= 0f; }
    ...
}
```

官方 `Item.Durability` 的 getter（`Item.cs:1158-1162`）：

```csharp
public float Durability
{
    get { return this.GetFloat(Item.DurabilityVariableHash, 0f); }
```

—— **缺省值就是 `0f`**。官方自己每次拿 `Durability <= 0f` 判「已损坏」时**一律先过 `UseDurability`**
（`Item.cs:956`：`if (this.UseDurability && this.Durability <= 0f) { active = false; }`；
`UseDurability => MaxDurability > 0f`，`Item.cs:1101-1105`）。Mod 这一处**漏掉了 `UseDurability` 守卫**。

官方唯一扣护甲耐久的地方（`Health.cs:360-368`）：

```csharp
if (damageInfo.damageType != DamageTypes.realDamage && !damageInfo.ignoreArmor)
{
    float num4 = flag2 ? this.HeadArmor : this.BodyArmor;
    if (characterMainControl && LevelManager.Instance.IsRaidMap)   // ← raid 图门控
    {
        Item item = flag2 ? characterMainControl.GetHelmatItem() : characterMainControl.GetArmorItem();
        if (item) item.Durability = Mathf.Max(0f, item.Durability - damageInfo.armorBreak);
    }
```

**失败场景（两条互斥支路，必中其一）**

1. 选手护甲**不使用耐久**（`MaxDurability == 0`，官方大量低级护甲/背心属此类）：
   `Durability` 返回缺省 `0f` → `IsActiveFighterArmorBroken()` 从**登场第 1 帧就恒为 true**
   → `broken_shield_charge` 在开局立刻开窗，「护甲首次破损后 5 秒」的设计语义被抹平，
   收益（视距 ×1.25 / 转身 ×1.2）与代价（`shootCanMove=false`）在开局就一起吃掉。
2. 选手护甲**使用耐久**，且 Mode H 擂台 `LevelConfig.isRaidMap == false`：
   官方 `Health` 那段扣耐久整个被跳过，耐久永不下降 → 该战痕**永不触发**，
   与 CR-2026-09-03-019 修复前的状态相同（只是坏法从「triggerId 拼错」换成了「事实源不产出」）。

擂台是非 raid 图这一前提，仓库自己在两处写死了：
`ModeH/ModeHSpawnBridge.cs:82-84`（「官方 Boss preset 默认 canDieIfNotRaidMap=false…
但 Mode H 认证需要在**非 raid 图（如竞技场）**可控击杀 Boss」）与
`ModeH/ModeHProductionCertification.cs:465`（`if (!handle.Health.CanDieIfNotRaidMap)` 直接拒绝候选——
这条检查只有在 `!IsRaidMap` 时才有意义，因为官方 `Health.cs:418` 的死亡门是
`if (!LevelManager.Instance.IsRaidMap && !this.CanDieIfNotRaidMap)`）。

**玩家影响**：8 条战痕之一的行为与描述不符——要么开局白送一对利弊，要么整条战痕拿了等于没拿。
两种都违反本系统「不允许收益生效、代价失效 / 不允许无条件生效」的冻结契约。

**建议修复**
- 先补官方口径的守卫：`_activeFighterArmorItem.UseDurability && _activeFighterArmorItem.Durability <= 0f`
  （这一条无论 raid 与否都必须加，属纯 bug 修复）；
- 再确认 `LevelConfig.isRaidMap` 在擂台场景的实际取值（`DebugAndTools/F3DebugCheatMenuActions.cs:936`
  已有现成诊断项 `LevelConfig.IsRaidMap`）。若为 false，则「首次破甲」在擂台没有事实源，
  需要改判据（例如订阅 `Item.onDurabilityChanged`，或直接改用 `damageInfo.armorBreak > 0` 的首次受击）
  或按 owner 裁决换一个可达的 trigger；
- 顺带给 `tests/ModeHScarTriggerWiringGuard.py` 补一条「触发条件的事实源在本模式可产出」的断言，
  否则同一个坑会以第三种形态再犯。

**验证需求**：静态确认，未运行验证。需实机：进擂台读 `LevelConfig.IsRaidMap`；
让穿甲选手挨打后看 `broken_shield_charge` 是否开窗、以及穿无耐久护甲的选手是否开局即开窗。

---

### F-B：触发型伤病/战痕的判定键每帧重新拼字符串，战斗热路径持续产生 GC 垃圾

- **严重级**：P2 Minor
- **兼容分类**：`SAFE`（纯性能，行为不变）
- **位置**：`ModeH/ModeHCombatControl.cs:479-524`（`EvaluateTriggeredInjuries`，每帧由 `Tick` 调用）；
  `ModeH/ModeHInjuryAndScarSystem.cs:285,301,320,323`（三处 `_consumedTriggers.Add(拼接串)` + 一处 `scar_not_verified` 拼接）

**证据**

`ModeHCombatControl.Tick`（`:270`）每帧无条件调 `EvaluateTriggeredInjuries()`。该方法里的 5 个条件
**在条件成立之后每帧都会继续调用下去**（没有"已消费"短路，短路发生在被调方的 HashSet 里）：

```csharp
_injuryAndScar.OnHealthFractionChanged("old_wound", fraction);   // :483
_injuryAndScar.OnEnemyCountChanged("spirit", _telemetry.LiveEnemyCount); // :484
if (IsActiveFighterArmorBroken())      TryOpenScarWindow("broken_shield_charge", "armor_first_break", ...); // :499
if (_lowestEnemyHealthFraction <= 0.20f) TryOpenScarWindow("blood_rush", "enemy_first_low_health", ...);    // :507
if (_telemetry.ActiveFighterTookRangedDamage) TryOpenScarWindow("longshot_memory", "first_ranged_damage_taken", ...); // :514
if (_activeFighter.IsRelay)            TryOpenScarWindow("relay_expert", "relay_entered", ...);             // :522
```

而被调方的去重键是**运行时拼出来的**：

```csharp
// ModeHInjuryAndScarSystem.cs:301
if (!_consumedTriggers.Add("spirit|" + _activeProfileId)) return;
// :285
if (!_consumedTriggers.Add("old_wound|" + _activeProfileId)) return;
// :323
if (!_consumedTriggers.Add(scarId + "|" + _activeProfileId + "|" + _matchIndex)) return false;
// :320 —— blood_rush 每帧走到这一支（该战痕永不可用，见 §4 对 CR-2026-09-03-012 的复核）
failureReasonId = "scar_not_verified:" + scarId;
```

`:323` 那条还带 `int` → `string` 装箱格式化；`:320` 那条是 `blood_rush` 专属——
它对任何 key 都不可用，因此**每帧**在 `IsEntryUsable` 处返回 false 并拼一个丢弃的原因串。此外每次进 `TryOpenScarWindow` 还要走
`GetScar()` 的 O(n) 线性扫描 + `IsEntryUsable()` → `ModeHCommandCompatibilityRegistry.HasVerifiedBehavior` 的逐分量查询。

**失败场景**：一场 180 秒比赛里，`spirit` 的条件（敌军 ≥2）在绝大多数时间成立；
`longshot_memory`（吃过远程伤害）与 `relay_expert`（接力已登场）一旦成立就**再也不会变回 false**。
于是每帧稳定产生 2–5 个短命字符串，60 FPS × 180 秒 ≈ 2–5 万次分配/场，全部落在 Gen0。
AGENTS §4.12 与 `CODE_REVIEW.md` §6「每帧热路径」明确点名这类。

**玩家影响**：擂台战斗期周期性 GC 卡顿（帧时间毛刺）。不影响正确性。

**建议修复**：把去重键改成不分配的形式——例如按 (entryId, profileId) 用
`HashSet<string>` + 预先在 `BindFighter` 里一次性拼好的 per-entry key 数组，
或把 `_consumedTriggers` 换成 `HashSet<ValueTuple<string,string>>`（C# 7.3 可用）/
自定义 `struct` key + `IEqualityComparer`；最省事的做法是在 `ModeHCombatControl` 侧
为这 4 条触发各加一个 `bool _xxxTriggerConsumed` 闩，命中一次后不再下探。

**验证需求**：静态确认，未运行验证。可用 Unity Profiler 的 GC Alloc 列在擂台战斗期确认。

---

### F-C：生成期副作用检测的三个计数器没有任何写入者，「额外掉落 / 旧模式追踪」这两类副作用恒检测不到

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHSpawnBridge.cs:338-403`（`ModeHSpawnDiagnostics`）；
  消费点 `ModeH/ModeHSpawnTransaction.cs:186`、`ModeH/ModeHProductionCertification.cs:332,352`

**证据**

全仓 grep（排除 `鸭科夫源码/`、`Build/`）只有这些命中：

```
ModeHProductionCertification.cs:98 :  sink._diagnostics.DamageEventCount++;
ModeHProductionCertification.cs:107:  sink._diagnostics.DeathEventCount++;
```

`ExtraDropEventCount`、`LegacyTrackingEventCount`、`PeakUnexpectedActiveCount` **只有声明、归零与读取，没有任何 `++` / 赋值**。
于是 `HasWindowSideEffects()`（`ModeHSpawnBridge.cs:396-403`）里三个 `|| xxx > 0` 恒为 false。

`ModeHSpawnTransaction` 的类注释（`:15-16`）把这三类写成了硬契约：
「任何创建失败、阵营设置失败、点位重叠、**额外掉落/追踪副作用**或数量超过计划，都触发整批逆序回收」。

**失败场景**：生成 await 窗口内若真的发生额外掉落（`BeforeCharacterSpawnLootOnDead` 被别的子系统挂上）
或旧模式 tracking 事件，事务不会回滚，认证也不会记为副作用——契约承诺的两类保护是空的。

**玩家影响**：无直接可见影响（这是内部审计能力的缺口，不是玩法缺陷）。
维护影响是：注释与守卫都在承诺一个不存在的保护，后续改动会据此放松别处的防御。

**建议修复**：二选一——(a) 在认证的掉落 / tracking 订阅处补 `++`（与 `DamageEventCount` 同形态）；
(b) 若确认不打算实现，就删掉这三个字段并改写 `ModeHSpawnTransaction` 的类注释与 `HasWindowSideEffects`，
不要留一个恒 false 的检查。

**验证需求**：调用点 grep 已确认，静态确认。

---

### F-D：`ModeHInjuryAndScarSystem.RestoreScarWindows` 零调用点——§17.4 重建链清理的漏网

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHInjuryAndScarSystem.cs:340-354`

**证据**：全仓 grep `RestoreScarWindows` 只有该定义一处，无任何调用点。

CR-2026-09-03-018 已按 §20.3 删掉整条局中重建链（`ModeHCombatControl.RestoreFromSnapshot`、
`ModeHCombatTelemetry.RestoreFromSnapshot`、`ModeHCommandController.RestoreFromSnapshot`、
`ModeHBattleSnapshot.Validate/IsPositionUsable/TryRestoreHealth`、`ModeHSnapshotRebuildPlan`），
三个文件里都留了「已于 2026-09-03 移除」的说明注释。**唯独这一个没删也没注释**——
它的唯一潜在调用者正是被删掉的 `ModeHCombatControl.RestoreFromSnapshot`。

**失败场景**：无运行时影响（不可达）。风险是后来者看到它以为重建链还活着，接回一半。
这正是该 finding 的修复说明里点名要防止的「只接回一半」。

**建议修复**：删除该方法，或按同批其余三处的写法留一条「已随 §20.3 收敛移除」的注释；
同时把 `tests/ModeHBattleSnapshotGuard.py` 的「必须保持缺席」断言集合补上这个符号名。

**验证需求**：调用点 grep 已确认。

---

### F-E：`ReadHealthFraction` 的 XML 注释被新插入的 `ResetStaticCaches` 截断，且注释承诺（读不到返回 1）与实现（`null` 返回 0）相反

- **严重级**：P3 Note
- **兼容分类**：`SAFE`（注释）/ 语义面 `COMPAT`
- **位置**：`ModeH/ModeHCombatTelemetry.cs:292-297`、`:346-364`

**证据**（本轮未提交改动引入，`git diff HEAD -- ModeH/ModeHCombatTelemetry.cs` 可见）：

```csharp
292  /// <summary>读取生命比例；读不到时返回 1（不因读数失败伪造濒死事实）。</summary>
293  /// <summary>
294  /// 宿主销毁 / 关停时的静态缓存复位（StaticCacheLifecycleGuard 要求）。
...
297  internal static void ResetStaticCaches()
```

原属 `ReadHealthFraction` 的那行 summary 被留在了新插入的 `ResetStaticCaches` 头上，
形成同一成员上的**两个 `<summary>`**；`ReadHealthFraction`（`:346`）现在完全没有文档。

同时该行承诺的语义与实现不一致：

```csharp
public static float ReadHealthFraction(CharacterMainControl character)
{
    if (character == null) return 0f;      // ← 注释说「读不到返回 1」
    ...
    catch (Exception) { return 1f; }       // ← 只有异常分支才是 1
```

**失败场景**（语义面）：`character == null` 会被下游当作「濒死」：
`ModeHCombatControl.TryEvaluateCowardice:423`（`blood` 胆怯的血线门 ≤0.35）、
`EvaluateTriggeredInjuries:482`（`old_wound` 触发门 ≤0.35）、
`ModeHCombatTelemetry.ResolveSpecialKill:489`（`last_stand` 标签门 ≤0.20）
三处都会因为「角色引用没了」而误判成「快死了」。当前调用序列下这几处的 `Character`
在正常流程中非空（`RelayPending` 期间 `TickActiveCombat:504` 会提前 return），
所以暂列 P3；但这是一个纯靠调用序保护的默认值，改一次调用序就会变成真 bug。

**建议修复**：把 summary 移回 `ReadHealthFraction`，给 `ResetStaticCaches` 保留它自己那段；
并把 `character == null` 与 catch 分支统一（按注释语义应统一返回 `1f`，
若确实想让 null 表示「已不在场」，就改注释并在三个消费点显式区分）。

**验证需求**：静态确认。

---

### F-F：`RollCoward` 的种子子域对 `blood` / `crowd` 两种胆怯撞号

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHCombatControl.cs:450-451`；`ModeH/ModeHStateModel.cs:475-479`

**证据**

```csharp
ModeHSeedStream stream = ModeHSeedStream.Create(
    _runSeed, ModeHSeedStream.Domains.Coward, _matchIndex * 10 + anomalyId.Length);
```

而 `AnomalyCowardBlood = "blood"`（长度 5）与 `AnomalyCowardCrowd = "crowd"`（长度 5）
**长度相同**，`AnomalyCowardStrong = "strong"` 是 6。于是同一 `matchIndex` 下
blood 与 crowd 用的是**完全相同的 seed 子域**。

**失败场景**：先发带 `blood`、接力带 `crowd`（或反之）时，两次胆怯判定读同一条随机流的同一位，
结果高度相关（同一个随机值分别与两个 baseChance 比较）。用字符串长度当子域号本身也很脆：
将来任何一个 anomaly ID 改名都可能悄悄换掉或撞上别人的随机序列。

**玩家影响**：极小（概率相关性，不是必现）。

**建议修复**：子域号改用稳定枚举/常量（例如 0/1/2 的显式映射），或把 `_activeProfileId`
一并纳入子域派生，别用 `.Length`。

**验证需求**：静态确认。

---

### F-G：`ModeHSpawnTransaction` 收下 `sceneGeneration` / `ownerToken` 但从不使用

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHSpawnTransaction.cs:33-34,89-90`

**证据**：`_sceneGeneration` 与 `_ownerToken` 在 `Begin` 里赋值后，全文件再无读取。
生成期的 owner / generation 校验实际发生在调用方
（`ModeHRuntimeModule_CombatFlow.cs:292,308,341` 的 `IsCallbackStillValid`）。

**失败场景**：无运行时影响。维护面上会让人误以为事务自己会做过期检查——
`SpawnBatch` 的取消判据其实只有 `IsActive`（即事务自身是否被 `Cancel`/`RollbackAll`），
与场景代次无关。

**建议修复**：要么删参数与字段，要么在 `SpawnBatch` 的每次迭代里真的用上它们
（当前由调用方兜住，删掉更符合最小化原则）。

**验证需求**：静态确认。

---

### F-H：`SpawnBatch` 的每帧生成预算计数是死逻辑

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHSpawnTransaction.cs:124-140,199-201`

**证据**

```csharp
int spawnedThisFrame = 0;
for (...)
{
    if (spawnedThisFrame >= ModeHConfig.MaxSpawnPerFrame) { spawnedThisFrame = 0; yield return null; }
    ...
    spawnedThisFrame++;
    ...
    yield return null;      // ← 循环尾部无条件让帧
}
```

循环体尾部**无条件** `yield return null`，所以进入下一次迭代时永远已经跨帧了；
`spawnedThisFrame` 在检查点恒为 0（上一轮 `++` 后跨帧，但变量不复位…… 实际上它会累加，
只是 `MaxSpawnPerFrame == 1` 时第 2 轮起 `1 >= 1` 成立，于是**多让一帧**）。
净效果：每个角色实际占用 2 帧而不是设计的 1 帧，且注释宣称的
「显式计数而不是靠 await 的副作用，常量改动时行为随之改变」并不成立——
把 `MaxSpawnPerFrame` 改成 3 也仍然是一帧一个。

**玩家影响**：入场生成比设计慢一倍（4 个角色 ≈ 8 帧，不可感知），但常量失去调节能力。

**建议修复**：删掉循环尾部那个无条件 `yield return null`，让预算计数真正生效；
或删掉预算计数并改写注释与 `ModeHPerformanceGuard` 的引用断言。
注意 `tests/ModeHSpawnTransactionGuard.py:116` 已把 `MaxSpawnPerFrame = 1` 冻结，
且 `:98` 要求 `SpawnBatch` 里至少两处 `yield return null;` —— 改动要同步守卫。

**验证需求**：静态确认。

---

### F-I：认证逐 key 超时后仍在 pending 的 `CreateIsolatedAsync` 会把角色与 clone preset 永久泄漏在场景里

- **严重级**：P2 Minor
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHProductionCertification.cs:320-331,340-351`

**证据**

```csharp
Cysharp.Threading.Tasks.UniTask<ModeHSpawnHandle> scavTask =
    ModeHSpawnBridge.CreateIsolatedAsync(audited, stableKey, Teams.scav, map.StagingPos, _diagnostics);
while (scavTask.Status == Cysharp.Threading.Tasks.UniTaskStatus.Pending)
{
    if (Time.realtimeSinceStartup >= keyDeadline) break;   // ← 超时就不等了
    yield return null;
}
try { scavHandle = scavTask.GetAwaiter().GetResult(); }     // ← 任务仍 Pending
catch (Exception e) { failure = "certification_scav_create:" + e.GetType().Name; }
_activeScavHandle = scavHandle;                              // ← null
```

超时 break 之后 task 仍在跑。`GetAwaiter().GetResult()` 对未完成的 UniTask 会抛
（被 catch 吞成 `certification_scav_create:*`），`scavHandle` 保持 null，
于是 `_activeScavHandle = null` → 随后的 `ReleaseDiagnosticPair()`（`:436`、`:617-627`）
`Recycle(null)` 什么也不回收。

但 `CreateIsolatedAsync` 内部**已经**做了三件有副作用的事（`ModeHSpawnBridge.cs:78-101,119`）：
`Instantiate(auditedPreset)` 克隆 preset、`ModeHDeathSuppressionRegistry.RegisterPreset(clone)`、
`clone.CreateCharacterAsync(...)`。任务最终完成时角色会被创建在 `map.StagingPos`
（inactive + invincible），返回的 handle 被丢弃，**没有任何引用能回收它**。

**失败场景**：全池 180 秒预算被前面的 key 吃掉后，靠后的 key 的 `keyDeadline`
（`min(keyStart+15s, poolDeadline)`）会在角色创建期间到期——尤其是首次认证在慢盘/低配机上。
每命中一次泄漏一只隐藏角色 + 一个 clone `ScriptableObject` + 一条死亡抑制表 preset 登记
（`_activeCount` 永远减不回去，`IsSuppressionArmed` 于是在整场剩余时间内恒 true，
`CharacterOnDeadPatch` 的快路径失去零成本早返）。

**玩家影响**：擂台里躺着若干看不见的隐藏 Boss 实例（inactive，不参战不结算），
直到切图才随场景卸载；本局内额外死亡掉落抑制表恒为「已武装」。不会导致玩法错误，
但违反 `ModeHSpawnBridge` 的「任何失败都整批回收」契约。

**建议修复**：超时分支不要直接丢弃 task——保留它并在其完成后回收，
例如把等待改成「超时置 `timedOut` 标志，但仍继续 `yield return null` 直到 task 非 Pending，
拿到 handle 后立刻 `ModeHSpawnBridge.Recycle(handle)`」；或给 `CreateIsolatedAsync`
加一个取消信号，让它在返回前自己 Recycle。`ModeHSpawnTransaction.SpawnBatch:159-162`
是正确形态（无条件等到非 Pending），可作对照。

**验证需求**：静态确认，未运行验证。实机复现需人为把 `CertificationPerKeyTimeoutSeconds` 调小。

---

### F-J：观战租约的类注释承诺了两个实现里没有的步骤（持有物表现锁定 / 控制目标确认）

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHSpectatorLease.cs:11-15`（类注释）vs `:116-154`（`TryAcquire`）、`:183-265`（`Release`）

**证据**

类注释冻结了这两条：

> 获取顺序固定：…… -> 移动到审计后的 modeHSpectatorPos -> **隐藏/锁定持有物表现并确认控制目标仍为玩家身体**
> 释放顺序固定：停止接收拍铃 -> **恢复控制目标与持有物显示** -> 仍无敌且中立时恢复原位置 -> ……

实现里 `TryAcquire` 只有 5 步（token/input、invincible、team、position、cursor），
`Release` 只有 5 步（bell、position、team、invincible、input+cursor）。
**没有任何 `SetControllingCharacter`、`LevelManager.ControllingCharacter` 校验，
也没有任何持有物/武器显示相关调用**（全文件 grep 无 `Controlling`、无 `Weapon`、无 `Hold`）。

**失败场景**：进入 Mode H 时若控制目标不是玩家身体（例如玩家正在载具/受控状态里），
租约照常把「玩家身体」搬上看台，而镜头与输入仍绑在别的角色上——注释承诺的 fail-closed 检查缺席。
概率低（Mode H 入口在竞技场交互物上），但这是注释与实现不一致的典型。

**建议修复**：二选一——补上那一步确认（`LevelManager.Instance.ControllingCharacter == _player`，
不成立就拒绝并回滚），或把注释改成实际步骤。持有物那一条按 §17.6.5 的说明
（`CanUseHand`/`CanControlAim` 保持原版 false 即为第二道保险）本来就不需要，
应从注释里删掉而不是补实现。

**验证需求**：静态确认（grep 全文件）。

---

### F-K：晚到 spawner 的 `created` 原值被硬编码成 false，恢复后可能重复刷一批原生敌人

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHArenaIsolationLease.cs:344-350` vs `:152-162`

**证据**

初次冻结时是**读**出来的：

```csharp
bool originalCreated = false;
if (_createdField != null) { object raw = _createdField.GetValue(root); if (raw is bool) originalCreated = (bool)raw; }
bool originalActive = root.gameObject.activeSelf;
_frozenOriginalCreated.Add(originalCreated);
```

而 `CheckLateSpawners` 里是**写死**的：

```csharp
_frozenOriginalCreated.Add(false);                       // ← 没有回读实际值
_frozenOriginalActive.Add(root.gameObject.activeSelf);   // ← 这一个是对的
```

官方 `CharacterSpawnerRoot.created`（`鸭科夫源码/TeamSoda.Duckov.Core/CharacterSpawnerRoot.cs:309`）
是「本 spawner 是否已刷过」的闩：`:213` `if (this.created) return;` `:217` `this.created = true;`。

**失败场景**：一个晚到的 spawner 若在被发现时 `created == true`（它已经刷过了），
`Release`→`RestoreSpawners` 会把它写回 false 并重新 `SetActive(true)`，
于是 Mode H 离场后该 spawner **再刷一批**原生敌人。
当前 `CheckLateSpawners` 返回 false 会直接进技术恢复，所以这是恢复路径上的边缘场景。

**建议修复**：与初次冻结用同一段读取逻辑（把那 5 行抽成一个 `CaptureSpawnerState(root)` 复用）。

**验证需求**：静态确认。

---

### F-L：三个逐 effect 遥测出口零调用点（`ValidateHeldEffects` / `AppliedEffectIds` / adapter 的 `LastError`）

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHCommandController.cs:254-257`、`ModeH/ModeHCommandAdapters.cs:161,164,167-178`

**证据**：全仓 grep（排除 `鸭科夫源码/`、`Build/`）——

- `ValidateHeldEffects` 只有定义，无调用点；
- `AppliedEffectIds` 只有定义，无调用点（且每次 get 都新建一个 `List<string>`）；
- `ModeHCommandAdapter.LastError` / `ModeHCommandController.LastError` 无读者
  （grep 命中的 `LastError` 全是其他类的同名成员）。

这三个是 CR-2026-09-02-013 之前那套「生产期逐 effect 遥测」的残留。
认证改由 `ModeHCommandCertificationProbe` 直接调 `_adapter.Validate()` +
`RecordEffectStatus` 之后，生产战斗期不再做保持率测量——**这是设计选择，不是缺陷**，
但留下的三个出口会让人以为生产期仍在测量。

**建议修复**：删除三者，或在注释里写明「测量已收敛到认证探针」。

**验证需求**：调用点 grep 已确认。

---

### F-M：伤病 `armor`（护具受损）是**完全空操作**——唯一的效果开关 `IsArmorKitDisabled` 零读者

- **严重级**：**P1 Major**（5 条伤病之一整条无效果；且它是最容易被抽中的两条之一）
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHInjuryAndScarSystem.cs:60,73,105,191,245-252`；
  `Assets/Data/ModeH/Scars.json`（`injuries[].injuryId == "armor"`）

**证据**

`Scars.json` 里 `armor` 只有一条分量，而且是纯自结算：

```json
{ "injuryId": "armor", "scope": "self_settled",
  "components": [ { "effectId": "armor.kit_suppression", "controlPointId": "command_scale",
                    "op": "self_settled_kit_slot_disabled", "slot": "Armor", "selfSettled": true } ] }
```

代码侧把它翻译成一个 bool：

```csharp
// ModeHInjuryAndScarSystem.cs:245-252（ApplySelfSettledComponents）
if (string.Equals(component.Op, "self_settled_kit_slot_disabled", StringComparison.Ordinal))
{
    if (string.Equals(component.TargetSlot, "Armor", StringComparison.Ordinal))
    {
        _armorKitDisabled = true;
    }
    continue;
}
```

而这个 bool 的**唯一出口 `public bool IsArmorKitDisabled` 全仓库零读者**。
`grep -rn "IsArmorKitDisabled\|_armorKitDisabled" --include=*.cs .`（排除 `Build/`）只有 5 处命中，
**全部在 `ModeHInjuryAndScarSystem.cs` 自己文件内**（字段声明、属性声明、两处复位、一处置位）。

装配器 `ModeHLoadoutKitApplicator.TryApply` 完全不查它；
`ModeHRuntimeModule_CombatFlow.BuildDefaultKitSelection`（挑默认 kit）也不查它。

**失败场景**：选手在某场倒地 → `ResolveDownEvent` → `PickInjury` 抽到 `armor`
（`armor` 与 `spirit` 是仅有的两条**全自结算**伤病，对任何 stable key 恒可用，
其余三条要过实测，因此 `armor` 的入选概率显著高于 1/5）→ 之后每一场：
结算页与选手卡照常显示「护具受损」，赔率照常按带伤扣分，
**但战斗里 Armor 槽的整备照常生效，选手没有受到任何惩罚**。

**玩家影响**：一条伤病「拿到了但永远不生效」，与 CR-2026-09-03-019 修复的战痕问题同型。
更糟的是方向：这次是**代价失效**——玩家白赚。赔率侧仍按「带伤」给惩罚项，
数值上是「扣了赔率但没吃代价」，与 §17.4「不允许收益生效、代价失效」的对称约束反向违背。

**建议修复**：把 `IsArmorKitDisabled` 接进 kit 装配的入口。最小接线点有两处，
建议**两处都接**（前者管玩家看到的可选项，后者是 fail-closed 的最后一道）：
1. `ModeHRuntimeModule_CombatFlow.BuildDefaultKitSelection` / 整备编辑 UI 的可选 kit 过滤，
   在 `kit.Spec.ReplaceSlot == "Armor"` 时跳过；
2. `ModeHLoadoutKitApplicator.TryApply`：新增一个「本场被禁用的槽位」入参，
   命中时按 `kit_apply_slot_suppressed:<kitId>` 拒绝该件（**不要整批 fail**——
   整批失败会走 `AbortMatchSpawning` 变成技术中止，那是把一条伤病升级成开局失败）。

注意 `ModeHCombatControl.OnFighterEntered` 里 `ApplyStandingInjury` 的调用时机
（`ModeHRuntimeModule_CombatFlow` 中 `TryApply` 在 `:319`/`:652`、`OnFighterEntered` 在 `:352`）
**晚于** kit 装配，所以不能直接在装配时读 `_armorKitDisabled`——
需要把 profile 的 `injuryId` 提前传给装配侧，或把 `ApplyStandingInjury` 提前。这一点必须一起改。

**验证需求**：调用点 grep 已确认（静态确认，未运行验证）。
实机：让选手带 `armor` 伤病上场，确认 Armor 槽为空且结算页仍显示「护具受损」。

---

### F-N：认证诊断注册表整条不通电——`ModeHEventRouter` 的诊断优先级保护结构性从未生效

- **严重级**：P2 Minor（当前调用序下无实际危害；但契约文本承诺的一层保护完全不存在，且有两份并行的写-only 注册表）
- **兼容分类**：`SAFE`
- **位置**：`ModeH/ModeHEventRouter.cs:68,97,234-255`；
  `ModeH/ModeHProductionCertification.cs:45,73-76,128-145,607-615,629-637`

**证据**

`ModeHEventRouter` 的类注释把它列为冻结契约：

> `ProductionCertifying` 的 diagnostic 注册表**查询优先**，命中只交认证，绝不进入战斗遥测、结果 CAS、伤病/战痕或奖励路径

实现里 `HandleHealthHurt` / `HandleHealthDead` 确实先查 `_diagnostics`。
但**写入 `_diagnostics` 的三个入口全部零调用点**：

```
$ grep -rn "ModeHEventRouter.RegisterDiagnostic|ModeHEventRouter.UnregisterDiagnostic|ModeHEventRouter.ClearDiagnostics" --include=*.cs .   (排除 Build/)
（无输出）
```

于是 `_diagnostics` 恒为空、`DiagnosticCount` 恒 0，`NotifyDiagnosticHurt/Dead` 从不由路由触发
（它们实际只被 `TryControlledKill` 内部临时挂的 `UnityAction` 调用，`ModeHProductionCertification.cs:477-486`）。

同时 `ModeHProductionCertification` 自己维护了**第二份**注册表 `_diagnosticHealthIds`
（`RegisterDiagnostic` / `UnregisterDiagnostic` 正常读写），
但它的唯一查询口 `IsDiagnosticHealth(Health)` 与 `IsDiagnosticRegistryEmpty`
**同样零读者**。`IsDiagnosticSinkBound`（「正式开战前诊断事件接收端必须已解绑」）也零读者。
`HasObservedDamage` / `HasObservedDeath` 零读者 —— `_observedDamageByKey` / `_observedDeathByKey`
两张表被写满后从不被查询。

**失败场景**：当前调用序下诊断与战斗不重叠——认证在 `DriveCertification` 里跑完才
`CreateDraftingSeason`，而 `ModeHEventRouter.Bind` 只在 `InitializeCombatRuntime` 调用，
所以认证期间路由根本没订阅。因此**今天没有可观测的错误行为**。
风险在于：三处契约文字都在承诺一层不存在的隔离，任何把认证挪到「路由已绑定」之后的改动
（例如诊断页的「强制重新认证」将来支持局中重跑）都会直接让认证角色的伤害/死亡
灌进战斗遥测与结果 CAS，而当前没有任何断言能发现。

**建议修复**：二选一并同步注释——
(a) 真接线：`ModeHProductionCertification.RegisterDiagnostic/UnregisterDiagnostic` 里
    同时调 `ModeHEventRouter.RegisterDiagnostic/UnregisterDiagnostic`，
    `Run` 的收尾与 `Cancel` 调 `ClearDiagnostics`，并在 `InitializeCombatRuntime` 前
    断言 `IsDiagnosticRegistryEmpty && !IsDiagnosticSinkBound`；
(b) 删掉整条死支路（路由的 `_diagnostics` 三个入口 + `ModeHProductionCertification`
    的 `_diagnosticHealthIds` 与四个零读者查询口），并把两个类注释改成实际语义。
推荐 (a)：`ModeHIsolationGuard` 已经在守「正式开战前 diagnostic registry 必须为空」这类不变式，
接线后那条断言才有对象。

**验证需求**：调用点 grep 已确认。

---

### F-O：`GetVerifiedEffectIds` 零调用点——§17.6.4「候选卡文案只能由已通过的 effect 生成」未接线

- **严重级**：P2 Minor
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHCommandCompatibilityRegistry.cs:463-480`

**证据**：全仓 grep `GetVerifiedEffectIds` 只有定义一处，零调用点。
方法自己的文档写着「候选卡/选令文案**只能**由已通过的 effect 生成（§17.6.4）」。

**失败场景**：`center` / `press` / `hold` / `all_in` / `weakness` 这几条都是
`PartiallyVerified`（点火型与 marker 型分量恒 `ReportOnly`，见 F-0 的同一机制），
但选令 UI 展示的是整条 `descKey`，不做逐 effect 过滤。
玩家读到的口令描述包含未通过实测的效果（例如 `center` 的「移动到擂台中心」、
`press` 的「转火最近敌人」在该 key 上并未被认证证明有效）。

**玩家影响**：口令描述可能承诺当前 key 上未验证的效果。属预期管理问题，不影响结算正确性。
（UI 侧的具体呈现属同批 `ModeH_content_ui_guards` 报告范围，这里只记注册表侧的接线缺口。）

**建议修复**：选令页与候选卡按 `GetVerifiedEffectIds(stableKey, commandId)` 过滤展示项，
或删掉该方法并把 §17.6.4 的这条承诺从注释里移除（需 owner 拍板取哪种）。

**验证需求**：调用点 grep 已确认。

---

### F-P：整备 kit 的 16 个固定 typeId 从不在运行时核对，官方物品表变动时会在开局中途炸而不是在内容门槛处优雅拒绝

- **严重级**：P2 Minor
- **兼容分类**：`COMPAT`
- **位置**：`ModeH/ModeHLoadoutKitRegistry.cs:184-213`（`TryResolveRuntimeTypeIds`，零调用点）、
  `:121-131`（`ValidateInternal` 的静态置位）；`Assets/Data/ModeH/LoadoutKits.json`（16 件全部 pinned）

**证据**

静态审计只按 JSON 字段置位，**不碰官方物品表**：

```csharp
kit.ResolvedTypeId = spec.TypeId;
kit.ResolvedQuality = spec.GameQuality;
kit.Available = spec.TypeId > 0;                       // ← 只看 JSON 写了没有
kit.FailureReason = kit.Available ? null : "pending_runtime_resolve";
```

真正回查官方表的是 `ResolveOne`（`:216-240`），pinned 分支会做
`TryReadQuality(spec.TypeId, out quality)`，读不到就 `Available = false; FailureReason = "pinned_type_missing"`。
但它的唯一入口 `TryResolveRuntimeTypeIds` **全仓库零调用点**
（`IsRuntimeResolved` 同样零读者，`_runtimeResolved` 恒 false）。

`LoadoutKits.json` 当前 16 件 kit 的 `typeId` 全部 > 0（2 / 43 / 238 / 1074 / 782 / 254 / 35 / 44 /
1286 / 869 / 885 / 46 / 1175 / 785 / 920 / 1174），所以今天不会出现「`typeId==0` 的 kit 永久不可用」。

**失败场景**
1. 官方更新改动/移除任一 typeId → 静态审计仍报 `Available = true`
   → `ModeHLoadoutKitApplicator.TryApplyOne:128` 的 `ItemAssetsCollection.InstantiateSync(typeId)` 返回 null
   → `kit_apply_instantiate_failed` → `TryApply` 整批 `Recycle` 并 false
   → `DriveCompleteMatchSpawning:319-325` 走 `AbortMatchSpawning("starter_kit_failed")`
   → 技术重试耗尽后 `Suspended`。玩家看到的是「模式开不了局」，而不是「某件装备不可用」。
2. 品质区间（`MinGameQuality`/`MaxGameQuality`）对 pinned typeId 从不校验。
3. 将来任何新增的 `typeId: 0` + `resolveTags` 形态 kit 会**永久停留在 `pending_runtime_resolve`**、
   静默不可用——这正是本仓库反复出现的静默失败形态。

**建议修复**：在 Mode H 内容门槛（`ModeHRuntimeGates` 的 content-ready 判定）
或进场准备阶段调用一次 `TryResolveRuntimeTypeIds`，让 pinned typeId 也过一次
`TryReadQuality` 复核；解析失败只让该 kit 不可用（方法内部本来就是这个语义），
不阻断整个模式。同时补一条守卫：「`LoadoutKits.json` 的每个 kit 要么 `typeId>0` 且能被运行时复核，
要么有 `resolveTags`」。

**验证需求**：调用点 grep 已确认（静态确认，未运行验证）。
16 个 pinned typeId 与当前官方版本的一致性**本轮无法核对**（仓库内没有官方物品表数据），
列入验证需求：实机跑一次 `MODE_H_STARTER_KITS`（CR-2026-09-02-016 已有该用例）即可覆盖 8 件 starter，
8 件 reward kit 仍需另行确认。

---

### F-Q：一批只写不读的诊断出口（write-only accessors）

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：见下表（全部经全仓 grep 确认零外部引用）

| 位置 | 成员 | 说明 |
| --- | --- | --- |
| `ModeHSpawnTransaction.cs:45,51,54,57` | `IsCommitted` / `FighterCount` / `SpawnedStableKeys` / `FailureReasonIds` | 事务的四个只读诊断面无人读；`_spawnedStableKeys` 与 `_failureReasonIds` 是纯写入 |
| `ModeHSpawnTransaction.cs:398` | `RecycleEnemy` | 「回收单个已死亡的敌军实例」零调用点——敌军尸体实际要等 `ReleaseCombatRuntimeObjects` 的 `RollbackAll` 才回收（上限 3 只，无实际危害） |
| `ModeHSpawnBridge.cs:335` | `ActiveWindowKey` | 只在 `BeginCreateWindow`/`EndCreateWindow` 赋值，无读者 |
| `ModeHCombatControl.cs`（`StandInPerformer` 属性） | `StandInPerformer` | 注释写「供诊断页展示」，诊断页不读 |
| `ModeHCombatTelemetry.cs` | `GetCachedHealthFraction` / `GetSeedSequence` | 零调用点；`_lastKnownHealthFraction` 因此是纯写入字典（`UpdateHealthFraction` 每次受伤都写） |
| `ModeHInjuryAndScarSystem.cs:76` | `ActiveWindowCount` | 零读者 |
| `ModeHSpectatorLease.cs:64` | `PlayerBody` | 零读者 |
| `ModeHArenaIsolationLease.cs:51,54` | `FrozenSpawnerCount` / `ClearedNativeEnemyCount` | 零读者（类内 DevLog 直接用 `_frozenSpawners.Count`） |
| `ModeHStandInPerformer.cs:50,53` | `PatternId` / `ConsecutiveFailures` | 零读者 |
| `ModeHPresetRegistry.cs:44,56,174` | `HasProductionPool` / `BoundSignature` / `CoversAllArchetypes` | 零读者；`CoversAllArchetypes` 与 `EvaluateThreshold` 的原型覆盖检查功能重复 |
| `ModeHProfileRegistry.cs:223` | `GetProductionByArchetype` | 零调用点（`ModeHPresetRegistry.GetProductionKeysByArchetype` 是实际在用的那个，但它自己也零读者） |
| `ModeHCommandCompatibilityRegistry.cs:78` | `MatrixSignature` | 零读者 |
| `ModeHCommandAdapters.cs`（`OwnerEntryId` 属性） | `OwnerEntryId` | 零读者 |
| `ModeHControlPointHarness.cs` 全部三个入口 | `ProbeCommandHoldRate` / `ReadControlPoint` / `DumpKitCatalog` | **按设计**：文件本身是 `AllowDevControlPointHarness`（编译期常量 false）门控的开发 harness，零调用点是预期状态，不算缺陷，此处仅登记 |

**影响**：无运行时影响。维护面：这些出口会让人以为存在对应的诊断/校验路径。
`_lastKnownHealthFraction` 与 `_observedDamageByKey/_observedDeathByKey`（见 F-N）
是纯写入的字典，每场比赛持续增长（键数量分别是参赛者数与 key 数，都是个位数，无泄漏风险）。

**建议修复**：按「删或接」逐条处理。优先接的两条是
`ModeHPresetRegistry.CoversAllArchetypes`（可替代 `EvaluateThreshold` 里的手写覆盖检查）
与 `ModeHProductionCertification.HasObservedDamage/HasObservedDeath`
（可作为 `RecordPassed` 的额外前置断言）。

---

## 二、未验证线索

### U-1：Mode H 擂台场景的 `LevelConfig.isRaidMap` 实际取值未经实机确认

F-A 的第二条支路完全取决于它。仓库内两处代码把「擂台是非 raid 图」当既定事实
（`ModeHSpawnBridge.cs:82-84` 的注释、`ModeHProductionCertification.cs:465` 的必需检查），
但没有任何运行时证据被记录下来。`DebugAndTools/F3DebugCheatMenuActions.cs:936` 已有现成诊断项。
**建议在下一次实机 smoke 时顺手记一条日志**，它同时决定：
armor 耐久是否会下降、`GetHelmatItem` 侧是否同理、以及官方 `DeadBodyManager` 是否在擂台建尸体。

### U-2：`LoadoutKits.json` 里 8 件 reward kit 的 typeId 与当前官方版本的一致性

见 F-P。CR-2026-09-02-016 的 `MODE_H_STARTER_KITS` 用例只覆盖 8 件 starter。
reward kit 只在赛季推进解锁后才会被装配，走的是同一条 `InstantiateSync` 路径，
一旦 typeId 失效表现同样是「开局中途技术中止」。

### U-3：并发改动期间的 `appliesWhen` 求值层（CR-2026-09-03-020 的实施）

本次审查期间（09:20–09:40）另一会话正在把 `appliesWhen` 条件层落地：
`ModeH/ModeHCommandAdapters.cs` 从 587 行增至 762 行（新增 `ModeHEffectConditions`），
`ModeHCombatControl.cs` 1006→1032（新增 `_arenaConditionId` / `_lastEntryBatchIndex` 与
`_fireContext` 的 4 个新条件输入），`ModeHInjuryAndScarSystem.cs` 603→614，
`ModeHCombatTelemetry.cs` 515→529，`ModeHEventRouter.cs` 397→402，
`ModeHRuntimeModule_CombatFlow.cs` 1157→1180。
我对这批**未落定**的代码只做了接线抽查（4 个新条件输入均有生产者、`BeginMatch` 已传参），
**未做完整审查**，不在本报告的 confirmed findings 内。
下一轮审查应把 `ModeHEffectConditions` 与 adapter 的「分量随条件上下线」重申路径当作新面重审——
它引入了「窗口生效期内 modulation 可以被中途 restore / 重新 apply」这一新语义，
与 `Restore()` 的「一次性、之后 `_ai = null`」旧假设存在潜在冲突。

---

## 三、已核对且干净的关键面

### 3.1 Harmony 补丁目标逐项核对表（对照 `鸭科夫源码/`）

Mode H 自己只有两个 Harmony 补丁类，都在 `ModeH/ModeHHarmonyPatches.cs`：

| 补丁类 | 目标 | 官方源码位置 | 形态 | 结论 |
| --- | --- | --- | --- | --- |
| `ModeHStandInCanMovePatch` | `CA_ControlOtherCharacter.CanMove` | `TeamSoda.Duckov.Core/CA_ControlOtherCharacter.cs:18` | `public override bool CanMove()`，本类**显式声明**、零参、无重载 | ✅ `AccessTools.DeclaredMethod` 可命中；`[HarmonyPostfix]` + `ref bool __result` 签名正确 |
| `ModeHStandInCanRunPatch` | `CA_ControlOtherCharacter.CanRun` | 同上 `:92` | `public override bool CanRun()`，同上 | ✅ 同上 |

- 两个补丁都是 postfix、只写 `__result`、不返回 false、不改执行流 —— 与 `Patches/AGENTS.md` 的
  「优先幂等、时机无关的拦截点」一致。
- 明确**不**打 `CanUseHand` / `CanControlAim`（官方 `:97` / `:103` 同样是本类显式 override），
  这是有意的第二道保险，注释与实现一致。
- 快门 `ModeHRuntimeGates.IsModeHStandInActive` 是静态 bool，未激活时零分配早返；
  `ShouldUnfreeze` 全程 no-throw、fail-closed，异常按 5 秒限频告警。符合 §4.7。
- Mode H **没有**给 `Health.Hurt` / `CurrentHealth` setter 打自己的补丁：
  临时角色的隔离靠 `SetInvincible(true)` + `SetActive(false)`，不需要像 Mode G 那样在
  `BossRushHealthHurtContextPatch` 里加 staging 屏障。`Patches/Combat/BossLethalHealthProtectionPatch.cs`
  的致死钳制链（逆鳞 / 龙皇 / 龙裔 / 遗种巢四个消费者）里**没有** Mode H 分支，符合设计。
- 与 `Patches/Combat/CharacterOnDeadPatch.cs` 的交互已核对：Mode G / Mode H / PetNest
  三张抑制表在同一个 Prefix 里以 `||` 并列，任一命中都只跳过**同样的**两个额外掉落 handler
  （霜之哀伤蓝 Boss、女巫镰刀），不存在优先级或互斥冲突；原版 `OnDead` 与 `Health.OnDead` 照常执行。
  三张表都有各自的静态 armed bool 快路径，异常一律 fail-open=false。

### 3.2 反射 / 字符串绑定目标逐项核对表

| 位置 | 绑定字符串 | 官方源码 | 结论 |
| --- | --- | --- | --- |
| `ModeHArenaIsolationLease.cs:441` | `typeof(CharacterSpawnerRoot).GetField("created", NonPublic\|Instance)` | `CharacterSpawnerRoot.cs:309` `private bool created;` | ✅ 存在且形态一致（官方 `:213/:217` 正是用它当「已刷过」闩） |
| `ModeHLoadoutKitApplicator.cs:37-39` | `typeof(ItemSetting_Gun).GetField("_bulletCountCache", Instance\|NonPublic)` | `ItemSetting_Gun.cs:512` `private int _bulletCountCache = -1;` | ✅ 存在；绑定缺失时代码显式拒绝（`kit_apply_ammo_cache_binding_missing`），不静默 |
| `ModeHLoadoutKitApplicator.cs:340` | `"ControlMindType".GetHashCode()` | `ItemAgent_Gun.cs:1888` `private static int ControlMindTypeHash = "ControlMindType".GetHashCode();` | ✅ 逐字同算法；读的是 `Item.GetStatValue(hash)`，与官方 `:87` 同一取值路径 |

### 3.3 官方 API 面逐项核对（本轮全部回查 `鸭科夫源码/`，无一缺失）

- 事件：`Health.OnHurt` / `Health.OnDead` = `public static event Action<Health, DamageInfo>`（`Health.cs:123,128`）✅
  —— 静态事件、命名 handler、单次订阅 + 对称退订，符合 AGENTS §4.6。
- `Health`：`Hurt(DamageInfo)`（`:298`，返回 `bool`）、`OnHurtEvent` / `OnDeadEvent`（`UnityEvent<DamageInfo>`，`:439,451`）、
  `MaxHealth`（`:44`）、`CurrentHealth`（`:98`）、`IsDead`（`:137`）、`Invincible`（`:147`）、
  `SetInvincible(bool)`（`:508`）、`CanDieIfNotRaidMap`（`:83`）✅
- `DamageInfo`：`DamageInfo(CharacterMainControl fromCharacter = null)`（`:77`）、
  `damageValue`（`:110`）、`ignoreArmor`（`:116`）、`toDamageReceiver`（`:147`）、
  `damagePoint`（`:151`）、`fromWeaponItemID`（`:162`）✅
- `CharacterMainControl`：`Team`（`:72`）、`SetTeam`（`:1083`）、`SetPosition`（`:2253`）、
  `aiCharacterController`（`:3244`）、`mainDamageReceiver`（`:3363`）、`GetArmorItem()`（`:2265`）、
  `ControlOtherCharacter(CharacterMainControl, float=-1f)`（`:458`）、
  `SetMoveInput(Vector3)`（`:715`）、`SetRunInput(bool)`（`:955`）✅
- `LevelManager`：`ControllingCharacter`（`:147`）、`SetControllingCharacter`（`:498`）✅
- `CharacterRandomPreset`：`nameKey`(155) / `isBoss`(158) / `dropBoxOnDead`(167) /
  `specialAttachmentBases`(173) / `team`(179) / `showName`(185) / `setActiveByPlayerDistance`(231) /
  `aiCombatFactor`(343) / `isVehicle`(404) / `canDieIfNotRaidMap`(182) ✅
- **`AICharacterController` 的 13 个控制点全部存在且类型匹配**（§17.6.2 白名单）：
  `skillSuccessChance`(526,float) `itemSkillChance`(670,float) `itemSkillCoolTime`(673,float)
  `sightDistance`(538,float) `sightAngle`(544,float) `combatTurnSpeed`(504,float)
  `patrolTurnSpeed`(501,float) `baseReactionTime`(547,float) `shootCanMove`(492,bool)
  `skillCoolTimeRange`(519,Vector2) `nextReleaseSkillTimeMarker`(529,float)
  `searchedEnemy`(467,DamageReceiver) `noticed`(535,bool) `forceTracePlayerDistance`(541,float)；
  方法 `SetNoticedToTarget(DamageReceiver)`(283) 与 `MoveToPos(Vector3)`(394) ✅
  —— **没有出现 AGENTS §14 点名的 `MoveSpeed` 那类不存在的 stat key**。
  `Commands.json` 的 `controlPointWhitelist` 16 项 = 上述 14 个真实字段/方法 + 2 个 Mode H 自结算
  伪控制点（`coward_mitigation` / `command_scale`，不写原版字段），逐项对得上。
- 物品链：`ItemAssetsCollection.InstantiateSync(int)`（`:147`）、`Slot.Plug(Item, out Item)`（`Items/Slot.cs:129`）、
  `SlotCollection.GetSlot(string)`（`Items/SlotCollection.cs:66`）、`Inventory.AddItem(Item)`（`:543`）、
  `Inventory : IEnumerable<Item>`（`:15`，`foreach (Item x in gunItem.Inventory)` 合法）、
  `Item.Detach()`(816) / `DestroyTree()` / `MaxStackCount`(146) / `StackCount`(1033) /
  `Inventory`(504) / `GetStatValue(int)`(1524) / `Durability`(1158) / `MaxDurability`(1112) /
  `UseDurability`(1101)、`ItemSetting_Gun.TargetBulletID`(18) / `BulletCount`(42) / `Capacity`(76) /
  `SetTargetBulletType(int)`(164) / `IsValidBullet(Item)`(238) / `GetBulletCount()`(326) ✅

### 3.4 生成事务 / 敌对性安全网（AGENTS §4.5）—— 干净

- `ModeHSpawnBridge.TryPrepareForArena:200` 无条件 `handle.Character.SetTeam(handle.Team)`，
  提交后 `VerifyTeamsAssigned` 逐个核对（敌军必须 `wolf`、选手必须 `scav`），
  下一帧再由 `VerifyTeamsStableNextFrame` 复查一次漂移，双向 `Team.IsEnemy` 断言齐备。
  任一步失败都 `RollbackAll()` 并把事务置 `_cancelled`（`IsActive` 随之为 false）→ fail-closed 成立。
- **距离休眠已解除**：`TryActivate:248` 在 `SetActive(true)` 之后调用
  `SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep(handle.Character)`，
  位置与注释一致（AGENTS §14 的第一条教训已落实到 Mode H）。
- clone 归一化齐备：`aiCombatFactor=1f`、`dropBoxOnDead=false`、`team`、`canDieIfNotRaidMap=true`；
  抑制表在 `CreateCharacterAsync` **之前**登记 preset、返回后补登记 Health/Character；
  回收顺序严格是「角色引用 → preset 引用 → 销毁 clone」。
- `AI` 的 `forceTracePlayerDistance = 0f` / `searchedEnemy = null` / `noticed = false` 在隔离期清零，
  且回退取 AI 组件用的是 `GetComponentInChildren<AICharacterController>(true)`（带未激活对象），
  与官方 `AICharacterController.Init` 的 `SetParent` 事实一致 —— 注释里的警告是对的。
- 生成失败能退回状态机：`AbortMatchSpawning` → `ReleaseCombatRuntimeObjects` + `RollbackAll`
  + `TryReturnRealStakeOnAbort` + `RestoreMatchReservationAndSnapshot` + `RequestTechnicalRetry`，
  四步顺序与注释一致（押品退还必须先于 `RestoreMatchReservationAndSnapshot`，代码确实如此）。

### 3.5 手工 `MoveNext()` 驱动协程一律透传 `Current`（AGENTS §14 第三条教训）—— 全部合规

本轮逐个核对了 Mode H 里全部 6 处手工驱动：

| 位置 | 写法 | 结论 |
| --- | --- | --- |
| `ModeHRuntimeModule_MatchFlow.cs:1051` | `while (inner.MoveNext()) yield return inner.Current;` | ✅ |
| `ModeHRuntimeModule_CombatFlow.cs:290-294`（enemy batch） | `yield return enemyBatch.Current;` | ✅ |
| `ModeHRuntimeModule_CombatFlow.cs:306-310`（fighter batch） | `yield return fighterBatch.Current;` | ✅ |
| `ModeHRuntimeModule_CombatFlow.cs:622-626`（relay batch） | `yield return batch.Current;` | ✅ |
| `ModeHRuntimeModule_SceneFlow.cs:388`（认证） | `yield return inner.Current;`（带整段说明注释） | ✅ |
| `ModeHCommandCertificationProbe.cs:65,83,97` + `ModeHProductionCertification.cs:401` | `yield return group.Current;` / `yield return probe.Current;` | ✅ |

另核对 `catch` 体内无 `yield return`（CS1631，`IteratorYieldInCatchGuard` 守的那条）：
`ModeHProductionCertification.CertifyKey:388-412` 用的是 `try { try{...}catch{ failure=...; break; } ... } finally {...}`，
`yield return` 在内层 catch **之外**，合规；`ModeHCommandCertificationProbe.ProbeGroup:146-182`
是 `try { ... yield return null ... } finally { ... }`，没有 catch 子句，合规。

### 3.6 事件订阅生命周期（AGENTS §4.6）—— 干净

`ModeHEventRouter` 是 Mode H 唯一订阅官方静态事件的地方：
`Bind` 幂等（同 owner 重复调用只换 sink，换 owner 直接拒绝 `router_owner_conflict`）；
订阅失败走 `TryUnsubscribe()` 回滚，保证「要么全订阅要么全未订阅」；
`Unbind` 幂等且清空全部注册表；`ResetStaticCaches` 调 `Unbind`，并已登记进
`ModeHRuntimeModule.ResetModeHStaticCaches`（连同本轮新增的 `ModeHCombatTelemetry.ResetStaticCaches`）。
事件门 `IsContextValid` 做 owner token + run owner active + runId + sceneGeneration + matchIndex 五重比对。
`ModeHStandInCanMove/CanRunPatch` 是 Harmony 补丁不涉及订阅。

### 3.7 编译清单 —— 全部登记

25 个在审文件逐个在 `compile_official.bat` 中命中（各 1 次，无重复、无遗漏）。
`ModeHCompileManifestGuard.py` 存在并针对 ModeH 另有约束。

### 3.8 其他已核对无问题的点

- `ModeHDeathSuppressionRegistry`：`_activeCount` 只由 preset 登记增减，
  `IsSuppressionArmed` 的零分配快路径成立；三张 HashSet 全部存 `GetInstanceID()`（int），
  不持有 Unity 引用，无跨局对象泄漏；`ResetStaticCaches` 已登记。
- `ModeHCombatTelemetry` 的结果 CAS 用 `Interlocked.CompareExchange`，
  已有结果后一律拒绝二次结算；`TryClaimResult` 是唯一写 `_result` 的地方。
- `ModeHStandInPerformer`：只写 `SetMoveInput` / `SetRunInput`，不碰瞄准/攻击/技能/inventory；
  目标点全部是 `_elapsedSeconds`/`_phase` 的确定性函数，无全局随机、无场景扫描；
  官方 `Movement.SetMoveInput`（`Movement.cs:250`）把 `moveInput` 存为**持久字段**，
  所以 0.5 秒重设一次目标能产生连续移动而不是单帧抽动 —— 设计成立。
- `ModeHEncounterPlanner` 全类纯计算，无角色创建、无玩家资产访问；
  `TryAssignBatches` 的批次收缩循环在每轮末尾检查空表，不会越界；
  `TryRepairSwap` 的步数上界 `available.Count * count + 8` 有限，不会死循环。
- `ModeHArenaIsolationLease.ClearNativeEnemies` 的 fail-closed 已到位：
  `player == null` 时直接拒绝隔离（而不是继续销毁），
  `ShouldPreserveNativeCharacter` 保留玩家 / `Teams.player` / PetNest 随从 / `INPCController` /
  一切非 `Team.IsEnemy(player, x)` 的角色，异常时保留。`FindObjectsOfType` 只在取租约时跑一次，
  不在 Update 路径。
- `ModeHControlPointHarness` 全部三个入口零调用点是**预期状态**：
  它由编译期常量 `ModeHAvailability.AllowDevControlPointHarness = false` 门控，
  文件头已写明「发布构建恒为 false / 不构成向 owner 交付样机」。不计为缺陷。
- 未发现 Update/协程里的 `FindObjectsOfType`、LINQ、每帧日志或每帧闭包分配
  （唯一的每帧分配是 F-B 的字符串拼接）。
- 未发现 `string.Format` 参数错配、遍历中改集合（`RefreshBattleSnapshotContext` 与
  `ModeHInjuryAndScarSystem.Tick`/`RestoreAll` 都是倒序 `RemoveAt`）、除零
  （`ReadHealthFraction` 对 `max <= 0f` 有守卫，`ComputeEffectiveThreat` 用 `long` 防溢出）。

---

## 四、对前几轮已 Fixed 条目的复核

> 结论先说：**9 条中 6 条修复正确且无回归；2 条修复正确但归因说明或实际收益与台账描述不符；
> 1 条留下了清理残留。没有发现被"修复"改坏的地方。**

### CR-2026-09-03-002（濒退制「休息一场解除带伤」）—— ✅ 修复正确，接线完整

- `ModeHInjuryAndScarSystem.ResolveRestRecovery`（`:404-412`）清 `injuryId` + 复位 `Available`；
- `ModeHRuntimeModule_CombatFlow.ResolveRestRecovery`（`:936-943`）以
  `_combatTelemetry.HasRested(profileId)` 为判据，starter / relay 两席各调一次（`:793-794`），
  顺序在两次 `ResolveDownInjury` 之后、`ApplyRetirement` 之前 —— 与台账描述一致；
- `_restedProfileIds` 确实只存运行时（声明在 `ModeHRuntimeModule_SceneFlow.cs:106`，不进任何 DTO），
  **且有消费者**：`ModeHRuntimeModule_SettlementFlow.cs:48-50` 在结算页逐条展示。
  台账说「结算页消费两个文案 key」，实测成立。

### CR-2026-09-03-004（ERROR 互换击杀渗入图鉴与日报）—— ✅ 修复正确

`ModBehaviour.IsModeHRunInProgressSafe()` 门控在
`Integration/Codex/CodexKillCollector.cs:93,165`（OnGlobalDead / OnGlobalHurt）与
`Integration/DailyReport/DailyReportStatsCollector.cs:224`（`IsActive` 总闸）各就位；
`CodexTuning.ModeIdModeH` 常量与 `:287` 的 `FormatModeName` 分支按台账保留（存档兼容面）。
与 PetNest / RandomEvents 用的是同一个门面，形态一致。

### CR-2026-09-03-012（伤病/战痕/公开异常三层内容整体不生效）—— ✅ 主体修复正确；遗留项仍然成立且已恶化一点

- 探针确实依次驱动 Commands → Injuries → Scars（`ModeHCommandCertificationProbe.Run:59-100`，
  三段共用 `ProbeGroup`）✅
- 注册表新增条目级 `GetBehaviorEntryStatus`，伤病/战痕不设 `PartiallyVerified`
  （任一分量非 Verified 即整条 `ReportOnly`，`:432-461`）✅
- `BuildCommandStatuses` 同时落盘口令与行为条目（`:690-709`），
  `RestoreCertificationEffects` 用 `GetBehaviorEffectIds` 做条目级查询（`:305`）—— 缓存往返闭合 ✅
- 四个公开异常已进 `CommandCompatibility.json` 的 `selfSettledEffects`
  （`blood` / `crowd` / `error` / `strong`，加 `steady.coward_mitigation`）✅
- `BuildBehaviorSnapshot` 有两个填充点：`ModeHDraftController.cs:122`（抽签）与
  `ModeHTransferMarket.cs:356`（转会签入）—— 与台账描述一致 ✅
- **遗留仍然成立**：`blood_rush` 的 `blood_rush.searchedEnemy`（`op = fire_lowest_health_target`）
  在 `ReadField` 里读不到 → 恒 `ReportOnly` → `blood_rush` 对任何 key 都不可用，战痕池实际 7/8。
  **新增的负作用**：CR-2026-09-03-019 为 `blood_rush` 补的每帧触发调用
  （`ModeHCombatControl.cs:507-511`）因此是**死代码**，而且它每帧走到
  `TryOpenScarWindow` 的 `:318-322` 拼一个 `"scar_not_verified:blood_rush"` 字符串再丢掉
  —— 属 F-B 的一部分。

### CR-2026-09-03-013（ERROR 完整互换从未被调用 + 租约不让渡输入）—— ✅ 修复正确

- 唯一生产调用点 `TryBeginErrorSwapIfDue` 在 `TickActiveCombat:512` 每帧轮询，
  排在终局分支之后（「终局已锁定后不再开新互换」），注释与位置一致 ✅
- `_errorSwapAttempted` 闩在**引用检查之前**置位（`ModeHCombatControl.cs:578`），
  正是台账说的「没有它会每帧重入」✅
- `YieldInputForErrorSwap` / `ReclaimInputAfterErrorSwap` 只动本租约 token、
  `_inputDisabled` 保持 true，Release 分支不变 ✅；
  由 `SyncErrorSwapInputYield`（状态翻转才调 InputManager，O(1) 零分配）驱动，
  **且 `ReleaseCombatRuntimeObjects:1068-1073` 补了兜底收回** —— 结算/技术中止路径不会再进
  `TickActiveCombat`，这条兜底是必要的，实现正确。
- 台账里「`RestoreFromSnapshot` 补 §17.6.5 第 8 条的重建」这一半已被 CR-2026-09-03-018 整体删除，
  `ModeHCombatControl.cs:865-873` 留有说明注释，两条 finding 的最终状态自洽 ✅

### CR-2026-09-03-014（`ApplyRetirement` 零调用点）—— ✅ 修复正确

`ModeHRuntimeModule_CombatFlow.cs:806` 调用，位置在两次 `ResolveDownInjury` +
两次 `ResolveRestRecovery` 之后、虚拟筹码结算之前，与台账的排序理由完全一致；
`false` 返回只 DevLog、不跳状态机（注释写明了理由），无回归。
台账自陈的「替补顶上夺冠时 `substituteHistory` 为空」遗留仍在
（`BuildHallOfFameRecord:1026-1034` 有对应注释），未扩大。

### CR-2026-09-03-017（口令点火目标无生产者）—— ⚠️ 代码修复正确，但**归因说明与实际收益都与台账不符**

**修复本身核对通过**：
- `ModeHCombatControl.RefreshFireTargets`（`:908-974`）按遥测存活名单计算最近/最残，
  参照点是当前登场选手 ✅；`ModeHCombatTelemetry.GetLiveEnemyAt`（零分配按下标访问）✅；
  生命归零的不计入「最残」✅；按 `CommandReassertIntervalSeconds` 节流、拍铃走
  `RefreshFireContext(0f, true)` 强制重扫 ✅；`BeginMatch` 清空两个目标 ✅；
  `SetFireTargets` 已删除（全仓只剩注释里提到它）✅；
  战痕开窗改用 `_sharedFireContext` ✅（`ModeHInjuryAndScarSystem.cs:51,207`）。
- `tests/ModeHCommandFireTargetGuard.py` 存在 ✅。

**两处与台账不符（应回填台账）**：
1. 台账的「为什么三层检查全绿还是漏了」写的是「`Validate()` 判据宽松 →
   认证把 `finish` 标成 `VerifiedBehavior` 正常发给玩家选」。
   **实测不成立**：探针在 `ApplyEffects` 之前就用 `ReadField == null` 把点火型分量滤掉了
   （`ModeHCommandCertificationProbe.cs:133-140`，`git diff` 确认该过滤在本轮之前已存在），
   `Validate()` 的宽松判据根本没被用到；`finish` 的两条 effect 恒为 `ReportOnly`。
2. 因此 **`finish` 从来就不在玩家可选列表里**（见 F-0），
   这条修复对 `finish` 是空转；实际受益的只有 `press` 的 `fire_notice_nearest`
   （`press` 靠另外 3 条可测分量拿到 `PartiallyVerified` 才可选）。
   `Scars.json` 里带 `fire_lowest_health_target` 的 `blood_rush` 同样不可用（见上条）。

### CR-2026-09-03-018（战场快照重建侧删除）—— ✅ 删除本身正确；⚠️ 三处清理残留

删除已核对完整：`ModeHBattleSnapshot.cs` 无 `ModeHSnapshotRebuildPlan` / `Validate` /
`IsPositionUsable` / `TryRestoreHealth`；`ModeHCombatControl` / `ModeHCommandController` /
`ModeHCombatTelemetry` 三处 `RestoreFromSnapshot` 均已移除并留有说明注释；
`_errorSwapRebuildProfileId` 已消失，`tests/ModeHStandInGuard.py:242-250` 与
`tests/ModeHBattleSnapshotGuard.py:48-53,131-138` 的断言方向确已反转为「必须保持缺席」✅

**残留三处**：
1. `ModeHInjuryAndScarSystem.RestoreScarWindows`（`:352`）**没删也没注释**，零调用点
   —— 它的唯一潜在调用者正是被删掉的 `ModeHCombatControl.RestoreFromSnapshot`（见 F-D）。
   `REBUILD_ABSENT_SYMBOLS` 只扫 `ModeHBattleSnapshot.cs`，覆盖不到它。
2. `ModeHBattleSnapshot.cs:256` 点名的守卫 **`ModeHSnapshotRebuildAbsenceGuard` 不存在**
   （`tests/` 下无此文件）。实际断言在 `tests/ModeHBattleSnapshotGuard.py` 里，
   注释指了一个不存在的文件名。（主会话审核 §附录 A.5 已独立发现同一点，此处交叉确认。）
3. `ModeHBattleSnapshot.cs:242-248` 与 `ModeHBattleSnapshotGuard.py:131-138` 的错误文本
   都断言「`Recovering` 没有任何一条出边通向战斗态 → 结构性不可达」。
   主会话已复核该结论**错误**（`Recovering → ErrorRecoveryPending → RelayPending → MatchFighting`
   三跳合法可达）。删除结论不受影响，但这段固化在源码与守卫里的理由会误导后来者。
   已由主会话记入台账，此处不重复立条。

### CR-2026-09-03-019（八条战痕只有两条能生效）—— ⚠️ 代码修复正确，但净效果只从 5 条提升到 6 条，不是 8 条

逐项核对：

| 战痕 | 修复动作 | 现状 |
| --- | --- | --- |
| `broken_shield_charge` | triggerId 改为 `armor_first_break` + 护甲耐久判据 | ✅ 逐字匹配；❌ **事实源不成立**（见 F-A：缺 `UseDurability` 守卫 + 官方扣耐久受 `IsRaidMap` 门控） |
| `blood_rush` | 新增 `enemy_first_low_health` 触发 + 复用点火扫描 | ✅ 逐字匹配；❌ 该战痕**永远开不出来**（`IsEntryUsableForKey` 恒 false，属 -012 的已知遗留），触发调用是死代码 |
| `longshot_memory` | 新增 `first_ranged_damage_taken` + 遥测 `ActiveFighterTookRangedDamage` + `fromWeaponItemID` 透传 | ✅ **真正修好了**（接口签名、路由透传、Gun/Melee tag 判定、缓存全部到位） |
| `crowd_favorite` | 删除多余触发调用（它是常驻） | ✅ `EvaluateTriggeredInjuries` 里确已无该调用 |
| `ApplySelfSettledComponents` | 同时识别 `self_settled_command_scale` 与 `self_settled`+`command_scale` | ✅ `:248-259`；`gatedByCondition` 跳过 `spirit` 防双倍施加 ✅ |

`tests/ModeHScarTriggerWiringGuard.py` 存在 ✅。
**净结果**：8 条里能生效的是 6 条（3 常驻 + `bell_dependence` + `relay_expert` + `longshot_memory`），
不是台账隐含的 8 条。建议把这一行回填进台账遗留。

### CR-2026-09-03-020（`appliesWhen` 条件层未求值）—— 状态已变：owner 已拍板，实施**正在本轮并发进行中**

台账状态是「Accepted — 实现语义需 owner 拍板，本轮未改」。
本次审查期间工作树里已出现完整实现（`ModeHCommandAdapters.ModeHEffectConditions`，
按重申节奏持续求值 = 台账推荐的第二种语义），配套的 4 个条件输入
（`ArenaConditionId` / `ReinforcementPending` / `FirstWaveAlive` / `ActiveFighterIsRelay` + `BellConsumed`）
均已有生产者。**本报告未审查这批未落定的代码**（见 U-3），台账状态需要在它落定后更新。

### CR-2026-09-02-005 / 011（认证拒绝已归一化的克隆 / 诊断伤害空来源）—— ✅ 修复正确

- `PassesStaticAudit` 已不再检查 `canDieIfNotRaidMap`（`:581-582` 有说明注释），
  改由 `TryControlledKill:465` 检查**实际生成角色**的 `handle.Health.CanDieIfNotRaidMap` ✅
- 伤害来源用对侧诊断 clone，显式拒绝 null / 自身 / 主玩家（`:458`）✅
- 走完整 `DamageInfo` 的 `Hurt`，断言 `IsDead && observedHurt && observedDeath` 三者齐全 ✅
- 临时监听在 `finally` 无条件退订（`:514-518`）✅，异常输出 stableKey/team/已观察事件/完整栈 ✅

### CR-2026-09-02-013（口令矩阵无人写入 / 缓存不恢复逐效果证据）—— ✅ 修复正确

- `RecordEffectStatus` 有生产调用点（探针 `:131` 写 `ReportOnly`、`:215` 升 `VerifiedBehavior`）✅
- **绑定三签名在测量之前**（`ModeHProductionCertification.Run:228-230`，注释点明了顺序理由）✅
- `RestoreCertificationEffects` 只接受 `Passed` 记录中已知 effectId 且状态在合法区间的条目，
  口令级状态由 `GetCommandStatus` 重新派生，不能靠缓存整体 Passed 绕过 ✅
- 采样是「先读后重申」（`ProbeGroup:167-169` 的 `Validate()` 在 `Tick()` 之前），
  跨 ≥3 帧、累计 ≥0.3 秒，双角色取交集，还原后逐分量回读核对 ✅
- **新增的正确细节**：`ProbeGroup:196-201` 把保持性投影到 `controlPointId`，
  解决了 `relay_expert` 两条分量写同一控制点时「自己盖自己」的问题 —— 逻辑正确。

### CR-2026-09-02-016（冻结弹药写入装备槽）—— ✅ 修复正确

- `TryApplyAmmo` 不再用 `TryPlug`，改为同步填新造枪弹匣 + 临时选手库存，
  按 `MaxStackCount` 分堆、严格保留冻结总量（`loaded = min(AmmoCount, gun.Capacity)`，
  余量进角色 Inventory）✅
- 逐实例登记进 `application.CreatedItems`，不合并进旧堆，任何失败逆序 `Recycle` ✅
- `_bulletCountCache` 反射置 -1 后用公开 `GetBulletCount()` / `BulletCount` 双重回读核对
  （`:255-260`），**绑定缺失时显式拒绝**（`:233-234`）而不是静默 ✅
- 新造枪的默认弹药先清空（`:237-242`），保证总量精确 ✅
- 反射目标 `ItemSetting_Gun._bulletCountCache` 经官方源码确认存在（`:512`）✅

---

## 五、验证缺口汇总（供 owner / 后续实机 smoke）

1. **`LevelConfig.isRaidMap` 在 Mode H 擂台的实际取值**（决定 F-A 走哪条支路）。
2. 开一局到选令页，确认「终结 / finish」不在列表里（F-0）。
3. 让选手带 `armor` 伤病上场，确认 Armor 槽是否仍有装备（F-M）。
4. 穿甲选手挨打后 `broken_shield_charge` 是否开窗；穿无耐久护甲的选手是否开局即开窗（F-A）。
5. Unity Profiler 看擂台战斗期的 GC Alloc（F-B）。
6. 把 `CertificationPerKeyTimeoutSeconds` 调小复现认证超时，确认是否留下隐藏角色（F-I）。
7. 8 件 reward kit 的 typeId 与当前官方版本一致性（F-P / U-2）。
8. **本轮未做**：编译与 guard（按只读约束，未跑 `compile_official.bat` 与任何 guard 脚本）。

---

## 六、并发写入声明

本次审查期间（2026-09-04 09:10–09:45）另一会话持续在改 `ModeH/`。
以下文件在我读过之后又发生了变化，本报告中涉及它们的 finding 已在 09:40 用当前工作树**逐条复验**：

| 文件 | 我读取时 | 09:40 复测 |
| --- | --- | --- |
| `ModeHCommandAdapters.cs` | 587 行 | 762 行（新增 `ModeHEffectConditions`） |
| `ModeHCombatControl.cs` | 1006 行 | 1032 行 |
| `ModeHRuntimeModule_CombatFlow.cs` | 1157 行 | 1180 行 |
| `ModeHCombatTelemetry.cs` | 515 行 | 529 行 |
| `ModeHInjuryAndScarSystem.cs` | 603 行 | 614 行 |
| `ModeHEventRouter.cs` | 397 行 | 402 行 |

其余 19 个在审文件在整个审查期间行数未变。
新出现的未跟踪文件 `ModeH/ModeHWarehouseStakeJournalStorageBuffer.cs` 不在本报告范围
（属押品/持久化，归 `ModeH_flow_persistence`）。
本报告引用的行号以 09:40 的工作树为准；F-0 / F-C / F-G / F-H / F-I / F-J / F-K / F-N / F-O / F-P
所在文件全程未变动。

---

## ==== Integration_core_ModBehaviour_Config_L10n ====

# 集成总线核心 + ModBehaviour + 配置层 + 本地化注入 审查报告

> 审查范围：自 commit `f9b83c0` 起（含未提交工作树改动）的
> `ModBehaviour.cs` / `Integration/` 总线核心 / 既有 Boss / NPC / 婚姻 / 许愿台 / 快递 /
> `Config/` / `Localization/` + 三项全仓横切核对（ModConfig 白名单、本地化 key、TypeID 台账）。
> 全程只读审查，未修改仓库任何文件。所有结论均为**静态确认，未运行验证**（无 Windows 编译、无实机）。

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

**统计：P0 = 0，P1 = 1，P2 = 3，P3 = 4，共 8 条。**

按严重级排序的索引（正文按发现顺序编号，F-n 编号稳定，便于交叉引用）：

| 顺位 | 编号 | 严重级 | 一句话 |
| --- | --- | --- | --- |
| 1 | **F-4** | **P1 Major** | `BossRush_RandomEvent_MerchantShop` 全仓零注入——随机事件商人的交互提示显示原始 key |
| 2 | **F-3** | **P2 Minor** | 本轮 9 个新 TypeID（500059-500067）全部漏进掉落黑名单，会被许愿台/日报当随机奖励发出（其中「无血脉遗种蛋」是永远孵不出的死物） |
| 3 | **F-7** | **P2 Minor** | 全 Mod 本地化在启动时冻结语言，游戏内切语言后约 1150 条 `BossRush_*` 文本停在旧语言 |
| 4 | **F-1** | **P2 Minor** | `ModeFItemConfigHelper` 模板化把 4 个物品从 `LocalizationInjectionGuard` 覆盖面里摘掉（运行时无影响，是回归防护的损失） |
| 5 | **F-2** | P3 Note | `docs/Bossrush使用物品ID表.md` 台账落后两档（仍写 500001-500061、缺 6 行明细），且这份文件不在任何 guard 核对面内 |
| 6 | **F-5** | P3 Note | `TryInjectCodexBookIntoShop` 没有开关闸，与本轮新加的注释说法相反（当前不可达） |
| 7 | **F-6** | P3 Note | 只修了便携安全区的「双份文案」，同函数里另两件丧尸物品仍是双份且两份内容不同 |
| 8 | **F-8** | P3 Note | `WishFountainInteractable` 是全仓唯一没做 `otherInterablesInGroup` 预初始化的运行时交互体，且 `base.Awake()` 异常被裸 `catch {}` 吞掉 |

**范围内没有发现 P0 Blocker。**

### F-1：`ModeFItemConfigHelper` 模板化把 4 个物品从 `LocalizationInjectionGuard` 覆盖面里摘了出去

- **严重级**：P2 Minor
- **兼容分类**：`SAFE`（运行时行为不变）+ `OPERATIONAL`（guard 覆盖面收缩）
- **位置**：
  - `D:\code\ykf\BossRushMod\Integration\Items\ModeFItemConfigHelper.cs:34`
  - `D:\code\ykf\BossRushMod\Integration\Items\BarbedWirePackConfig.cs:17`
  - `D:\code\ykf\BossRushMod\Integration\Items\EmergencyRepairSprayConfig.cs:17`
  - `D:\code\ykf\BossRushMod\Integration\Items\FoldableCoverPackConfig.cs:17`
  - `D:\code\ykf\BossRushMod\Integration\Items\ReinforcedRoadblockPackConfig.cs:17`
- **证据**：
  - `tests/LocalizationInjectionGuard.py` 用 `RE_ASSIGN_IDENT = \.DisplayNameRaw\s*=\s*([A-Za-z_]\w*)\s*;` 抓赋值，
    再用**同文件**的 `const string` 把标识符解析回字面量；解析不出来的只打 `[WARN]` 并跳过核对。
  - `git show f9b83c0:Integration/Items/BarbedWirePackConfig.cs` 等四个文件在改动前都是
    `item.DisplayNameRaw = LOC_KEY_DISPLAY;`，与同文件 `public const string LOC_KEY_DISPLAY = "BossRush_BarbedWirePack";`
    在一起，guard 能解析 → 四个 key 都被核对。
  - 模板化后这四处赋值全部消失，唯一赋值点变成
    `ModeFItemConfigHelper.cs:34  item.DisplayNameRaw = locKeyDisplay;`（**方法参数**，无同文件 const）。
  - 实跑 `python tests/LocalizationInjectionGuard.py`（只读，不写文件）输出：
    `[WARN] Integration/Items/ModeFItemConfigHelper.cs:34 DisplayNameRaw = locKeyDisplay`，
    `已核对 BossRush_* 显示名 key: 18 个`。改动前同一批文件应为 22 个。
- **失败场景**：将来有人删掉/写错 `InjectModeFItemLocalization()` 里那五行
  `InjectModeFItemLoc(XxxConfig.LOC_KEY_DISPLAY, ...)`（`Integration/BossRushIntegration.cs:503-543`），
  guard 仍然 PASS，游戏内四件 Mode F 工事包直接显示 `*BossRush_BarbedWirePack*`。
  这正是 4.4 这条 golden rule 想拦的失败形态，而它现在拦不住这四件。
- **玩家影响**：当前**无**——`Integration/BossRushIntegration.cs:503` 的
  `InjectModeFItemLocalization()` 仍逐个注入这五个 key（已 grep 确认接线在
  `Integration/BossRushIntegration_StartAndScene.cs:60`）。这是**回归防护**的损失，不是现网 bug。
- **建议修复**（二选一，均不改运行时行为）：
  - a) 在四个 Config 里保留 `item.DisplayNameRaw = LOC_KEY_DISPLAY;` 语义的可解析形态，
    例如让 `ConfigureSimpleConsumable` 返回后各 Config 再显式 assert/引用一次；或
  - b) 给 `LocalizationInjectionGuard.py` 增加「调用点实参解析」：把
    `ModeFItemConfigHelper.ConfigureSimpleConsumable(item, tag, TYPE_ID, LOC_KEY_DISPLAY, ...)`
    的第 4 个实参当作 DisplayNameRaw 来源。b) 更符合「不为过 guard 改产品代码」。
- **验证需求**：改后重跑 `python tests/LocalizationInjectionGuard.py`，
  「已核对 key 数」应回到 22 且 `ModeFItemConfigHelper` 的 WARN 消失。

### F-2：`docs/Bossrush使用物品ID表.md` 台账落后两档，且这份文件不在任何 guard 的核对面内

- **严重级**：P3 Note
- **兼容分类**：`SAFE`（纯文档）
- **位置**：
  - `D:\code\ykf\BossRushMod\docs\Bossrush使用物品ID表.md:3` — `> 所有已发布自定义 TypeID 均在 500001-500061 范围内。`
  - 同文件明细表最后一行停在 `:135`（`500061 鸭皇图鉴`），**500062-500067 六个 ID 没有明细行**，
    只在 `:192-193` 的数字段落摘要里出现过一次。
- **证据**：
  - `grep -n "50006[0-9]" docs/Bossrush使用物品ID表.md` 仅命中 134/135（500060、500061 明细）与 190-193（摘要）。
  - `Integration/BackMountain/BackMountainItems.cs:33-48` 实际定义了 `DragonSeed=500062` … `PhantomMushroom=500067`。
  - `docs/contracts.md` §1 与 `AGENTS.md` §4.3 都已写到 `500001-500067`，**只有这份 ID 表还写 500061**。
  - `tests/TypeIdLedgerGuard.py:22-24` 的事实源是 `docs/contracts.md` + `AGENTS.md`，
    **完全不读 `docs/Bossrush使用物品ID表.md`**，所以这份漂移不会被守卫发现。
    而 `AGENTS.md:85` 又写「下一可用：500068，**以 `docs/Bossrush使用物品ID表.md` 实际末尾为准**」——
    被指定为权威的那份文件反而是唯一没被守卫的一份。
- **失败场景**：下一个加物品的人按 AGENTS.md 的指示去读 ID 表，看到「均在 500001-500061 范围内」
  与只到 500061 的明细表，判断下一可用是 500062，于是**复用** 500062-500067。
  TypeID 复用是 `BREAKING`（AGENTS.md §4.3 / contracts §1），后果是老档里的种子/出击餐变成新物品。
  `TypeIdLedgerGuard` 不会拦（500062 在 contracts 已登记范围内，属合法字面量）。
- **玩家影响**：当前无。风险在下一次加物品时兑现。
- **建议修复**：补 500062-500067 六行明细并把第 3 行范围改成 500001-500067；
  或给 `TypeIdLedgerGuard.py` 增加对 ID 表首行范围与明细行的三方一致性核对。
- **验证需求**：静态确认，未运行验证。

### F-3：本轮 9 个新 TypeID（500059-500067）全部漏进掉落黑名单 → 会被星愿许愿台与日报签到当作随机奖励发出去

- **严重级**：P2 Minor（其中「无血脉遗种蛋」一项接近 P1：玩家会拿到一件**永远孵不出来的死物**）
- **兼容分类**：`COMPAT`（补黑名单不破坏存档，只影响未来 roll 结果）
- **位置**：
  - `D:\code\ykf\BossRushMod\Config\LootBlacklistRegistry.cs:117-135`（硬编码兜底表）
  - `D:\code\ykf\BossRushMod\Assets\Data\LootBlacklist.json`（JSON 正表，末行 `500016, 500045, 500046, 500058`）
  - 消费方 1：`D:\code\ykf\BossRushMod\Integration\WishFountain\WishFountainRewardPoolBuild.cs:379-424`
  - 消费方 2：`D:\code\ykf\BossRushMod\Integration\DailyReport\DailyReportRewards.cs:212-240`
- **证据**：
  1. 黑名单正表与兜底表**同时**只登记到 `500058`；`500059`（遗种蛋）、`500060`（词缀熔石）、
     `500061`（鸭皇图鉴）、`500062-500067`（后山种子与出击餐）**一个都不在**。
     对照：同类「非随机掉落」的 `500045`/`500046`/`500058`（尸潮邀请函/信标/便携安全区）都在表里。
  2. 九件新物品**全部**带 `Special` tag、品质 4 或 5：
     - `Integration/Items/RelicEggConfig.cs:50` `QUALITY = 5`、`:96` `AddTagToItem(item, "Special")`
     - `Integration/AffixForge/AffixForgeStoneConfig.cs:59` `QUALITY = 5`、`:105` `Special`
     - `Integration/Codex/CodexBookItem.cs:56` `QUALITY = 5`、同文件 `Special`
     - `Integration/BackMountain/BackMountainItems.cs:86-130` 种子 quality 4 / 出击餐 quality 5、`:210` `Special`
  3. 许愿台基础奖池 `EnumerateWishRewardBasePoolCandidateIds`（`:379`）遍历 `tagsData.AllTags`，
     逐个当 `requireTag` 搜索，`minQuality = WISH_REWARD_MIN_QUALITY`（`WishFountainService.cs:123` = **4**）、
     `maxQuality = 8`，唯一的剔除手段就是 `LootBlacklistRegistry.Contains(id)`（`:413`）。
     `BuildWishRewardExcludeTags`（`:826-830`）排除的是 `Character` / `DestroyOnLootBox` /
     `DontDropOnDeadInSlot` / `LockInDemoTag` / `Quest` —— **不含 `Special`**。
  4. 日报签到奖励 `BuildCandidates`（`DailyReportRewards.cs:212`）更宽：
     `filter.requireTags = null`，只卡品质区间，然后 `if (LootBlacklistRegistry.Contains(id)) continue;`。
     连 tag 这一层都没有。
  5. 官方 `ItemAssetsCollection.GetAllTypeIds`（`鸭科夫源码/ItemStatsSystem/ItemAssetsCollection.cs:235-257`）
     **显式把 `dynamicDic` 并进结果集**，所以 Mod 用 `AddDynamicEntry` 注册的物品一定会被 `Search` 搜到——
     不能指望「自定义物品不在官方表里所以搜不到」。
- **失败场景**（按影响从重到轻）：
  1. **无血脉遗种蛋**：许愿台/日报发出的 500059 是从 prefab 直接实例化的，
     没有 `PetNest_Lineage` KV（只有 `RelicEggConfig.TryStampLineage` 在 Boss 掉落路径上写，
     `Integration/Items/RelicEggConfig.cs:113`）。
     `PetNestHatchService.cs:93-99` 读不到血脉即 `failureReasonId = "lineage_unknown"` 并**保留蛋**，
     `PetNestUIPages.cs:356-366` 把该蛋渲染成「血脉未知」且**不给孵化按钮**。
     结果：玩家背包里多一枚占 MaxStack=1 格子、永远孵不出来、也没有任何提示说明为什么的蛋。
  2. **后山种子/出击餐绕过战役解锁**：500062-500067 是「由战役章节 token 解锁的后山内容」，
     现在没打战役也能从许愿台/日报白拿。
  3. 词缀熔石、鸭皇图鉴变成额外的免费产出渠道，绕开设计好的哥布林商店/Boss 掉落/基地商店。
- **另有文档不一致（同一处）**：`AGENTS.md` 第 14 节 2026-09-01 条目写
  「注意熔石带 `Special` tag，**不会**进星愿许愿台奖池（该池按 tag 排除 Special）」。
  与代码相反：许愿台**不排除** `Special`，反而在 `WishFountainRewardPoolBuild.cs:616-617`
  把 `Special` 当作 `gift` / `healing` 两个类别的 **requireTag**。
  按这条错误结论做的判断（「熔石不用进黑名单」）正是本 finding 的成因。
- **玩家影响**：许愿台/日报奖励池被 9 件不该出现的物品稀释；其中遗种蛋是纯负面（死物 + 占格）。
- **建议修复**：把 `500059`-`500067` 九个 ID 同时补进
  `Assets/Data/LootBlacklist.json` 与 `Config/LootBlacklistRegistry.cs:CreateFallbackBlacklistIds()`
  （两处必须一致，否则 JSON 失效时行为漂移）；并修正 `AGENTS.md` 第 14 节那句关于 Special 的描述。
  若产品上确实希望某几件保留在奖池（例如熔石），需 owner 明确拍板，并单独处理遗种蛋的血脉问题。
- **验证需求**：静态确认，未运行验证。实机复验方式：基地反复许愿 / 连续签到，观察是否 roll 出
  「血脉未知」的遗种蛋或后山种子。

### F-4：`BossRush_RandomEvent_MerchantShop` 全仓零注入——随机事件商人的交互提示会显示原始 key

- **严重级**：P1 Major（按 `CODE_REVIEW.md` §3 的分级表，「本地化缺失」明确列在 P1；
  实际影响面是随机事件商人这一个交互提示，不阻断主流程）
- **兼容分类**：`COMPAT`（补一条注入即可，不动任何已发布 key）
- **位置**：`D:\code\ykf\BossRushMod\RandomEvents\RandomEventEffectsBridge_Spawn.cs:571`
  ```csharp
  interact.Setup(shop, RandomEventsTuning.LocalizationPrefix + "MerchantShop");
  ```
  `Setup` 把它直接写进官方交互名 override（同文件 `:834-844`）：
  ```csharp
  this.overrideInteractName = true;
  this._overrideInteractNameKey = displayNameKey;
  ```
- **证据**：
  - `RandomEvents/RandomEventsTuning.cs:26` `LocalizationPrefix = "BossRush_RandomEvent_"`，
    所以实际 key 是 `BossRush_RandomEvent_MerchantShop`。
  - 全仓（排除 `Build/`、`鸭科夫源码/`）`grep -rn "MerchantShop\"" --include="*.cs"` **只有这一处**；
    没有任何 `InjectLocalization` / `map[...]` 写这个 key。
  - `Localization/` 目录下**根本没有 RandomEvents 的本地化文件**
    （只有 AffixForge / BackMountain / Campaign / Codex / DailyReport / ModeH / PetNest 七个），
    `RandomEventsTuning.LocalizationPrefix` 的唯一引用者就是上面那一行。
  - 对照：同类交互键都有注入——`BossRush_AffixForge` 在
    `Localization/AffixForgeLocalization.cs:112`（`map[InteractNameKey] = ...`）、
    `BossRush_DailyReport_Interact` 在 `Localization/DailyReportLocalization.cs`、
    `BossRush_ModeD_NextWave` / `BossRush_ModeG_Preview` 等在
    `Localization/LocalizationInjector_NpcUiAndItems.cs:496-533` 的字典里。
- **失败场景**：局内触发「流动商人」类随机事件后走到商人跟前，交互提示显示
  `*BossRush_RandomEvent_MerchantShop*`（AGENTS.md §4.4 描述的经典星号占位符形态）。
- **玩家影响**：随机事件商人交互提示是原始 key，中英文都一样。
- **建议修复**：给随机事件补一个 `Localization/RandomEventsLocalization.cs`（形态照
  `DailyReportLocalization.cs`），至少注入 `MerchantShop` 一条，并挂进
  `InjectLocalization_Extra_Integration()`；或直接在
  `LocalizationInjector_NpcUiAndItems.cs` 的交互名字典里补一行。
- **验证需求**：静态确认，未运行验证。实机复验：触发随机事件商人，看提示是否带星号。

### F-5：`TryInjectCodexBookIntoShop` 没有开关闸，与本轮新加注释的说法相反

- **严重级**：P3 Note（`codexEnabled` 已被 `ForceContentSystemSwitchesOn` 钉死为 true，
  当前不可达；属于注释与实现不一致 + dormant 契约的潜在破口）
- **兼容分类**：`SAFE`
- **位置**：
  - `D:\code\ykf\BossRushMod\Integration\BossRushIntegration.cs:435-437`（本轮新增）
    ```csharp
    // 鸭皇图鉴：面板的唯一入口就是这本书，漏了这行整个图鉴系统买不到也打不开。
    // 内部自带基地普通商人判定与开关 dormant 早返。
    if (TryInjectCodexBookIntoShop(shop)) injectedCount++;
    ```
  - `D:\code\ykf\BossRushMod\Integration\Codex\CodexBookItem.cs:332-337`
    ```csharp
    internal bool TryInjectCodexBookIntoShop(StockShop shop)
    {
        if (!IsBaseHubNormalMerchantShop(shop)) { return false; }
        ...
    ```
- **证据**：`TryInjectCodexBookIntoShop` 只有 `IsBaseHubNormalMerchantShop` 一道闸，
  **没有** `IsCodexConfiguredEnabled()`。开关 dormant 早返只存在于外层的
  `InjectCodexBookIntoShops`（`CodexBookItem.cs:381-385`）。
  而 `TryInjectAllBossRushItemsIntoShop` 走的是 Harmony 商店 Awake 路径
  （`Patches/BaseHub/BaseHubShopAwakePatch.cs:20`），完全不经过外层方法。
- **失败场景**：若将来把 `codexEnabled` 重新放回 ModConfig UI（`ConfigContentSystemSwitches.cs`
  的注释明确说「将来若要重新放出某个开关，只需恢复它的 Register 调用并登记回白名单」），
  关掉图鉴后商人货架上仍会出现鸭皇图鉴——违反本仓库反复强调的 dormant 契约。
- **玩家影响**：当前无。
- **建议修复**：在 `TryInjectCodexBookIntoShop` 开头补 `if (!IsCodexConfiguredEnabled()) return false;`
  （与 `BloodhuntTransponderConfig` / `FateEchoRelicConfig` 的同类注入一致），
  或把 `BossRushIntegration.cs:436` 那句注释改成事实。
- **验证需求**：静态确认，未运行验证。

### F-6：本轮只修了便携安全区的「双份文案」，同一函数里另外两件丧尸物品仍是双份且**两份内容不同**

- **严重级**：P3 Note（两段文案都读得通，玩家看到的是硬编码那份；问题是同一物品在不同
  查表路径上会给出**两段不同的描述**，且 Config 侧那份是死文案）
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\Localization\LocalizationInjector.cs:568-582`
  ```csharp
  public static void InjectZombieModeLocalization()
  {
      ZombieTideInvitationConfig.InjectLocalization();   // :570
      ZombieTideBeaconConfig.InjectLocalization();       // :571
      PortableSafeZoneDeviceConfig.InjectLocalization(); // :572
      InjectZombieModeString("BossRush_ZombieTideInvitation", "尸潮邀请函", ...);
      InjectZombieModeString("BossRush_ZombieTideInvitation_Desc", "进入末日丧尸模式的入场凭证。撤离失败时不退还。", ...);
      InjectZombieModeString("BossRush_ZombieTideBeacon", "尸潮信标", ...);
      InjectZombieModeString("BossRush_ZombieTideBeacon_Desc", "在准备倒计时阶段使用，立即开始下一波。本局工具，不能带出。", ...);
      InjectZombieModeString("BossRush_PortableSafeZoneDevice", "便携安全区装置", ...);
      // ↓ 本轮把这一条改成引用 Config 常量了
      InjectZombieModeString("BossRush_PortableSafeZoneDevice_Desc",
          PortableSafeZoneDeviceConfig.DESCRIPTION_CN, PortableSafeZoneDeviceConfig.DESCRIPTION_EN);
  ```
- **证据**（Config 侧常量 vs 同 key 的硬编码，**内容不同**）：
  | key | `XxxConfig.DESCRIPTION_CN`（先注入，被覆盖） | `InjectZombieModeString` 硬编码（后注入，生效） |
  | --- | --- | --- |
  | `BossRush_ZombieTideInvitation_Desc` | 进入末日丧尸模式的邀请函。**确认选择地图前不会消耗。** | 进入末日丧尸模式的入场凭证。**撤离失败时不退还。** |
  | `BossRush_ZombieTideBeacon_Desc` | 末日丧尸模式专用信标，**可反复使用**。 | **在准备倒计时阶段使用，立即开始下一波。本局工具，不能带出。** |
  - 两个 Config 的 `InjectLocalization()` 同时注入了 `Item_<TypeID>_Desc`
    （`ZombieTideInvitationConfig.cs:163`、`ZombieTideBeaconConfig.cs:170`），
    而 `InjectZombieModeString` **只覆盖 `BossRush_*` 那一条**。
    于是同一件物品：走 `DisplayNameRaw`（= `BossRush_ZombieTideInvitation`）的界面显示硬编码文案，
    走官方 `Item_500045_Desc` 的界面显示 Config 文案——**两段不同的描述**。
  - 显示名同理：`InjectZombieModeString("BossRush_ZombieTideInvitation", "尸潮邀请函", ...)`
    仍是字面量，只是当前恰好与 `DISPLAY_NAME_CN/EN` 一字不差，属于「没踩到的同一个坑」。
- **失败场景**：改任一 `XxxConfig.DESCRIPTION_*` 后游戏内物品描述纹丝不动（被后注入的硬编码盖掉），
  但另一处界面又变了——排查成本高，正是本轮给便携安全区加注释想避免的情形。
- **玩家影响**：同一物品在不同界面描述不一致（现网即存在，非本轮引入）。
- **建议修复**：把这两条（以及三条显示名）一并改成引用各自 Config 常量，
  与本轮 `BossRush_PortableSafeZoneDevice_Desc` 的写法一致；
  或者删掉重复的 `InjectZombieModeString`，只留 Config 侧注入。
  **需 owner / 丧尸模式负责人确认哪一段文案才是当前真实行为**（邀请函是否「撤离失败不退还」、
  信标是否「可反复使用」），再决定保留哪一份。
- **验证需求**：静态确认，未运行验证。

### F-7：全 Mod 本地化在启动时一次性冻结语言，游戏内切换语言后所有 `BossRush_*` 文本停留在旧语言

- **严重级**：P2 Minor（既有架构问题，非本轮引入；但本轮新增 7 个本地化文件后受影响面
  从数百条扩大到约 1150 条 key，属于「问题被放大」）
- **兼容分类**：`SAFE`（诊断；修起来是 `COMPAT`）
- **位置**：
  - `D:\code\ykf\BossRushMod\Localization\LocalizationHelper.cs:27-39`
    ```csharp
    public static bool InjectLocalization(string key, string value)
    { ... LocalizationManager.SetOverrideText(key, value); ... }
    ```
    —— 一个 key 只存**一个**字符串，不分语言。
  - 各注入点统一写成 `L10n.T(cn, en)`（例：`Localization/PetNestLocalization.cs:41`
    `map[PetNestTuning.LocalizationPrefix + suffix] = L10n.T(cn, en);`），
    `L10n.T` 依赖 `L10n.IsChinese` → `LocalizationManager.CurrentLanguage`（`Localization/L10n.cs:46-53`），
    **在注入的那一刻**求值。
  - `ModBehaviour.InjectLocalization()`（`ModBehaviour.cs:859-862`）全仓**只有一个调用点**：
    `Integration/BossRushIntegration_StartAndScene.cs:82`，位于 `Start_Integration()`，只跑一次。
- **证据**：
  - 全仓（排除 `Build/`、`.codex_tmp/`、`鸭科夫源码/`）`grep -rn "OnSetLanguage" --include="*.cs"`
    **零命中**——Mod 从不订阅官方的语言切换事件。
  - 官方自己是订阅并即时刷新的（反编译源码里 `Duckov/Options/UI/OptionsUIEntry_Dropdown.cs:70`、
    `Duckov/UI/MainMenu/ContinueButton.cs:49` 等都挂了 `LocalizationManager.OnSetLanguage`），
    所以切语言后**官方文本立刻变、Mod 文本不变**，同一屏上会出现中英混排。
  - 本轮跑的静态核对：注入 key 约 **1153** 条，全部走这一条路径。
- **失败场景**：玩家在设置里把语言从中文切到英文（不重启游戏），随后打开遗种巢 / 图鉴 /
  日报 / 词缀锻造 / 征程 / 后山 / Mode H 任一面板，界面文案仍是中文；物品名同理。
- **玩家影响**：切语言后 Mod 文本不跟随，直到重启游戏。
- **补充说明（澄清一个常见误判）**：本仓库**不存在**「中文有 key、英文缺 key 会显示 `*BossRush_*`」
  这种失败形态——`SetOverrideText` 是语言无关的单值覆盖，中英走的是**同一批 key**。
  真正的失败形态是上面这条「值被冻结在注入时的语言」。
- **建议修复**：把 `ModBehaviour.InjectLocalization()` 做成幂等可重入（现有各 `Inject()`
  都是覆盖写，天然幂等），在 `AlwaysOnRuntimeHooks` 里订阅 `LocalizationManager.OnSetLanguage`
  重跑一次，并在 `OnDestroy` 退订（4.6 幂等 + 退订）。
  需 owner 确认是否值得为此付一次全量重注入的开销（约 1150 次 `SetOverrideText`，只在切语言时发生）。
- **验证需求**：静态确认，未运行验证。实机复验：游戏内切语言后打开任一 Mod 面板。

### F-8：`WishFountainInteractable` 是全仓唯一没做 `otherInterablesInGroup` 预初始化的运行时交互体，且 `base.Awake()` 的异常被裸 `catch {}` 完全吞掉

- **严重级**：P3 Note（**实际影响很小**，见下面的影响分析；报它是因为它是 commit `1c69f85`
  那一批修复的**漏网的第 8 个**，且失败时零日志）
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\Integration\WishFountain\WishFountainInteractable.cs:22-58`
  ```csharp
  protected override void Awake()
  {
      ...
      try { base.Awake(); }
      catch { }          // ← 裸 catch，连 DevLog 都没有
      ...
  }
  ```
- **证据**：
  - 官方 `InteractableBase.Awake()`（`鸭科夫源码\TeamSoda.Duckov.Core\InteractableBase.cs:180`）
    **无条件**遍历 `foreach (InteractableBase interactableBase in this.otherInterablesInGroup)`，
    该字段声明在 `:662-663`：`[SerializeField] private List<InteractableBase> otherInterablesInGroup;`
    —— **没有内联初始化器**。
  - 本仓库 commit `1c69f85`（「修复运行时创建的交互体未初始化官方私有字段」）的判断是：
    运行时 `AddComponent` 的交互体该字段为 null，`base.Awake` 会抛 NRE，
    因此必须在 `base.Awake()` **之前**建空表。
  - 全仓其余 7 个运行时交互体都做了这件事：
    `Campaign/CampaignBoardInteractable.cs:35`、`Campaign/CampaignFinalBossInteractable.cs:34`、
    `Integration/BackMountain/ShowcaseInteractable.cs:31`、
    `Integration/DailyReport/DailyReportInteractable.cs:35`、
    `PetNest/PetNestInteractable.cs:58`、`Integration/AffixForge/GoblinAffixForgeInteractable.cs:44`、
    `Integration/Wedding/WeddingChapelInteractable.cs:210`（后者还带了解释注释）。
    **只有 `WishFountainInteractable` 没有。**
- **影响分析（为什么不是 P1）**：
  - `base.Awake()` 里在那句 `foreach` 之后**只剩一句** `this._interactbleList = new List<InteractableBase>();`
    （`InteractableBase.cs:190`），而该字段在 `:669` 已有内联初始化器
    `private List<InteractableBase> _interactbleList = new List<InteractableBase>();`，
    所以即使这句被跳过也不会留下 null。
  - 交互时走的 `GetInteractableList()`（`:19-34`）有短路保护
    `if (!this.interactableGroup || this.otherInterablesInGroup.Count <= 0) return ...;`，
    `interactableGroup` 是运行时组件默认 false，短路生效，**不会**在交互路径上 NRE。
  - 碰撞体与 layer 的初始化在 `foreach` **之前**（`:167-179`），不受影响。
  - 结论：功能上目前没有可观察的破绽；代价是每次创建许愿台交互体可能吞掉一次 NRE，
    而且因为是裸 `catch {}`（连 `DevLog` 都没有，与同批的 `WeddingChapelInteractable` 不同），
    将来 `base.Awake()` 里再加任何一句都会静默失效。
- **建议修复**：在 `base.Awake()` 之前补一行
  `NPCInteractionGroupHelper.GetOrCreateGroupList(this, "[WishFountain]");`，
  并把 `catch { }` 改成带 `DevLog` 的 catch，与同批 7 个交互体对齐。
- **验证需求**：静态确认，未运行验证。实机复验：dev 构建下进基地，看 Player.log
  是否有来自 `InteractableBase.Awake` 的 NRE（当前被吞，需临时加日志才看得到）。

## 二、未验证线索（需实机或 owner 确认，**不作为 confirmed bug**）

1. **`MutatorUI` 从 IMGUI 迁到 uGUI 后新增了一块常驻的射线拦截区**。
   `Integration/Mutators/MutatorUI.cs:167` 用 `BossRushUI.CreateCanvasRoot(..., BossRushUILayers.HudOverlay, true)`
   创建 Canvas，`interactive: true` 会 `raycaster.enabled = true`
   （`Common/UI/BossRushUI.cs:350-352`，注释明说「HUD 类界面必须让点击穿透过去」）。
   面板与每一行的 `Image` 都没设 `raycastTarget = false`（只有文字和 accent 条设了），
   于是屏幕左侧一块约 224×N（1080p 参考分辨率）的区域在整局里持续吃 uGUI 射线；
   旧 IMGUI 实现的 `GUI.Label` / `GUI.DrawTexture` 不消费鼠标事件。
   **缓解因素**：`Tick()` 在 `!InputManager.InputActived || Time.timeScale <= 0f` 时会
   `SetCanvasVisible(false)`，而打开背包/商店/模态都会关掉 gameplay 输入，所以最容易出事的
   场景已被覆盖；官方源码里也 grep 不到 `IsPointerOverGameObject`，射击大概率不受影响。
   **需实机确认**：开局带词条时，左侧面板覆盖区域的点击是否仍能正常打到游戏世界/官方 HUD。
2. **`AwenDepositTokenUsage` 在「快递站失败但回退已交付」时给出的是「没有可寄送物品」的提示**。
   `Integration/Items/AwenDepositTokenUsage.cs:91-100`：先扣运费（`:87` `TryPayDeliveryFee`），
   再 `int shippedCount = CourierService.QuickDeliverItems(rootItems, null, false);`（3 参重载，
   丢弃本轮新增的 `fallbackDeliveredCount`）。`shippedCount == 0` 时走 `GetNoItemsMessageText()`。
   本轮 `CourierService` 新增的 out 参数正是为区分这种情况（`CourierService.cs:85-125`），
   但这个调用点没跟进。物品是安全的（已由 `SendToPlayer` 交给玩家），只是提示词误导，
   且运费不退。**需 owner 判断**是否也把这个调用点切到 4 参重载。
3. **`AffixForgeStoneDropService.ClearAllTracking()` 的分层与遗种巢不一致**。
   `Integration/IntegrationRuntimeHooks.cs:15` 把它放在集成总线 hook 里，
   而遗种巢的同类清理在 `PetNest/PetNestRuntimeModule.cs:114`（自己的 RuntimeModule.OnSceneLoaded）。
   两者都会在场景加载时先于 Boss 生成执行，功能上等价；只是词缀锻造没有自己的 RuntimeModule，
   所以借了总线 hook。**不是缺陷**，但若将来给词缀锻造加 RuntimeModule，应一并迁过去，
   否则会出现两处清理点。
4. **`MutatorUI` 的 Canvas 挂了 `DontDestroyOnLoad`**（`MutatorUI.cs:168`），
   只在 `HideAll()` / `ResetStaticCaches()` 两处销毁。切场景时若两者都没被调用，
   Canvas 会跨场景存活，靠 `Tick()` 的 `MutatorManager.IsActive` 判断隐藏。
   静态看没问题（`ModBehaviour.cs:369` 与 `Integration/Mutators/MutatorRuntimeBridge.cs:61`
   两处调 `HideAll()`），**需实机确认**连续多局、中途退出竞技场时左侧面板确实消失。


## 三、已核对且干净的关键面

### 3.1 ModConfig 白名单核对表（`Register* → IsHandledModConfigOptionKey → TryLoadSingleModConfigValue`）

结论：**三条链路完全闭合，无漏登记、无死转发。**

| 键（`ModName + …`） | 注册点 | 白名单 | 单键转发 |
| --- | --- | --- | --- |
| `_waveIntervalSeconds` | `Config/Config.cs`（滑条） | OK `ConfigModConfigKeys.cs:30` | OK `Config.cs:447` |
| `_EnableRandomBossLoot` | Config.cs | OK | OK `Config.cs:456` |
| `_UseLegacyBossLootProbabilities` | Config.cs | OK | OK `Config.cs:465` |
| `_UseInteractBetweenWaves` | Config.cs | OK | OK `Config.cs:474` |
| `_LootBoxBlocksBullets` | Config.cs | OK | OK `Config.cs:483` |
| `_InfiniteHellBossesPerWave` | Config.cs | OK | OK `Config.cs:492` |
| `_BossStatMultiplier` | Config.cs | OK | OK `Config.cs:502` |
| `_milestoneRestBonusSeconds` | Config.cs | OK | OK `Config.cs:512` |
| `_ModeDEnemiesPerWave` | Config.cs | OK | OK `Config.cs:522` |
| `_EnableDragonDash` | Config.cs | OK | OK `Config.cs:532` |
| `_AchievementHotkey` | Config.cs（下拉） | OK | OK `Config.cs:541` |
| `_UseWolfModelForWildHorn` | Config.cs | OK | OK `Config.cs:553` |
| `_EnableDeathWraithSystem` | Config.cs | OK | OK `Config.cs:562` |
| `_EnableMutators` | Config.cs | OK | OK `Config.cs:571` |
| `_MutatorCount` | Config.cs | OK | OK `Config.cs:580` |
| `_ModeHEnabled` | 不注册（恒开内容） | OK（仅兼容） | OK `Config.cs:589`（强制置 true） |
| `_RandomEventsFrequency` | `ConfigRandomEvents.cs:111`（滑条，本轮新增） | OK `ConfigModConfigKeys.cs:48` | OK `Config.cs:600` → `ConfigRandomEvents.cs:84` |
| `_ModeGAbandonHotkey` | `ConfigModeG.cs:75`（下拉） | OK `ConfigModConfigKeys.cs:49` | OK `Config.cs:551` → `ConfigModeG.cs:51` |
| `_BackMountainUnlockAll` | `ConfigBackMountain.cs:91`（开关，本轮新增） | OK `ConfigModConfigKeys.cs:50` | OK `Config.cs:601` → `ConfigBackMountain.cs:63` |

**未注册的休眠 Register 方法（有意为之，非缺陷）**：
`RegisterPetNestModConfigOption` / `RegisterCodexModConfigOption` /
`RegisterDailyReportModConfigOption` / `RegisterAffixForgeModConfigOption` 全仓零调用点
（grep 确认），对应的 `TryLoadXxxSingleModConfigValue` 因不在白名单而不可达。
这是 `Config/ConfigContentSystemSwitches.cs` 明文写的「内容系统恒开、开关不进 UI」策略，
`ConfigModConfigKeys.cs:46-47` 已注明「登记了也永远收不到变更事件，属于死条目」。
实跑 `ModConfigOptionChangeGuard: PASS (exposed delegated keys: 3, content systems forced on: 8)`。

**`ForceContentSystemSwitchesOn` 覆盖面 vs 实际字段（8/8 一致）**：

| 强制字段（`ConfigContentSystemSwitches.cs:38-45`） | 声明处与默认值 |
| --- | --- |
| `petNestEnabled` | `ConfigPetNest.cs:29` = true |
| `dailyReportEnabled` | `ConfigDailyReport.cs:30` = true |
| `codexEnabled` | `ConfigCodex.cs:39` = `CodexDefaultEnabled` |
| `affixForgeEnabled` | `ConfigAffixForge.cs:43` = `AffixForgeDefaultEnabled` |
| `randomEventsEnabled` | `ConfigRandomEvents.cs:47` = true |
| `campaignEnabled` | `ConfigCampaign.cs:31` = true |
| `backMountainEnabled` | `ConfigBackMountain.cs:33` = true |
| `modeHEnabled` | `Config/Config.cs:83` = true |

`backMountainUnlockAll`（调试旋钮，默认 false）正确地不在强制列表内。

**`LoadConfigFromFile` 的 `JsonUtility` 全字段回灌（已核对，无缺陷）**：
`Config/Config.cs:177` 的 `JsonUtility.FromJson<BossRushConfig>(json)` 先走默认构造器
（C# 字段初始化器随之执行），再用 JSON 里存在的字段覆盖，所以老档缺字段时取的是
**C# 默认值**（8 个内容开关全是 true），不是 false。
`:190` `config = loaded;` 整体替换之后，`:199` 立刻 `ForceContentSystemSwitchesOn();`；
调用顺序是 `LoadConfigFromFile()`（`BossRushIntegration_StartAndScene.cs:70`）
→ `LoadConfigFromModConfig()`（`:75`），后者已不再读这 8 个键（只读频率档与旁路旋钮）。
历史残留的 false 无法回灌。
唯一理论缺口：若 `File.ReadAllText` / `FromJson` 抛异常，`ForceContentSystemSwitchesOn()`
会被 catch 跳过——但那条路径上 `config` 保留的就是构造器默认值（同样全 true），无实际影响。

`Config/Config.cs` 当前 1163 行，仍在 `LargeFileBudgetGuard` 的 1200 行硬上限内（实跑 PASS）。

### 3.2 本地化三张清单

**清单 A：查表消费了但未注入 —— 1 条**

| key | 消费点 | 判定 |
| --- | --- | --- |
| `BossRush_RandomEvent_MerchantShop` | `RandomEvents/RandomEventEffectsBridge_Spawn.cs:571` | 真缺失，见 F-4 |

静态扫描初版还报出 19 条，逐条 grep 复核后全部证伪：它们由
`Localization/LocalizationInjector_NpcUiAndItems.cs:496-533` 的字典字面量初始化器
`{ "BossRush_Xxx", L10n.T(...) }` 注入，或由 `map[常量标识符] = ...` 注入
（例：`BossRush_AffixForge` 在 `Localization/AffixForgeLocalization.cs:112`），
只是正则不认这两种写法。`BossRush_Actor_*` 是运行时按 actorId 拼键并当场注入
（`Integration/Dialogue/DialogueActorFactory.cs:160`、`:247`），也不是缺失。

**清单 B：注入了但静态零消费 —— 约 400 条，逐类核查后全部不构成缺陷**

1. **官方查表用的同义键**：`Item_<TypeID>`、`Item_<TypeID>_Desc`、中文名/英文名同名键、`Var_*`。
   官方在不同界面用不同形式查表，Mod 侧只能全注入（`CodexLocalization.cs:48-51` 有说明）。
2. **运行时拼键消费**：`BossRush_PetNest_*` 的绝大多数由 `PetNestUIPages.cs:104` 的
   `T(suffix)`、`PetNestUI.cs:192` 的 `keys[i]` 这类动态后缀消费，静态扫不到（见清单 C）。
3. **跨文件引用**：如 `BossRush_AffixForgeStone` 由 `Achievement/AchievementMedalConfig.cs` 注入、
   由物品 `DisplayNameRaw` 消费。

没有发现「一个子系统整体注入了却没有任何消费方」的情况。

**清单 C：后缀非字面量、静态无法判定的运行时拼接 —— 16 处**

| 位置 | 形态 |
| --- | --- |
| `PetNest/PetNestUIPages.cs:104` | `GetLocalizedText(PetNestTuning.LocalizationPrefix + suffix)` |
| `PetNest/PetNestUI.cs:192` | `... + keys[i]` |
| `PetNest/PetNestHatchRevealView.cs:206` | `... + "Personality_" + 运行时性格 id` |
| `PetNest/PetNestExpeditionRevealView.cs:228 / :230` | `... + "Dest_" + record.destinationId` / `"Risk_" + ...` |
| `Integration/AffixForge/AffixItemData.cs:433` | `AffixDefinitions.NameLocKeyPrefix + def.Id` |
| `Integration/AffixForge/AffixDefinitions.cs:146 / :149` | `..._Name_` / `..._Desc_` + 词缀 id + 档位 |
| `Campaign/CampaignDialoguePlayer.cs:87` | `BossRush_Campaign_` + 章节 id |
| 其余 8 处为上述同族 | |

注入侧同样按同一套 id 集合循环生成（`AffixForgeLocalization.AddAffixNamesAndDescs` 直接遍历
`AffixDefinitions.GetAll()`，`PetNestLocalization.AddPersonalities` / `AddDestinationsAndRisk` 同理），
静态无法完全证明一一对应，只能靠实机看有没有星号占位符。

**中英双语 key 集合一致性**：见 F-7 —— 本仓库不存在「中/英两套 key」的模型，
`LocalizationManager.SetOverrideText(key, value)` 是语言无关的单值覆盖，
所以「en 缺 key 会显示 `*BossRush_*`」这个担心的前提不成立；真正的风险是 F-7 的语言冻结。

**注入时机 vs 物品创建**：`ModBehaviour.InjectLocalization()` 在 `Start_Integration()` 的 `:82` 执行，
而 `InitializeDynamicItems()` 在延后 bootstrap 里才跑（`Integration/IntegrationDeferredBootstrap.cs:42`），
注入确实早于物品创建。
7 个新增本地化文件全部挂进了 `InjectLocalization_Extra_Integration()`
（`BossRushIntegration_StartAndScene.cs:43/45/47/49/51/53/57`，另加 `:55` 的 `BackMountainItems.InjectLocalization()`）。

**guard 与白名单**：`tests/localization_injection_allowlist.txt` 只有注释、零条目，
本轮没有把任何新债务塞进白名单（符合 `tests/AGENTS.md` 的「新增代码默认不进白名单」）。
实跑 `python tests/LocalizationInjectionGuard.py` -> PASS，但覆盖面因 F-1 少了 4 个 key。

### 3.3 TypeID 台账核对表（500058-500067）

| TypeID | 物品 | 常量定义 | `BossRushDynamicItemRegistry` | `ItemContentRegistry` | `docs/...ID表.md` | `docs/contracts.md` 1 |
| --- | --- | --- | --- | --- | --- | --- |
| 500058 | 便携安全区装置 | `Config/ConfigItemIds.cs:28` | OK `:343` | OK `:27` | OK 明细行 | OK |
| 500059 | 遗种蛋 | `Config/ConfigPetNest.cs:110` | OK `:348` | OK `:30` | OK 明细行 | OK |
| 500060 | 词缀熔石 | `Config/ConfigAffixForge.cs:128` | OK `:370` | OK `:31` | OK 明细行 | OK |
| 500061 | 鸭皇图鉴 | `Config/ConfigCodex.cs:138` | OK `:374` | OK `:32` | OK 明细行 | OK |
| 500062 | 龙裔之种 | `BackMountain/BackMountainItems.cs:33` | OK `:356` | 走 `BackMountainItems.RegisterConfigurators()` | **缺明细行**（F-2） | OK |
| 500063 | 龙皇焰种 | `:36` | OK `:357` | 同上 | **缺**（F-2） | OK |
| 500064 | 幽魂孢子 | `:39` | OK `:358` | 同上 | **缺**（F-2） | OK |
| 500065 | 龙息果 | `:42` | OK `:359` | 同上 | **缺**（F-2） | OK |
| 500066 | 焚心椒 | `:45` | OK `:360` | 同上 | **缺**（F-2） | OK |
| 500067 | 幽影蘑菇 | `:48` | OK `:361` | 同上 | **缺**（F-2） | OK |

- **动态注册表覆盖 10/10，无遗漏**——契约第 6 节「重启后退化成 `FallbackItem_<id>`」的风险已排除。
- **无复用**：`TypeIdLedgerGuard` 实跑 PASS，「保留空洞 [500009, 500047] 源码中零命中」。
  `Config/ConfigItemIds.cs:24-26` 顺手删掉了与台账矛盾的 `SpeedrunGauntletTicket = 500047` 残留常量，正确。
- `tests/typeid_literal_allowlist.txt` 只有 1 条正当豁免
  （`Achievement/BossRushAchievementManager.cs:500000` 成就分数阈值），未塞新债务。
- **`ItemContentRegistry` 未登记 500062-500067 不是缺陷**：`BackMountainItems.EnsureRuntimeRegistration`
  （`BackMountainItems.cs:301-388`）在克隆 prefab 后自己调用 `ConfigureItem`，兜底路径自洽；
  `RegisterConfigurators()` 由 `GardenSeedInjector.EnsureItemsRegistered()`（`:105-129`）幂等触发。

### 3.4 其余已核对干净的面

- **`TeleportDebugMonitor.cs` 删除干净**：`compile_official.bat` 无该条目；全仓（排除 `Build/`
  编译产物）零引用。`OfficialCompileListFileExistenceGuard: PASS (767 sources)`。
- **`ReflectionCache` 拆分正确**：`Common/Utils/ReflectionCache.cs`（`BossRush.Common.Utils`
  命名空间，动态字典缓存）与 `Common/Infrastructure/BossRushEagerReflectionCache.cs`
  （`BossRush` 命名空间，静态 `FieldInfo`）各司其职，两者都在 `compile_official.bat`（`:135`、`:144`）。
  `DeathWraithCombatLoadout.cs:800` 与 `PhantomWitchBoss.cs:226` 改用后者，
  反射目标 `CharacterRandomPreset.characterIconType` 经官方源码核对存在
  （`鸭科夫源码/TeamSoda.Duckov.Core/CharacterRandomPreset.cs:188-190`，`[SerializeField] private`，
  绑定标志 `NonPublic|Instance` 正确）。
- **4.5 敌对性安全网未被破坏**：`ModBehaviour.cs:1153-1165` 只是在原判断前加了一个遗种巢随从
  豁免分支，原 `else if (!Team.IsEnemy(Teams.player, character.Team)) character.SetTeam(Teams.wolf);`
  逐字保留，非随从路径行为完全一致。
  `SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep` 在标准路径与三个自定义 Boss 的
  `SetActive(true)` 之后调用，位置一致。
- **`ModBossPresetLookup` 收口语义逐字一致**：`Utilities/ModBossPresetLookup.cs:37-49` / `:60-72`
  与三个 Boss 的原实现逐句比对——`Find` 的 null 处理、遍历顺序、返回值；`Matches` 的
  `preset == null -> false`、三个短路条件顺序全部相同。**找不到 preset 时的 fallback 行为与从前无差别。**
- **`ModeFItemConfigHelper.ConfigureSimpleConsumable` 数值/tag/图标零漂移**：逐字比对 diff 前后，
  四个物品的 `MaxStackCount` / `Value` / `Quality` / `StackCount=1` / `name` / 两个隐藏成员写法 /
  `"Special"` tag / `ModeFItemUsageHelper.AttachToItem` / 成功与失败两条日志文案全部不变
  （唯一副作用是 F-1 的 guard 覆盖面）。
- **`IntegrationDeferredBootstrap` 新增 6 个延后步骤全部幂等**：
  `InitPetNestBuilding`（`PetNestBuilder.cs:83` `petNestBuildingInjected`）、
  `InitDailyReportMailbox`（`DailyReportMailboxBuilder.cs:93`）、
  `InitCampaignBoardBuilding`（`CampaignBoardBuilder.cs:76`）各有布尔闸；
  `RestorePetNestBuildings` / `RestoreDailyReportMailboxes` 用「协程非空即跳过」去重
  （`PetNestBuilder_DataEventsAndRuntime.cs:299`、`DailyReportMailboxRuntime.cs:302`）；
  `RegisterCampaignNotesForScene` 转发到 `CampaignNoteBridge.EnsureNotesRegistered()`。
  每步之间都插了 `ShouldContinueDeferredBaseSceneSetup_Integration(sceneName, sceneHandle)`
  场景一致性闸，形态与既有步骤一致。**重复进基地不会双注入。**
  另注：后山展示柜没走延后 bootstrap，而是由 `BackMountainRuntimeModule.cs:317` 触发
  `InitBackMountainShowcase()`，同样由 `backMountainShowcaseInjected` 布尔闸保证幂等，属另一种合法接线。
- **图鉴商店注入幂等**：`TryInjectCodexBookIntoShop`（`CodexBookItem.cs:339-353`）先遍历
  `shop.entries` 查 `ItemTypeID == 500061`，命中即返回 false。Harmony 路径
  （`Patches/BaseHub/BaseHubShopAwakePatch.cs`）与延后路径（`InjectCodexBookIntoShops`）叠加不会上架两本。
- **图鉴库存存档订阅成对**：`OnCollectSaveData_CodexBookStock` / `OnSetFile_CodexBookStock`
  在 `BossRushIntegration_StartAndScene.cs:102-103` 订阅、`:139-140` 退订，
  handler 实体在 `CodexBookItem.cs:462` / `:487`。
  `EventSubscriptionLifecycleGuard: PASS (69 subscriptions checked, 0 allowlisted)`。
- **`NPCModuleRegistry` 自动发现不会因新基类出错**：`AutoDiscoverModules`（`:354`）显式跳过
  `type.IsAbstract || type.IsInterface`，新增的 `ArenaSupportNpcModuleBase` 是 `internal abstract`，
  不会被实例化；`RegisterInternal` 另有 `registeredTypes` / `registeredNpcIds` 双重去重。
  提取到基类的 `ShouldSpawnInScene` 与哥布林/护士原实现逐字相同
  （唯一变化是把 `XxxAffinityConfig.NPC_ID` 换成接口成员 `NpcId`，两者返回同一常量）。
- **UI 常量替换无行为变化**：`ZombieModeUIHelper.ConfigureCanvasScaler`
  （`ZombieMode/ZombieModeUIHelper.cs:442-452`）设 `ScaleWithScreenSize` + `1920x1080` +
  `matchWidthOrHeight = 0.5f`，未设 `screenMatchMode`——而 `CanvasScaler` 新建时该字段默认就是
  `MatchWidthOrHeight`，与被删掉的显式赋值等价，注释里的说明属实。
  `sortingOrder` 由裸数值改为 `BossRushUILayers` 常量：`OriginalConfirmDialogueAdapter` 10 ->
  `ModalConfirm(3200)`、`NPCMarriageSystem` 32000 -> `WeddingCutscene(32000)`（不变）、
  `WishFountainRewardAnimationView` 1000 -> `Modal(3000)`、`WishFountainUI` 1100 -> `HudOverlay(1200)`。
  注意抽奖动画与许愿台面板的相对次序**翻转了**（原来面板压住动画，现在动画压住面板）——
  抽奖动画本就是全屏遮罩层，压住面板是正确方向，判定为**有意修正**而非回归。
  `ZombieModeUIHelper.ApplyButtonColors` 把 `pressedColor` 改成 `Color.Lerp(normal, black, 0.18f)`，
  与许愿台原来手写的 primary/secondary 两组 pressed 色（约 0.7 倍亮度）接近，属可接受的观感统一。
- **`OnDestroy` 顺序与重复落盘均安全**：`ModBehaviour.OnDestroy()` 里
  `PetNest*` / `DailyReport*` / `Codex*` 的 `TryFlushOnHostDestroy()` 先于对应的 `ResetStaticCaches()`；
  随后 `runtimeModuleHost.OnDestroy()`（最后一步）会让 `PetNestRuntimeModule.OnDestroy`（`:208`）与
  `CodexRuntimeModule.OnDestroy`（`:164`）再落盘一次——已核对这不会写空档：
  `CodexSaveCoordinator.FlushBatch`（`:131-142`）开头 `if (!CodexPersistence.HasPendingWrite) return true;`，
  而 `CodexPersistence.ResetStaticCaches`（`:447-460`）把 `_pendingActive=false`、`_pendingJson=null`，
  `HasPendingWrite`（`:69-72`）随即为 false，第二次 flush 是 no-op。属冗余但无害。
  `StaticCacheLifecycleGuard: PASS`。
- **`_enemyPresetInitializationScanCount = 0` 复位有效**：字段在
  `LootAndRewards/LootAndRewards.cs:223` 声明，`WavesArena/WavesArena.cs:696` 自增、`:826` 读取，
  `BossRushIntegration_StartAndScene.cs:260` 在 `OnDestroy` 路径复位，与既有的
  `_enemyPresetsInitialized = false` 成对。
- **Mode H 早注册改动**：`deferArenaCommit = deferArenaCommitForModeG || deferArenaCommitForModeH`
  （`BossRushIntegration_StartAndScene.cs:378-384`）与 Mode G 完全同形；
  `BossRushMapSelectionHelper.HasPendingModeHEntryIntent()` 存在
  （`MapSelection/BossRushMapSelectionHelper.cs:256`）。被跳过的四件事
  （`bossRushArenaActive`、`PreCacheMapSpawnerPositions`、`ScheduleModeEStartupWarmup`、`ContinuousClear`）
  与 Mode G 分支逐字一致，不影响非 H/G 过图路径。
- **`SetupBossRushInGroundZero` 的 Mode G 新分支逻辑正确**：新增的 `else if (!openedModeG)`
  （`BossRushIntegration_TravelAndSetup.cs:418`）只在「`LastConfirmationAttemptedStart == false`
  且确认页从未打开」时命中，原有「已打开但被取消」分支被推到最后的 `else`，语义不互相吞；
  `if (!openedModeG) TryRefundModeGPendingPrepaidTicket();` 仍在两分支之外统一执行。
  新增的 Mode H 早退分支（`:319-327`）放在 Mode E 判断之前、`yield break`，不影响其它入场模式。
- **`DragonKingBoss` 熔石/遗种蛋挂接正确且成对**：生成侧 `TryTrack`（`:363` / `:372`）紧跟手动
  `BeforeCharacterSpawnLootOnDead` 订阅之后，此时 `customPreset.nameKey = DragonKingConfig.BossNameKey`
  已在 `:280` 赋值，`PetNestDropService.ResolveLineageKey`（`PetNest/PetNestDropService.cs:325-332`）
  读 `character.characterPreset.nameKey` 能取到；
  `PetNestLineageCatalog.AddCustomLineages`（`:166-186`）**无条件**收录三个自定义 Boss key，
  `IsKnownLineage` 判定通过，掉落链完整。退订侧两处成对：离场清理（`:158-170`）与死亡清理（`:633-645`）。
  `AffixForgeInvariantGuard: PASS（12 条词缀，订阅成对，AFX_ 互斥，死契保命，事务回读，熔石掉落覆盖龙王）`。
- **`AffixForgeStoneDropService.ClearAllTracking()` 场景清理接线有效**：
  `IntegrationRuntimeHooks.OnSceneLoadedIntegrationRuntime`（`:15`）在 `OnSceneLoaded_Integration`
  之前执行，且整个 `ModBehaviour.OnSceneLoaded` 早于任何 Boss 生成，不会误清本局追踪。
- **`PhantomWitchBoss` 新增的 `phantomWitchDeathPresentations` 字典无泄漏**：
  三处成对维护——`:307` 写入、`:398` 与 `:713` 移除、`:127` 整表 `Clear()`，
  与既有的 `phantomWitchInstances` 完全同步。新增的女巫敌对性兜底（`:251-268`）
  与 4.5 标准路径同款且放在随从生成之前，符合注释所述。
- **`.gitignore` / `guards.yml` / `run_guards.bat` 变更未造成 CI 硬失败**：新 CI 只跑
  `python tools/run_guards.py`，不跑编译（符合 4.2）。被 guard 读取的 `docs/contracts.md`
  已在 git 索引内（`git ls-files docs` 命中）；未纳管的 `docs/Bossrush使用物品ID表.md` 与
  `docs/制作教程/BossRushUI_图集规格.md` 分别被 `PetNestEggItemRegistryGuard.py:124-128`
  （缺失只告警不判红）与 `BossRushUISkinLoaderGuard.py`（只在文档注释里提及）软处理，
  fresh clone 不会红。`.gitignore` 把 `compile_official.bat` / `test_bossrush_official.bat`
  改为纳管，属 `OPERATIONAL` 变更，与 4.1 的「显式清单」要求一致。
- **`LootBlacklistRegistry` 的代码改动本身无缺陷**：只是把回退兜底的 `DevLog` 升级成
  `CriticalLog("loot-blacklist-fallback", ...)`；`ModBehaviour.CriticalLog(string, string)`
  存在于 `DebugAndTools/DebugAndTools.cs:515`。问题在内容上（F-3）。
  同一模式的 `BossRushDynamicItemRegistry.cs:211` / `:234` 两处 bundle 加载失败也升级为 `CriticalLog`，
  合理（bundle 加载失败会直接导致物品退化）。
- **`WikiBookItem.cs` 删除的是真死代码**：被删的 `WIKI_BOOK_DISPLAY_NAME_CN/EN`、
  `WIKI_BOOK_DESCRIPTION_CN/EN` 与两个包装属性全仓零引用；实际生效的是 `LocalizationInjector`
  的 `WIKI_BOOK_NAME_CN`（冒险家日志）。`InjectWikiBookLocalization` 收口到
  `InjectItemLocalization` 后注入的 key 集合逐字不变（`BossRush_WikiBook` / `_Desc` 改由
  `canonicalKey + "_Desc"` 运行时拼出，源码 grep 不到但运行时相同，
  且 `冒险家日志` / `冒险家日志_Desc` 两条额外注入被保留）。
- **`DragonKingAssetManager` 改动是纯乱码修复**：`:457` 把一条 GBK 乱码日志文案改回中文，无行为变化。
- **`CourierService.QuickDeliverItems` 新重载向后兼容**：3 参旧重载（`:72-76`）转发到 4 参新重载，
  返回值语义（进快递站的件数）不变，既有调用方无需改动。
  `Integration/DailyReport/DailyReportRewards.cs:81-98` 已正确使用新重载并在
  `sent <= 0 && fallbackDelivered <= 0` 时才销毁物品——这正是新参数存在的理由，接线正确。
- **`MutatorUI` 迁移的抑制契约与释放路径完整**：`Tick()` 的抑制判定在显示之前、抑制时清悬停、
  `InputManager` 抛异常按抑制处理；`ResetStaticCaches` 挂在 `ModBehaviour.cs:781` 的 OnDestroy 路径；
  `ShowBanner()` 末尾 `DestroyCanvas()` 保证词条变化后重建。
  `MutatorUiOverlaySuppressionGuard: PASS`。`ModBehaviour.OnGUI` 里的 `MutatorUI.DrawGUI()` 已删干净。

## 四、对前几轮已 Fixed 条目 / 相关 commit 的复核

以下条目**不重复报告**，只回答「修复是否真的正确、有无回归」。全部为静态确认，未运行验证。

| 条目 / commit | 复核结论 | 证据 |
| --- | --- | --- |
| **CR-2026-09-03-015** 焚天龙皇不掉词缀熔石 | **修复正确，无回归** | `Integration/DragonKing/DragonKingBoss.cs:372` 生成侧 `AffixForgeStoneDropService.TryTrack(this, character)` 紧跟手动 `BeforeCharacterSpawnLootOnDead` 订阅；`:164` 离场清理与 `:640` 死亡清理两处 `ClearTracking`，成对。服务内部 `TryTrack` 开头先 `ClearTracking` 保证幂等。`AffixForgeInvariantGuard: PASS`（含「熔石掉落覆盖龙王」断言）。同一批并联的遗种巢（`:363` / `:158` / `:633`）同理正确，且 `PetNestLineageCatalog.AddCustomLineages` 无条件收录 `DragonKingConfig.BossNameKey`，血脉解析链完整。 |
| **CR-2026-07-05-001/002/003** 焚天龙铳 baseline 与热路径 | **未被本轮改动触及，修复完好** | 本轮 diff 里 `Integration/DragonKing/` 只改了 `DragonKingBoss.cs` 与 `DragonKingAssetManager.cs`（`changed_cs_files.txt` 确认），`Weapons/DragonKingBossGun*.cs` 一行未动。`DragonKingBossGunReforgeBaselineGuard: PASS`。 |
| **CR-2026-07-01-001** 售货机 UI 崩溃（延迟注入商品未缓存 `itemInstance`） | **修复正确，且自动覆盖本轮新增的鸭皇图鉴** | `Patches/Economy/StockShopGetItemInstanceDirectPatch.cs` 的 Harmony Prefix 是**按 typeID 通用**的：缓存未命中即 `ItemAssetsCollection.InstantiateSync(typeID)` 写回 `itemInstances`，不针对具体物品。本轮新加的 500061 走同一条延后注入路径（`IntegrationDeferredBootstrap` 的 `InjectCodexBookIntoShops`），自动受保护，无需额外改动。补丁里对 Mode E 贝壳商店的 `PassOriginal` 例外仍在。 |
| **CR-2026-07-02-001** 许愿台弹幕不接入新拉取结果 | **修复完好** | `WishFountainUI.cs` / `WishFountainDanmakuView.cs` 的「更新内容源不重置滚动」能力仍在；本轮对 `WishFountainUI.cs` 的改动只有 3 处 `ApplyPanelSkin`、1 处 sortingOrder 常量化、1 处按钮配色收口，全部在 UI 构建路径，未触及弹幕数据流。 |
| **CR-2026-07-02-002** 失败结果被 45 秒 TTL 缓存 | **修复完好** | `WishFountainFetchPipeline.cs:251-271` `TryReturnRecentDanmakuResult`：TTL 内**只有** `lastDanmakuFetchSucceeded == true` 才短路返回，失败一律 `return false` 走重试。本轮未改该文件。 |
| **CR-2026-07-02-003** 关闭后未解绑静态弹幕回调 | **修复完好** | `WishFountainUI.cs` 三处调用 `CancelDanmakuFetch()`（`:202` / `:281` / `:727`），实现在 `:780`，转发到 `WishFountainFetchPipeline.CancelRecentWishesRequest`（`:68-79`，成对 `-=`）。`WishDanmakuFetchLifecycleGuard: PASS`。 |
| **commit 89c913e** Boss preset 查找收口 | **等价重构，语义逐字一致** | 见 3.4：`ModBossPresetLookup.FindByNameKey` / `Matches` 与三个 Boss 原实现逐句比对，null 处理、遍历顺序、三个短路条件顺序全部相同。**找不到 preset 时的 fallback 行为未变。** |
| **commit a7b50e2** 模板化 / 分派表化 / 编码修复 | **数值与行为等价，但留下一个 guard 覆盖面回退（F-1）** | 四个 Mode F 物品的 `ConfigureSimpleConsumable` 逐字等价（见 3.4）；`ModBehaviour` 三个重复场景常量删除后 `SceneRuntimeGate.IsBaseHubSceneName` 是唯一事实源，全仓 18 处调用点均正确（含本轮新增的 `NPCModuleRegistry.cs:288`、`ShowcaseBuildingBuilder.cs:103`、`CampaignBoardBuilder.cs:122`）。`DragonKingAssetManager.cs:457` 乱码修复正确。**唯一问题是 F-1。** |
| **commit afe0a88** 死代码清理 | **清理干净，无残留** | `TeleportDebugMonitor.cs` 已从磁盘与 `compile_official.bat` 双向移除，全仓零引用（只剩 `Build/decompiled*/` 编译产物副本，属构建输出）。`SpeedrunGauntletTicket = 500047` 已删且原地留注释（`Config/ConfigItemIds.cs:24-26`），`TypeIdLedgerGuard` 确认 500047 源码零命中。`OfficialCompileListFileExistenceGuard: PASS (767 sources)`。 |
| **commit e714a94 / 1cd9c6d** 内容系统总开关恒开 | **修复正确，且已正确扩展到 8 个** | 原提交覆盖 5 个开关，本轮已扩展到 8 个（新增 `campaignEnabled` / `backMountainEnabled` / `modeHEnabled`），与 `ModConfigOptionChangeGuard.CONTENT_SYSTEM_SWITCHES` 完全对齐（实跑 `content systems forced on: 8`）。`petNestEnabled` 默认值已是 `true`。`IsHandledModConfigOptionKey` 提取到 `ConfigModConfigKeys.cs` 后 guard 已双路径查找（`HANDLED_KEY_SOURCES`），不会瞎掉。详见 3.1。 |
| **commit 27d5425 / 5d2a0e3** 三/四系统接线 | **接线完整，本轮补齐了当时漏的两个 TypeID** | 后山 6 个 TypeID（`BossRushDynamicItemRegistry.cs:354-361`）与词缀熔石 / 鸭皇图鉴（`:368-375`）现已全部登记，10/10 覆盖（见 3.3）。`5d2a0e3` 提到的重铸互斥 `AFX_` 前缀仍在（`AffixForgeInvariantGuard: PASS`，含「AFX_ 互斥」断言）。模块注册的单实例纪律（先存字段再注册同一引用）在 `Common/Lifecycle/BossRushRuntimeModuleRegistration.cs:26-45` 保持。 |
| **commit 5842911** 鸭皇图鉴商店与库存持久化 | **修复正确** | `priceFactor = 1f` 且带注释说明旧实现的 `1/rawValue` 会把售价压成 1 金（`CodexBookItem.cs:355-357`）；库存走 `LoadCodexBookStockFromSave()`（`:432-459`，用官方拼写 `SavesSystem.KeyExisits`，注释已警告不要"纠正"）；存档订阅在 `BossRushIntegration_StartAndScene.cs:102-103` / `:139-140` 成对。上架幂等（`:339-353`）。**唯一残留问题是 F-5 的开关闸缺失（当前不可达）。** |
| **commit 1c69f85** 运行时交互体私有字段初始化 | **修复正确，但漏了第 8 个（F-8）** | 提交点名的三个（日报报箱 `DailyReportInteractable.cs:35`、征程公告板 `CampaignBoardInteractable.cs:35`、后山展柜 `ShowcaseInteractable.cs:31`）都已在 `base.Awake()` 之前建空表，另有 `PetNestInteractable.cs:58`、`GoblinAffixForgeInteractable.cs:44`、`CampaignFinalBossInteractable.cs:34`、`WeddingChapelInteractable.cs:210` 共 7 处；`WishFountainInteractable` 是唯一没做的（F-8，实际影响很小）。 |

### 4.1 本轮实跑的 guard（只读，未修改仓库）

`ModConfigOptionChangeGuard` / `TypeIdLedgerGuard` / `OfficialCompileListFileExistenceGuard` /
`PetNestEggItemRegistryGuard` / `LargeFileBudgetGuard` / `MutatorUiOverlaySuppressionGuard` /
`LocalizationInjectionGuard` / `DragonKingBossGunReforgeBaselineGuard` / `AffixForgeInvariantGuard` /
`WishDanmakuFetchLifecycleGuard` / `EventSubscriptionLifecycleGuard` / `StaticCacheLifecycleGuard`
—— **全部 PASS**。（`LocalizationInjectionGuard` 的 PASS 存在 F-1 描述的覆盖面缩水。）

### 4.2 本轮**未能**验证的面

- Windows 编译（`compile_official.bat`）：按 AGENTS.md 4.2 只能在装有《鸭科夫》的 Windows 机器上做，
  且本次为**只读审查**，未运行任何编译或部署脚本。
- 全部实机行为：F-3 的奖池 roll、F-4 的星号占位符、F-7 的切语言、F-8 的 NRE、
  以及未验证线索里的四条，都需要进游戏确认。
- `docs/官方本地化表/` 未纳管，无法核对 Mod 注入的 key 是否与官方 key 冲突。

### 4.3 编译清单（AGENTS.md 4.1）逐项核对

本范围内全部新增 `.cs` 均已进 `compile_official.bat`（每个文件名恰好命中 1 次）：

`ModeHLocalization.cs`、`PetNestLocalization.cs`、`DailyReportLocalization.cs`、
`CodexLocalization.cs`、`AffixForgeLocalization.cs`、`CampaignLocalization.cs`、
`BackMountainLocalization.cs`、`RelicEggConfig.cs`、`ModBossPresetLookup.cs`、
`ConfigModConfigKeys.cs`、`ConfigContentSystemSwitches.cs`、`ConfigItemIds.cs`、
`BossRushEagerReflectionCache.cs`。

删除的 `TeleportDebugMonitor.cs` 已从清单移除。
双向一致性由 `OfficialCompileListFileExistenceGuard` 守卫，实跑 `PASS (767 sources)`。

---

*报告结束。全程只读，未修改仓库任何文件；唯一写入的是本报告与 scratchpad 下的临时核对脚本。*

---

## ==== RandomEvents_Waves_Loot_Utilities_Patches ====

# 随机事件 / 波次竞技场 / 掉落奖励 / Utilities / Patches / UIAndSigns 审查报告

> 状态：**已完成**。范围见任务简报。基线 commit `f9b83c0`（含未提交工作树改动）。
> 审查方式：新增文件读全文；修改文件读 diff + 上下文；Harmony/反射目标逐项对照 `鸭科夫源码/`。
> 绝对只读：本轮未修改仓库任何文件，未跑编译/部署。
>
> **结果**：7 条 confirmed finding —— **P0 × 1、P1 × 2、P2 × 0、P3 × 4**；另有 5 条未验证线索。
> 其中 **F-1（P0）**、**F-3（P1）** 分别说明 CR-2026-09-03-009 与 CR-2026-09-03-010
> 两条本轮 Fixed 条目「修了但没修到位」；**F-2（P1）** 说明本轮两处新增的
> 「失败要给玩家解释」修复在正式构建里完全无声。
>
> | 编号 | 严重级 | 一句话 |
> | --- | --- | --- |
> | F-1 | P0 | 乱入 Boss 抽到三只自定义 Boss 时夺走 `currentBoss` / 写进 `currentWaveBosses`，标准竞技场卡波或跳波 |
> | F-2 | P1 | `ShowMessage()` 的官方通知反射目标恒 null 且 `statusMessage` 无渲染方，136 处提示在正式构建里全部不可见 |
> | F-3 | P1 | 空投「翻箱保护」判据选错，玩家开着战利品界面时 `Interacting` 已是 false，箱子照旧 45s 消失 |
> | F-4 | P3 | `MagicBlend` 补丁的 `Settled` 表只在宿主销毁时清，注释却写「切场景清」 |
> | F-5 | P3 | 三个运行时交互体没做 1c69f85 的 `otherInterablesInGroup` 前置初始化 |
> | F-6 | P3 | 共享刷怪核心提交回调失败销毁角色时不解绑掉落追踪 |
> | F-7 | P3 | 随机事件商人 / 巡游鸭没接距离休眠解除，玩家跑远再回来商人消失 |

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1：乱入 Boss 抽到三只自定义 Boss 时会夺走 `currentBoss` / 写进 `currentWaveBosses`，标准竞技场卡波或跳波（CR-2026-09-03-009 修复被绕开）

- **严重级**：P0 Blocker（核心玩法不可用：标准/无间炼狱竞技场可被永久卡波）
- **兼容分类**：`COMPAT`
- **状态**：新发现，静态确认，未运行验证

#### 位置

- `RandomEvents/RandomEventCatalog.cs:638-651`（乱入池 = `GetFilteredEnemyPresets()` 全量，无任何自定义 Boss 排除）
- `RandomEvents/RandomEventEffectsBridge_Spawn.cs:92-101`（`SpawnEnemyCoreInternalAsync(..., skipDragonDescendant: false, skipDragonKing: false, ...)`）
- `Utilities/EnemySpawnCore.cs:783-833`（按 preset 分流到三个专用生成方法）
- `Integration/DragonKing/DragonKingBoss.cs:265-271`
- `Integration/PhantomWitch/PhantomWitchBoss.cs:207-213`
- `Integration/DragonDescendant/DragonDescendantBoss.cs:110-121`
- `WavesArena/WavesArena.cs:481-520`（`IsCurrentWaveBossMember` 的真相来源正是这两个被污染的容器）
- `WavesArena/WavesArenaSpawnerControl.cs:190-226`（自愈路径在 `currentBoss` 为已销毁对象时早返）

#### 证据

乱入事件挑 preset 时不做任何排除：

```csharp
// RandomEvents/RandomEventCatalog.cs:639-654
try { pool = owner.GetFilteredEnemyPresets(); }
...
EnemyPresetInfo preset = pool[UnityEngine.Random.Range(0, pool.Count)];
```

而 `enemyPresets` 里**明确包含**三只自定义 Boss——`InitializeEnemyPresets()`（`WavesArena/WavesArena.cs:716-722`）在过滤之后依次调
`RegisterDragonDescendantPreset()` / `RegisterDragonKingPreset()` / `RegisterPhantomWitchPreset()`，
其中 `DragonKingBoss.cs:734` 是 `enemyPresets.Add(dragonKingPreset);`；
`GetFilteredEnemyPresets()`（`BossFilter/BossFilter.cs:215-219`）只按玩家的启用名单过滤，默认全开。

共享生成核心随后按 preset 分流到专用生成方法（`Utilities/EnemySpawnCore.cs:783/801/818`），
而这三个专用方法**无条件写波次状态**：

```csharp
// Integration/DragonKing/DragonKingBoss.cs:264-271
// 设置为当前Boss
currentBoss = character;

// 多Boss模式支持
if (bossesPerWave > 1 && currentWaveBosses != null && !currentWaveBosses.Contains(character))
{
    currentWaveBosses.Add(character);
}
```

`PhantomWitchBoss.cs:206-213` 与 `DragonDescendantBoss.cs:110-121`（`isChildProtectionSummon == false` 分支）逐字相同。
乱入桥传的是 `isChildProtectionSummon` 缺省（false），所以龙裔那条也会写。

#### 失败场景

设 `bossesPerWave == 1`（默认档），本波真 Boss 已生成，`currentBoss = 真Boss`：

1. 随机事件抽中 E3，preset 恰为龙王/女巫/龙裔（概率 = 3 / 池大小，实测池约十余项，即 ~10-25%）。
2. 专用生成方法执行 `currentBoss = 乱入Boss`，真 Boss 被挤出状态机。
3. 玩家击杀真 Boss：`OnEnemyDiedWithDamageInfo` 单 Boss 分支比对 `currentBoss`（现在是乱入者）不命中，
   不调 `HandleBossDeath`；掉落漏斗那条调到了 `HandleBossDeath`，但 `IsCurrentWaveBossMember(真Boss)`
   → `IsSameWaveBossInstance(currentBoss=乱入者, 真Boss)` → false → **早返，波次不推进**。
4. 120s 后事件到期，`DespawnRandomEventIntruderBoss` 销毁乱入者，`currentBoss` 变成已销毁 Unity 对象。
5. 自愈路径 `TryFixStuckWaveIfNoBossAlive`（`WavesArenaSpawnerControl.cs:202-205`）
   `if (bossMb == null) return;` —— Unity 的 `==` 重载对已销毁对象返回 true，**直接早返，不自愈**。
   竞技场从此永久停在这一波：场上无 Boss、倒计时不启动、`ProceedAfterWaveFinished` 永不被调用。

多 Boss 档（`bossesPerWave > 1`）是另一条故障：乱入者被 `currentWaveBosses.Add`，
于是 `IsCurrentWaveBossMember(乱入者)` 返回 **true**——CR-2026-09-03-009 想堵的「跳波」
在这条路径上**原样复现**：击杀乱入者会 `bossesInCurrentWaveRemaining--` 并可能提前
`ProceedAfterWaveFinished()`（`bossesInCurrentWaveRemaining` 在开波时按真 Boss 数设定，
名单里多一只但计数没加，等于少打一只真 Boss 就推波）。最后一波则提前放通关演出。

附带污染（同一根因）：龙王乱入还会播龙王 BGM、弹 `L10n.DragonKingAppeared`、
写 `dragonKingInstances` 与套装效果；龙裔乱入会覆盖全局单例 `dragonDescendantInstance`
（`DragonDescendantBoss.cs:106`），把本波真龙裔的引用挤掉。

#### 为什么两个守卫都是绿的（假绿）

- `tests/RandomEventsWaveIsolationGuard.py` 只对 `RandomEvents/` 目录做**文本**扫描，
  断言目录内不出现 `currentWaveBosses` / `ProceedAfterWaveFinished` 等符号。
  本缺陷的写入发生在 `Integration/DragonKing|PhantomWitch|DragonDescendant`，
  经 `EnemySpawnCore` 间接到达，文本扫描看不见。
  （该 guard 的 docstring 恰好点名了本失效模式：「乱入 Boss 被计入本波……卡波；
  或乱入 Boss 死亡被当成波次 Boss 死亡……跳波」。）
- `tests/WavesArenaBossMembershipGuard.py` 断言的是 `HandleBossDeath` 里分界线的**位置**
  与 helper 引用了哪些符号，不验证这些容器的写入方是否只有波次路径。

#### 玩家影响

标准 BossRush / 无间炼狱在开启随机事件（默认开启，内容开关恒开）时，
每次乱入事件都有约 1/5 概率把本局打成死局：真 Boss 打死了也不推波，只能退出重开，
整局进度与战利品作废。无间炼狱尤其严重（长局、里程碑奖励按波次发放）。

#### 建议修复（Needs owner confirmation on 取舍）

两条互斥方案，任选其一，不要同时做：

1. **乱入池排除托管 Boss**（改动最小、与「乱入 Boss 绝不进波次状态」的既有设计一致）：
   在 `RandomEventBossIntrusion.OnTrigger`（`RandomEventCatalog.cs:650`）挑 preset 时
   用**已有的共享判据** `IsManagedBossPreset(preset)` 剔除三只自定义 Boss
   （该谓词已被 `WavesArena/WavesArena.cs:1011` 与 `Utilities/EnemySpawnCore.cs:872` 使用，
   直接复用，不要再写一份名字启发式）；剔除后池为空则 `CanTrigger` 返回 false。
   注意 `PruneNonBossEnemyPresetsFromCache`（`WavesArena.cs:1011-1014`）恰好用同一个谓词
   **保留**这三只，而 `IsBossEnabled`（`BossFilter/BossFilter.cs:167-181`）对未登记名字默认返回 true，
   所以它们默认就在乱入池里——这不是边缘情况。
2. **给三个专用生成方法加「不登记波次」开关**：照 `DragonDescendant` 现成的
   `isChildProtectionSummon` 形态，把 `currentBoss = character` / `currentWaveBosses.Add`
   收进一个 `registerAsWaveBoss` 参数，由 `EnemySpawnCoreOptions` 透传（乱入传 false）。
   代价是要动三个 Boss 文件与 options 契约。

无论选哪条，都建议补一条守卫：断言乱入生成路径拿到的 preset 不会命中
`IsManagedBossPreset`，或断言三个专用生成方法的波次写入受参数门控。
另建议给 `TryFixStuckWaveIfNoBossAlive` 的单 Boss 分支加一条
「`currentBoss` 为已销毁对象且场上无本波 Boss 存活」的兜底（当前是无条件早返，等于自愈失效）。

#### 验证需求

- Windows 真编译 + guard 全跑。
- 实机：标准竞技场（`bossesPerWave=1`）里用 F3 `TryForceTrigger(BossIntrusion)` 反复触发，
  直到抽中龙王/女巫/龙裔；确认真 Boss 击杀后波次仍推进，且乱入者到期销毁后不卡波。
- 实机：`bossesPerWave>1` 档同样验证乱入者不进 `currentWaveBosses`。

### F-2：`ShowMessage()` 在正式构建里对玩家完全不可见（反射目标绑不上 + `statusMessage` 无渲染方），本轮新增的两处「失败要有解释」修复因此静默

- **严重级**：P1 Major（玩家可见主流程回归：136 处提示全部无声；本轮两处新修复的目的正是「不要静默失败」）
- **兼容分类**：`COMPAT`
- **状态**：新发现，静态确认，未运行验证。根因是长期既有债，但本轮新增的调用点让它变成本批交付的功能缺口

#### 位置

- `Common/Infrastructure/BossRushEagerReflectionCache.cs:91`（绑定目标与绑定标志双错）
- `UIAndSigns/UIAndSigns.cs:285-301`（`ShowMessage_UIAndSigns`）
- `UIAndSigns/UIAndSigns.cs:204-212`（`UpdateMessage_UIAndSigns`，只倒计时不显示）
- `ModBehaviour.cs:622-632`（`OnGUI` 只画调试面板，不画 `statusMessage`）
- 本轮新增的受害调用点：`WavesArena/BossRushEntryFlow.cs:211-217`、`WavesArena/WavesArenaEntryAndTeleport.cs:41`

#### 证据

绑定：

```csharp
// Common/Infrastructure/BossRushEagerReflectionCache.cs:56, 91
const BindingFlags publicStatic = BindingFlags.Public | BindingFlags.Static;
...
NotificationText_ShowNext = typeof(NotificationText).GetMethod("ShowNext", publicStatic);
```

官方实现（`鸭科夫源码/TeamSoda.Duckov.Core/Duckov/UI/NotificationText.cs`）：

```csharp
public static void Push(string text)   // ← 真正的公开入口
...
private UniTask ShowNext()             // ← private、实例、零参数
```

三重不匹配：名字（应为 `Push`）、绑定标志（`ShowNext` 是 `NonPublic|Instance`）、
签名（`ShowNext` 零参数，调用点却传了一个 string）。
`GetMethod("ShowNext", Public|Static)` **恒返回 null**，于是：

```csharp
// UIAndSigns/UIAndSigns.cs:295-298
if (BossRushEagerReflectionCache.NotificationText_ShowNext != null)   // 恒 false
{
    BossRushEagerReflectionCache.NotificationText_ShowNext.Invoke(null, new object[] { msg });
}
```

另外两条出口也不通：

- `statusMessage`（`UIAndSigns.cs:47` 声明、`:288` 赋值）全仓库**没有任何渲染方**——
  `grep -rn "statusMessage"` 只命中声明、赋值、一处注释与 `Integration/BossRushIntegration_StartAndScene.cs:461` 的清空；
  `ModBehaviour.OnGUI()`（`ModBehaviour.cs:622`）只调 `DrawDebugToolsRuntimeGui()`。
- `UpdateMessage_UIAndSigns`（`UIAndSigns.cs:204-212`）只 `messageTimer -= Time.deltaTime`，
  函数体里那句注释「由于未引用UI库，暂时只打印日志」正是当年的遗留说明。
- `DevLog` 带 `[Conditional("BOSSRUSH_DEV")]`，正式构建整条剥离。

对照组：`ShowBigBanner_UIAndSigns`（`UIAndSigns.cs:433-467`）用的是
`NotificationText.Push(text)`——公开静态、签名匹配，横幅是能显示的。
两条通道一条通一条不通，正好说明这不是「设计上就不显示」。

#### 失败场景

全仓库 136 处 `ShowMessage(...)`（`grep -rn "ShowMessage(" --include=*.cs`，已排除定义行）全部无声。
本轮新增/改动的调用点里至少两处**修复目的就是「别静默失败」**，因此这两个修复实际未生效：

1. `WavesArena/BossRushEntryFlow.cs:207-217`（本轮新增分支）：
   Mode G 确认页未能打开时退款并 `ShowMessage("宿命回响入口准备失败，请稍后重试。")`。
   代码注释写着「只退款不解释等于静默失败」——而这句解释玩家看不到，
   玩家的观感仍然是「带着船票和信物进图，什么都没发生」。
2. `WavesArena/WavesArenaEntryAndTeleport.cs:41`（本轮新增 Mode H 真实资产风险门）：
   `ShowMessage(L10n.T(ModeHRuntimeGates.ResolveLegacyBlockedMessageKey()))` 后 `return`，
   玩家点路牌毫无反应且没有任何提示，与「入口坏了」无法区分。

其余高频受害项：`"BossRush已经在进行中！"`、
`"Boss池为空！请至少启用一个Boss。(Ctrl+F10 打开设置)"`（`WavesArenaBossSpawning.cs:466`）、
`"所有敌人已击败！你赢了！"`（`LootAndRewardsVictoryRewards.cs:68`）、
`"BossRush挑战失败！"`（`LootAndRewardsRandomBossLoot.cs:188`）。

#### 玩家影响

任何走 `ShowMessage` 的失败原因、入场拒绝、胜负结算文案在正式构建里都不出现。
玩家只能看到「点了没反应」。dev 构建下 `DevLog` 还在，所以开发期一直看得见——
这正是它长期没被发现的原因。

#### 建议修复

`ShowMessage_UIAndSigns` 直接调 `Duckov.UI.NotificationText.Push(msg)`
（公开静态，不需要反射），并保留既有的 `GameplayValidationSuppressNotifications` 门。
`BossRushEagerReflectionCache.NotificationText_ShowNext` 随之删除（零调用点）。
若希望与 `ShowBigBanner` 区分视觉层级，可复用 `EnsureNotificationDurationAtLeastTwoSeconds`
之外的时长，或走 `BossRushUI` 共享库另建轻提示——但**不要**保留当前这条恒 null 的反射路径。
`statusMessage` / `messageTimer` 若不再需要应一并删除，否则会继续误导后来人。

#### 验证需求

- Windows 真编译。
- 正式（非 dev）构建实机：BossRush 已在进行中时再点一次路牌，确认屏幕出现提示；
  Boss 池清空后点路牌确认出现「Boss池为空」。

### F-3：空投「翻箱保护」的判据选错——玩家开着战利品界面时 `InteractableBase.Interacting` 已经是 false，CR-2026-09-03-010 的修复实际不生效

- **严重级**：P1 Major（本轮 P1 修复未达成目的；玩家眼前的箱子仍会连同未取走的物品一起消失）
- **兼容分类**：`COMPAT`
- **状态**：新发现，静态确认（逐层读官方反编译源码），未运行验证

#### 位置

- `RandomEvents/RandomEventAirdropHold.cs:83`（`if (!_validationCrate.Interacting) return;`）
- `RandomEvents/RandomEventAirdropHold.cs:85-87`（只在 `remaining < 3s` 才续期）
- `RandomEvents/RandomEventAirdropHold.cs:146-150`（注释里的官方关界面链条描述有误）
- `RandomEvents/RandomEventEffectsBridge_Loot.cs:69-72`（`EnsureLocalInventory` 造出的 Inventory）
- 官方：`鸭科夫源码/TeamSoda.Duckov.Core/InteractableLootbox.cs:225-256`、`:286-296`、`:305-311`、`:368-372`
- 官方：`鸭科夫源码/TeamSoda.Duckov.Core/CA_Interact.cs:186-192`
- 官方：`鸭科夫源码/ItemStatsSystem/Inventory.cs:785`

#### 证据

判据本身：

```csharp
// RandomEvents/RandomEventAirdropHold.cs:83
// InteractableBase.Interacting == (interactCharacter != null)：无副作用的纯属性，
// openning 与 looting 两个子态都覆盖。
if (!_validationCrate.Interacting) return;
```

但官方 `InteractableLootbox` 在进入 `looting` 的**下一帧**就会自己停掉交互：

```csharp
// 鸭科夫源码/TeamSoda.Duckov.Core/InteractableLootbox.cs（OnUpdate，looting 分支）
Item item = this.FindFistUninspectedItem();
if (!item)
{
    base.StopInteract();      // ← 没有待鉴定物品就立刻停交互
    return;
}
```

```csharp
// 同文件，FindFistUninspectedItem
if (!this.Inventory) return null;
if (!this.Inventory.NeedInspection) return null;   // ← 这一条决定一切
return this.Inventory.FirstOrDefault((Item e) => !e.Inspected);
```

空投箱的 `Inventory` 是 Mod 自己造的本地库存：
`CreateRandomEventAirdropLootbox` 调 `InteractableLootboxInventoryHelper.EnsureLocalInventory`，
后者反射调官方 `private void CreateLocalInventory()`，其实现是
`Inventory inventory = base.gameObject.AddComponent<Inventory>();`。
运行期 `AddComponent` 出来的组件字段取 C# 默认值，而
`Inventory.needInspection` 声明为 `private bool needInspection;`（`Inventory.cs:785`，**无初始值**）。
全官方源码里唯一写它的地方是 `CharacterMainControl.cs:1311`
（`inventory.NeedInspection = interactableLootbox.needInspect;`，只在**尸体掉落箱**创建路径上），
空投箱这条路径不经过它。

结论：空投箱 `Inventory.NeedInspection == false` → `FindFistUninspectedItem()` 恒返回 null
→ 进 `looting` 的下一帧就 `StopInteract()` → `CA_Interact.OnStop()` →
`interactingTarget.InternalStopInteract()`（`CA_Interact.cs:186-192`）→ `interactCharacter = null`
→ **`Interacting == false`**。

而战利品界面此时**不会关**：`OnInteractStop` 派发的静态事件
`InteractableLootbox.OnStopLoot` 在整个官方源码里**没有任何订阅方**
（`grep -rn "OnStopLoot" 鸭科夫源码/` 只有三处：事件声明、派发点、以及
`LootView.cs:196` 那个从未被 `+=` 的私有方法）。所以界面照常开着，玩家继续搬东西。

`Mod 侧 lootbox.needInspect = true`（`RandomEventEffectsBridge_Loot.cs:64`）救不了这条：
`needInspect` 是 `InteractableLootbox` 上的字段，只被 `LootView` 用于拾取门控
（`LootView.cs:490` / `:553`），`FindFistUninspectedItem` 读的是 `Inventory.NeedInspection`。

#### 失败场景

事件时长 45s，`OnTick` 只在 `remaining < AirdropHoldOpenGraceSeconds(3s)` 时才尝试续期
（`RandomEventAirdropHold.cs:85-87`）。要续上，必须在第 42~45 秒这个 3 秒窗口里
`Interacting == true`——而按上文，`Interacting` 只在「按住 E 的读条阶段」为真，
界面一开就变假。于是：

玩家在第 40 秒跑到箱子边，按 E 开箱（读条 1~2s），界面打开、`Interacting` 转 false，
玩家开始往背包里搬东西 → 第 45 秒事件到期 → `EndActiveEvent(Expired)` →
`OnCleanup` → `CloseAirdropLootViewIfOpen()` 关掉界面 → `ClearScope` 销毁箱子。
**这正是 CR-2026-09-03-010 描述的原始症状，一字不差。**

唯一能触发续期的窗口是「读条阶段恰好横跨第 42~45 秒」，续 3 秒后玩家还是同样结局。

#### 附带：注释与实现不符（同一处）

`CloseAirdropLootViewIfOpen`（`RandomEventAirdropHold.cs:146-150`）写着
「官方路径优先：StopInteract 同步走到 OnInteractStop -> InteractableLootbox.OnStopLoot
-> LootView.OnStopLoot -> Close()」。这条链在最后一跳断了（`OnStopLoot` 无订阅方），
真正关掉界面的是它下面那段手动 `view.Close()` 兜底。注释误导后来人，
且会让人以为「官方链条已覆盖」而删掉兜底。

#### 玩家影响

权重最高的事件（30/133 ≈ 22%）在最常见的用法下仍然当着玩家的面吞掉整箱战利品，
且玩家的界面被强制关闭。修复上线但症状照旧，属于「已修但没修好」。

#### 建议修复

把判据从「箱子正被交互」换成「战利品界面正开在这一箱上」——这才是玩家实际在翻箱的状态。
推荐做法是**静态事件闩**，而不是每帧读 `LootView.TargetInventory`：

- `OnTrigger` 时订阅官方静态事件 `InteractableLootbox.OnStartLoot`（命名方法 + 缓存委托，
  AGENTS 4.6），回调里 `_lootSessionOnCrate = (lootbox == _validationCrate)`；
  `OnCleanup` 成对退订。
- `OnTick` 的判据改为
  `_validationCrate.Interacting || (_lootSessionOnCrate && LootView.Instance != null && LootView.Instance.open)`，
  并在 `LootView.Instance.open` 转 false 时把闩清零。

**不要**在 OnTick 里直接读 `LootView.Instance.TargetInventory`：它内部会走
`targetLootBox.Inventory`（`LootView.cs:56-60`），而 `InteractableLootbox.Inventory`
是**非纯 getter**——每次调用都会 `orCreateInventory.DisplayNameKey = this.displayNameKey`，
`hasBeenInspectedInLootBox` 为真时还会 `SetMarkerUsed()`
（`InteractableLootbox.cs:107-142`）。放进每帧路径正好踩中
`RandomEventAirdropHoldGuard` 想挡的那件事。

硬帽、只延 Expired、关界面先于 ClearScope 三条不变式全部保持不变；
`RandomEventAirdropHoldGuard` 需要同步新增「闩必须成对订阅/退订」的断言。

顺带修掉上面那段错误注释。

#### 验证需求

- Windows 真编译 + `RandomEventAirdropHoldGuard` 同步后重跑。
- 实机：F3 强制触发 E1，跑到箱子边开箱后原地翻箱 60 秒以上，
  确认箱子不在 45 秒消失、且到 120 秒硬帽后才强制关界面并销毁。

### F-4：`MagicBlendInitializationOrderPatch.Settled` 只在宿主销毁时清空，注释却写「跨场景清」——整会话无界增长且失效 ID 不退表

- **严重级**：P3 Note（长会话内存缓慢增长；注释与实现不一致，误导后续维护）
- **兼容分类**：`SAFE`
- **状态**：新发现，静态确认

#### 位置

- `Patches/Compatibility/MagicBlendInitializationOrderPatch.cs:26-33`（注释）
- `Patches/Compatibility/MagicBlendInitializationOrderPatch.cs:141-146`（`ResetStaticCaches` 注释同样写「切场景 / 宿主销毁」）
- `Utilities/AlwaysOnRuntimeHooks.cs:269-280`（唯一调用点）

#### 证据

注释：

```csharp
// Patches/Compatibility/MagicBlendInitializationOrderPatch.cs:30-32
/// 但跨场景会累积失效 ID，由 <see cref="ResetStaticCaches"/> 清。
...
/// 复位两张静态表（切场景 / 宿主销毁）。
```

而 `ResetStaticCaches()` 全仓库只有一个调用点，位于
`Utilities/AlwaysOnRuntimeHooks.cs` 的 `CleanupAlwaysOnRuntimeOnDestroy()`
（方法起始行 `:245`，调用在 `:273`）——那是**宿主销毁**路径。
同文件的场景卸载钩子 `OnSceneUnloadAlwaysOnRuntime()`（`:239`）里没有它。

`Settled` 会为**每一个触发过动画状态切换的 Animator**（含完全没挂 `MagicBlending` 的）
记一个 `instanceID`（`:60-64`）。玩家一次会话里跨十几张图，表会攒到数万条 int。

#### 影响

内存开销本身很小（`HashSet<int>`，万级也就百 KB 量级），真正的问题是
「注释说会在切场景清、实际不会」——下次有人按注释判断生命周期就会判错。
另有一条**理论**风险：Unity 的 `GetInstanceID()` 在对象销毁后可能被回收复用，
复用到 `Settled` 里的旧 ID 会让新角色跳过本该做的延后（回到官方异常路径）。
该风险取决于具体 Unity 版本的 ID 分配策略，**未验证**，放在此处仅作备注。

#### 建议修复

把 `ResetStaticCaches()` 追加到 `OnSceneUnloadAlwaysOnRuntime()`（与注释一致），
或者把注释改成「仅宿主销毁」。二选一，别让两者继续背离。

---

### F-5：`Interactables/BossRushLootboxInteractables.cs` 的三个运行时交互体没做 1c69f85 的 `otherInterablesInGroup` 前置初始化

- **严重级**：P3 Note（当前后果被 `try/catch` 与官方字段初始化器双重兜住，但模式不一致）
- **兼容分类**：`SAFE`
- **状态**：新发现，静态确认

#### 位置

- `Interactables/BossRushLootboxInteractables.cs:598-617`（`BossRushDeleteLootboxInteractable.Awake`）
- `Interactables/BossRushLootboxInteractables.cs:691-707`（`BossRushCarryInteractable.Awake`）
- `Interactables/BossRushLootboxInteractables.cs:1061-1080`（`TrashCanInteractable.Awake`）
- 对照正确写法：`RandomEvents/RandomEventEffectsBridge_Spawn.cs:858-864`（`RandomEventMerchantShopInteractable.Awake` 先 `NPCInteractionGroupHelper.GetOrCreateGroupList` 再 `base.Awake()`）

#### 证据

官方 `InteractableBase.Awake()`（`鸭科夫源码/TeamSoda.Duckov.Core/InteractableBase.cs:164-190`）
在末尾无条件遍历 `otherInterablesInGroup`：

```csharp
foreach (InteractableBase interactableBase in this.otherInterablesInGroup)   // :180
{ ... }
this._interactbleList = new List<InteractableBase>();                        // :190
```

运行时 `AddComponent` 出来的组件该字段为 null → 第 180 行 NRE →
第 190 行不执行。commit `1c69f85` 正是为此给
`DailyReportInteractable` / `CampaignBoardInteractable` / `ShowcaseInteractable`
补了前置初始化，但同一模式的这三个类没跟上。

#### 为什么当前不炸

1. 三处的 `base.Awake()` 都包在 `try { ... } catch {}` 里，异常被吞，连日志都没有；
2. 被跳过的 `_interactbleList = new List<>()` 有**字段初始化器**兜底
   （`InteractableBase.cs:669`：`private List<InteractableBase> _interactbleList = new List<InteractableBase>();`），
   构造时已经非 null；
3. `GetInteractableList()`（`:19-34`）里 `!this.interactableGroup ||` 的短路让
   null 的 `otherInterablesInGroup` 不会被解引用——这三个类的 `interactableGroup` 保持默认 false。

所以现在是「每次创建静默抛一次被吞的 NRE」，功能无损。
但只要将来有人给它们置 `interactableGroup = true`（比如挂子交互），第 22 行立刻 NRE。

#### 建议修复

三处 `base.Awake()` 之前各加一行
`NPCInteractionGroupHelper.GetOrCreateGroupList(this, "[BossRushLootbox]");`，
与 1c69f85 建立的模式对齐。

---

### F-6：共享刷怪核心在提交回调失败销毁角色时不解绑掉落追踪

- **严重级**：P3 Note（既有顺序问题；本轮新并联的两个掉落服务自带剪枝，实际影响很小）
- **兼容分类**：`SAFE`
- **状态**：新发现，静态确认

#### 位置

- `Utilities/EnemySpawnCore.cs:447-465` 与 `:469-475`（分帧路径：先登记后提交）
- `Utilities/EnemySpawnCore.cs:1023-1042` 与 `:1054-1062`（同帧路径：同样先登记后提交）
- `Utilities/EnemySpawnCore.cs:483-525`（`CompleteModeEFSpawnPostprocessJobFailure`，销毁角色但不清追踪）

#### 证据

两条路径都是 `RegisterBossRandomLootTracking(character, ...)` 在前、
`InvokeSpawnCoreCommitCallback(...)` 在后；提交失败时：

- 分帧路径 → `CompleteModeEFSpawnPostprocessJobFailure(job, "commit_failed", destroyCharacter: true)`
  → `UnityEngine.Object.Destroy(character.gameObject)`（`:508-521`），**没有** `ClearBossRandomLootTracking`；
- 同帧路径 → `UnityEngine.Object.Destroy(character.gameObject)`（`:1058`），同样没有。

留下的残留：`bossSpawnTimes` / `bossOriginalLootCounts` / `countedDeadBosses` /
`trackedBossLootHooks` / `bossRushLootboxPathBosses` 五张表里各一条以「已销毁角色」为 key 的条目，
外加本轮新并联的 `PetNestDropService` / `AffixForgeStoneDropService` 两个 per-character handler
（`LootAndRewards/LootAndRewards.cs:339-342`）。

#### 为什么影响有限（不升级为 P2）

- `RefreshBossRushLootboxPathTrackingForTrackedBosses()`（`LootAndRewards/LootAndRewards.cs:445-490`）
  会把 null key 逐条摘掉；
- `AffixForgeStoneDropService.TryTrack` 每次注册前跑 `PruneDestroyedEntries()`
  （`Integration/AffixForge/AffixForgeStoneDropService.cs:62-70`），PetNest 侧同形；
- 提交回调失败本身是罕见路径（随机事件那条 `ConfigureRandomEventIntruderBoss` 只在
  `spawnContext == null || character == null` 时返回 false）。

#### 建议修复

两条销毁分支各补一次 `ClearBossRandomLootTracking(character)`（它自身幂等、null 安全），
或把 `RegisterBossRandomLootTracking` 挪到提交回调**之后**。后者更彻底，
但要确认没有 commit 回调依赖「进 commit 时掉落追踪已就位」。

### F-7：随机事件的商人与巡游鸭没接距离休眠解除，玩家跑远再回来商人会消失

- **严重级**：P3 Note（CR-2026-09-02-004 建立的口径在两条新生成路径上没跟上）
- **兼容分类**：`COMPAT`
- **状态**：新发现，静态确认，未运行验证

#### 位置

- `RandomEvents/RandomEventEffectsBridge_Spawn.cs:442`（商人 `CreateCharacterAsync(position, facing, sceneBuildIndex, null, false)`）
- `RandomEvents/RandomEventEffectsBridge_Spawn.cs:312`（巡游鸭同形调用）
- 对照：`Utilities/EnemySpawnCore.cs:438` / `:1013`、`ModBehaviour.cs:1209` 等 7 个已接入点

#### 证据

官方注册点是 `CharacterMainControl.SetRelatedScene`：

```csharp
// 鸭科夫源码/TeamSoda.Duckov.Core/CharacterMainControl.cs:1891-1900
public void SetRelatedScene(int _relatedScene, bool setActiveByPlayerDistance = true)
{
    ...
    if (setActiveByPlayerDistance)
    {
        SetActiveByPlayerDistance.Register(base.gameObject, this.relatedScene);
    }
}
```

`CharacterRandomPreset.setActiveByPlayerDistance` 默认 `true`（`CharacterRandomPreset.cs:231`），
`CreateCharacterAsync` 传入 `relatedScene != -1` 时会走这条注册
（AGENTS.md 第 14 节 2026-09-01 条目与 CR-2026-09-02-004 都已确认这条链；
`Integration/NPCs/DuckNpc/DuckNpcFactory.cs:326` 用 `SetRelatedScene(sceneIndex, false)` 显式退出，
反证 Mod 侧已知这套机制）。

`SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep` 的 7 个调用点里
（`grep -rn "ReleaseFromPlayerDistanceSleep"`）没有这两条随机事件路径。

#### 失败场景

`SetActiveByPlayerDistance.FixedUpdate` 每帧对登记对象执行
`gameObject.SetActive(距玩家 < 100m)`（`SetActiveByPlayerDistance.cs:150-158`）。
神秘商人时长 60s、生成在玩家身前 6m：玩家若在这 60 秒里追着 Boss 跑出 100m 再回来，
商人会被关掉——回来时看不见也交互不了，事件结束时才被销毁。
巡游鸭（22s，起点距玩家 22m）同理，但纯装饰，影响更小。

#### 玩家影响

「神秘商人路过」偶发地变成空事件：横幅播了、方位指了，跑回去人不见了。
不会卡波次（不参与波次账），也不会泄漏（事件结束仍会 Destroy）。

#### 建议修复

两处 `CreateCharacterAsync` 成功之后各加一次
`SpawnedEnemyActivationHelper.ReleaseFromPlayerDistanceSleep(character)`；
巡游鸭那条放在现有 `duck.gameObject.SetActive(true)` 之后
（`RandomEventEffectsBridge_Spawn.cs:337-339`），商人那条放在 `SetTeam` 之后
（`:457`）。helper 自身 no-throw、null 安全。
`tests/SpawnedEnemyActivationGuard.py` 可顺带把 `RandomEvents/RandomEventEffectsBridge_Spawn.cs`
加进「必须接保护的路径」清单。

## 二、未验证线索（需实机或 owner 确认）

以下条目**不是** confirmed finding，只有静态线索或属于产品取舍，需实机或 owner 拍板。

1. **随机事件与「鸭王征程」并存未做任何门控（需 owner 拍板）**
   `RandomEvents/RandomEventModeGate.cs:88` 的允许名单是 `IsActive || IsModeDActive`，
   没有任何战役相关判据；`tests/RandomEventsModeGateGuard.py` 的禁入名单里也没有战役。
   于是第 1 章（标准图、无伤目标）与终章（竞技场打「冠军之影」）期间随机事件照常触发：
   血月给全场敌人 +25% 移速 / +20% 伤害，Boss 乱入再加一只计划外 Boss。
   这不构成状态机错误（无伤判定只看玩家是否挨打），但**显著改变契约难度**，
   且玩家无法预期。是否要在战役章节期间禁用（或只禁惩罚性事件）属产品决策。

2. **金鸭雨可能发不满 30000**
   `RandomEvents/RandomEventEffectsBridge_Loot.cs:493-498`：
   `amount = Math.Min(amount, cash.MaxStackCount)`（`MaxStackCount > 0` 时）。
   30000 分 10~15 堆，每堆 2000~3000；若现金物品（TypeID 451）的 `MaxStackCount`
   小于每堆金额，玩家实际拿到的会少于 `GoldenDuckRainTotalCash`。
   官方 `MaxStackCount` 的实际值需实机读，静态查不到。

3. **`MagicBlending` 永不初始化的角色会被本补丁永久静音**
   `MagicBlendInitializationOrderPatch.Prefix` 在 `blending != null && !IsInitialized(blending)`
   时一律 `return false`（跳过官方实现）。官方 `MagicBlending._isInitialized`
   只在 `Start()` 里置真（`鸭科夫源码/.../MagicBlending.cs:189` → `InitializeMagicBlending()` → `:150`）。
   组件被禁用 / `Start` 抛异常（例如 `_rigComponent` 为 null）时它永远为 false，
   该 animator 的**所有** MagicBlend 状态回调会被永久吞掉，且每个新状态哈希还会各起一条
   10 帧协程。原行为是抛异常，新行为是静默失效——两者都不好，但失效模式不同。
   需实机确认是否存在这种角色。

4. **`_holdExtendedSeconds` 的硬帽语义与玩家预期**（若采纳 F-3 的修复）
   120s 上限到顶后会强制关界面并销毁箱子。玩家正在翻箱时被强制关界面，
   体验上仍是「东西没拿完就没了」，只是从 45s 推到 165s。是否要改成
   「到顶后只停止延期、等玩家自己关界面再销毁」需 owner 拍板（当前实现是前者，owner 已拍过一次）。

5. **`ShowMessage` 修好之后的信息密度**（F-2 的后续）
   136 处提示一旦全部接通官方 `NotificationText.Push`，会与 `ShowBigBanner` 共用同一条通知队列。
   `ShowBigBanner_UIAndSigns` 有 1.25s 去重窗口，`ShowMessage` 没有。
   接通前建议先评估战斗中的刷屏量，必要时给 `ShowMessage` 也加去重。

## 三、已核对且干净的关键面

### 3.1 Harmony 补丁逐项核对表（范围内 = `Patches/` 全部 21 条）

`RandomEvents/`、`WavesArena/`、`LootAndRewards/`、`Utilities/` 四个目录
**零 `[HarmonyPatch]`**（`grep -rn "\[HarmonyPatch" RandomEvents/ WavesArena/ LootAndRewards/ Utilities/` 无命中），
与随机事件桥「零新增 Harmony patch」的声明一致。

| # | 补丁位置 | 目标 | 官方定义位置 | 重载 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 1 | `Patches/BaseHub/BaseHubBoatPatch.cs:13` | `InteractableBase.Start` | `InteractableBase.cs:194` `protected virtual void Start()` | 唯一 | ✅ |
| 2 | `Patches/BaseHub/BaseHubShopAwakePatch.cs:13` | `StockShop.Awake` | `Duckov/Economy/StockShop.cs:155` `private void Awake()` | 唯一 | ✅ |
| 3 | `Patches/Combat/BossLethalHealthProtectionPatch.cs:16` | `Health.Hurt`（`nameof`） | `Health.cs:298` `public bool Hurt(DamageInfo)` | 唯一 | ✅ |
| 4 | `Patches/Combat/BossLethalHealthProtectionPatch.cs:116` | `Health.CurrentHealth` setter | `Health.cs:98/104` 公开属性含 setter | — | ✅ |
| 5 | `Patches/Combat/CharacterOnDeadPatch.cs:14` | `CharacterMainControl.OnDead` | `CharacterMainControl.cs:1279` `private void OnDead(DamageInfo)` | 唯一 | ✅ |
| 6 | `Patches/Combat/ProjectileHalfObstaclePatch.cs:13` | `Projectile.Init(ProjectileContext)` | `Projectile.cs:181`（另有 `:41` 的无参重载） | **两个**，已显式指定类型数组 | ✅ |
| 7 | `Patches/Compatibility/MagicBlendInitializationOrderPatch.cs:20` | `MagicBlendState.OnStateEnter` | `KINEMATION/MagicBlend/Runtime/MagicBlendState.cs:10` | 该类只声明 3 参重载（基类 `StateMachineBehaviour` 的 4 参重载未被覆写） | ✅（见下方备注） |
| 8 | `Patches/Death/DeadBodyAppendPatch.cs:6` | `DeadBodyManager.AppendDeathInfo` | `Duckov/DeadBodyManager.cs:23` `private void AppendDeathInfo(DeathInfo)` | 唯一 | ✅ |
| 9 | `Patches/Death/DeadBodySpawnPatch.cs:12` | `DeadBodyManager.SpawnDeadBody` | `Duckov/DeadBodyManager.cs:109` `private UniTask SpawnDeadBody(DeathInfo)` | 唯一 | ✅ |
| 10 | `Patches/Death/DeadBodyTouchedPatch.cs:12` | `DeadBodyManager.NotifyDeadbodyTouched` | `Duckov/DeadBodyManager.cs:121` `private static void NotifyDeadbodyTouched(DeathInfo)` | 唯一 | ✅ |
| 11 | `Patches/Death/TombLootboxPatch.cs:14` | `InteractableLootbox.CreateFromItem` | `InteractableLootbox.cs:390` `public static` | 唯一（含默认参数） | ✅ |
| 12 | `Patches/Economy/StockShopGetItemInstanceDirectPatch.cs:38` | `StockShop.GetItemInstanceDirect` | `Duckov/Economy/StockShop.cs:144` `public Item GetItemInstanceDirect(int)` | 唯一 | ✅ |
| 13 | `…/ItemAssetsCollectionDynamicRegistrationPatch.cs:174` | `ItemAssetsCollection.GetMetaData(int)` | `ItemStatsSystem/ItemAssetsCollection.cs:179` | 显式类型数组 | ✅ |
| 14 | `…:184` | `ItemAssetsCollection.GetPrefab(int)` | `ItemAssetsCollection.cs:195` | 显式 | ✅ |
| 15 | `…:194` | `ItemAssetsCollection.InstantiateSync(int)` | `ItemAssetsCollection.cs:147` | 显式 | ✅ |
| 16 | `…:204` | `ItemAssetsCollection.InstantiateAsync(int)` | `ItemAssetsCollection.cs:136` | 显式 | ✅ |
| 17 | `…:214` | `ItemAssetsCollection.InstantiateAsync_Local(int)` | `ItemAssetsCollection.cs:124`（**实例**方法，非 static） | 显式 | ✅（Harmony 支持实例方法，prefix 未声明 `__instance` 合法） |
| 18 | `…:224` | `ItemTreeData.InstantiateAsync(ItemTreeData)` | `ItemStatsSystem/…/ItemTreeData.cs:102` | 显式 | ✅ |
| 19 | `…:234` | `InventoryData.LoadIntoInventory(InventoryData, Inventory)` | `ItemStatsSystem/…/InventoryData.cs:34` | 显式 | ✅ |
| 20 | `…:256` | `ItemAssetsCollection.InstantiateFallbackItem(int)` | `ItemAssetsCollection.cs:171` | 显式 | ✅ |
| 21 | `Patches/UI/ItemUIUtilitiesElementFactorFormatPatch.cs:54` | `ItemUIUtilities.GetPropertyValueTextPair(Item)` | `ItemUIUtilities.cs:209` 扩展方法 | 唯一，已显式 | ✅ |

**#7 备注**：`MagicBlendState` 只覆写了 3 参 `OnStateEnter`，
`AccessTools.Method(type, name)` 先查声明成员，因此当前不歧义。
但若官方将来补上 4 参重载（`OnStateEnter(Animator, AnimatorStateInfo, int, AnimatorControllerPlayable)`），
这条会立刻变成 `AmbiguousMatchException`。按 `Patches/AGENTS.md`「官方方法有重载时必须显式指定参数类型」，
**建议现在就补上类型数组**（`new Type[]{ typeof(Animator), typeof(AnimatorStateInfo), typeof(int) }`），
成本为零、消除未来漂移。

**重复命中同一目标（已知、非本轮引入，未发现冲突）**：
`Health.Hurt` 同时被 `Patches/Combat/BossLethalHealthProtectionPatch` 与 `ModeE/ModeEHarmonyPatch` 前缀；
`StockShop.GetItemInstanceDirect` 同时被 `Patches/Economy/…` 与 `ModeE/ModeEHarmonyPatch` 前缀
（后者由 `GetModeEShellShopPatchDisposition` 分流，前者据此早返）；
`Projectile.Init(ProjectileContext)` 同时被 `Patches/Combat/…` 与 `ZombieMode/…` 前缀。
三组都没有声明 Harmony priority，执行顺序由 Harmony 默认（按 owner/加载顺序）决定——
当前三组的语义互不依赖顺序，属可接受；改动其中任一方前需重新评估。

### 3.2 反射 / 字符串绑定逐项核对表

| 绑定位置 | 目标字符串 | 官方定义 | 结论 |
| --- | --- | --- | --- |
| `Patches/Compatibility/MagicBlendInitializationOrderPatch.cs:25` | `AccessTools.Field(MagicBlending, "_isInitialized")` | `MagicBlending.cs:350` `private bool _isInitialized;` | ✅ |
| `Common/Infrastructure/BossRushEagerReflectionCache.cs:59` | `InteractableBase."otherInterablesInGroup"`（NonPublic\|Instance） | `InteractableBase.cs:663` `private List<InteractableBase>` | ✅ |
| `…:63` | `MultiInteraction."interactables"` | `MultiInteraction.cs:50` `private List<InteractableBase>` | ✅ |
| `…:67` | `NotificationText."duration"` | `Duckov/UI/NotificationText.cs:70` `private float`（绑定标志含 NonPublic） | ✅ |
| `…:69` | `NotificationText."durationIfPending"` | `NotificationText.cs:74` `private float` | ✅ |
| `…:73` | `StockShop."merchantID"` | `StockShop.cs:423` `private string` | ✅ |
| `…:75` | `StockShop."itemInstances"` | `StockShop.cs:465` `private Dictionary<int, Item>` | ✅ |
| `…:77` | `StockShop."accountAvaliable"` | `StockShop.cs:440` `private bool`（官方拼写就是 `Avaliable`） | ✅ |
| `…:79` | `StockShop."refreshAfterTimeSpan"` | `StockShop.cs:432` `private long` | ✅ |
| `…:81` | `StockShop."refreshStockOnStart"` | `StockShop.cs:448` `private bool` | ✅ |
| `…:83` | `StockShop."lastTimeRefreshedStock"` | `StockShop.cs:462` `private long` | ✅ |
| `…:87` | `CharacterRandomPreset."characterIconType"` | `CharacterRandomPreset.cs:190` `private CharacterIconTypes` | ✅ |
| **`…:91`** | **`NotificationText.GetMethod("ShowNext", Public\|Static)`** | **官方是 `NotificationText.cs:50` `private UniTask ShowNext()`（实例、零参）** | ❌ **恒 null，见 F-2** |
| `Utilities/InteractableLootboxInventoryHelper.cs:68` | `InteractableLootbox."CreateLocalInventory"` | `InteractableLootbox.cs:368` `private void CreateLocalInventory()` | ✅ |
| `…:79` | `InteractableLootbox."inventoryReference"` | `InteractableLootbox.cs:487` `private Inventory` | ✅ |
| `BossLootBoxLoaderReflection` `randomCount` | `LootBoxLoader.cs:154` `private Vector2Int` | | ✅ |
| 同上 `qualities` | `LootBoxLoader.cs:172` `private RandomContainer<int>` | | ✅ |
| 同上 `tags` | `LootBoxLoader.cs:161` `private RandomContainer<Tag>` | | ✅ |
| 同上 `excludeTags` | `LootBoxLoader.cs:165` `private List<Tag>` | | ✅ |
| 同上 `randomPool` | `LootBoxLoader.cs:176` `private RandomContainer<LootBoxLoader.Entry>` | | ✅ |
| 同上 `fixedItems` | `LootBoxLoader.cs:191` `private List<int>` | | ✅ |
| 同上 `fixedItemSpawnChance` | `LootBoxLoader.cs:196` `private float` | | ✅ |
| 同上 嵌套 `Entry.itemTypeID` | `LootBoxLoader.cs:204/209` `public int` | | ✅ |
| 同上 `RandomContainer<T>.entries` / `Entry.value` / `Entry.weight` | `RandomContainer.cs:250/257/260` 全为 public | | ✅ |

编译期绑定（非反射，csc 保证存在，此处只核对**语义**）：
`SetActiveByPlayerDistance.Unregister(GameObject,int)` 返回 `bool`
（`SetActiveByPlayerDistance.cs:47-51`，因此 `while (Unregister(...)) {}` 会终止，不会死循环）；
`InteractableBase.Interacting`（`:121`，纯属性 `interactCharacter != null`）、
`InteractableBase.StopInteract()`（`:319`）；
`LootView.Instance` / `TargetInventory`（`LootView.cs:20/54`，
**注意 `TargetInventory` 内部会走非纯 getter `InteractableLootbox.Inventory`**）；
`CharacterMainControl.SetSleeping(bool)`（`:191`）、`SetTeam(Teams)`（`:1083`）、
`BeforeCharacterSpawnLootOnDead`（`:133`）、`dropBoxOnDead`（`:3250`）、
`SetRelatedScene(int,bool)`（`:1891`）；
`Health.OnDead` / `OnHurt` 静态事件（`:128` / `:123`）、`SetInvincible`（`:508`）；
`WeatherManager.Instance` / `ForceWeather` / `ForceWeatherValue` / `SetForceWeather(bool,Weather)`
（`WeatherManager.cs:23/28/33/168`，静态方法内部裸用 `Instance`，Mod 侧两处均已先判空）。

### 3.3 其他已核对且未发现问题的面

- **编译清单（AGENTS 4.1）**：本轮 16 个新增 `.cs`
  （12 个 `RandomEvents/*`、`Config/ConfigRandomEvents.cs`、`Utilities/SpawnedEnemyActivationHelper.cs`、
  `Utilities/ModBossPresetLookup.cs`、`Patches/Compatibility/MagicBlendInitializationOrderPatch.cs`）
  逐个 `grep` 命中 `compile_official.bat`，无遗漏。
- **ModConfig 白名单陷阱**：`BossRush_RandomEventsFrequency` 五处接线齐全——
  批量加载 `Config/Config.cs:405`、单键热更新 `Config/Config.cs:600`、注册 `Config/Config.cs:892`、
  白名单 `Config/ConfigModConfigKeys.cs:49`、内容开关恒开 `Config/ConfigContentSystemSwitches.cs:41`。
  总开关 `randomEventsEnabled` 按「内容系统恒开」口径不进 UI，符合既定约定。
- **AGENTS 4.11（变异不变式）**：`RandomEvents/` 全目录无 `LootChange`、无 loot 质量/数量/类型消费；
  空投箱走自建 `LootBoxLoader` 配置，与共享变异系统零耦合。
- **AGENTS 4.6（订阅幂等 + 退订）**：
  `RandomEventIntruderDeathWatcher` 用命名方法 + 缓存 `UnityAction<DamageInfo>` 实例，
  `Subscribe`/`Unsubscribe` 成对且幂等，`OnCleanup` 与 `OnTick` 两处剪枝都走
  `RunScopedRegistry.ForEachReverse`（遍历中删除安全）；
  `Utilities/PlayerLifecycleRuntimeHooks.cs` 的 8 对 `Health.OnDead/OnHurt` 全部命名 handler，
  注册（`ModBehaviour.cs:614`）与退订（`ModBehaviour.cs:771`）各一处、成对。
- **权重抽样**：`RandomEventDirector.PickWeightedEvent` / `BuildPickBuffer` 无除法，
  全零/负权重时返回 null 并进冷却，不会除零、不会死循环；
  `MaxEventsPerRunByFrequency = {0,2,3,5}` 与 tier clamp `[1, len-1]` 匹配，无越界。
- **HUD 热路径与 UI 规范（AGENTS 4.14）**：`RandomEventHud` 用
  `BossRushUI.CreateCanvasRoot` + `BossRushUILayers.HudOverlay` + `BossRushUIColors` token，
  全部 `raycastTarget = false`；只在「事件切换」或「整秒变化」时写 TMP 文本，
  图标「缺图」结论也入缓存，不重复读盘；自造 Sprite/Texture 带 `HideAndDontSave` 且在
  `ResetStaticCaches` 显式销毁。`UIAndSigns/` 本身不建 Canvas，无层级问题。
- **每帧成本（AGENTS 4.12）**：`RandomEventDirector.Tick` 在 Dormant 下只有一次 float 加 + 两次比较；
  模式门面读取被节流到 0.5s 一次；`CollectEventBuffTargets`（内含 `FindObjectsOfType`）
  由血月按 2s 节流，`TickModeEFSpawnPostprocessScheduler` 队列空时 O(1) 早返；
  `UIAndSigns/BossRushInteractionScan.FindInteractionTargets` 是 1s 间隔的有限次协程且成功即停。
- **事件效果回滚**：五条结束路径（Expired / RunEnded / SceneChanged / SwitchDisabled / HostDestroyed
  + TriggerFailed）全部汇聚到 `EndActiveEvent`；玩家死亡经
  `OnPlayerDeathInBossRush_LootAndRewards:147 SetBossRushRuntimeActive(false)` → 签名转 0
  → 下一次 0.5s 轮询触发 `HandleRunSignatureChanged` → `EndActiveEvent(RunEnded)`。
  血月的天气还原有三重兜底（`RestoreWeather` 幂等闸 + `Scope.RegisterCleanup` + `OnCleanup` 显式调用），
  属性 Modifier 由 `RuntimeStatModifierTracker.RemoveAll` 摘除。
  当前事件目录里**没有**时间缩放与掉落倍率类效果，无对应回滚缺口。
- **宿主销毁顺序**：`ModBehaviour.OnDestroy` 先 `CleanupRandomEventsRuntimeOnDestroy()`（`:818`，只清静态缓存）
  再 `runtimeModuleHost.OnDestroy()`（`:825` → `RandomEventsRuntimeModule.OnDestroy` →
  `ShutdownRuntime(HostDestroyed)`）。调度器持有 `_activeEvent` 直接引用，
  先清目录缓存不影响后续 `OnCleanup`，顺序安全。
- **`ModBossPresetLookup`**：3 个调用方（`DragonKingBoss.cs:433/696`、
  `PhantomWitchBoss.cs:745/753`、`DragonDescendantBoss_RuntimeAndCleanup.cs:325`）
  传入的都是各自 Config 的 `BossNameKey/CN/EN` 三元组，语义与收口前逐字一致；
  ModeH / PetNest / Codex / Campaign 并不直接调用它，而是经这三个 `IsXxxPreset` 间接使用，无分歧。
- **`CharacterOnDeadPatch` 三注册表并存**：Mode G / Mode H / PetNest 三张表都是
  只读身份查询、以 `||` 并联，彼此无优先级耦合；
  `PetNestDeathSuppressionRegistry` 是 `PetNestCompanionAgent` 静态表的薄封装（无第二份簿记，无清理缺口），
  `ModeHDeathSuppressionRegistry` 的 register/unregister 在 `ModeHSpawnBridge` 成对，
  并有 `ModeHRuntimeModule.cs:379` 的 `ResetStaticCaches`。命中后只跳过 Mod 自己的两个额外掉落 handler，
  不返回 false、不影响官方 `OnDead`。
- **`BossLethalHealthProtectionPatch` 钳制范围**：钳制链是 `ReverseScale → DragonKing → DragonDescendant → PetNest随从`
  的**短路链**（每级命中即 `return`），且整链只在 `BossRushHealthHurtContextPatch.IsInsideHurt`
  且 `value <= 0f` 时才进入。PetNest 那级另有 `IsCompanionArmed` 静态闸 + `IsCompanionHealth` 身份判定，
  钳到 1 血后由 `PetNestDownedHandler` 在下一 tick 退场，不会产生「Boss 或随从无敌」。
- **`ModeExtractionPointFactory`**：`ClearPersistentEvents`（整体 `new UnityEvent()` 替换）
  在 `ResolveFallbackActions` 之前，后者因此恒返回 `(true, true)`，Mod 自己负责通知与返程；
  `successDispatched` 闭包闩每次 `ConfigureEvents` 新建，而监听器每次 configure 前都被清空，
  不会出现跨局残留或重复返程。
- **`SafeRuntime` 语义**：只把日志出口从 `DevLog` 换成 `CriticalLog`，
  `Run`/`Try` 的吞吐语义（吞异常、`Try` 返回 fallback）逐字未变；
  `LogWarningOnce` 按 label 去重，`CriticalLog` 再按 key 去重并有 64 条上限，无刷屏风险。
- **Harmony 逐类 apply（cf04729 / c83c673）**：`ApplyHarmonyPatchesPerClass` 保持
  同程序集、同类型顺序、同 `CreateClassProcessor`，全成功时与 `PatchAll` 等价；
  `HasHarmonyPatchMetadata` 同时认「类上有特性」与「方法上有特性」，不会漏掉合法写法；
  失败经 `CriticalLog` 有声。仓库里有 18 个带 `Cleanup()` 方法但无 Harmony 元数据的类
  （`Achievement/AchievementEntryUI.cs:368`、`Common/UI/BossRushUISkinLoader.cs:171` 等），
  历史上的「3 个 Cleanup 方法 patch 失败」正是它们被 `PatchAll` 误判所致，
  新门控会把它们整体跳过；**是否真的消失需实机 Player.log 确认**（见第四节）。

## 四、对前几轮已 Fixed 条目的复核

| 条目 | 台账状态 | 本轮复核结论 |
| --- | --- | --- |
| CR-2026-09-03-009 非本波 Boss 推波 | Fixed | **修复本身正确，但存在被绕开的路径 → 见 F-1（P0）** |
| CR-2026-09-03-010 空投箱到时即销毁 | Fixed | **修复未达成目的 → 见 F-3（P1）** |
| CR-2026-09-03-011 战役武装时机 | Fixed | 复核通过（范围外主体），但其 `ShowMessage` 类提示受 F-2 影响 |
| CR-2026-09-02-004 距离休眠退订用主场景索引 | Fixed | 修复正确；**两条随机事件生成路径没跟上 → 见 F-7（P3）** |
| CR-2026-09-02-006 撤离工厂先清后判 | Fixed | 复核通过，无回归 |
| CR-2026-09-02-009 / -012 | Fixed | F3 harness 内部，本范围不涉及，未复核 |
| CR-2026-09-01-010 共享队列与模式清理 | Fixed | 复核通过（`ModeRuntimeHooks` 调度器提前推进正确） |
| commit 261a09b 随机事件 Boss 乱入 | — | 复核通过（预设缓存预热 + 队列推进），但**未覆盖自定义 Boss preset 分流 → F-1** |
| commit 8e64fa3 共享运行时三修 | — | 复核通过，细节见下 |
| commit c83c673 / cf04729 Harmony 逐类 apply | — | 静态复核通过，需实机日志确认 3 个 Cleanup 失败已消失 |
| commit 1c69f85 运行时交互体私有字段 | — | 修复正确，但**同模式的三个类没跟上 → 见 F-5（P3）** |
| commit ecc3bc1 SpawnCore options 门控透传 | — | 复核通过 |

### 4.1 CR-2026-09-03-009（非本波 Boss 推波）

**修复本身逐条核对通过**：

- 分界线位置正确：`WavesArena/WavesArena.cs:357-366` 的
  `countedDeadBosses` 去重 / `UnregisterEnemyRecovery` / `CheckBossKillAchievementsOnce`
  在 `:378` 的 `if (!IsCurrentWaveBossMember(bossMain))` 之上；
  无间炼狱现金池（`:394`）、`currentWaveBosses.RemoveAt`（`:438`）、
  `defeatedEnemies++`（`:444`）、`bossesInCurrentWaveRemaining`（`:448`）、
  `ProceedAfterWaveFinished()`（`:452`/`:459`）全在其下。
- `modeDActive` 判在 helper 内（`:498`）而非方法开头，Mode D 的 Boss 击杀成就保住——
  确认 `CheckBossKillAchievementsOnce` 确为 Mode D 计数的唯一有效入口之一。
- fail-closed：`IsCurrentWaveBossMember` 与 `IsSameWaveBossInstance` 的 catch 里都是
  `DevLog` + 落到 `return false`，没有 `return true`。
- `tests/WavesArenaBossMembershipGuard.py` 的 5 组断言与实现一致，反向验证描述可信。

**未覆盖面（F-1）**：成员判定的真相来源是 `currentBoss` / `currentWaveBosses`，
而这两个容器的写入方不止波次路径——三个自定义 Boss 的专用生成方法会无条件写它们，
且乱入事件恰好可以经共享刷怪核心分流到那三条路径。
`RandomEventsWaveIsolationGuard` 只做 `RandomEvents/` 目录内的文本扫描，看不到这条间接路径。

### 4.2 CR-2026-09-03-010（空投箱到时即销毁）

**架构判断正确**：「延到期而不是延清理」这个结论经复核成立——
`EndActiveEvent`（`RandomEventDirector.cs:586-596`）确实在 `OnCleanup` 之后
无条件再跑一次 `ctx.Scope.Clear`，在 `OnCleanup` 里放行确实无效；
唯一能延后的确实是 `TickEventActive` 的 `ctx.ElapsedSeconds >= ctx.DurationSeconds`
（`RandomEventDirector.cs:278`），且 `OnTick` 在同帧比较之前执行（`:264-282`），改 `DurationSeconds` 当帧生效。
硬帽、只延 `Expired`、关界面先于 `ClearScope`、拆文件避开两个 guard 的计数——四条都落实了。

**但判据选错**：见 F-3。`InteractableBase.Interacting` 在玩家真正翻箱时已经是 false。
修复上线后症状与修复前一致。

### 4.3 CR-2026-09-02-004（距离休眠退订）

`Utilities/SpawnedEnemyActivationHelper.cs:56-60` 遍历
`SceneManager.sceneCount` 逐个 `buildIndex` 调 `Unregister`，
官方 `Unregister` 返回 `bool`（`SetActiveByPlayerDistance.cs:47-51`），
`while` 循环在移除不到时立即退出，不会死循环，也能清掉重复登记。
`tests/SpawnedEnemyActivationGuard.py` 反向断言「不得用 `characterObject.scene.buildIndex`」
与「不得 `FindObjectsOfType`」，方向正确。

调用点核对（AGENTS 第 14 节要求的「两个激活点 + 三个自管激活的托管 Boss」）：

| 期望位置 | 实际 | 结论 |
| --- | --- | --- |
| 共享核心 分帧激活点 | `Utilities/EnemySpawnCore.cs:438` | ✅ |
| 共享核心 同帧激活点 | `Utilities/EnemySpawnCore.cs:1013` | ✅ |
| 龙裔遗族 | `Integration/DragonDescendant/DragonDescendantBoss.cs:175` | ✅ |
| 焚天龙皇 | `Integration/DragonKing/DragonKingBoss.cs:317` | ✅ |
| 幽灵女巫 | `Integration/PhantomWitch/PhantomWitchBoss.cs:318` | ✅ |
| 标准 Boss 路径（8e64fa3 补） | `ModBehaviour.cs:1209` | ✅ |
| Mode H 生成桥 | `ModeH/ModeHSpawnBridge.cs:248` | ✅ |
| Mode G 冻结分支 | 两处 `HoldForExternalCommit` 分支**刻意不调**（`EnemySpawnCore.cs:429-433` / `:1004-1008`） | ✅ 符合「Mode G 不适用」 |
| 原版 spawner 角色 | 未接入 | ✅ 符合「原版不适用」 |
| **随机事件商人 / 巡游鸭** | **未接入** | ❌ 见 F-7 |

### 4.4 commit 8e64fa3 三项复核

1. **距离休眠**：见 4.3。
2. **撤离工厂先清后判**：`Utilities/ModeExtractionPointFactory.cs:67-73` 顺序为
   `ClearPersistentEvents` → `ResolveFallbackActions` → `ConfigureEvents`，与注释一致；
   `ResolveFallbackActions` 的注释诚实说明了「当前循环恒不执行、恒返回 (true,true)」，
   保留防御分支的理由成立。`successDispatched` 闩位置正确（在 `IsCurrentArea` 判定之前短路）。
3. **终章 Boss 销毁前补 `ClearBossRandomLootTracking`**：`Campaign/CampaignFinalBoss.cs`（范围外）未逐行复核；
   但共享核心的提交失败分支仍缺同一处理，见 F-6。

### 4.5 commit ecc3bc1（SpawnCore options 门控透传）

`ScheduleModeEFSpawnPostprocessAsync` 新增 `options` 形参（无默认值，强制调用方显式传），
`ModeEFSpawnPostprocessJob` 增字段，`FinalizeModeEFSpawnPostprocessJob` 三处门控
（`HoldForExternalCommit` 冻结 / `ApplySharedMutators` / 跳过 Legacy 提交）
与同帧路径逐条对齐，`options == null` 时行为与改动前逐字相同。复核通过。

配套的 `Utilities/ModeRuntimeHooks.cs` 把 `TickModeEFSpawnPostprocessScheduler()`
提到 `TickWavesArenaRuntime` 早返之前，是随机事件乱入 Boss 走分帧路径的必要条件；
队列空时 `EnemySpawnCore.cs:233-236` O(1) 早返，未引入新的每帧成本。复核通过。

### 4.6 commit c83c673 / cf04729（Harmony 逐类 apply + 关键失败日志）

静态复核通过（见 3.3 末条）。**仍需实机确认**：
Player.log 里历史上稳定出现的「3 个 `Cleanup` 方法 patch 失败」是否已消失。
若仍在，说明失败源不是「无元数据类被误处理」，需要按 `HarmonyBindingSelfCheck`
与 `ApplyHarmonyPatchesPerClass` 的 `CriticalLog` 输出重新定位。

---

## 附：审查进度日志（内部用）

- [x] 规范阅读：AGENTS.md、Utilities/AGENTS.md、Patches/AGENTS.md、CODE_REVIEW.md、
      CR-2026-09-03-009/010/011、CR-2026-09-02-004/006/009/012、CR-2026-09-03-016
- [x] RandomEvents/：12 个新增文件全文 + `Config/ConfigRandomEvents.cs` + 3 个 guard
- [x] WavesArena/：diff + `WavesArena.cs` 成员判定全文 + `SpawnerControl` 自愈路径 + `EnemyMaintenance` 清怪循环
- [x] LootAndRewards/ + Interactables/：diff + `RegisterBossRandomLootTracking` 漏斗 + 三个自定义 Boss 并联点
- [x] Utilities/：diff 全文 + `SpawnedEnemyActivationHelper` / `ModBossPresetLookup` / `SafeRuntime` /
      `ModeExtractionPointFactory` / `ModeRuntimeHooks` / `PlayerLifecycleRuntimeHooks` / `EnemySpawnCore` 关键段
- [x] Patches/：5 个目标文件全文 + 21 条 `[HarmonyPatch]` 逐项对照官方源码
- [x] UIAndSigns/：diff + `BossRushInteractionScan` 全文 + `ShowMessage`/`ShowBigBanner` 通道
- [x] Harmony / 反射核对表（第 3.1、3.2 节）
- [x] 已 Fixed 条目复核（第四节）

### 未做 / 做不了

- **没有运行编译、没有跑 guard、没有实机**：本轮为绝对只读审查（任务要求）。
  所有结论标注为「静态确认，未运行验证」。
- `DebugAndTools/F3*`、`Campaign/`、`ModeH/`、`PetNest/`、`Integration/` 主体不在本范围，
  只在追踪调用链时按需读取，未做系统审查。
- `Common/Infrastructure/BossRushEagerReflectionCache.cs` 严格说不在范围内，
  但本范围三个文件（`Utilities/Utilities.cs`、`Patches/Economy/…`、`Interactables/…`、`UIAndSigns/…`）
  本轮刚改成引用它，且任务要求核对反射目标，故一并核对并报出 F-2。

---

## ==== DebugTools_Build_Guards_Tooling ====

# F3 调试/验收工具 + 构建 + 守卫框架 + 工具链 审查报告

> 审查范围：自 commit f9b83c0 起（含未提交工作树改动）的 `DebugAndTools/`、构建脚本
> （`compile_official.bat` / `run_guards.bat` / `test_bossrush_official.bat` / `verify_syntax.bat`）、
> 守卫框架与横切守卫（`tools/run_guards.py`、`tools/verify_syntax.py`、`tests/` 横切 guard）、
> 工具脚本、`.github/workflows/guards.yml`、`.gitignore`、`Assets/Data/GameplayCoverage.json`。
>
> 只读审查，未修改仓库任何文件。基线（主会话实测，直接采信）：编译 Build succeeded / 0 error / 0 warning；
> `python tools/run_guards.py` → 517 脚本 / PASS=513 / NEW-FAIL=4 / KNOWN-RED=0。

**状态：审查完成。** 10 条 confirmed finding（P0=0 / P1=1 / P2=3 / P3=6），
另有 5 条未验证线索。逐文件覆盖，未抽样：
`DebugAndTools/` 18 个 `F3GameplayValidation*.cs` 全读、4 个 `F3DebugCheatMenu*` 与
`NPCTeleportUI.cs` 读 diff；4 个 `.bat`、5 个 `tools/` 脚本、CI workflow、`.gitignore` 全读；
19 个横切/框架 guard 逐个读并**逐个实跑**。

> **重要：任务书给的 guard 基线已过期。** 任务书说 4 个 NEW-FAIL
> （`EmptyCatchGuard` / `GameplayValidationCoverageGuard` / `ModeHBattleSnapshotGuard` / `ModeHStandInGuard`），
> 本次实跑 `python tools/run_guards.py` 得到 **519 脚本 / PASS=518 / NEW-FAIL=1 / KNOWN-RED=0**：
> 那 4 个已全部转绿，取而代之的是一个任务书里没有的新红项
> `StaticCacheLifecycleGuard`（见 F-1）。

## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）

### F-1：`StaticCacheLifecycleGuard` 曾实测红 —— `ModeHCombatTelemetry` 静态缓存缺 `ResetStaticCaches`
### 　　（**审查期间已被另一会话修复；本条留档并附复核结论**）

> **状态更新（审查收尾时复测）**：这条在我发现时是**当时工作树上唯一的红项**，
> 之后被另一并发会话修掉了。复核修复正确：
> `ModeH/ModeHCombatTelemetry.cs:283-286` 新增
> `internal static void ResetStaticCaches() { _rangedWeaponCache.Clear(); }`，
> 并在 `ModeH/ModeHRuntimeModule.cs:380` 的静态缓存复位漏斗末尾接线
> （紧跟 `ModeHDeathSuppressionRegistry.ResetStaticCaches();`，同一批 11 个 ModeH 复位调用之内）。
> 复测 `python tests/StaticCacheLifecycleGuard.py` → **PASS**，
> `python tools/run_guards.py` → **519 脚本 / PASS=519 / NEW-FAIL=0 / KNOWN-RED=0 / guard runner: PASS**。
> 保留本条是因为它记录了「任务书基线已过期」这一事实，也说明白名单没有被用来掩盖它。

- **严重级**：P1 Major（发现时 guard/CI 红 = 合并阻断；运行时为跨局残留）
- **兼容分类**：`SAFE`（只影响静态缓存生命周期）
- **位置**：`D:\code\ykf\BossRushMod\ModeH\ModeHCombatTelemetry.cs:70`
- **证据**：
  ```csharp
  private static readonly Dictionary<int, bool> _rangedWeaponCache = new Dictionary<int, bool>();
  ```
  该字段（连同 `IsRangedWeapon` 在 `:286` 的读写）由 commit `29ab1e6`「Mode H 步骤 10：战斗、遥测与战场快照」引入，
  在本次审查区间内。`ModeHCombatTelemetry` 类里没有 `ResetStaticCaches` / `ClearStaticCaches` / `ForceCleanup`，
  也不在 `tests/static_cache_allowlist.txt` 里。
- **实测**：
  ```
  python tests/StaticCacheLifecycleGuard.py
  → [FAIL] ModeHCombatTelemetry (ModeH\ModeHCombatTelemetry.cs): 缺少 ResetStaticCaches 方法
    汇总: PASS=147, WARN=11, FAIL=1
  python tools/run_guards.py
  → 519 脚本 / PASS=518 / NEW-FAIL=1 / KNOWN-RED=0 / guard runner: FAIL
  ```
  **注意：任务书给的基线（4 个 NEW-FAIL：EmptyCatch / GameplayValidationCoverage / ModeHBattleSnapshot / ModeHStandIn）
  在当前工作树上已全部转绿，剩下的唯一红项是这条，是任务书里没有的。**
- **失败场景**：`.github/workflows/guards.yml` 跑的就是 `python tools/run_guards.py`，退出码 1 → CI 红。
  运行时侧：Mod 重载 / `OnDestroy_*` 路径不清该表，weaponTypeId→是否远程 的判定会跨 Mod 生命周期沿用；
  动态注册物品的 TypeID 若在不同会话里指向不同物品，遥测会把近战记成远程（或反之）。
- **玩家影响**：间接——Mode H 战斗遥测口径错误（影响赛后统计/评分），不会崩。主要影响是门禁红。
- **建议修复**：给 `ModeHCombatTelemetry` 加 `internal static void ResetStaticCaches() { _rangedWeaponCache.Clear(); }`，
  并在 `OnDestroy_*` 漏斗（例如 `Integration/BossRushIntegration_StartAndScene.cs` 的 `OnDestroy_Integration()`，
  或 ModeH 自己的卸载漏斗）里调用。**不要**加进白名单——`tests/AGENTS.md` 规定新增代码默认不进白名单。
- **验证需求**：`python tests/StaticCacheLifecycleGuard.py` 转绿 + `compile_official.bat`。

### F-2：F3 验收「出击餐」用例的 finally 还原写错了 API，会把测试用的餐留在存档并静默给玩家加速

- **严重级**：P2 Minor（Dev 门控，但污染测试档并**影响同一轮后续所有竞技场用例的结论**）
- **兼容分类**：`SAFE`（仅 Dev 路径）
- **位置**：`D:\code\ykf\BossRushMod\DebugAndTools\F3GameplayValidationBackMountain.cs:205-217`
- **证据**：用例自己在 `:191` 写明语义——
  ```csharp
  // ClearForRun 清的是本局 Modifier，不清登记；基地下调用应幂等无副作用。
  ```
  但 `finally` 里却拿 `ClearForRun()` 当「清登记」用：
  ```csharp
  finally
  {
      // 还原玩家原本的登记，别把测试用的餐留在存档里。
      try
      {
          if (original != 0) RaidMealService.RegisterMeal(original);
          else RaidMealService.ClearForRun();   // ← 这行不清登记
      }
      ...
  }
  ```
  对照产品实现 `Integration/BackMountain/RaidMealService.cs:209-224`：`ClearForRun()` 只做
  `RuntimeStatModifierTracker.RemoveAll(_records, "RaidMeal")` + `_appliedThisRun = false`，
  **完全不碰存档 key**。真正清登记的是同文件 `:108` 的 `private static bool ClearRegisteredMeal()`
  （写 `SavesSystem.Save<int>(RaidMealSaveKey, 0)`），**是 private，用例拿不到**。
  而用例在 `:182` 刚刚 `RegisterMeal(BossRushItemIds.EmberChili)` 落过盘
  （`RaidMealService.cs:78` `SavesSystem.Save<int>(...)` + 回读校验）。
- **失败场景**（`original == 0`，即玩家没吃过饭 —— 这是默认情况）：
  1. 3/7 阶段 `BACKMOUNTAIN_RAID_MEAL` 跑完，存档里留着 `EmberChili` 登记；
  2. 4/7 阶段 `LoadScene(竞技场)` → `Integration/BackMountain/BackMountainRuntimeModule.cs:358`
     `RefreshCharacterBoundEffects()` 的非基地分支调 `RaidMealService.ApplyForRun()`；
  3. `RaidMealService.cs:131+` 消费登记并给主角挂上
     `MoveSpeed` / `RunSpeed` / `WalkSpeed` / `ReloadSpeedMultiplier` 四个 Modifier；
  4. 本该弹出的「出击餐生效：」提示走 `ModBehaviour.ShowMessage` → `UIAndSigns/UIAndSigns.cs:287`
     `if (GameplayValidationSuppressNotifications) return;` **被验收自己抑制**，测试者看不到任何痕迹；
  5. 从 `MODE_STANDARD_WAVE` 起的整个 4/7~6/7 阶段，玩家角色都带着一份没人申报的移动/装填加成。
     `MODE_F_BLOODFIRE`（`DebugValidateModeFBloodfire`，断言的正是 `WalkSpeed`/`RunSpeed` 类速度 Modifier）
     与这份加成叠在同一批 stat 上，结论可信度受损；
  6. `ValidateFinalCleanState` 只数 `ValidationTemporaryModifierCount`（= Mode F 血火 modifier），
     **抓不到**这份残留。
- **玩家影响**：正式构建里 `DevModeEnabled == false`，玩家不受影响。影响的是「一键验收」这份报告本身
  的可信度，以及被标记的测试档（下一次真人出击会白拿一份加速）。
- **建议修复**：三选一——
  (a) 把 `RaidMealService.ClearRegisteredMeal()` 提升为 `internal`（或新增
      `internal static bool DebugClearRegisteredMealForValidation()`），finally 里 `original == 0` 分支改调它；
  (b) 用例改成只在 `original != 0` 时才动登记，`original == 0` 时不做写入实验；
  (c) 用例结束前主动 `ApplyForRun()` 消费掉自己造的登记（但会污染基地场景状态，不推荐）。
  顺带把 `:191` 那条注释与 finally 的意图对齐。
- **验证需求**：Dev 构建实机跑一次 F3 全量验收，跑完在基地 dump `RaidMealService.ReadRegisteredMeal()`
  应为 0；并核对 4/7 阶段 Player.log 里没有出击餐 `AddModifier` 记录。**静态确认，未运行验证。**

### F-3：用例隔离壳只挡住「用例自身帧」的异常，`yield return` 出去的子协程炸掉会整套卡死且通知永久静音

- **严重级**：P2 Minor（Dev 门控；但一旦触发，运行器在本次游戏进程内永久不可用）
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\DebugAndTools\F3GameplayValidationStages.cs:129-152`
  （壳）、`DebugAndTools\F3GameplayValidationRunner.cs:408-508`（RunSuite 的 try/finally）
- **证据**：`RunIsolatedCase` 的 try/catch 只包住 `inner.MoveNext()` 这一句：
  ```csharp
  while (true)
  {
      bool moveNext = false;
      try { moveNext = inner.MoveNext(); }
      catch (Exception e) { Record(caseId + "_UNHANDLED", "FAIL", ...); needsReclaim = true; }
      ...
      if (!moveNext) break;
      yield return inner.Current;          // ← Current 是子 IEnumerator 时，由 Unity 接管驱动
  }
  ```
  而用例正文大量 `yield return <子 IEnumerator>`：`RunModeD` 的 `yield return WaitSeconds(8f)`、
  `yield return _host.ValidationDefeatModeDWave(...)`；`RunModeH` 的
  `yield return RunModeHStarterKits(map)` / `yield return RunModeHErrorSwap(map)`
  （`F3GameplayValidationModes.cs:129/132`）。这些子协程被 `yield` 到上层后由 Unity 的调度器
  直接 `MoveNext()`，**根本不经过 `RunIsolatedCase` 的那句 try**，所以壳上那段注释
  （`Stages.cs:80-87`「单个用例炸掉只记它自己的 FAIL」）对子协程不成立。
  子协程里没有 catch 的代表：`F3GameplayValidationModeHErrorSwap.cs:90-175` 整个 try 只有
  `finally` 没有 `catch`；`F3GameplayValidationModeHKits.cs:32-35` 的
  `ModeHLoadoutKitRegistry.GetKit(kitId)` / `FindKitProbeProfile(kit)` 在 try 之外。
- **失败场景**：子协程抛异常 → Unity 记 log 并终止整条协程链 → `RunSuite` 挂在 `yield` 点上，
  它的 `finally`（`Runner.cs:496-507`）不会执行，于是：
  1. `_host.GameplayValidationSuppressNotifications` 永远停在 `true`
     （只在 `Runner.cs:433/504` 复位）→ `UIAndSigns.cs:287` 的 `ShowMessage_UIAndSigns`
     与 `:435` 的 `ShowBigBanner_UIAndSigns` **本次游戏进程内全部静音**，玩家/测试者
     再也看不到任何 Mod 提示，且没有任何入口能复位；
  2. `_routine` 停在非 null → `TryStart` 恒返回「已有验收正在运行」，
     `TryCancel` 只设 `_cancelRequested` 没人消费，`Update()` 的中断恢复要求 `_routine == null`
     也永不触发 → 运行器永久卡死，只能重启游戏；
  3. 不产 `SUMMARY`、不 `ClearRunMarker()`。这一条有兜底：下次进基地
     `RecoverInterruptedRunIfNeeded`（`Runner.cs:1018`）会补 `RECOVERY | CANCELLED`——
     但它同样不复位上面两项，而且要求 `_routine == null`，本进程内跑不到。
- **玩家影响**：正式构建 `DevModeEnabled == false`，玩家不受影响。影响的是验收流程本身的可靠性。
- **建议修复**：
  (a) 把子协程也纳入手工驱动：`RunIsolatedCase` 里遇到 `inner.Current is IEnumerator` 时压栈自己驱动
      （或给每个子协程加自己的 try/catch 兜底）；
  (b) 无论如何，`GameplayValidationSuppressNotifications = false` 与 `_routine = null` 要有
      一条不依赖 `finally` 的复位路径——例如 `Update()` 里加「`_routine != null` 但协程已死
      （心跳超过 N 秒没推进）则强制收尾」的看门狗，或把抑制标志改成带截止时间的租约。
- **验证需求**：Dev 构建里人为在 `RunModeHErrorSwap` 抛一次异常，观察 Player.log 是否出现
  `SUMMARY` 行、以及之后 Mod 提示是否还出得来。**静态确认，未运行验证**——
  「Unity 不会把子协程异常沿父协程的 try/finally 传播」这一条是基于 Unity 协程调度器
  分帧驱动嵌套 IEnumerator 的实现推断的，需实机确认。

### F-4：`MODE_H_ERROR_SWAP` 的结果表达式在「未通过且无原因」时判 PASS（潜在假绿）

- **严重级**：P3 Note（当前静态追踪不到可达路径，但表达式本身违反「不伪造 PASS」）
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\DebugAndTools\F3GameplayValidationModeHErrorSwap.cs:177-178`
- **证据**：
  ```csharp
  Record("MODE_H_ERROR_SWAP", passed ? "PASS" : (reason == null ? "PASS" : "FAIL"),
      sw.ElapsedMilliseconds, metrics, passed ? null : reason);
  ```
  `passed == false && reason == null` → 记 **PASS**。同文件里其余三态处理都很规范
  （前置不满足一律 `SKIP`），唯独这一句把「没结论」翻译成了「通过」。
  逐路径追踪后当前每条 `passed == false` 的路径都会给 `reason` 赋值，所以暂时不可达；
  但这是一条「以后任何人加一条早退分支忘了写 reason，就会静默变绿」的地雷，
  与 AGENTS §14「无 code-drivable 入口的项如实 SKIP，不伪造 PASS」的口径相反。
- **建议修复**：改成 `passed ? "PASS" : "FAIL"`，`reason` 为空时补
  `reason = "no_outcome_recorded"`；或按同文件既有风格记 `SKIP`。
- **验证需求**：静态改动，`compile_official.bat` + `GameplayValidationRunnerGuard` 即可。

### F-5：`BACKMOUNTAIN_SHOWCASE_LEDGER` 的注释与实现不符，且它走的是绕过 `CanDisplay` 的低层入口

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\DebugAndTools\F3GameplayValidationBackMountain.cs:81-122`
- **证据**：注释（`:84`）写「用一个后山产出物 TypeID 当探针（……所以 `CanDisplay` 可能拒绝
  ——**那种情况记 SKIP 而不是判红**，因为拒绝本身是正确行为）」。但实现里：
  1. 从头到尾没调过 `CanDisplay`，只调 `ShowcaseService.TryDisplay(int typeId)`；
  2. 拒绝时走的是 `:119` `if (!displayed) reason = "登记被拒绝: TryDisplay 返回 false";`
     → `RunSyncCaseGated` 把 `false` 翻成 **FAIL**，不是 SKIP；
  3. `:96-100` 的「找不到探针」分支同样 `return false` → FAIL，注释却写「本项需人工复测」。
  更关键的是产品侧两条入口语义不同：`Integration/BackMountain/ShowcaseService.cs:103` 的
  `CanDisplay(Item)` 明确写着「菜地种子和出击餐不能登记为战利品」并对
  `BackMountainItems.GetDefinition(item.TypeID) != null` 直接拒绝；而 `:147` 的
  `TryDisplay(int typeId)` **只查 `typeId > 0` / 槽位是否满 / 是否重复**，不查资格。
  用例挑的探针恰恰是 `DragonFruit(500065)` / `EmberChili(500066)` / `PhantomMushroom(500067)`
  ——三个都是 `BackMountainItems` 定义的出击餐，`CanDisplay` 一定会拒绝、`TryDisplay` 一定会放行。
  所以这条用例既没验到资格规则，也不会像注释说的那样 SKIP。
- **失败场景**：展示柜被玩家填满（`ShowcaseSlotCount = 8`）或三个探针都已登记时，
  用例记 FAIL 而不是 SKIP——一次正确的产品行为被算成红项。
- **建议修复**：探针改成一件满足 `CanDisplay` 的高品质战利品并显式先跑 `CanDisplay`；
  「找不到可用探针 / 被资格规则正当拒绝」两种情况改走 `Record(..., "SKIP", ...)`；
  同步修正注释。（`TryDisplay(int)` 是否应该内联 `CanDisplay` 的资格判断属 BackMountain 子系统议题，
  已列入未验证线索，不在本报告范围内下结论。）
- **验证需求**：静态改动 + 实机跑一次 3/7 阶段。

### F-6：`RunModeHErrorSwap` 对 `map.ArenaSpawnPoints[0]` 无边界保护，是全仓唯一未加保护的取用点

- **严重级**：P3 Note
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\DebugAndTools\F3GameplayValidationModeHErrorSwap.cs:105`
- **证据**：
  ```csharp
  if (!ModeHSpawnBridge.TryPrepareForArena(request.Handle, map.ArenaSpawnPoints[0], out prepareError)
  ```
  全仓另外两处消费同一字段都先做长度校验：
  - `ModeH\ModeHArenaIsolationLease.cs:264` `if (_map.ArenaSpawnPoints == null || _map.ArenaSpawnPoints.Length == 0)`
  - `ModeH\ModeHCommandCertificationProbe.cs:28` `if (map == null || map.ArenaSpawnPoints == null || map.ArenaSpawnPoints.Length < 2)`
  该字段来自地图配置（`ModeH\ModeHMapSupportRegistry.cs:173` `map.ArenaSpawnPoints = config.modeHSpawnPoints;`），
  不是编译期常量。
- **失败场景**：认证走缓存命中路径（`MODE_H_CACHE_HIT` 分支本来就 `expectCache == true`）时，
  本会话不重跑 `ModeHCommandCertificationProbe` 的 `Length >= 2` 校验；若该会话的地图配置
  没能加载出刷新点，`[0]` 抛 `IndexOutOfRangeException`。因为整个方法只有 `finally` 没有 `catch`，
  并且它是被 `yield return` 出去由 Unity 驱动的（见 F-3），异常会直接把整套验收打死。
- **建议修复**：照 `ModeHCommandCertificationProbe` 的写法先判空判长度，不满足记 `SKIP`
  （理由 `arena_spawn_points_missing`）。
- **验证需求**：静态修改；**静态确认，未运行验证**。

### F-7：`run_guards.bat` / `verify_syntax.bat` 的 fallback 分支永远返回失败（cmd 解析期展开）

- **严重级**：P2 Minor（`OPERATIONAL`；只在没有 `py` 启动器的机器上触发，产生**恒定假红**）
- **兼容分类**：`OPERATIONAL`
- **位置**：`D:\code\ykf\BossRushMod\run_guards.bat:19-26`、
  `D:\code\ykf\BossRushMod\verify_syntax.bat:23-30`（两处结构完全相同）
- **证据**：
  ```bat
  py -3 --version >nul 2>nul
  if errorlevel 1 (
      python --version >nul 2>nul
      if errorlevel 1 ( echo [FAIL] ... & exit /b 1 )
      python "tools\run_guards.py" %*
      exit /b %ERRORLEVEL%          ← 在整个 if(...) 块被**解析**时就展开了
  )
  py -3 "tools\run_guards.py" %*
  exit /b %ERRORLEVEL%              ← 这一行在块外，是独立语句，正确
  ```
  cmd 会在读到 `if ... ( ... )` 这个复合语句时一次性展开块内所有 `%VAR%`。
  进入该分支的前提正是 `py -3 --version` 失败（`py` 不存在时 ERRORLEVEL = 9009），
  于是块内的 `exit /b %ERRORLEVEL%` 被展开成 `exit /b 9009`，
  **无论 `python tools\run_guards.py` 实际返回什么**。
- **实测**（scratchpad 里复刻同一结构，不改仓库）：
  ```
  errlvl_probe.bat:
      nosuchcommand_xyz >nul 2>nul
      if errorlevel 1 ( cmd /c exit 0 & echo [%ERRORLEVEL%] & exit /b %ERRORLEVEL% )
  输出: INNER: parse-time ERRORLEVEL was [9009]
  observed exit code: 9009        ← 尽管紧邻的 `cmd /c exit 0` 成功
  ```
- **失败场景**：Windows 上只装了 `python`（Microsoft Store 版或未勾选 py launcher 的安装）
  的开发者，`run_guards.bat` 全绿也会以 9009 退出，`verify_syntax.bat` 同理。
  这会让人以为 guard 红了，或让某个包装脚本据此中止。
  本机装了 `py`，所以走的是正确的块外分支——**这也是这个 bug 至今没被发现的原因**。
- **玩家影响**：无（构建工具链）。影响的是协作者与自动化。
- **建议修复**：块内改用延迟展开或直接跳出块：
  ```bat
  if errorlevel 1 goto :use_python
  py -3 "tools\run_guards.py" %*
  exit /b %ERRORLEVEL%
  :use_python
  python --version >nul 2>nul
  if errorlevel 1 ( echo [FAIL] ... & exit /b 1 )
  python "tools\run_guards.py" %*
  exit /b %ERRORLEVEL%
  ```
  （或在块内写 `setlocal enabledelayedexpansion` + `exit /b !ERRORLEVEL!`。）
- **验证需求**：在无 `py` 的环境（或临时把 `py -3 --version` 改成必败）跑一次全绿 guard，
  确认退出码为 0。**已用等价 .bat 复现，未在真实无 py 环境验证。**

### F-8：`.gitignore` 的 docs 放行清单漏了 `docs/contracts.md`，与它自己写的规则相矛盾

- **严重级**：P3 Note（`OPERATIONAL`）
- **兼容分类**：`OPERATIONAL`
- **位置**：`D:\code\ykf\BossRushMod\.gitignore:32-48`
- **证据**：`.gitignore` 新增注释明确写了规则——
  > docs/ 默认 local-only（AGENTS.md §13）。但下面这几份被 tests/ 的 guard 直接读取
  > （**文件不存在时 guard 是硬失败，不是 skip**），不纳管会让 fresh clone 一跑 guard 就红，
  > 因此显式放行。**新增此类「guard 依赖的文档」时必须同步加到这里。**

  放行了 4 份文档，但漏了 `docs/contracts.md`。它正是这一类：
  `tests/TypeIdLedgerGuard.py:160`
  ```python
  if not CONTRACTS.exists():
      return None, None, "docs/contracts.md 不存在（该文件是 TypeID 台账的事实源）"
  ```
  `main()` 里 `if err: return fail(err)`（`:232-233`）——**硬失败，不是 skip**。
  实测 `git check-ignore --no-index -v docs/contracts.md` → 命中 `/docs/*`（被忽略）。
- **失败场景**：目前不炸，因为 `docs/contracts.md` 是 `/docs` 规则收紧之前就已跟踪的文件
  （`git ls-files docs/` 能看到），fresh clone 仍拿得到。但一旦有人
  `git rm --cached docs/contracts.md`、或从 tarball 重建仓库，`git add` 会被忽略规则拒绝，
  CI 上 `TypeIdLedgerGuard` 直接红且报错信息指向一个「本该 local-only」的文件，很难排查。
- **建议修复**：在 `.gitignore` 补 `!/docs/contracts.md`（它已在索引里，加了也不改变现状，
  只是把规则写实）。顺带核对：`ManualSmokeChecklistCoverageGuard`、
  `ModBehaviourInstanceClassificationGuard`、`PortableSafeZoneDeviceBundleGuard`、
  `ZombieModeWindowsVerificationScriptGuard` 的四份依赖**已正确放行**；
  `PetNestEggItemRegistryGuard` 读 `docs/Bossrush使用物品ID表.md` 但缺失时只跳过不判红
  （`tests/PetNestEggItemRegistryGuard.py:126` `if table is not None:`），**不需要**放行，
  与 `.gitignore` 里那条「反例」的口径一致。
- **验证需求**：`git check-ignore --no-index docs/contracts.md` 应不再命中。

### F-9：`test_bossrush_official.bat` 的 wiki 同步失败检查是死代码，且备用游戏路径拼错

- **严重级**：P3 Note（`OPERATIONAL`）
- **兼容分类**：`OPERATIONAL`
- **位置**：`D:\code\ykf\BossRushMod\test_bossrush_official.bat:50-57`（第 54 行是那句死检查）与 `:193`
- **证据 1（死检查）**：
  ```bat
  ) else (
      echo [INFO] Using Node.js: %NODE_EXE%
      pushd "%MOD_SOURCE_DIR%\wiki-site"
      "%NODE_EXE%" scripts\sync-content.mjs
      if %ERRORLEVEL% NEQ 0 (
          echo [WARN] Wiki sync failed, continuing deployment...
      )
      popd
  )
  ```
  与 F-7 同一个坑：`%ERRORLEVEL%` 在整个 `if/else` 复合语句被解析时展开，
  取的是 `call :find_node` 之后的值，**不是 node 的退出码**。所以 wiki 同步真失败时
  `[WARN]` 不会打印（反之亦然）。应改成 `if errorlevel 1 (`。
- **证据 2（路径拼写）**：`:193`
  ```bat
  call :try_game_path "D:\sofrware\steam\steamapps\common\Escape from Duckov"
  ```
  `sofrware` 是 `software` 的错拼。对照 `compile_official.bat:1109` 同一位置写的是
  `D:\software\steam\steamapps\common\Escape from Duckov`。
  在把游戏装在 `D:\software\steam\...` 的机器上，这条兜底探测永远失效
  （不致命，还有 `%~dp0..\..\..\..` 与另外两条兜底 + 手动 `GAME_PATH`）。
- **建议修复**：`if %ERRORLEVEL% NEQ 0` → `if errorlevel 1`；`sofrware` → `software`。
- **验证需求**：静态；**本次未运行该脚本**（会部署到游戏目录，按任务要求禁跑）。

### F-10：新增的 `F3GameplayValidationModeHErrorSwap.cs` 没同步进 `GameplayValidationRunnerGuard` 的 `CASE_FILES`

- **严重级**：P3 Note（守卫同步缺口，AGENTS §4.10）
- **兼容分类**：`SAFE`
- **位置**：`D:\code\ykf\BossRushMod\tests\GameplayValidationRunnerGuard.py:17-30`（`CASE_FILES` 元组）
- **证据**：磁盘上 18 个 `DebugAndTools/F3GameplayValidation*.cs`，逐个在 guard 源码里查名字：
  ```
  F3GameplayValidationModeHErrorSwap.cs         NOT in guard      ← 唯一漏网
  其余 17 个                                     in guard
  grep -c ErrorSwap tests/GameplayValidationRunnerGuard.py → 0
  ```
  后果是该文件既不进 `code` 拼接（于是所有 token 不变式——
  `yield return inner.Current;` 透传、`"SKIP"` 留痕、`Destroy(gameObject)` 禁令等——
  **对它一律不生效**），也不走 guard 末尾那个
  「拆分出来的用例文件同样必须进清单：漏登记不会报错，只是用例静默不存在（AGENTS.md 4.1）」的循环。
- **失败场景**：这正是 **F-4**（`reason == null → PASS`）与 **F-6**（`ArenaSpawnPoints[0]` 无保护）
  所在的文件——两条都恰好落在守卫的盲区里。编译清单本身有
  `OfficialCompileListFileExistenceGuard` 双向兜底（实测该文件在清单 L175），
  所以「静默不编译」的风险不成立；缺的是用例壳不变式那一层。
- **建议修复**：把 `Path("DebugAndTools/F3GameplayValidationModeHErrorSwap.cs")` 加进 `CASE_FILES`。
  更根本的做法是把 `CASE_FILES` 从手写元组改成
  `sorted(Path("DebugAndTools").glob("F3GameplayValidation*.cs"))`（`tools/gameplay_coverage.py:18`
  已经是这么做的，所以它**没有**漏掉这个文件，`MODE_H_ERROR_SWAP` 已正常登记进
  `GameplayCoverage.json`）——手抄清单迟早会漂。
- **验证需求**：改后 `python tests/GameplayValidationRunnerGuard.py` 仍应 PASS
  （已核对 ErrorSwap 文件里有 `yield return inner.Current;`、有 `"SKIP"`、无 `Destroy(gameObject)`）。

## 二、未验证线索

- **`ShowcaseService.TryDisplay(int)` 绕过 `CanDisplay` 的资格规则**
  （`Integration/BackMountain/ShowcaseService.cs:147` vs `:103`）。如果产品侧还有别的调用点
  直接用 `TryDisplay(int)`，玩家就能把菜地种子/出击餐登记成战利品拿加成。
  我只确认了 F3 验收在用它；**没有全量核对 BackMountain 的 UI 调用链**，
  这属于 BackMountain 子系统审查范围，请该范围的同事确认。
- **`IsValidationOwnedCharacterName` 的前缀集缺 `ModeH_` / `ModeG_`**
  （`DebugAndTools/F3GameplayValidationDiagnostics.cs:201-209`）：只认
  `BossRush_ / ModeD_ / ModeE_ / ModeF_ / RndEvt_ / ZombieMode_`。
  我 grep 过 `ModeH/` 未发现给生成角色改名的代码（`ModeHSpawnBridge` 里没有 `.name =`），
  所以现状下大概率不是漏网——Mode H 残留角色会被 `ValidationCountHostileCharacters`
  按敌对性抓到。但如果将来 Mode H 给角色加前缀命名，这里需要同步。**未验证。**
- **`RunModeFExtraction` 的 `resolved` 是循环不变量**
  （`DebugAndTools/F3GameplayValidationDeepFlows.cs:140-148`）：
  `bool resolved = _host.ModeFSuccessfulExtractionCountForValidation != completedBefore;`
  在 `DebugTriggerModeFExtractionForValidation` 返回后**立即**求值一次，之后循环里的
  `if (!triggered || !resolved) break;` 永远读的是那一次的值。若撤离结算是异步的
  （官方 `CountDownArea.onCountDownSucceed` → 返程），第一帧 `resolved` 必为 false，
  循环立刻 break，`returnedToBase` 恒 false → 假 FAIL。我没能确认结算是同步还是异步，
  **需实机跑一次 `MODE_F_EXTRACTION` 看 metrics 里的 `resolved=` 值**。
- **`RunModeHStarterKits` / `RunPublishedItemCases` 在中途被 abort 时记 FAIL 而不是 SKIP**
  （`F3GameplayValidationModeHKits.cs:72`：`allPassed = kitIds.Count > 0 && passed == kitIds.Count && !ShouldAbort();`）。
  整体 `SUMMARY` 状态仍会正确显示 `CANCELLED`/`TIMEOUT`，所以不是假绿；
  但 `failed_ids` 会混进「其实只是被取消」的用例，误导排查。属口径瑕疵，**未定性为缺陷**。
- **验收会给测试档写盘的清单**（供 owner 确认是否可接受，非缺陷）：
  `BossRush_Validation_DedicatedSlot_v1`、`BossRush_Validation_RunMarker_v1`
  （`Runner.cs:254-255`，都走 `SavesSystem.SaveFile(false)`）；
  日报整段（`F3GameplayValidationPersistence.cs:41-76`，DayIndex +1、重置签到态、`SaveFile`）；
  图鉴整段（`F3GameplayValidationEconomy.cs:247-249`）；后山展示柜与出击餐登记；
  Mode H 赛季归档（`DebugFinishValidationSeason`）；丧尸撤离会真发钱
  （`F3GameplayValidationZombie.cs:33` `succeed.Invoke()` → `EconomyManager`）。
  这些都在「必须先手动标记专用测试档」的前提下发生，UI 文案已警告，属设计内。

## 三、已核对且干净的关键面

### 3.1 编译清单双向 diff（自建脚本，未依赖 guard）

脚本：`scratchpad/compile_list_diff.py`（从 `compile_official.bat` 正则抽取所有 `*.cs` token，
与磁盘上 `.cs` 全量遍历双向比对；排除 `鸭科夫源码/`、`Assets/Editor/`、`Build/`、`tests/`、
`tools/`、`docs/`、`wiki-site/`、`.codex_tmp/`、`.git/`、`.qoder/`、`.kiro/`、`.claude/`、
`skills/`、`codex-skills/`、`.github/`）。结果：

跑了两次（审查开始与收尾，期间有并发会话往 `Integration/NPCs/DuckNpc/` 加了两个新文件）：

| 检查项 | 首次（767 文件时） | 收尾复测（769 文件时） |
| --- | --- | --- |
| 清单中 `.cs` 条目数 | 767（去重后仍 767，**无重复条目**） | 769（同样无重复） |
| 磁盘上生产 `.cs` 数 | 767 | 769 |
| 清单有、磁盘无 | **0** | **0** |
| 磁盘有、清单无 | **0** | **0** |
| 大小写不一致（逐级目录实名核对） | **0** | **0** |

新增的 `Integration\NPCs\DuckNpc\DuckNpcFaceRandomizer.cs`（清单 L629）与
`DuckNpcOutfitter.cs`（L630）均已登记。

点名核对：

- `DebugAndTools/TeleportDebugMonitor.cs`：磁盘已删除，清单中无残留 → 干净。
- `ReflectionCache.cs` 改名：清单 L135 `Common\Infrastructure\BossRushEagerReflectionCache.cs`
  与 L144 `Common\Utils\ReflectionCache.cs` **是两个不同文件，都在磁盘上存在**，
  非「改名后漏删旧条目」。
- 未跟踪新文件也已登记：`DebugAndTools\F3GameplayValidationModeHErrorSwap.cs`（L175）、
  `Integration\NPCs\DuckNpc\*.cs` 五个（L627-631）。
- 17 个 `F3GameplayValidation*.cs` 全部在清单内（由上表 0/0 保证）。

### 3.2 Assets 部署路径 ↔ 运行时读取路径核对表

运行时读取入口只有两处：`Common/Data/JsonDataRegistry.cs:45-46`
（`Assets/Data/<sub>/<file>`，`sub` 为空时退回 `Assets/Data/<file>`）与直接 `Path.Combine(GetModPath(), ...)`。

| 磁盘 JSON | 运行时读取者 | `compile_official.bat` 部署段 | 结论 |
| --- | --- | --- | --- |
| `Assets/Data/GameplayCoverage.json` | `F3GameplayValidationCoverage.cs:20` | `Assets\Data\*.json` 段 | ✅ |
| `Assets/Data/LootBlacklist.json` | `Config/LootBlacklistRegistry.cs:49` | 同上 | ✅ |
| `Assets/Data/Audio/BgmTracks.json` | `Audio/BossBgmCoordinator.cs:124` | `Assets\Data\Audio\*.json` 段 | ✅ |
| `Assets/Data/Campaign/Chapters.json` | `Campaign/CampaignContentCatalog.cs:131` | `Assets\Data\Campaign\*.json` 段 | ✅ |
| `Assets/Data/ModeH/*.json`（7 个） | `ModeH/ModeHContentCatalog.cs:204` | `Assets\Data\ModeH\*.json` 段 | ✅ |

`xcopy` 无 `/S`，不递归，所以每个子目录必须单列——三个子目录**都已单列**，且磁盘上
`Assets/Data` 只有这三个子目录，无遗漏。

### 3.3 `.gitignore`（未提交改动）

diff 内容：移除对 `compile_official.bat` / `test_bossrush_official.bat` / `cleanup_old_files.bat`
的忽略（这三个现在应当纳管，正确）；把 `/docs` 整目录忽略改成 `/docs/*` + 三条
「目录放行 → 目录内容再忽略 → 单文件放行」的三段式（这是 git 唯一能重新纳入
被忽略父目录下文件的正确写法）。用 `git check-ignore --no-index -v` 逐条实测：

- `docs/testing/2026-05-14-final-runtime-smoke.md` → 命中 `!` 放行行 ✅
- `docs/testing/other.md` → 被 `/docs/testing/*` 忽略 ✅（放行确实是单文件粒度）
- `docs/制作教程/便携安全区装置_Unity资源制作约定.md`、
  `docs/末日丧尸模式/末日丧尸模式_goal执行文档.md` → 放行 ✅
- `docs/架构说明/x.md`、`docs/contracts.md` → 仍被 `/docs/*` 忽略（`docs/contracts.md`
  是历史已跟踪文件，fresh clone 仍能拿到）

未把不该忽略的源文件忽略掉：`Assets/Data/**` 子目录（`Assets/Data/ModeH/NewFile.json`
实测 NOT-IGNORED）、`tests/`、`tools/`、`.github/`、四个 `.bat` 全部可纳管。
未把不该进仓库的东西放进来：`git ls-files` 中无 `.dll`/`.pdb`/`.exe`/`Build/`/密钥/`.bak`；
`/.claude`、`/鸭科夫源码`、`/skills`、`/.codex_tmp`、`Assets/config/wish_config.dat` 仍被忽略。
`compile_official.bat.bak` **磁盘上不存在也未被跟踪**，无需处理（且 `*.bak` 已在忽略列表）。

### 3.4 `tests/empty_catch_budget.txt` 预算变更核对

`919 → 968`，唯一改动来自 commit `0f2f414`（响应文件改造）。核对结论：**不是掩盖回归**——
- 该 commit 本身没有新增任何空 catch（只改 bat + 10 个 guard）；+49 是对同批 41 个新 `.cs`
  的滞后同步，commit message 逐条写明了每个 guard 同步的理由；
- 本次实测 `python tests/EmptyCatchGuard.py` → **PASS，count=968，budget=968**，
  即预算**没有留任何余量**，任何新增空 catch 都会立刻转红。任务书里给的「976 超预算 968」
  在当前工作树上已不复现（`Integration/NPCs/DuckNpc/` 当前空 catch 数为 0）。
- 本轮 `DebugAndTools/` 空 catch 共 60 处，全部在**改动前就存在的**文件里
  （`DebugAndTools.cs` 14、`F3DebugCheatMenuPlayerStats.cs` 16 等）；
  **17 个新增 `F3GameplayValidation*.cs` 一个空 catch 都没有**。

### 3.5 `tests/known_red_guards.txt`

当前只有注释、**零条已知红项**，与主会话 `KNOWN-RED=0` 一致。文件本身是本轮新增
（f9b83c0 时不存在）。纪律说明写明「新写的 guard 不许进这里」，与 `tests/AGENTS.md` 一致。

### 3.6 白名单 diff 摘要（`tests/AGENTS.md`「新增代码默认不进白名单」核对）

| 白名单 | f9b83c0→现在 | 非注释条目数 | 结论 |
| --- | --- | --- | --- |
| `tests/localization_injection_allowlist.txt` | 新增文件 | **0** | 只有格式说明，零豁免 ✅ |
| `tests/repowiki_reference_allowlist.txt` | 新增文件 | **0** | 同上 ✅ |
| `tests/typeid_literal_allowlist.txt` | 新增文件 | **1** | 唯一条目 `Achievement/BossRushAchievementManager.cs:500000 \| 成就奖励分数阈值，不是物品 TypeID` ——确属误报（不是 TypeID）✅ |
| `tests/static_cache_allowlist.txt` | **未改动**（`git diff` 空） | — | 没有把本轮新债务塞进去 ✅（正因如此 F-1 才红出来） |
| `tests/known_red_guards.txt` | 新增文件 | **0** | ✅ |
| `tests/empty_catch_budget.txt` | 919→968 | — | 见 3.4，非掩盖 ✅ |

### 3.7 正式构建里的 Dev 门控（任务点 a：非 dev 构建有没有暴露后门）

`ModBehaviour.cs:466-478`：
```csharp
private const bool HardcodedDevModeEnabled = false;
internal static bool DevModeEnabled
{
    get {
#if BOSSRUSH_DEV
        return true;
#else
        return HardcodedDevModeEnabled;   // = false
#endif
    }
}
```
`compile_official.bat:11-15` 只在 `BOSSRUSH_DEV_BUILD` 环境变量存在时才注入 `/define:BOSSRUSH_DEV`，
默认不注入。逐个入口核对（**全部有门**）：

| 入口 | 门控 | 位置 |
| --- | --- | --- |
| F3 热键本身 | `if (!DevModeEnabled) { 若已显示则关闭; return; }` | `F3DebugCheatMenu.cs:110-117` |
| 验收 runner 附着 | `if (!ModBehaviour.DevModeEnabled \|\| host == null) return;` | `F3GameplayValidationRunner.cs:305` |
| 标记测试档 | `if (!ModBehaviour.DevModeEnabled) { reason=...; return false; }` | 同上 `:325` |
| 启动验收 | `TryStart` → `_instance == null` 直接拒（非 dev 时 `EnsureAttached` 不建对象）+ `CheckStartGate` 再查一次 | `:351-362`、`:390` |
| 取消验收 | `_instance == null` → 拒 | `:364-374` |
| 产品侧验收入口 | `DebugStartCampaignFinalBossForValidation`（`Campaign/CampaignFinalBoss.cs:355`）、`ValidationKillCommittedStandardBoss`（`Diagnostics.cs:106`）、`ValidationDefeatModeDWave`（`:127`）、`ValidationStartZombie`→`ValidationCompleteZombieExtraction`（`Zombie.cs:16`）、`DebugStartSingleBossVictoryForValidation`（`WavesArena/WavesArenaBossSpawning.cs:139`）、`DebugValidateCodexFilterRefresh`（`Codex.cs:12`）、`SetValidationRejectStore`（`Integration/DailyReport/DailyReportPersistence.cs:377`）、`SetValidationRewardBackend`（`PetNest/PetNestExpeditionService.cs:50`）全部首行 `if (!DevModeEnabled) return/false` | — |

**未发现**「一键发放全部 Mode H 押品」「解锁全部章节」这类不可逆写盘作弊项：
`grep -rn "UnlockAll\|GrantAll\|发放全部\|解锁全部\|SaveFile(" DebugAndTools/F3DebugCheatMenu*.cs`
零命中；65 个 `CreateActionButton` 里唯一会持久化的是
`AdvanceDailyReportOneDayFromF3`（`F3DebugCheatMenuActions.cs:802`，日报快进一天）与
「验收测试」页的三个按钮，后者已由「必须先手动标记专用测试档」二次确认。

### 3.8 任务点 b：产品代码里的验收耦合（AGENTS §14「不为凑绿开测试后门」）

全仓 grep `GameplayValidation` / `ForValidation` / `IsValidationRunning` / `TestHook`：

- **真正在产品执行路径上分叉的只有三处**，且都双重门控、默认惰性：
  1. `UIAndSigns/UIAndSigns.cs:287` 与 `:435`：`if (GameplayValidationSuppressNotifications) return;`
     ——字段默认 false，只有 runner 会置 true（并在 `finally` 复位；见 F-3 的例外）。
  2. `Integration/DailyReport/DailyReportPersistence.cs:347`：
     `if (_validationRejectStore && ModBehaviour.DevModeEnabled) return false;`
     ——setter 自身 `if (!ModBehaviour.DevModeEnabled) return;`，正式构建里恒 false，分支恒不命中。
  3. `PetNest/PetNestExpeditionService.cs:526/770/799`：`_validationRewardBackend` 为 null 时
     走真实 `EconomyManager.Add` / `GrantOneItem`；setter 同样 dev 门控。这是注入式测试缝，
     不改变默认行为。
- 其余 `*ForValidation` 成员（`Campaign/CampaignFinalBoss.cs:72`、`ModeF/ModeFBounty.cs:260/271`、
  `ModeF/ModeFExtraction.cs:178/186/200`、`ModeF/ModeFPhases.cs:263/274`、
  `ModeE/ModeELifecycle.cs:55`、`ModeH/ModeHRuntimeModule_SceneFlow.cs:329`、
  `WavesArena/WavesArenaBossSpawning.cs:111/132`）**全部是只读访问器或独立的 `Debug*` 入口**，
  不在任何产品分支的条件里出现——即「加了一扇只有 Dev 能推的门」，不是「给产品路径塞了 if(testing)」。
- `DebugAndTools/F3GameplayValidationDeepFlows.cs:11-16` 的注释把这条纪律写死了
  （「断言取自产品状态，不取自入口的返回值本身」），实现与之相符：
  例如 `MODE_F_BOUNTY` 断言的是 `ModeFPlayerBountyMarksForValidation` 的**增量**而不是
  `DebugAwardModeFBountyMarkForValidation` 的返回值。

**结论：未发现为凑绿在产品代码开测试后门。** 三处缝隙建议在 `docs/contracts.md` 登记备案（P3 建议，非缺陷）。

### 3.9 任务点 c：假 PASS 模式排查

| 反模式 | 排查结果 |
| --- | --- |
| 断言恒真 | 只有 `SamplePerformance` 的 baseline 采样 `if (baseline \|\| p95 <= ...)` 对 `BASELINE_10S` 恒 PASS——它的语义是「采基线」不是「判性能」，可接受（P3 观察） |
| catch 吞掉后标 PASS | `RunSyncCase`（`Runner.cs:892-906`）catch 一律 `Record(FAIL)`；`RunIsolatedCase` catch 一律 `Record(_UNHANDLED, FAIL)`；未发现 catch→PASS |
| SKIP 被算成 PASS | 覆盖账本 `AutomaticNotPassed` 用 `Outcome(id) != "PASS"`（`Coverage.cs:79`），SKIP/WARN/NOT_RUN 一律计入未通过；`HasPending` 再叠加 `ManualCount > 0`，所以 `COVERAGE` 状态基本恒为 `INCOMPLETE`——诚实 |
| 超时算 PASS | `ShouldAbort()` 置 `_suiteTimedOut` 后 `RunIsolatedCase`/`RunSyncCaseGated` 记 SKIP；`Finish` 的状态优先级 `CANCELLED > ABORTED_DIRTY > TIMEOUT > 用例结果`（`Runner.cs:961-968`）三态分离正确 |
| 重试覆盖失败 | `GameplayCoverageReport.Record`（`Coverage.cs:138-144`）显式 `if previous == "FAIL" return;`，同轮内 FAIL 不可被后续 PASS 抹掉 |
| **例外** | `MODE_H_ERROR_SWAP` 的三元式在「无 passed 无 reason」时判 PASS —— 见 **F-4** |

`RunIsolatedCase` 的 `needsReclaim` 模式正确（catch 里只置标志，`yield return ForceReclaimArena()`
在 catch 之外，`Stages.cs:103-122` / `:137-149`），`IteratorYieldInCatchGuard` 实测 PASS。
手工 `MoveNext()` 均**透传了 `Current`**：`Stages.cs:151` `yield return inner.Current;`、
`ModeHErrorSwap.cs:154` `while (inner.MoveNext()) yield return inner.Current;`——
未重犯 AGENTS §14 记载的 Mode H 认证丢 `Current` 那个坑。

### 3.10 空引用 / 越界 / 除零 / `string.Format` / 遍历中改集合（新增 `.cs` 定向扫描）

- `string.Format`：17 个新增 `F3GameplayValidation*.cs` 中**零处**使用；
  `F3DebugCheatMenuPlayerStats.cs:57/98` 是既有代码，占位符与参数个数一致。
- 除法：新增文件中零处整数/浮点除法；p95 取样用乘法 + `Mathf.Clamp(..., 0, Count-1)`，
  且 `frames.Count > 0` 已前置判断（`Runner.cs:528`）。
- 遍历中改集合：逐条核对全部 19 处 `foreach`——
  `Codex.cs:23` 的 `bossEnabledStates` 在 `break` **之后**才写（`:36`），不在枚举期内；
  `Diagnostics.cs:138` 迭代的是 `modeDCurrentWaveEnemies.ToArray()` 快照
  （正是 CR-2026-09-02-010 的修法）；其余均为只读遍历。**未发现问题。**
- 越界：唯一未加保护的下标是 `ModeHErrorSwap.cs:105` 的 `map.ArenaSpawnPoints[0]` —— 见 **F-6**。
- 空引用：`_host` 在 `RunSuite` 内被直接解引用（`Runner.cs:450/475`），但 `TryStart` 前置
  `CheckStartGate` 已判 `_host == null`，且 `Update()` 每帧回填 `ModBehaviour.Instance`；
  真为 null 时会被 F-3 描述的路径吞掉，已并入该条。

### 3.11 每帧分配 / 静态残留 / 双实例

- **正式构建零成本**：`EnsureAttached` 在非 dev 直接 return，runner 的 `GameObject`
  根本不会被创建，`Update()` 不存在。
- **Dev 构建**：`F3GameplayValidationRunner.Update()`（`Runner.cs:376-385`）在
  `_recoveryChecked == false` 期间每帧调 `IsBaseScene()` → `SceneManager.GetActiveScene().name`，
  Unity 的 `Scene.name` 每次都分配新 string。`_recoveryChecked` 只在**进入基地**后才置 true，
  所以从进程启动到第一次进基地（主菜单、竞技场整局）都在每帧分配一个短字符串。
  同理 `CheckF3DebugCheatMenuHotkey` 每帧调一次 `EnsureAttached`。
  仅 Dev 构建、单帧一个小 string，**不作为缺陷提报**，记为观察项。
- **静态残留**：`_instance` / `_status` / `_lastReportPath` 是 static 且无 `OnDestroy` 复位；
  但 runner 是 `DontDestroyOnLoad` 且 `_instance == null`（Unity fake-null）会重建，
  重载 Mod 不会产生双实例。`ModBehaviour.criticalLogEmittedKeys` 有
  `ResetCriticalLogStaticCaches()` 且已由 `ArchitectureStructureGuard` 断言接进
  `CleanupAlwaysOnRuntimeOnDestroy`。
- 唯一的静态缓存生命周期缺口是 **F-1**（ModeH 侧）。

### 3.12 构建脚本其余核对

- **响应文件三个坑**：`compile_official.bat:64-77` 的注释已固化；实际写法
  `(` 括号块 + 块尾一次 `)>"%OUTPUT_DIR%\bossrush.rsp"`（`:887`，单 `>` 截断重写）、
  全部用 `echo(`、注释里已无全角标点（逐行核对 `::`/`rem` 段落，未再出现 `，。（）`）。
  `echo(%BOSSRUSH_DEFINE_ARGS%` 在未开 DEV 时写出**空行**而不是 `ECHO is on.` ✅。
- **`BOSSRUSH_DEV_BUILD` 语义**：`if defined BOSSRUSH_DEV_BUILD` ——只看「是否定义」，
  所以 `set BOSSRUSH_DEV_BUILD=0` 也会出 **dev 包**。AGENTS 文档写的是 `=1`，
  一致但有轻微脚枪；记为观察项，不提缺陷。
- **`errorlevel` 处理**：`set "BUILD_EXIT_CODE=%ERRORLEVEL%"` 紧跟 csc 调用、且是独立语句
  （不在括号块内），展开时机正确；部署段全部用**动态**的 `if errorlevel 1`（不是 `%ERRORLEVEL%`），
  没有 F-7 那个坑。脚本退出码 `exit /b %BUILD_EXIT_CODE%` 只反映编译结果。
  **观察项（非缺陷）**：DLL 拷贝失败时只打 `WARNING: Auto deploy failed`、整个 Assets 部署段
  被跳过，脚本仍 `exit 0`。端到端由 `test_bossrush_official.bat` 第 5 步重做 DLL/Assets 部署
  并硬判失败兜住，单跑 `compile_official.bat` 时需人眼看 WARNING。
- `Build/bossrush.rsp` 会写入解析后的 `%GAME_PATH%` / `%WORKSHOP_PATH%` 本机绝对路径，
  但 `Build/` 已被 `.gitignore` 忽略，不会入库。
- `compile_official.bat.bak`：磁盘上不存在、未被跟踪，且 `*.bak` 已在忽略列表——无需处理。

### 3.13 守卫框架（`tools/run_guards.py`）核对

- **`--changed-only` 会不会漏跑相关 guard**：`filter_by_changed_files`（`tools/run_guards.py:108-138`）
  把每个改动文件的「完整路径 / 文件名 / 去扩展名主干（≥4 字符）」当子串在 guard 源码里搜。
  实测任务点名的那个例子——用 runner 自己的 `collect_scripts` + 同一套 stem 规则模拟
  「改了 `Config/ConfigModConfigKeys.cs`」：
  ```
  selected: 4 个 guard，其中包含
  ['ModConfigOptionChangeGuard.py', 'ModeHConfigApiGuard.py']
  （grep -c ConfigModConfigKeys → ModConfigOptionChangeGuard.py:1、ModeHConfigApiGuard.py:2）
  ```
  所以**不会漏跑** `ModConfigOptionChangeGuard`。机制上仍是启发式：guard 正文里不提该文件就会漏，
  但 `selected or names` 的兜底保证「一个都没选中时回退全量」，且默认入口是全量跑，
  `.github/workflows/guards.yml` 用的也是全量（不带 `--changed-only`）——**CI 不受这个启发式影响**。
- **退出码**：`if new_failures or stale_baseline: return 1`（`:241-243`），
  已知红项失败不计入——语义正确，STALE-BASELINE 判失败也正确。
- **UTF-8**：runner 自身 `_force_utf8_output()`，子进程 env 里强制
  `PYTHONIOENCODING=utf-8` + `PYTHONUTF8=1`（`:143-146`），CI workflow 里再设一遍。✅
- **收集范围**：只收 `*Guard.py` / `*PropertyTest.py` / `*Tests.py`，`SKIP_ALWAYS` 只排除
  `SmokeLogScan.py`。核对 `tests/*.py` 全量 522 个，落选的 4 个是
  `SmokeLogScan.py`（需实机日志）+ `modeh_canonical_json.py` / `modeh_guard_util.py` /
  `petnest_guard_util.py`（三个共享工具模块，无 `main`）——**没有真 guard 被静默漏跑** ✅。
- **观察项（P3，未提为缺陷）**：runner 没有 SKIP 三态，
  `tests/OfficialNameReferenceGuard.py` 在拿不到官方本地化表时 `skip()` 返回 0，
  在汇总里与「真的验过了」的 PASS 无法区分（CI 上恒走这条路，因为
  `docs/官方本地化表/` 不纳管）。该 guard 自己在文件头把这个设计写清楚了，属有意为之。
  `tests/RepowikiReferenceGuard.py:47-50` 在 `.qoder/repowiki` 缺失时同样直接 PASS
  （当前该目录已跟踪，CI 上存在）。

### 3.14 `tools/verify_syntax.py` 与 `tools/gameplay_coverage.py`

- `verify_syntax.py`：`--with-bcl` 确为 **default=True**（`:118`，另有 `--no-bcl` 反向开关），
  与 AGENTS §14 的说明一致。PASS 条件是「CS1xxx 零条」，且 PASS 文案明确写
  「这不是编译验证……必须在装有《鸭科夫》的 Windows 机器上跑 compile_official.bat」——
  没有夸大。它解析编译清单的三条正则实测得到 **767 个源文件、0 个不存在**，与我的独立统计一致。
  csc 中文 Windows 下输出 GBK 的坑这里**不会中招**：Python 以 `encoding="utf-8", errors="replace"`
  解码，中文消息变成替换字符，但 `path(l,c): error CSxxxx:` 这段是纯 ASCII，正则照常匹配。
- `gameplay_coverage.py`：`validate()` 是真校验，不是走过场——
  它从 17 个 `F3GameplayValidation*.cs` 里正则抽出全部
  `RunSyncCase(Gated)?/RunIsolatedCase/VerifyArenaCleanup/SamplePerformance/WaitRuntimeReady/Record`
  的字面 ID，要求**双向**登记（清单缺 ID → 报错；清单有而源码找不到 → 报错）；
  再要求每个 Wiki 功能条目有人工场景、每个「生产模块域」（由编译清单反推）有 `sources` 映射。
  `GameplayValidationCoverageGuard.py` 在此之上又硬断言了 30 余个源码 token
  （含 `previous == "FAIL"`、`NOT_RUN`、`MANUAL_PENDING`、丧尸 `succeed.Invoke();` 必须出现 2 次）。
  **这是本轮最不容易假绿的一组 guard。**
- `tools/modeh_stamp_data.py`：有 `--check` 只读模式，写回路径只碰
  `Assets/Data/ModeH/*.json` 七个登记文件，规范化实现与 C# 端 `ModeHCanonicalDigest.cs`
  以及 `tests/modeh_canonical_json.py` 三处镜像。无破坏性操作。
- `tools/gen_codex_art.py`：**不含任何密钥字面量**，走 `OPENAI_BASE_URL` / `OPENAI_API_KEY`
  环境变量，只在文档字符串里指向 local-only 的 `docs/AI生图API和密钥.md`；
  机器相关路径经 `os.path.expanduser("~")` 拼接。
- `tools/build_codex_bundle.ps1`：**不含密钥**；但含两条硬编码本机绝对路径——
  `$UnityProj = 'D:\code\ykf\duckov_modding-main\UnityFiles\BossRush'`（`:13`）与
  `$UnityExe = 'C:\Program Files\Unity\Hub\Editor\2022.3.62f3\Editor\Unity.exe'`（`:14`），
  且没有环境变量兜底（对比 `compile_official.bat` 的 `GAME_PATH` 多路探测）。
  破坏性操作只有 `New-Item -ItemType Directory -Force` 与 `Copy-Item -Force`
  覆盖 `Assets/ui/codex_portraits`（已 gitignore），**不会破坏仓库**。
  个人工作区路径进了跟踪文件，与 AGENTS §12 的口径略有出入，记为观察项。

### 3.15 `.github/workflows/guards.yml`

`ubuntu-latest` + Python 3.12 + `python tools/run_guards.py --verbose`，
设了 `PYTHONIOENCODING`/`PYTHONUTF8`，并在注释里写明 CI 不跑编译、编译只能在装游戏的 Windows 上做。
`concurrency` 带 `cancel-in-progress`。触发面 push(main) / PR / 手动。
唯一实质风险已在 **F-8** 记录（`docs/contracts.md` 的纳管靠历史跟踪而非显式放行）。
**当前 CI 会红**，因为 F-1。

## 四、对前几轮已 Fixed 条目的复核

逐条读当前代码核对（不重复报告已修项，只判「修得对不对、有没有回归」）。

| Finding | 修复应有的形态 | 当前代码 | 复核结论 |
| --- | --- | --- | --- |
| **CR-2026-09-02-001**（F3 直载基地子场景黑屏 / 失败返程污染后续用例） | 走官方 `LoadBaseScene`、核对完整就绪、每个场内用例前恢复竞技场、加载中不叠加第二次加载 | `Scenes.cs:50` `SceneLoader.Instance.LoadBaseScene(null, true)`；`IsRuntimeReady`（`:128-138`）同时查 `!IsSceneLoading` + 场景名 + `LevelManager.AfterInit` + 玩家 active/alive + `GameCamera.isActiveAndEnabled`；`RunIsolatedCase:96` 每个用例前 `EnsureArenaForCase`；`Scenes.cs:30-37` 先等 `SceneLoader.IsSceneLoading` 落地，超时记 FAIL 不叠加 | **修复正确，无回归** ✅ |
| **CR-2026-09-02-002**（终章 `DamageInfo` 零值初始化空引用） | 用 `new DamageInfo(player)` 并填目标与位置 | `Runner.cs:617-624`：`new DamageInfo(player)` + `toDamageReceiver` + `damagePoint`，且带 5 行注释解释 struct 无参 new 不走可选参数构造器。同一修法已扩散到 `Diagnostics.cs:111`（击杀标准 Boss）与 `:160-164`（Mode D 逐只击杀，还补了 `SetInvincible(false)`） | **修复正确且已横向铺开** ✅ |
| **CR-2026-09-02-003**（引用三个不存在的 API 阻断编译） | 去掉 `damage.damageCreator`、`_arenaLease.Dispose()`、`_spectatorLease.Dispose()` | 全仓 `grep damageCreator` → **零命中**；主会话实测编译 0 error 0 warning | **已修，无回归** ✅ |
| **CR-2026-09-02-008**（Mode F 读瞬时结算标志） | 改比较生产成功结算**计数增量**，并同时验证模式退出与基地就绪 | `DeepFlows.cs:137-150`：`completedBefore` → `ModeFSuccessfulExtractionCountForValidation != completedBefore`，PASS 条件是 `triggered && resolved && !IsModeFActive && returnedToBase` | **修法方向正确**；但残留一个新风险：`resolved` 只在事件返回后**求值一次**，之后作为循环不变量参与 `if (!triggered \|\| !resolved) break;`（`:146`）。若结算是异步的，第一帧必 break、`returnedToBase` 恒 false → 假 FAIL。已列入「未验证线索」 ⚠️ |
| **CR-2026-09-02-009**（在 Boss 完成登记前击杀创建中对象） | 等生产登记的活跃 Boss 再定点 Hurt，并验 `IsDead` | `Diagnostics.cs:95-100` `ValidationGetCommittedStandardBoss()` 要求 `IsActive && bossesPerWave == 1 && Health != null && !IsDead && activeInHierarchy`；`DeepFlows.cs:225-245` 有界轮询到登记后才 `ValidationKillCommittedStandardBoss`，后者 `:116` 校验 `IsDead` 才返回 true | **修复正确，无回归** ✅ |
| **CR-2026-09-02-010**（多波把波次推进误当生成完成） | 快照逐只 Hurt，推进后有界等待新波活跃角色，两波都必须有真实活跃/存活/敌对登记对象 | `Depth.cs:88-133`：`ValidationDefeatModeDWave` 回调式清波 → 12s 等 `ModeDWaveIndex > firstWave` → 手工兜底开波 → **再**用 `nextWaveDeadline` 8s 轮询 `secondPlayable > 0`；PASS 需 `advanced && firstPlayable > 0 && secondPlayable > 0` | **修复正确，无回归** ✅ |
| **CR-2026-09-02-012**（同帧连击触发外部 Mod 的 `long.Parse` FormatException） | 每次真实击杀后让出一帧 | `Diagnostics.cs:138-148`：`foreach` 内 `ValidationDefeatModeDEnemy` 之后 `yield return null;`，并带注释说明「首个 tween 的 setter 尚未运行时同帧第二次击杀会读到非数字经验文本」 | **修复正确，无回归** ✅（且迭代的是 `ToArray()` 快照，不会撞集合修改） |
| **CR-2026-09-02-014**（丧尸撤离要求正数净化点却没准备样本） | 先用生产入口收 3 点并回读增量，再走撤离 UI/事件、两次派发、钱包差值、退出与返基地 | `Zombie.cs:19-43` 全部满足；且 `GameplayValidationCoverageGuard.py` 把这 8 个 token 连同「`succeed.Invoke();` 必须出现 2 次」写成了硬断言，防回归 | **修复正确并已加守卫锁死** ✅ |
| **CR-2026-08-31-009 / CR-2026-09-01-010**（两轮 F3 报告暴露的运行时回归） | 归口由 `tests/LatestPlayerLogRegressionGuard.py` 守 | 该 guard **不读本机 Player.log**，只读仓库源码断言 token（MagicBlend 补丁登记、6 个 Interactable 的 `GetOrCreateGroupList` 必须在 `base.Awake()` 之前、`GameplayValidationSuppressNotifications` 抑制必须 ≥2 处且有复位、`TickModeEFSpawnPostprocessScheduler` 必须排在 `TickWavesArenaRuntime` 之前、Mode D 敌对性修正必须早于登记、Mode E 清理四步顺序、Mode E/F 预设租约……）。本次实测 **PASS** | **修复仍在位，无回归** ✅。副作用：CI 上行为确定，**不会假绿也不会假红**（回答任务点）。 |
| 任务书列的 4 个基线红项 | — | 本次实测 `EmptyCatchGuard`、`GameplayValidationCoverageGuard`、`ModeHBattleSnapshotGuard`、`ModeHStandInGuard` **全部 PASS**。`GameplayValidationCoverageGuard` 转绿靠的是工作树里未提交的 `Assets/Data/GameplayCoverage.json` 新增 `DUCK_NPC_TOOLS` 域（含 2 条人工用例、`sources: Integration/NPCs/DuckNpc`）；两个 ModeH guard 对应的「未收尾编辑」已收尾 | **基线已过期**；当前唯一红项是 **F-1**（`StaticCacheLifecycleGuard`） |

### 4.1 一处「放宽 guard」的专项复核（结论：不是掩盖）

`tests/StaticCacheLifecycleGuard.py:131` 把「调用点归属方法」的向前搜索窗口由 5000 放宽到 12000。
这类改动最容易变成掩盖债务，所以做了**对照实验**（脚本 `scratchpad/run_guard_window.py`，
只在内存里替换常量后 exec，未改仓库任何文件）：

```
window=12000 → FAIL 1：ModeHCombatTelemetry 缺少 ResetStaticCaches
window=5000  → FAIL 2：上面那条 + EquipmentHelperIcon「有 ResetStaticCaches 但未在 OnDestroy 路径上被调用」
```

即放宽只翻转了 `EquipmentHelperIcon` 一个类。逐行核对该类的调用点：
`Integration/BossRushIntegration_StartAndScene.cs:232` `EquipmentHelperIcon.ResetStaticCaches();`
确实位于同文件 `:118` 声明的 `void OnDestroy_Integration()` 方法体内
（该方法把几十个子系统的清理串起来，末尾调用距方法声明约 5169 字符，正好越过旧窗口）。
**放宽的是启发式的归属射程，不变式本身没动；`EquipmentHelperIcon` 是真绿不是假绿。** ✅

（另注：这个放宽是**单调**的——`method_sigs[-1]` 取的是最近的一个方法声明，
扩大窗口只能把「一个都找不到 → 跳过」变成「找到了」，不会把原本判对的改判错。）

---

## 五、汇总

| 严重级 | 条数 | 编号 |
| --- | --- | --- |
| P0 Blocker | 0 | — |
| P1 Major | 1（**已在审查期间由另一会话修复并复核通过**） | F-1（`StaticCacheLifecycleGuard` 曾实测红） |
| P2 Minor | 3 | F-2（出击餐残留污染后续用例）、F-3（子协程异常整套卡死 + 通知永久静音）、F-7（两个 .bat fallback 分支恒返回失败） |
| P3 Note | 6 | F-4（潜在假绿三元式）、F-5（注释与实现不符 + 绕过 `CanDisplay`）、F-6（`ArenaSpawnPoints[0]` 无保护）、F-8（.gitignore 漏放行 `docs/contracts.md`）、F-9（wiki 同步检查死代码 + 路径拼写）、F-10（新用例文件漏进 `CASE_FILES`） |

**剩余待处理**：9 条（F-2 ~ F-10）。F-1 已由另一会话修复并经本报告复核。

**当前门禁实测状态（本次亲跑，只读）**

审查期间工作树被并发会话改动过两次，故记两次快照：

```
（审查中段）
guard runner: 共 519 个脚本，并发 8
=== 新增失败 ===
  [FAIL] StaticCacheLifecycleGuard.py  exit=1  8.0s
汇总: PASS=518  NEW-FAIL=1  KNOWN-RED=0  用时 15.9s
guard runner: FAIL

（审查收尾复测）
guard runner: 共 519 个脚本，并发 8
汇总: PASS=519  NEW-FAIL=0  KNOWN-RED=0  用时 18.0s
guard runner: PASS
```

`EmptyCatchGuard` 两次均 PASS，count=968 / budget=968（零余量）。
编译未重跑（按任务要求禁跑 `compile_official.bat`），直接采信主会话的
Build succeeded / 0 error / 0 warning。

**并发改动提示**：审查期间 `Integration/NPCs/DuckNpc/`、`ModeH/ModeHCombatTelemetry.cs`、
`ModeH/ModeHRuntimeModule.cs`、`DebugAndTools/F3DebugCheatMenu{Actions,Ui}.cs`、
`docs/制作教程/捏脸NPC工具.md` 等文件被其他会话改动过。
F-2 ~ F-10 的代码引用均在收尾时按行号复查过仍成立；若在此之后又有改动，请以最新代码为准。

**本报告未修改仓库任何文件**；scratchpad 里仅新增三个只读辅助脚本
（`compile_list_diff.py`、`run_guard_window.py`、`errlvl_probe.bat`）。

---

# 附：重派代理用的《审查简报》

# 审查简报（所有审查代理必读，先读本文件再动手）

任务：审查 BossRushMod 仓库自 commit `f9b83c0` 以来的**全部**代码改动（105 个提交 + 未提交工作树），
找出**已确认**的缺陷。仓库 `D:\code\ykf\BossRushMod`。《鸭科夫 / Escape from Duckov》的 Unity Mod，
C# 7.3，命名空间 `BossRush`，无 `.csproj`，`compile_official.bat` 显式列出每个 `.cs`。

## 0. 硬性约束：只读

**不得修改仓库任何文件。** 不要 Edit/Write 仓库内文件，不要做任何改状态的 git 操作，
**不要运行 `compile_official.bat` / `test_bossrush_official.bat`**（它们会部署到游戏目录，主会话已跑过）。
可以运行 `python tests/XxxGuard.py` 或 `python tools/run_guards.py --filter Xxx` 读结果。
你唯一可写的文件是分配给你的报告路径。

## 1. 纪律：边查边落盘

上一轮代理在接近完成时被中断，全部研究成果丢失。因此：
**每审完一组文件就把该部分写进你的报告文件，不要攒到最后一次性写。** 文件可反复重写/追加。
先写一个带小标题的骨架，然后逐节填。

## 2. 必读的仓库规范（按顺序）

1. `AGENTS.md`（根目录）——唯一事实来源。重点：
   - 第 4 节 Golden Rules 4.1–4.14（4.1 新增 `.cs` 必须登记编译清单；4.3 TypeID 严格递增；
     4.4 `DisplayNameRaw` 必配本地化注入；4.5 刷怪敌对性安全网；4.6 事件订阅幂等+退订；
     4.7 防御式 catch 是有意策略**不要当 bug**；4.8 Config 三层归位；4.9 Hooks 分层；
     4.10 guard 同步；4.11 变异词条不变式；4.12 重运行时工作按实际使用门控；
     4.13 同步 `.qoder/repowiki/`；4.14 UI 走共享库）
   - 第 5 节不可破坏契约、第 6 节兼容性分类、第 7 节 AI 修改前硬要求、第 8 节验证要求、
     第 10 节 off-limits、**第 14 节全部近期教训**（最重要，逐条读）
2. 你负责目录下的 `AGENTS.md`（若存在）：`Integration/`、`Patches/`、`Utilities/`、`ZombieMode/`、`tests/`、`docs/`
3. `CODE_REVIEW.md`：第 3 节严重级定义（P0 Blocker / P1 Major / P2 Minor / P3 Note）、第 6 节必查清单、第 7 节输出格式
4. `CODE_REVIEW_FINDINGS.md`：`grep -n "^### CR-" CODE_REVIEW_FINDINGS.md` 后读与你范围相关的条目正文
5. `docs/contracts.md`、`docs/架构说明/` 下与你范围相关的专项文档

## 3. 第 14 节的关键教训（本仓库反复踩的坑，审查时优先查这些形态）

- **静默失败是主要缺陷形态**：编译绿、guard 绿、日志最多一行 warning，功能实际不工作。
  典型是「实现完整但入口没接线」——方法零调用点、事件没订阅、条件永假。
  未被调用的 `internal` 方法编译完全合法，guard 断言结构不验证可达性。
  **所以每个关键方法都要 grep 全仓库确认调用点**（排除 `鸭科夫源码/` 与 `tests/`）。
- **官方距离休眠会关掉 Mod 刷的怪**：`setActiveByPlayerDistance` 默认 true 时角色进官方
  `SetActiveByPlayerDistance`，玩家跑远后怪被静默 `SetActive(false)` 但 `IsDead` 仍 false，
  波次永不结算。须经 `Utilities/SpawnedEnemyActivationHelper.cs` 解除。
- **角色没有 `MoveSpeed` 这个 stat**：官方移动只读 `WalkSpeed` / `RunSpeed` / `Moveability`。
  给 `"MoveSpeed"` 挂 Modifier 会被 `RuntimeStatModifierTracker` 当缺失 stat 静默丢弃。
- **手工 `MoveNext()` 驱动协程必须透传 `Current`**，否则子协程一次都不跑。
  正确写法见 `ModeHRuntimeModule_MatchFlow.DriveMatchSpawning`。
- **`JsonUtility` 只认公有字段**，不支持 Dictionary、属性、`long`；对两层对象数组会**静默留空**
  （只填顶层字段）。凡用它解析多层结构的都要查。反序列化 DTO 的 CS0649 是误报，
  应定点 `#pragma warning disable/restore 0649`。
- **ModConfig 白名单陷阱**：`OnModConfigOptionsChanged` 第一件事是
  `if (!IsHandledModConfigOptionKey(k)) return;`。键没登记进 `Config/ConfigModConfigKeys.cs`
  的白名单 → 玩家在 UI 里拨动它毫无反应且不报错。仓库踩过至少 6 次。
- **内容系统总开关恒开**（owner 2026-08-30 拍板）：玩法系统总开关不注册进 ModConfig UI、
  默认 true、并由 `ForceContentSystemSwitchesOn()` 抹平老档 false。调参旋钮照常注册。
- **物品未登记 `BossRushDynamicItemRegistry` → 重启后退化为官方 `FallbackItem`**（契约第 6 节）。
- **`catch` 子句体内不能 `yield return`**（CS1631）；`yield break` 可以。
- **UI 规范**：`sortingOrder` 用 `BossRushUILayers` 常量；遮罩用 `BossRushUIColors.Backdrop`；
  字体用 `BossRushUI.ApplyGameFont` / `ZombieModeUIHelper.GetGameFont()`，
  **绝不用 `Resources.GetBuiltinResource<Font>("Arial.ttf")`**（渲染不了中文）；
  新建文本用 TMP；`CanvasScaler` 必须调 `ZombieModeUIHelper.ConfigureCanvasScaler`。
- **本地化 key 双向对齐**：消费了但未注入 → 游戏内显示 `*BossRush_xxx*`；
  注入了但零消费 = 死键。`{0}` 占位数必须与 `string.Format` 实参数一致。中英都要有。

## 4. 主会话已完成的基线（不要重复做）

测量时间 2026-09-03 22:20–22:45，结果详见 `scratchpad/reports/00_lead_baseline.md`。

- **Windows 编译已跑**：`compile_official.bat` → **Build succeeded，exit 0，零 error 零 warning**，
  DLL 与全部 Assets 已部署。
- **全量 guard 已跑**：`python tools/run_guards.py --verbose` →
  **517 脚本 / PASS=513 / NEW-FAIL=4 / KNOWN-RED=0**，`tests/known_red_guards.txt` 为空。
- **4 个红项的归属已查清，任何代理都不需要再查**：
  1. `EmptyCatchGuard`（976 > 预算 968）：逐文件复算，超支的 8 个**全部**来自 22:04 之后
     新建的未跟踪模块 `Integration/NPCs/DuckNpc/`（`DuckNpcDebugProbe.cs` 2 + `DuckNpcFaceCatalog.cs` 6），
     扣掉后恰为 968 持平。guard 失败时会打印全量清单，那些不是新增债务。
     预算自 base 从 919 上调到 968（+49），是本轮新子系统带来的。
  2. `GameplayValidationCoverageGuard`：唯一报错也是 DuckNpc 缺测试映射，同源。
  3. `ModeHBattleSnapshotGuard`（11 条断言全红）与 4. `ModeHStandInGuard`（1 条）：
     21:17 有人从 `ModeH/ModeHBattleSnapshot.cs` 删除 §17.4 局中重建链
     （`ModeHSnapshotRebuildPlan` 类、`Validate`、`IsPositionUsable`、`TryRestoreHealth`）
     与 `ModeHCombatControl.cs:799` 的 `RestoreFromSnapshot`，但**没同步这两个 guard**。
     删除本身论证成立（全仓零调用点 + 冻结转换表 `Recovering` 无通向战斗态的出边）。
     删除注释承诺的 `ModeHSnapshotRebuildAbsenceGuard` **不存在**。
     `ModeHStandInGuard` 断言的 `error_swap_rebuild_profile_mismatch` 从未出现在任何提交。
- **已复核为真正修好的**：ERROR 互换输入让渡（`ModeHSpectatorLease.cs:378/396` 新增
  `YieldInputForErrorSwap`/`ReclaimInputAfterErrorSwap`，有真实调用点
  `ModeHRuntimeModule_CombatFlow.cs:570/571`，`:1070` 释放路径兜底）；
  `_errorSwapAttempted` 每场一次闩（`ModeHCombatControl.cs:512` 置位、`:147` 开局复位、
  `RestoreErrorSwap` 不清除）。
- **已确认的一条新缺陷**（归 ModeH 内容/UI 代理写进报告）：
  `ModeH/ModeHRuntimeModule_MatchFlow.cs:933` 双前缀——`ModeHOddsController.cs:600` 已写入完整 key
  （`entry.LabelKey = LocalizationKeyPrefix + labelSuffix`），933 行又拼一次 →
  `BossRush_ModeH_BossRush_ModeH_<suffix>`，未注册。同文件 `:955` 的 `Button_LockIn` 写法正确可作对照。
  全库扫描该模式只此一处。

## 5. 并发写入警告（重要）

本仓库当前有 **6 个并行会话在写**。`find -mmin -360` 显示这些文件在 22:04–22:28 仍被改动，
**不属本次审查范围**，遇到不必深究，但报告里若涉及请注明：

| 时间 | 文件 |
| --- | --- |
| 22:04–22:16 | `Integration/NPCs/DuckNpc/*.cs`（5 个，未跟踪，捏脸 NPC 模块） |
| 22:14 | `DebugAndTools/F3DebugCheatMenu{Actions,Ui}.cs`（接 DuckNpc 的 F3 入口） |
| 22:15 | `compile_official.bat`（登记 DuckNpc + `F3GameplayValidationModeHErrorSwap.cs`） |
| 22:15 | `Utilities/AlwaysOnRuntimeHooks.cs` |
| 21:15–21:23 | `ModeH/` 下 7 个 `.cs` |

请用 `git diff HEAD -- <你的目录>` 区分「已提交改动」与「未提交改动」，
未提交部分在报告里单独标注——它可能是别的会话正在写的半成品。

## 6. 证据标准

- 每条 finding 必须有 `文件:行号` + 关键代码摘录 + 具体失败场景（什么状态/输入 → 什么错误结果）。
- 必须 grep 全仓库确认调用点/接线情况（排除 `鸭科夫源码/`）。
- 反射与 Harmony 目标必须去 `鸭科夫源码/`（官方反编译源码，对照用、非 Mod 源码）核对类名、
  方法名、参数签名、重载唯一性、字段是否存在。
- 静态推断必须写「静态确认，未运行验证」。拿不准的放「未验证线索」区，**不得混入 confirmed**。
- **不要**把以下当缺陷：防御式空 catch 本身、离散模式 bool、`docs/` local-only、
  已移除的 loot 变异类别。**不报纯风格问题。**
- 不重复报告 `CODE_REVIEW_FINDINGS.md` 里已标 Fixed 的条目，但要**逐条验证修复是否真的正确、
  有无引入回归**，写进报告第四节。

## 7. 报告格式（中文）

```
# <你的范围> 审查报告
## 一、Confirmed Findings（按 P0>P1>P2>P3 排序）
### F-<n>：<一句话标题>
- 严重级：P0 Blocker / P1 Major / P2 Minor / P3 Note
- 兼容分类：SAFE/COMPAT/SCHEMA+/SCHEMA-/WIRE+/WIRE-/BREAKING/OPERATIONAL
- 位置：`path:line`（可多处）
- 证据：代码摘录 + 调用链/grep 结果
- 失败场景：...
- 玩家/维护影响：...
- 建议修复：...
- 验证需求：编译/guard/实机
## 二、未验证线索（需实机或 owner 确认）
## 三、已核对且干净的关键面（简述查了什么、结论）
## 四、对前几轮已 Fixed 条目的复核（逐条：是否确实修好、有无回归）
```

无 finding 也要如实写「未发现 confirmed issue」。**系统地逐文件过，不要抽样。**
最终回复只需给出报告路径和 finding 数量摘要（P0/P1/P2/P3 各几条）。
