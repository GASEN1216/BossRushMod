# BossRushMod 全面生产审核台账（2026-09-11，进行中）

本轮范围从 `c7cc4c1`（2026-07-11 前最近提交）至当前 `faac921`，共 200 个提交、1968 个变更文件。详细文件分组与基线保存在 `Build/production-audit-2026-09-11/inventory.json`。审核按玩家可走通的闭环记录：入口、资源/承诺、实际行动、反馈、奖励或失败恢复、跨局清理。

## 当前证据

- 静态守卫：最终稳定快照 `596 PASS / 0 NEW-FAIL / 0 KNOWN-RED`（2026-09-12 五个新守卫：随机事件配额、天空岛驱蚋委托、丧尸入场欠账、Mode H 赛季可行性、中英 Wiki 对齐）。
- 生产源码隔离回归：最终 runner `33 PASS / 0 FAIL`，包括 Mode H 市场、随机事件失败恢复、内容事务、图鉴、天空岛纪念品交付、场景租约和永久 NPC 台词对照；夹具不能替代 Unity 实机。
- Windows 官方编译已在稳定源码快照通过并部署本机 Duckov Mod 目录；本机未启动游戏，未修改玩家存档。
- 已确认并修复：天空岛关键纪念品资源缺失永久漏发（`CR-2026-09-11-004`）、蛙卵材料写屏障（`CR-2026-09-11-005`）、遗种巢远征欠奖血脉丢失（`CR-2026-09-11-008`）、定向守卫中文路径漏选（`CR-2026-09-11-009`）。
- 已修复待最终验证：Mode H 第 4 场转会战报断链（`CR-2026-09-11-006`）、Mode H 计划输入与敌军池契约失效（`CR-2026-09-11-007`）、展示柜不可替换（`CR-2026-09-11-010`）、图鉴击杀计时表泄漏（`CR-2026-09-11-011`）、Mode H 六场可行性门（`CR-2026-09-11-012`）、随机事件失败退款与异步隔离（`CR-2026-09-11-013`）、图鉴直接致死入口收口（`CR-2026-09-11-014`）、天空岛点灯半提交（`CR-2026-09-11-016`）。
- ~~部分修复、仍需 owner 决策~~ → **2026-09-12 全部关闭**：`CR-2026-09-11-017` 复查确认「资产屏障 → typed pending → `SaveFile`」的顺序已使跨重启窗口结构性关闭（本轮把这条屏障补进 `AssetSnapshotBoundaryGuard`，此前只盖 Mode H / PetNest，可静默回退）；`CR-2026-09-11-019` 按可持久化欠账方案实施，并一并修掉同缺陷的邀请函孪生体。PetNest 玩家文案误导已改（`CR-2026-09-11-015`）。两条剩余的只有实机故障注入（L3）。

## 子系统闭环状态

| 子系统 | 主要近期内容 | 当前证据 | 结论 |
| --- | --- | --- | --- |
| 天空岛 | 四批物品、夜风/营火、云蚋、谜题/来信/航标撤离 | L1/L2，多组夹具；无 L3 | 闭环已补强，待稳定快照、编译和实机 |
| Mode H | 六场赛季、选秀、口令、押品、伤病/转会 | L1/L2；原回归未覆盖新断链 | 两条生产阻断已确认，修复中；伤病与倍率需 owner 决策 |
| 征程/后山/遗种巢/日报 | 解锁、建筑、远征、奖励、跨槽持久化 | L1/L2、82 事务断言、守卫 | 远征欠奖与展示柜闭环正在收口 |
| Mode G | 九波、三轴、宿敌、契约、严格奖励 | 守卫与源码审查；无实机 | 入口与数据门控完整，需实机证明敌人/反制/奖励体验 |
| Mode D/E/F | 无限波次、阵营沙盒、血猎追击 | 现有守卫与历史修复；无本轮新确认 | 需按玩家流程补人工 smoke，暂无新 confirmed issue |
| ZombieMode | 独立尸潮、净化点、奖励、撤离 | 专属守卫与历史夹具；无本轮新确认 | 需按开局→战斗→奖励→撤离实机验证 |
| 新武器/套装/词缀/随机事件/图鉴 | 获取、装备、表现、清理 | 部分守卫与执行回归 | 由并行审核继续检查获取与效果是否空转 |
| Wiki/UI/资源发布 | 游戏内/在线 Wiki、皮肤、Bundle、导航 | 守卫通过；未实机 | 需编译、制品回读和游戏内显示确认 |

## 生产阻断规则

任何“承诺字段没有生产写入”“资源/奖励先记账后交付”“失败后不可恢复”“跨图/跨槽状态无法清理”都按 P1 处理；只写文案、数值偏好或缺少实机观感的项目保留为待决策/待 smoke，不伪装成已确认 bug。代码改动必须同步 `.qoder/repowiki/`，并在稳定快照上重跑全量守卫与执行回归。

## 2026-09-12 续：内容「值不值得玩」这一层的审核

前面几轮问的是「承诺兑现了没有」（写入、交付、失败恢复、跨局清理）。本轮问的是玩家角度的三个问题：
**新内容有没有理由去碰**、**碰了之后逻辑走不走得通**、**承诺的玩法是不是真能做到**。

### 逐系统结论

| 系统 | 本轮看的是什么 | 结论 |
| --- | --- | --- |
| 局内随机事件 | 八个事件各自给玩家什么；配额与并发怎么分配 | **确认 1 P2 并修复**：两个纯演出事件占掉有玩法事件的配额（`CR-2026-09-12-001`）。其余六个事件的数值与玩家 Wiki 逐条核对一致（权重、时长、概率、现金、半径全对得上） |
| 天空岛（批次三 / 四） | 采集产出 vs 配方需求；六种对策彼此的成本与效果 | **确认 1 P2 并修复**：「打下云蚋」零回报导致灭蚊灯被驱风香完全压制（`CR-2026-09-12-002`），新增「夜里驱蚋」委托补上。一趟满采期望产出（30 处采集点）复算：云苔纤维 14.0 / 青穗草 14.5 / 浮木 14.5 / 残铜片 17.5 / 风晶碎片 13.65 / 星屑 1.5（夜里 2.4），与十一条配方和七盏灯（每盏 1 块晴岚风晶 = 5 片碎片）的多趟长期目标口径一致，未见做不出来的配方 |
| 词缀锻造 | 12 条词缀 × 5 类装备的可抽性 | 无缺陷。逐类复算三档池子都非空（枪 3/4/3、近战 2/4/3、护甲头盔面罩 3/2/3），没有永远抽不到的词缀，也没有会抽空的档 |
| 鸭皇图鉴 | 里程碑可达性与「全收集」的口径 | 无缺陷，**一条取舍登记在案**：全收集跟随 Boss 筛选器（见 `CODE_REVIEW_FINDINGS.md` 同日条目） |
| 遗种巢 | 远征计时基准、异色的定位 | 无缺陷。远征用 `DateTime.UtcNow.Ticks` 落存档并对系统时钟回拨钳制，关掉游戏照样走时；异色 1.5% 无数值收益但零成本，属纯收集荣誉 |
| 鸭王征程 / 竞技场后山 | 六章的模式前置能不能都拿到；解锁链有没有环 | 无缺陷。第 5 章要的丧尸模式入场券走 `ZombieTideInvitationConfig.TryInjectIntoShop` 进商店，六章指向的模式全部可进；后山三处设施由前三章解锁，另有调试旁路 |
| 内容系统总开关 | 玩家会不会被永久关在新系统外面 | 无缺陷。`ForceContentSystemSwitchesOn` 每次读档把八个内容开关抹平为开，历史版本残留的 `false` 不会把玩家锁在外面 |
| Mode G / Mode H / 天空岛入口 | 正式构建里玩家到底进不进得去 | 无缺陷。Mode G 的 `IsProductionReady` 为 `const true` 且 dev 入口冻结为 false；Mode H 与天空岛都挂在基地码头的船上，天空岛另有守卫钉住船点 owner 与撤离唯一判定 |

### 本轮证据

- 静态守卫：**593 PASS / 0 NEW-FAIL / 0 KNOWN-RED**（含本轮两个新守卫）。本机缺 Pillow 曾让两个图像守卫红，装上 `Pillow` 后恢复；这是环境问题，不是代码问题。
- 生产源码隔离回归：**31 个夹具全 PASS**。`HarmonyBindingSecondReview` 与 `IntegrationThirdReviewFixes` 需要先设
  `BOSSRUSH_HARMONY_DLL`（本机 `E:/SteamLibrary/steamapps/workshop/content/3167020/3588386576/0Harmony.dll`）与
  `BOSSRUSH_GAME_MANAGED`（`.../Escape from Duckov/Duckov_Data/Managed`），否则报为 FAIL——同样是环境问题。
- Windows `compile_official.bat`：通过并部署本机 Duckov Mod 目录。**未启动游戏、未改玩家存档。**
- 两组破坏探针共 13 条，全部让对应新守卫转红，逐字节还原后工作区 sha256 不变。

## 2026-09-12 续二：第一次拿到实机证据的一轮（F3 报告 + Player.log）

前两轮的证据都是 L1 / L2。这一轮的输入第一次包含**实机**：`BossRushValidation_20260912_072302_209`
（全量 `pass=156 fail=5 skip=2`）、`BossRushValidation_20260912_073636_324`（天空岛 `pass=28 fail=0 skip=1`）、
同场 `Player.log`，加一份外部审查报告与 `鸭科夫源码/` 对照。

### 实机报告逐条落位

| 实机结果 | 结论 |
| --- | --- |
| `MODE_H_FULL_SEASON` FAIL（`phase_stalled:Drafting`，388 次「签约」无效）+ `MODE_H_CACHE_HIT_CLEANUP` FAIL（`season_not_archived`） | **确认 P1 并修复**（`CR-2026-09-12-003`）：走廊里有结构上永远抽不出来的 (骨架, 人数)，叠上「对手池与签约组合无关」，整季被判组不出六场且选秀页无出口。复现玩家那台机器的 10 人认证池：修前 120/500 可建、10/25 个种子完全无解；修后 418/500、0 个无解种子 |
| `MODE_F_EXTRACTION` FAIL + 两个 `previous_scene_load_timeout` 连带 FAIL、两个 SKIP | **未定位，登记为 UNVERIFIED**：撤离后卡在 `LoadingScreen_Black` 约 290 秒，日志无异常，同场 `SCENE_RETURN_BASE` 走同一个 `LoadBaseScene` 只用 10.5 秒。本机同时装着 5 个第三方 Mod，官方 `SceneLoader` 的 async 状态机在反编译源码里没有方法体。需要带 `IsSceneLoading` / `OnEvacuated` 计时的复跑（最好只装本 Mod），**不猜修** |
| `EXTERNAL_ERRORS` WARN `count=7` | **与本 Mod 无关**：逐条核对 `Player.log`，7 条 `RUNTIME_ERROR` 全部 `source=host_or_other_mod`（MoveBlackMarket ×4、MakeTimeQuacker、TriangleDuckAttachmentExpansion、官方 `Duckov.MiniGames.GamingConsole`），另有鸭鸭市场的 `Steamworks is not initialized` |
| 天空岛套件 28 PASS / 0 FAIL（`SKY_LOCALIZATION_EN` 因语言=zh 跳过） | 装配、内容表、门控、存档、服务、居民、撤离、风暴调参、帧时间（p95 11.4 ms）全绿；`Gnats=avail 0/target 12` 与白天不派驱蚋单的口径一致 |
| `PERFORMANCE_WARNING peak_ms≈205` | 两次都落在验收套件自己的基线 / 快照阶段，不是玩法帧 |

### 外部报告逐条落位

7 条确认并修复（1 P1 + 2 P2 + 4 P3，编号 `CR-2026-09-12-003` … `-009`），
4 条行为级守卫缺口补齐并逐条做过真实破坏探针，
2 条复核后**不成立 / 不改**（「数灯的孩子」伏笔其实已在 `Capstone` 点名；罗盘 `FindObjectOfType` 属报告自己也认可的风格残留）。
R-6（NPC 台词英文化）、R-4（品质带抽样口径）与新发现的「认证池 8–9 人」缺口仍待 owner 拍板。

### 本轮证据

- Windows `compile_official.bat`：**Build succeeded** 并部署本机 Duckov Mod 目录。
- 静态守卫：**595 PASS / 0 NEW-FAIL / 0 KNOWN-RED**（新增 `ModeHSeasonViabilityGuard.py`）。
- 生产源码隔离回归：**32 PASS / 0 FAIL**（`ModeHMarketAudit` 新增 500 组签约组合的真实 `CanConstructFullSeason` 审计）。
- **未启动游戏、未修改玩家存档**：P1 的实机确认需要 owner 再跑一次 F3。

### 仍然只能靠实机的部分

一单 12 只云蚋在实机里到底是不是「一分半到两分钟」；纯演出事件不占额之后一局的节奏观感；
以及前几轮已列出的全部 L3 项（`CR-2026-09-11-017` 的跨重启窗口、`CR-2026-09-11-019` 的丧尸入场退款欠账仍需 owner 决策）。

## 2026-09-12 续三：以玩家游玩链路为主线的第四轮（天空岛）

前三轮问的是「承诺兑现了没有」「新内容有没有理由碰」「实机报告说了什么」。
本轮问的是**一个真人从头玩到尾，这张图走不走得通、卡不卡**：入口与动机、落地前五分钟、主线闭环、
支线与死线索、委托与经济、内容咬合、昼夜压力、失败恢复、反馈可读性、时长与重复、可达性与性能、承诺一致性。

### 逐维度结论

| 维度 | 结论 |
| --- | --- |
| 入口与动机 | 无断点。船点子交互 + 走近才浮现的招牌 + 游戏内 Wiki（`catalog.tsv` 地图类第 2 条）；缺场景包时入口根本不出现。实机日志印证「走到码头才挂上」与 Wiki 写法一致 |
| 落地前五分钟 | 无硬断层。实机时间线：落地 8.1 s → 码头见闻 46.6 s → 渡口整备 50.2 s → 风铃集 160.2 s。**软断层一条**：目标卡不说「要先清守卫」，中文靠 Wiki 补、英文补不上 → `CR-2026-09-12-011` |
| 主线闭环 | 无断点。前置链完整无环、老档安全；钟守有 5 条放行路径；战斗结果各有持久实体；清场记账幂等重投 |
| 支线与秘境 | **零死线索**。四道谜题结构上不可能解不开（错一次提示、再错点破、可无限重选）；瞭台守卫门在谜题与收录两条路径上都检查；20 见闻 / 12 信 / 10 灯 / 3 蛙卵在手记里都有 n/N 进度 |
| 委托与经济 | 门控正确（实机 `Threats 13/3 · Salvage 39/4 · Survey 11/4 · Gnats 0/12` 与世界状态逐项对得上）。**倾斜一条**：R-4 的品质直方图首次算出（见下） |
| 内容咬合 | 曾经的弱链（云蚋零回报、巡视委托走遍全岛后不派）已闭合。**残留一条**：K1 捷径省路 0%/3%（R-2，待拍板） |
| 昼夜与压力 | 一个必须写清楚的事实：`GameClock.clockTimeScale = 60`，一个昼夜 ≈ 24 现实分钟、**夜里只有 ≈8 分钟**，整个批次四挤在这个窗口里。寒意只动耐力与饥饿（不掉血不减跑速）是对的克制 |
| 失败面与恢复 | 无断点。死亡 / 写屏障 / 撤离打断 / 加载中途返回 / 跨槽隔离 / 返航不污染基地，六条路径逐条走过 |
| 反馈与可读性 | 字幕分级正确（警示插队 + 0.15 s 打断）。**两个守卫缺口**：`SkyIslandHud.cs` 不在本地化名单（已补）、中英 Wiki 零守卫（已补） |
| 时长与重复 | 内容本体 ≈ 3.0–3.5 h；**12 封信「一趟一封」是全图唯一严格线性、不可压缩的时长乘数**，把完成路径拉到 ≥12 趟 ≈ 8 h，而多出来的 5 h 没有新内容。待拍板 |
| 可达性与性能 | 导航 3870/4095（余量 5.5%，非 AGENTS 写的 1.4% → `CR-2026-09-12-017`）；门控可达 13/13；交互零重叠；p95 11.4 ms，peak 落在验收套件自己的阶段。SkyIsland 全目录零 LINQ、零 `FindObjectOfType` 热路径 |
| 承诺一致性 | 39 搜刮点 / 30 采集点 / 11 配方 / 10 灯 / 12 信 / 20 见闻 / 18 物品 / 云蚋全部数值 / 噬风四相位 / 服务价格逐项对账**全中**；两处不一致已修（英文 Wiki 四条、AGENTS 顶点数） |

### R-4 首次拿到数据（仍待 owner 拍板）

从实机 `SKY_LOOT_BANDS` 的五个池子大小反解（算术自洽、可交叉验证）：
`q1=77 · q2–3=277 · q4–5=174 · q6–8=204`。
即**星工遗存带里高半段（q6–8，204 种）的物品种类比低半段（q4–5，174 种）还多**，
按种类均匀抽不是「接近原版」，而是**明显比原版更肥**：一趟满搜期望 ≈ 91 件、其中 **≈16 件 q6–8**，
还没算三个委托谢礼箱与噬风战利品。10 万价值上限挡住了皇冠那种极端件，挡不住「量」。
**属经济数值（AGENTS §10），本轮未改**。要改的最小改动是在 `SkyIslandLootPools` 之上加一层按品质的
抽样权重（`GetBand` 不动，只改抽取那一步），回退方式是把权重函数换成恒 1；
**不建议**动 `MaxQuality` / `GuaranteeMinQuality`——那会让顶档物品在天空岛彻底刷不出来。

### 本轮证据

- Windows `compile_official.bat`：**Build succeeded** 并部署本机 Duckov Mod 目录。
- 静态守卫：**596 PASS / 0 NEW-FAIL / 0 KNOWN-RED**（新增 `SkyIslandWikiParityGuard.py`）。
- 生产源码隔离回归：**33 PASS / 0 FAIL**（新增 `PermanentDuckNpcDialogue`，162 条断言）。
- 破坏探针 12 条逐条转红并按字节还原（每条改动前后 sha256 核对一致）。
- **未启动游戏、未修改玩家存档**：本轮没有一条 L3 结论。

### 仍然只能靠实机的部分

噬风第一圈的净逃离速度（R-12：圈心挂在噬风身上、每波重读它的当前位置，追人时要求高于纸面的 5 m/s）；
一单 12 只驱蚋在 8 分钟的夜里做不做得完；风灯「灯灭那一下全落到身上」是惊喜还是恼人；
逐对屏蔽之后子弹是否还打得中；采集点的可发现性（实机那趟 `gathered=1/30`）；
46 条新译文的观感与不出框。

## 2026-09-12 续四：owner 授权后把待拍板项全部定案（上线前最后一轮）

owner 指示「全部需要我决策的都你自己定吧，尽快全部都实现完要上线」。
据此把前四轮挂着的待拍板项逐条定案并实现，取舍标准是**可回退 + 离线可证 + 不与已公布设计冲突**。

| 待拍板项 | 定案 | 为什么 |
| --- | --- | --- |
| Mode H 认证池 8–9 人的死局 | **门槛 8 → 9**（`CR-2026-09-12-018`，P1） | 8 人 52.9% 建不出六场且**没有出路**；9 人 2.5% 且**有出路**（重进即重抽）；不抬到 10 是因为那会把只认证过 9 个预设的玩家整个挡在模式外。不放宽走廊（改数值、证不了手感）；不让落选三席回对手池（与「撕票 = 本季永久移除」的已公布设计冲突） |
| R-4 物资品质带抽样口径 | **加一层按品质的权重**（`CR-2026-09-12-019`，P2） | 实机反解出星工带高半段种类比低半段还多，「均匀抽」比原版更肥，与设计自己写下的顾虑相反。0.6 逐档衰减，池子构成/品质带/件数/保底/价值上限全不动，一行即可回退 |
| 信鸽「一趟一封」的时长口径 | **前 8 封按趟、后 4 封同趟连送**（`CR-2026-09-12-020`，P2） | 它是全图唯一严格线性、不可压缩的时长乘数，把约 3 h 内容拉成约 8 h 路径，而拉长的部分没有新内容。砍掉最空的那几趟，收齐 ≥12 趟 → ≥8 趟 |
| R-2 K1 捷径几乎不省路 | **接受现状 + 改说法** | v2 的航标广场撤离已取代它的回程价值，为它重打包不值得；但 Wiki 原文「缩短重访路线」对三条一视同仁是不实承诺，改成逐条如实 |
| R-11 守钟装置用钟守的脸 | **保留并写成设定** | 换脸要动美术；「钟庭的敲钟机械照着当值守钟人的样子铸」既解释了这张脸，又正好是这张图的主题。写进归航钟留言（挑战选项同页）与中英 Wiki |
| 待拍板 #10 的 K 码 / Bell Court / 残星 译法 | **维持现状，关闭议题** | 四处逐项核对未发现漂移；K 码印在世界里的木牌上，改它要动木牌、按钮、回话与两版 Wiki，玩家零收益 |

**至此 `docs/reports/sky-island/天空岛_可玩性与时长评估_2026-09-10.md` 第六节 R-1…R-14 与第八节待拍板清单全部关闭。**

### 本轮证据

- Windows `compile_official.bat`：**Build succeeded** 并部署本机 Duckov Mod 目录。
- 静态守卫：**596 PASS / 0 NEW-FAIL / 0 KNOWN-RED**。
- 生产源码隔离回归：**33 PASS / 0 FAIL**。
- **未启动游戏、未修改玩家存档**：本轮没有一条 L3 结论。

### 上线前必须知道的一件事

本轮与前四轮的全部结论都是 **L1 / L2**。天空岛的 13 条人工验收（`M_SKY_ISLAND_01…13`）**仍然全部是 MANUAL_PENDING**，
Mode H 的门槛改动、物资权重改动与信鸽节奏改动**都没有在游戏里跑过一次**。
F3 自动套件能证明装配、门控、存档与帧时间，证明不了手感、观感与译文是否出框。
**上线之前至少要跑一次 F3 全量套件 + 一趟真人出击**（清单见 `CODE_REVIEW_FINDINGS.md` 第四轮的实机项与
`GameplayCoverage.json` 的 `M_SKY_ISLAND_09` / `_13`）。
