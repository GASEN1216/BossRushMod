# 代码结构优化验证记录

日期：2026-05-09

范围：

- runtime module 注册入口第一阶段。
- Mode D runtime module 外壳。
- 物品 / 装备内容注册 wrapper。
- Mode D 卡波自检 tick 从 `ModBehaviour.Update` 迁移到 `ModeDRuntimeModule` 转发。
- DebugTools runtime module 与 DevMode 热键 wrapper。
- Achievement runtime module 与成就 UI 热键 / 初始化 / 清理 wrapper。
- Common NPC runtime 边界与公共 NPC 生成 / 清理 wrapper。
- WavesArena、Mode E、Mode F、ZombieMode no-op runtime module 外壳。

## 已完成的命令行验证

以下验证已在当前工作区执行通过。最近一次刷新时间：2026-05-09 15:11 CST。

- `python3 tests/ArchitectureStructureGuard.py`
- `python3 tests/ContentRegistryGuard.py`
- `python3 tests/OfficialCompileListFileExistenceGuard.py`
- `git diff --check`
- `cmd.exe /c "echo.|compile_official.bat"`
- `cmd.exe /c "echo.|test_bossrush_official.bat"`

最近一次 `test_bossrush_official.bat` 中 8 个逻辑测试全部通过，脚本完成编译、Wiki 同步和 DLL 部署。

## 已提交的结构优化批次

- `ea6789d refactor(core): add runtime module registration baseline`
- `d13d861 refactor(moded): add runtime module shell`
- `9844390 refactor(integration): centralize content registration`
- `b420c1d test(integration): harden content registry guard order checks`
- `a2a27d1 refactor(moded): route integrity tick through runtime module`
- `2b5e890 refactor(debug): route debug tools through runtime module`
- `fc8f44a refactor(debug): centralize dev hotkey routing`
- `cee8a2b refactor(achievement): route achievement ui through runtime module`
- `72d1c49 refactor(npc): add common npc runtime boundary`
- `9f67d8a refactor(modes): add runtime module shells`
- `31a036f test(smoke): add bossrush log scan helper`

## 游戏内手工烟测清单

命令行验证不能替代真实游戏内行为确认。下一次进游戏时按以下清单记录结果：

- 启动游戏并进入 `Base_SceneV2`。
- 确认普通商人仍包含 BossRush 船票、冒险日记、成就奖章、阿文代币、砖石、尸潮邀请函等原有内容。
- 通过现有地图选择流程进入标准 BossRush 竞技场。
- 确认竞技场初始化、路牌选项、第一波开始、敌人生成、击杀结算和离场行为保持不变。
- 用裸体 + 船票流程进入 Mode D。
- 确认开局装备、Mode D 路牌选项、第一波开始、卡波自检间隔和离场清理行为保持不变。
- 装备或生成一个依赖装备 bootstrap 的现有自定义武器能力，例如 Frostmourne 或 FenHuangHalberd，确认能力仍能初始化和触发。

## 当前结论

代码结构迁移已通过静态 guard、官方编译、逻辑测试和部署脚本验证。是否“完全不影响游戏内实际行为”仍需要以上游戏内烟测完成后才能最终确认；在烟测完成前，不建议继续迁移 Mode E、Mode F、ZombieMode 或标准 BossRush 的实际 tick / cleanup 行为。
