# 代码结构优化游戏内烟测记录

日期：2026-05-09

验证对象：

- `Build/BossRush.dll`
- 部署路径：`D:\sofrware\steam\steamapps\common\Escape from Duckov\Duckov_Data\Mods\BossRush\BossRush.dll`

启动方式：

- 先运行 `test_bossrush_official.bat`。
- 再运行 `test_bossrush_smoke_manual.bat`，按提示启动游戏。
- 游戏内清单完成后运行 `python3 tests/SmokeLogScan.py` 扫描最新日志。

## 检查项

- [x] 启动游戏并进入 `Base_SceneV2`。已通过 `2026-05-09_15-31-18.log` 确认。
- [x] 普通商人仍包含 BossRush 船票、冒险日记、成就奖章、阿文代币、砖石、尸潮邀请函。用户手工烟测反馈正常。
- [x] 通过现有地图选择流程进入标准 BossRush 竞技场。用户手工烟测反馈正常。
- [x] 标准 BossRush 竞技场初始化正常。用户手工烟测反馈正常。
- [x] 路牌选项正常。用户手工烟测反馈正常。
- [x] 第一波开始正常。用户手工烟测反馈正常。
- [x] 敌人生成正常。用户手工烟测反馈正常。
- [x] 击杀结算正常。用户手工烟测反馈正常。
- [x] 离场行为正常。用户手工烟测反馈正常。
- [x] 通过裸体 + 船票流程进入 Mode D。用户手工烟测反馈正常。
- [x] Mode D 开局装备正常。用户手工烟测反馈正常。
- [x] Mode D 路牌选项正常。用户手工烟测反馈正常。
- [x] Mode D 第一波开始正常。用户手工烟测反馈正常。
- [x] Mode D 卡波自检间隔表现正常。用户手工烟测反馈正常。
- [x] Mode D 离场清理正常。用户手工烟测反馈正常。
- [x] Frostmourne 或 FenHuangHalberd 能力仍能初始化并触发。用户手工烟测反馈正常。
- [ ] 可选：DevMode 下 DebugTools 热键行为正常。
- [ ] 可选：成就热键仍能打开成就 UI。

## 结果

结论：通过，用户手工烟测反馈全部正常

记录：

- 执行人：用户
- 游戏版本：
- 开始时间：
- 结束时间：
- 发现的问题：
- 相关日志文件：
- 最近一次命令行验证刷新：2026-05-09 15:11 CST
- 命令行验证状态：静态 guard、官方编译、逻辑测试、Wiki 同步和 DLL 部署均已通过。
- 烟测后日志扫描命令：`python3 tests/SmokeLogScan.py`
- 最近一次日志扫描辅助结果：`SmokeLogScan: PASS`，扫描 `D:\sofrware\steam\steamapps\common\Escape from Duckov\2026-05-09_12-37-48.log`，共发现 8 个错误块，其中 BossRush 相关错误块为 0。该结果只作为辅助证据，不能替代游戏内清单。
- 启动记录：2026-05-09 15:31 CST 通过 `test_bossrush_smoke_manual.bat` 启动 `Duckov.exe`。
- 进程记录：观察到 `Duckov.exe` PID `23852` 正在运行。
- 后续进程检查：未再观察到 `Duckov.exe` 进程。
- 最新启动日志：`D:\sofrware\steam\steamapps\common\Escape from Duckov\2026-05-09_15-31-18.log`。
- 最新启动日志扫描：`SmokeLogScan: PASS`，共发现 3 个错误块，其中 BossRush 相关错误块为 0。
- 最新启动日志观察：已加载 `MainMenu`、`Base` 和 `Base_SceneV2`，日志包含 `Base_SceneV2 场景加载完毕` 和 `Player entered base`；BossRush 配置项完成注册；尚未确认商人库存和实际玩法流程。
- 用户反馈：已完成烟测，全部没问题。
