# 代码结构优化完成度审计

日期：2026-05-09 15:14 CST

## 目标重述

用户目标可以拆成两个硬性验收条件：

1. 按 `docs/实现方案/2026-05-09-代码结构优化方向.md` 和相关实施计划，把当前阶段应落地的结构优化做完。
2. 确保不影响游戏内实际行为。该条件不能只靠编译、静态 guard 或日志扫描证明，必须完成进游戏烟测。

## 审计结论

当前目标继续推进中。

代码结构迁移、静态 guard、官方编译、逻辑测试、Wiki 同步和 DLL 部署已有证据通过；用户已反馈原烟测清单全部正常。此后新增的常驻轻量 tick / 玩家生命周期事件 / 场景加载准备 / 场景切换清理 / 常驻销毁清理 / 调试销毁清理 / 装备能力 / gameplay support / WavesArena / Mode E / Mode F / ZombieMode wrapper 已通过静态 guard、官方编译和部署脚本验证，仍建议下一次进游戏补测角色缓存刷新、竞技场中心标记重置、UI 消息计时器、好感度延迟保存、玩家死亡 / 亡魂预缓存与记录、好感度 UI 场景切换清理、好感度系统销毁保存、实体模型工厂卸载、F3 调试菜单销毁、调试监听注销、龙套装冲刺、无间炼狱现金磁铁、敌人恢复监控、标准 BossRush 波次倒计时 / 波次自检 / 离场、Mode E 入口 / 波次自检、Mode F 入口 / 阶段 tick / 场景切换退出、ZombieMode 入口 / 波次 / 奖励 / 撤离 / 场景切换 / 销毁，以及 ZombieMode modal pause。

继续迁移时仍应保持小步，只搬入口或 wrapper，不改变玩法数值、调用条件和调用顺序。

## Prompt-to-Artifact Checklist

| 要求 / 交付物 | 证据 | 状态 |
| --- | --- | --- |
| 结构优化方向文档放在 `docs` 下 | `docs/实现方案/2026-05-09-代码结构优化方向.md` | 已满足 |
| 第一阶段实施计划放在 `docs` 下 | `docs/实现方案/2026-05-09-代码结构优化实施计划.md` | 已满足 |
| runtime module 注册入口存在，并在 `OnAwake` 分发前注册 | `ModBehaviour.cs` 调用 `RegisterRuntimeModules();`；`Common/Lifecycle/BossRushRuntimeModuleRegistration.cs` 集中注册模块；`tests/ArchitectureStructureGuard.py` 覆盖顺序 | 已满足 |
| no-op 架构哨兵模块存在 | `Common/Lifecycle/ArchitectureSentinelRuntimeModule.cs`；commit `ea6789d` | 已满足 |
| Mode D runtime module 外壳存在 | `ModeD/ModeDRuntimeModule.cs`；commit `d13d861` | 已满足 |
| 物品配置器注册从 `BossRushIntegration.cs` 收束到 registry wrapper | `Integration/Items/ItemContentRegistry.cs`；`tests/ContentRegistryGuard.py`；commit `9844390` | 已满足 |
| 装备加载和装备能力初始化 / 清理收束到 registry wrapper | `Integration/EquipmentContentRegistry.cs`；`tests/ContentRegistryGuard.py`；commit `9844390` | 已满足 |
| 常驻轻量 tick 收束 | `Utilities/AlwaysOnRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickAlwaysOnRuntime();`；wrapper 内保持 `UpdateMessage();` 在 `AffinityManager.UpdateDeferredSave();` 之前；`tests/ArchitectureStructureGuard.py` 覆盖顺序和主入口禁用直接调用 | 已满足 |
| 玩家生命周期事件收束 | `Utilities/PlayerLifecycleRuntimeHooks.cs`；`ModBehaviour.Awake` / `OnDestroy` 同位置调用 `RegisterPlayerLifecycleRuntimeEvents()` 与 `CleanupPlayerLifecycleRuntimeEvents()`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| 场景加载准备收束 | `Utilities/GameplayRuntimeHooks.cs` 中 `PrepareSceneRuntimeForLoad()`；`ModBehaviour.OnSceneLoaded` 同位置调用该 wrapper；wrapper 内保持角色缓存刷新标记、缓存计时器、竞技场中心标记和 `ObjectCache.RefreshIfNeeded()` 的原顺序 | 已满足 |
| 场景切换清理收束 | `Utilities/AlwaysOnRuntimeHooks.cs` 中 `OnSceneUnloadAlwaysOnRuntime()`；`Utilities/GameplayRuntimeHooks.cs` 中 `CleanupEnemyRecoveryForSceneChange()` 与 `CleanupCashMagnetForSceneChange()`；`ModBehaviour.OnSceneLoaded` 在原位置调用这些 wrapper；`tests/ArchitectureStructureGuard.py` 覆盖顺序和主入口禁用直接调用 | 已满足 |
| 常驻销毁清理收束 | `Utilities/AlwaysOnRuntimeHooks.cs` 中 `CleanupAlwaysOnRuntimeOnDestroy()`；`ModBehaviour.OnDestroy` 同位置调用该 wrapper；wrapper 内保持好感度清理在 `EntityModelFactory.Shutdown()` 之前；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| 调试销毁清理收束 | `DebugAndTools/DebugToolsRuntimeHooks.cs` 中 `CleanupDebugToolsOnDestroy()`；`ModBehaviour.OnDestroy` 同位置调用该 wrapper；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| 装备能力每帧 tick 收束 | `Integration/EquipmentRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickEquipmentAbilityRuntime();`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| gameplay support 每帧 tick 收束 | `Utilities/GameplayRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickGameplaySupportRuntime();`；wrapper 内保持 `UpdateCashMagnet();` 在 `UpdateEnemyRecoveryMonitor();` 之前；`tests/ArchitectureStructureGuard.py` 覆盖顺序和主入口禁用直接调用 | 已满足 |
| Mode D 卡波自检 tick 通过 runtime module wrapper 路由 | `ModeD/ModeDRuntimeModule.cs` 调用 `owner.TickModeDIntegrity(deltaTime);`；`ModeD/ModeDWaves.cs` 保留原逻辑；commit `a2a27d1` | 已满足 |
| DebugTools runtime module 与 DevMode 热键 wrapper | `DebugAndTools/DebugToolsRuntimeModule.cs`、`DebugAndTools/DebugToolsRuntimeHooks.cs`；commits `2b5e890`、`fc8f44a` | 已满足 |
| Achievement runtime module 与成就 UI 初始化 / 热键 / 清理 wrapper | `Achievement/AchievementRuntimeModule.cs`、`Achievement/AchievementRuntimeHooks.cs`；commit `cee8a2b` | 已满足 |
| Common NPC runtime 边界 | `Integration/NPCs/Common/CommonNpcRuntimeModule.cs`；commit `72d1c49` | 已满足 |
| WavesArena、Mode E、Mode F、ZombieMode runtime module 外壳 | `WavesArena/WavesArenaRuntimeModule.cs`、`ModeE/ModeERuntimeModule.cs`、`ModeF/ModeFRuntimeModule.cs`、`ZombieMode/ZombieModeRuntimeModule.cs`；commit `9f67d8a` | 已满足 |
| WavesArena 倒计时 / 波次自检 / 大兴兴清理收束 | `WavesArena/WavesArenaRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickWavesArenaRuntime(Time.deltaTime)` 并保留早退，后续同位置调用 `TickWavesArenaBossCleanupRuntime(Time.deltaTime);`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| Mode E 每帧自检 / 名牌 / 缩放更新收束 | `ModeE/ModeERuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickModeERuntime(Time.deltaTime);`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| Mode F 每帧 tick 收束 | `ModeF/ModeFRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickModeFRuntime(Time.deltaTime);`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| Mode F 场景切换退出收束 | `ModeF/ModeFRuntimeHooks.cs` 中 `CleanupModeFForSceneChange()`；`ModBehaviour.OnSceneLoaded` 同位置调用该 wrapper；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| ZombieMode 每帧 tick 收束 | `ZombieMode/ZombieModeRuntimeHooks.cs`；`ModBehaviour.Update` 同位置调用 `TickZombieModeRuntime(Time.unscaledDeltaTime);`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| ZombieMode `LateUpdate` modal pause 收束 | `ZombieMode/ZombieModeRuntimeHooks.cs`；`ModBehaviour.LateUpdate` 同位置调用 `LateUpdateZombieModeRuntime();`；`tests/ArchitectureStructureGuard.py` 覆盖 wrapper 内容和主入口禁用直接调用 | 已满足 |
| ZombieMode 场景切换 / 销毁清理收束 | `ZombieMode/ZombieModeRuntimeHooks.cs` 中 `CleanupZombieModeForSceneLoad(Scene scene)` 与 `CleanupZombieModeOnDestroyRuntime()`；`ModBehaviour.OnSceneLoaded` / `OnDestroy` 在原位置调用这些 wrapper；`tests/ArchitectureStructureGuard.py` 覆盖条件、失败原因和主入口禁用直接调用 | 已满足 |
| 手工烟测辅助脚本存在 | `test_bossrush_smoke_manual.bat`；commits `fa7b25e`、`073fa97` | 已满足 |
| 烟测后日志扫描辅助存在 | `tests/SmokeLogScan.py`、`tests/SmokeLogScanTests.py`；commit `31a036f`；用于人工烟测后扫描最新日志中的 BossRush 相关异常；当前最新日志扫描结果为 0 个 BossRush 相关错误块 | 已满足 |
| 架构 guard 通过 | `python3 tests/ArchitectureStructureGuard.py` 最近一次输出 `PASS` | 已满足 |
| 内容 registry guard 通过 | `python3 tests/ContentRegistryGuard.py` 最近一次输出 `PASS` | 已满足 |
| 官方编译清单 guard 通过 | `python3 tests/OfficialCompileListFileExistenceGuard.py` 最近一次输出 `PASS` | 已满足 |
| 官方 Windows 编译通过 | `cmd.exe /c "echo.|compile_official.bat"` 最近一次输出 `Build succeeded!`，并部署 DLL | 已满足 |
| 官方测试 / 同步 / 部署脚本通过 | `cmd.exe /c "echo.|test_bossrush_official.bat"` 最近一次 8/8 逻辑测试通过，编译、Wiki 同步、DLL 部署完成 | 已满足 |
| 不影响游戏内实际行为 | 原烟测清单由用户反馈全部正常；最新启动日志 `2026-05-09_15-31-18.log` 无 BossRush 相关错误块；常驻轻量 tick / 玩家生命周期事件 / 场景加载准备 / 场景切换清理 / 常驻销毁清理 / 调试销毁清理 / 装备能力 / gameplay support / WavesArena / Mode E / Mode F / ZombieMode / ZombieMode 场景切换清理 / ZombieMode 销毁清理 wrapper 后已通过命令行验证但还未单独进游戏补测这些路径 | 部分满足 |

## 缺口

已完成原烟测清单：

- 启动游戏并进入 `Base_SceneV2`。（已通过日志确认）
- 确认普通商人仍包含 BossRush 船票、冒险日记、成就奖章、阿文代币、砖石、尸潮邀请函。
- 通过现有地图选择流程进入标准 BossRush 竞技场。
- 确认竞技场初始化、路牌选项、第一波开始、敌人生成、击杀结算和离场行为保持不变。
- 通过裸体 + 船票流程进入 Mode D。
- 确认开局装备、Mode D 路牌选项、第一波开始、卡波自检间隔和离场清理行为保持不变。
- 装备或生成 Frostmourne 或 FenHuangHalberd，确认能力仍能初始化并触发。
- 可选验证：DevMode DebugTools 热键、成就热键。

新增结构优化后的补充建议：

- 下一次进游戏重点确认 Mode E 入口、名牌更新、波次自检和阵营死亡缩放批量更新。
- 下一次进游戏重点确认 Mode F 入口、阶段推进、悬赏 / 刷怪 tick 和撤离流程。
- 下一次进游戏重点确认 ZombieMode 入口、波次、奖励选择、安全区和撤离流程。
- 下一次进游戏重点确认标准 BossRush 波次倒计时、波次自检和离场时不再继续刷倒计时。
- 下一次进游戏重点确认龙套装冲刺仍按原热键和冷却触发。
- 下一次进游戏重点确认无间炼狱现金磁铁和敌人恢复监控仍按原节奏工作。
- 下一次进游戏重点确认 UI 消息计时器和好感度延迟保存仍覆盖菜单 / Loading / 基地 / 对局场景。
- 下一次进游戏重点确认 ZombieMode modal pause 仍按原时机拦截输入。
- 下一次进游戏重点确认场景切换时好感度 UI 卸载、敌人恢复监控和现金磁铁状态清理仍按原时机执行。
- 下一次进游戏重点确认退出游戏或重新加载模组时，好感度系统事件解绑和保存仍正常。
- 下一次进游戏重点确认退出游戏或重新加载模组时，实体模型工厂卸载仍正常。
- 下一次进游戏重点确认场景加载后的角色缓存刷新与竞技场中心重置仍按原时机工作。
- 下一次进游戏重点确认退出游戏或重新加载模组时，F3 调试菜单和调试监听注销仍正常。
- 下一次进游戏重点确认 Mode F 场景切换退出仍按原时机执行。
- 下一次进游戏重点确认 ZombieMode 在场景切换和模组销毁时仍按原失败原因清理。
- 下一次进游戏重点确认玩家死亡时 BossRush 失败流程、亡魂预缓存和亡魂记录仍按原时机触发。

## 下一步

运行：

```bat
test_bossrush_smoke_manual.bat
```

选择启动游戏，完成清单，并把结果写回：

- `docs/testing/2026-05-09-code-structure-smoke.md`
- `docs/测试分析/2026-05-09-代码结构优化游戏内烟测记录.md`

随后运行：

```bash
python3 tests/SmokeLogScan.py
```

日志扫描没有 BossRush 相关异常只能作为辅助证据，不能替代游戏内清单。

烟测通过后，再执行一次完成度审计。如果所有项目都有硬证据通过，才可以把目标标记为完成。

## 备注

当前仓库的 `docs/` 目录被 `.gitignore` 忽略，文档更新不会出现在普通 `git status --short` 中；需要提交文档时必须显式处理忽略规则。
