# 已知红项基线（tools/run_guards.py 读取）
#
# 格式：<脚本文件名> | <原因>
# 只登记「在当前基线上就已经是红的、且已知原因待办」的 guard。
# 这里的条目失败不会让 runner 退出码变红，但每次运行都会单独列出提醒。
#
# 纪律：
#   - 新写的 guard 不许进这里，新代码默认不进白名单（见 tests/AGENTS.md）。
#   - 修好之后必须从本文件删除；runner 会把「基线里登记但已转绿」报为 STALE-BASELINE 并判失败，
#     避免基线越积越多、掩盖真实回归。
