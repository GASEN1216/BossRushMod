新增内容需遵循 `AGENTS.md` 规则：物品/装备走 `ItemFactory` 或 `EquipmentFactory`；新增 TypeID 递增并登记到 `docs/Bossrush使用物品ID表.md`；`DisplayNameRaw = "BossRush_*"` 必须注入本地化；新增 NPC 实现 `INPCModule`；触及持久化时检查 `BossRush_` save key。编译验证使用 `compile_official.bat`，并在游戏内做 smoke 测试确认无占位符。