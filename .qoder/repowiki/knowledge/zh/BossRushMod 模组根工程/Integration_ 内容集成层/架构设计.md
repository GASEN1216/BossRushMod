顶层通过 IntegrationRuntimeHooks / IntegrationDeferredBootstrap / EquipmentRuntimeHooks 等入口在 Unity 生命周期中按顺序装配各子系统；每个子系统（Affinity、Bonus、DeathWraith、DragonKing、FlightTotem、Frostmourne、Items、Mutators、NPCs、NewWeapons、PhantomWitch、Reforge、ReverseScale、Wedding、WishFountain 等）遵循统一的三段式结构：Config 定义数据、Runtime/AbilityManager 承载行为、Bootstrap/Factory 负责注册到共享容器（EquipmentFactory、ItemFactory、EquipmentContentRegistry、SetBonusPlaceholderRegistry、NewWeaponPlaceholderRegistry）。跨子系统的协作集中在根级工厂与注册表上——装备与物品由 EquipmentFactory/ItemFactory 集中创建，UI 通过 UIImageViewer 等共享组件复用，NPC 相关子系统共用 Utils/NPC*Helper 与 Constants 中的常量。Boss 类（DragonKing、PhantomWitch、DeathWraith、DragonDescendant）还通过各自的 AbilityController + AssetManager + Config 组合对外暴露能力，并由 IntegrationRuntimeHooks 在场景加载时触发。

2026-09-02（COMPAT）：`BossRushDynamicItemRegistry.GetPublishedTypeIds()` 返回排序后的 ID 副本，本身不加载资源、不暴露可变 Plans。F3 工厂验收逐帧使用此快照，通过既有 EnsureRegistered/InstantiateSync 检查身份、名称和图标，再销毁游离实例；它不代替真实装备效果、获得途径与重启回读的人工场景。

## 2026-09-04 深度复审修复

`COMPAT`。普通 DuckNpcModule 的场景判定和生成循环同时排除 isPermanent 蓝图，小满只交 DuckNpcPermanentModule 持有，避免普通分身绕过婚姻/交互生命周期。克隆类材料（遗种蛋、词缀熔石、种子）配置时调用 ModeFItemConfigHelper.ClearInheritedUsage，清来源行为并解绑空 UsageUtilities；空行为表在官方仍可用，不能只 Clear 列表。

章节来源：`Integration/NPCs/DuckNpc/DuckNpcModule.cs`、`Integration/Items/ModeFItemConfigHelper.cs`、`Integration/AffixForge/AffixForgeStoneConfig.cs`。

## 2026-09-05 永久配偶恢复（COMPAT）

婚姻桥分别从教堂驻留和跨场景跟随入口提交 `PermanentSpouseRestoreRequest`。同 NPC、同场景 handle、同模式的在途请求合并；异步完成时重新核对 owner、请求代际、当前配偶、跟随标志与教堂存在状态。驻留完成后补 marker、清占位物并刷新专用子物体上的交互；跟随完成后重新取玩家位置再启用移动。回家、离婚收尾、拆除教堂与 runtime cleanup 使旧请求失效。

`PermanentDuckNpcModule.ForceSpawnAtAsync` 在真实 await 后再次检查场景 handle、模块代际与桥许可；失效或竞争失败时仅释放自己生成的对象，不注销后来登记的同名 NPC。常规生成也在 await 后检查代际与已婚状态，避免婚前生成迟到覆盖婚后实例。关系键与存档 schema 保持不变。

章节来源：`Integration/Wedding/WeddingModBehaviourBridge.cs`、`Integration/NPCs/DuckNpc/Permanent/PermanentDuckNpcModule.cs`。验证：`PermanentSpouseRestoreLifecycleGuard` 与 `tests/fixtures/RuntimeOwnership`；实机导航/切图仍需 smoke。

## 2026-09-05 永久 NPC 后继请求交接（COMPAT）

`PermanentDuckNpcModule.Spawn` 在 busy 期间合并保留最新场景请求，由当前请求的 finally 释放自己的 busy 后启动。请求携带 Mod owner、场景名/handle 与模块世代，后继启动时重新验证场景允许策略；重复入口由登记表去重。Destroy 清空等待请求并推进世代，保持在途生成的独占权直到异步返回。ResetStaticCaches 同样使旧世代的等待请求失效。

生成完成后先验证生命周期、未婚状态与登记竞争；未登记成功的局部角色由 finally 回收，不注销同名的新实例。连续 A→B→C 只交接最终 C，旧 finally 不得清掉后继的 active/busy。此修复不变更婚姻桥、TypeID、配置或存档契约。

章节来源：`Integration/NPCs/DuckNpc/Permanent/PermanentDuckNpcModule.cs`。验证：`PermanentNpcSuccessorRequestGuard` 与 `tests/fixtures/ContentSecondReview/`；真实 Unity 调度及切图 smoke 仍需另验。

## 2026-09-06 NPC 移动所有权与元素回血（COMPAT）

`DuckNpcMovement` 只持有 Seeker 请求与完成回调，实际走路仍交官方 `AI_PathControl`。
每次停止推进请求版本并取消 A*；即使旧成功回调已排队，也不能重新装回路径。
驻留 `Hold/Release` 与对话 `HoldForDialogue/ReleaseFromDialogue` 独立，时间暂停另有截止时间；
三者都优先于跟随调度。移动中的跟随每 0.6 秒重规划；尚未完成的慢请求保留到明确超时，
避免被计时器反复取消。停用会取消请求并归零官方移动输入。

冰霜/雷霆回血通过模块内 `SetBonusDamageObservation` 观察已有 `Health.Hurt` 目标：
只在主玩家实际启用套装时创建逐次上下文，在官方每元素最低 1 点处理之后采样贡献，
再按本击 `finalDamage` 分摊最终限额。不能事后重算护甲——官方在读取护甲后才扣耐久，
这击打坏护甲时读回值已经不同。上下文支持嵌套与异常 Finalizer 清理，不改原伤害。
IL 定位必须匹配唯一抗性调用、连乘、最低伤害、累加和最终字段写入；不匹配明确诊断，
缺少贡献时跳过回血，不能按原始元素因子猜测。没有新增存档/配置/TypeID。

验证入口：`IntegrationThirdReviewFixesGuard` 与 `tests/fixtures/IntegrationThirdReviewFixes/run.py`。
后者执行生产移动及完整官方 Hurt 方法体的真实 Harmony 补丁，并读取本机真实游戏 DLL 的 IL 验证插入点；
不启动游戏。A* 调度、教堂驻留及套装回血显示仍需游戏内 smoke。

## 2026-09-07 界面可读性与视觉整理（COMPAT）

`Integration/Affinity/AffinityUIManager.cs` 的关系提示采用共享深色皮肤、320×104 留白和 20/16 号文字，原有心形图标保留；名称、等级、进度条分区，装饰与文字均透传点击。`Integration/UI/ImageViewerUI.cs` 的图片通过锚点和 preserveAspect 自动适配画布，上下各留 110px，避免 4K 二次放大；标题/提示绑定游戏字体。资源失败时明确提示“图片暂不可用”，不再创建大渐变占位图或无限显示“加载中”。`Integration/Codex/CodexView.cs` 的高度使用共享逻辑视口。原输入与资源归属流程保持不变；高清屏、长标题、加载失败与重开待实机复测。

`Common/Effects/RingParticleEffect.cs`（飞行云雾与冰霜套装脚下雾的共享基类）在配置前停止并清空 AddComponent 默认播放；startColor 负责 tint，colorOverLifetime 只乘白色与透明度曲线，避免霜蓝 RGB 被重复相乘。粒子从透明淡入再消散，尺寸由 0.8 平滑增长至 1.3；共享材质显式赋给 sharedMaterial，纹理 Clamp/Bilinear，关闭粒子投影。保留发射器数、发射频率、粒子上限、寿命、半径、伤害/数值以及启停 owner。实际与场景地面/敌人预警叠加的观感待实机验证，不能仅凭 guard 认定特效已验收。

**复核补修**：好感度进度条此前是 `Image.Type.Filled` 却没有 sprite，Unity 会退回整块矩形、`fillAmount` 失效，条子恒满；现已赋 `BossRushUI.GetSolidSprite()`。`RingParticleEffect` 的 alpha 过去被 `startColor` 与 `colorOverLifetime` 各乘一遍（实际是 alpha²），现改为梯度只描述形状、强度由 `startColor` 一次给足；`LocalAlpha`/`WorldAlpha` 同步改写成旧值的平方（0.49 / 0.1225），**渲染结果与已发布版本一致**，今后调参是线性响应。图片查看器改用 `BackdropStrong` 强遮罩。

## 2026-09-07 套装延时技能归属（COMPAT）

`ThunderChainStep` 的 finally 与执行体都必须校验激活代数。卸装/重穿后旧协程虽然不再造成伤害，仍会因 yield break 进入 finally；不校验就会释放新链的 in-flight 标志并清掉命中去重表。当前只允许同代且结束的那一跳清理。
冰霜/雷霆的主角死亡分派器先推进 `setBonusGeneration`，再重置冷却，作废等待中的冰葬、引雷与反震，局内复活不会继承旧伤害。数值、冷却、掉落与存档不变。
`tests/fixtures/SetBonusCoroutines/` 链接完整生产技能协程并逐字提取死亡回调；修复前稳定复现 4 条失败，修复后 10 条断言通过。Unity 表现与战斗仍需实机。

## 2026-09-07 实机生命周期修复（COMPAT）

龙王 `OnBossHurt` 在护子回血/阶段回血/新技能之前检查 `isActiveAndEnabled`、角色、生命对象、IsDead 和 Dead 阶段。官方 Hurt 可能先派发死亡使 Mode G 冻结角色，再派发 OnHurt；死亡清理后到达的旧事件不能重新启动技能。正常护子与转阶段行为保留。

教堂普通与基地早期初始化均允许已放置 `wedding_chapel` 恢复 prefab，即使该档缺少 NPC 好感历史标记；空档仍需原解锁条件。只读取既有 BuildingManager.Any 结果，不补写好感或玩家建筑数据。注册/重绘仍幂等，费用与数量上限不变。

章节来源：`Integration/DragonKing/DragonKingAbilityController_AttackFlow.cs`、`Integration/Wedding/WeddingBuildingInjector.cs`、`tests/DragonKingChildSpawnCancellationGuard.py`、`tests/fixtures/GameplayLogFixes/README.md`。隔离回归覆盖失效/正常龙王及教堂恢复/锁定空档；Unity 实机待验。
