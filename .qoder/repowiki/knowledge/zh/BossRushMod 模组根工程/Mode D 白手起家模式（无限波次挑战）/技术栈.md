Unity + C#，基于 Duckov Modding SDK（`Duckov.Modding.ModBehaviour`、`Duckov.Utilities.GameplayDataSettings`），使用 `Cysharp.Threading.Tasks` 协程辅助，通过 `ItemStatsSystem.Stats` 的 `Modifier` 修改敌人 MaxHealth 实现波次数值缩放。