运行时入口是 `ModeHRuntimeModule`：由 `ModBehaviour` 持有**唯一实例**并注册给 `BossRushRuntimeModuleHost`，六个宿主回调（OnAwake/OnStart/OnSceneLoaded/OnUpdate/OnLateUpdate/OnDestroy）之外不新增生命周期，刻意规避 Mode G 曾出现的双实例分裂。持久状态机只有一个：`ModeHLifecycle`（22 个显式整数值），由 `ModeHStateMachine.TryTransition` 单点推进，`ModeHMatchPhase` 只从 lifecycle 派生，禁止第二个权威状态；owner token 只在 `ModeHRunState` 内存里，绝不写进任何持久 DTO。

分层职责：内容层 `ModeHContentCatalog` + 四个注册表（profile / kit / command-compatibility / preset）负责加载七份签名 JSON 并生成 `contentCatalogSignature`；准入层 `ModeHRuntimeGates` 提供五个 no-throw 只读结果，旧模式最终入口只读其中两个；隔离层 `ModeHArenaIsolationLease`（冻结原图 spawner、清理原生敌人、核对边界、逆序回滚）与 `ModeHSpectatorLease`（固定获取/释放顺序的观战租约）；生成层 `ModeHSpawnBridge` + `ModeHSpawnTransaction`（每帧一个、失败整批逆序回收）+ `ModeHProductionCertification`（逐 stable key 双 clone 对战诊断，结果按四签名缓存在 HallOfFame envelope）；赛季层 `ModeHDraftController` / `ModeHEncounterPlanner` / `ModeHOddsController` / `ModeHVirtualStakeController` / `ModeHTransferMarket`；战斗层 `ModeHEventRouter`（唯一 owner 上以命名 handler 订阅一次官方 static `Health.OnHurt/OnDead`，Health→participant 为 O(1) 字典路由，诊断注册表查询优先）+ `ModeHCombatTelemetry`（倒地 token 每人每场至多一次，结果 token 由 Interlocked CAS）+ `ModeHCombatControl` + `ModeHInjuryAndScarSystem` + `ModeHBattleSnapshot`；真实资产层 `ModeHWarehouseStakeJournal`（唯一仓库写入者）+ `ModeHInventoryPersistenceBridge` + `ModeHItemTreeNormalizer` + `ModeHRewardTransaction`；呈现层 `ModeHUI` / `ModeHUIPages` / `ModeHRecoveryPanel`。

口令、伤病与战痕**共用同一套字段修改设施**：`ModeHCommandAdapter.ApplyEffects` 是唯一入口，`Apply/Reassert/Restore/Validate` 四段齐全，原值快照 + 0.1 秒重申 + 幂等还原；`nextReleaseSkillTimeMarker` 是唯一例外（写入后不还原，只把所有权交还原版）。控制点严格限于设计提案 §17.6.2 白名单，`ModeHControlPointWhitelistGuard.py` 逐条断言禁用符号。

ERROR 完整互换拆成三个可独立验证的部件：控制权切换复用原版 `ControlOtherCharacter(target, -1f)` 并由 2 秒 deadline 与独立的 `LevelManager.ControllingCharacter` 复核兜底；解冻玩家身体是 Mode H 首发唯一允许的 Harmony 新增（恰好两个 postfix，`CanUseHand`/`CanControlAim` 保持原版 false 作为引擎层隔离）；看台表演由 `ModeHStandInPerformer` 只写移动意图驱动，连续 3 次失败即停演且绝不升级为技术中止。

## 2026-08-29 编排层接线（此前整个模式不可达）

**事故本身值得记一笔**：交付时 55 个源文件、29 条 guard 全绿、编译通过、契约文档写着「已一次性完整实现」，但模式在游戏里根本进不去——约 25/55 文件是不可达代码。三处独立缺件各自都足以致命：入口 `ModeHInteractable.TryOpenEntry` 零调用方且没有任何地方 AddComponent；`ModeHRuntimeModule` 的四个 `partial void` 只有声明没有实现体（C# 会把无实现的 partial 方法连同**全部调用点**静默删除，编译与 guard 都不报错，运行时什么都不发生）；九张地图 JSON 都没有 `modeH*` 五点位，`SupportedMapCount` 恒为 0。根因之上还有一层：29 条 guard 全部只断言构件内部形状，没有一条断言构件有人调用。

**入口**：基地船坞的交互组注入一个 `ModeHInteractable`（`UIAndSigns.TryInjectModeHEntryOption`），形态与 PetNest / 日报的建筑交互一致——动态 AddComponent，零新增 prefab 与美术资源。注入门是「开关开启或存在待恢复记录」，避免出现点了必被拒的死选项；恢复入口复用同一选项。不走竞技场路牌：路牌只在 Legacy 竞技场开战后存在，而 Mode H 必须在**切图之前**冻结入场意图。

**编排层拆四个 partial**（拆分只为 `LargeFileBudgetGuard` 的 1200 行上限，语义上就是 `ModeHRuntimeModule` 本身）：`_SceneFlow` 是运行时字段的唯一声明处，承载 `OnSceneLoadedInternal`（意图匹配 → 按 §19.2 顺序取 arena 与 spectator 双租约 → 认证协程 → 用固定 runSeed 原子创建 lifecycle=Drafting 的首份 Season 并读回）与 `ShutdownRuntimeInternal`（§18.3 十步顺序，释放严格与取得相反）；`_UiFlow` 承载 `OnTransitionApplied` 与恢复壳；`_MatchFlow` 承载 `OnUpdateInternal` 与选秀到锁盘的命令；`_SeasonFlow` 承载幕间路由、转会与赛季终局，其中 `OpenNextMatchBrief` 是 matchIndex 的唯一推进点。

**落盘策略裁决**：`OnTransitionApplied` 只做三件事——把 runState 投影进 Season、路由页面、标脏，**绝不自己落盘**。一局里状态转换有几十次，逐次写盘既违反 §20.3「每批至多一次 SaveFile」也违反 §17.8「MatchSettling 是唯一原子全量写入点」。真正落盘只发生在少数显式点（Drafting 创建、RosterLocked、MatchBrief、MatchSettling、Intermission、TransferWindow、HallOfFame、SeasonEnded、Suspended）。中间态不写盘不丢任何契约保障：§20.3 的恢复规则规定战前无事实 token 时一律回落同一场 MatchBrief。

**失败出口**：开局链任何一步失败都走统一的 `AbortSetup`——退款、逆序释放租约、回到无 run 状态。arena 租约一旦清过原生敌人就**强制离场**，绝不原地回落 Legacy BossRush（§19.2）。

**Dev F3 清理（2026-09-02）**：`ForceResetStateForValidation` 在清空 run 前经既有路径尝试返还押品，调用 `ReleaseRuntimeObjects` 停止认证/生成并逆序释放选手、spectator、arena 和 UI，最后清空临时赛季/地图及 run owner。租约 API 为 `Release(sceneGeneration)`，不存在 `Dispose()`。正式 `AbortSetup` 的强制离场契约保持不变，F3 在下一个场内用例前重新加载竞技场。晚间完整报告的 H 拒绝后清理、竞技场恢复及最终泄漏差值已通过；认证成功后的清理仍待覆盖。

**生产认证死亡门（2026-09-02）**：原 preset 的 canDieIfNotRaidMap=false 仅代表非 Raid 图保护。
SpawnBridge 早已只在独立 clone 打开该字段，静态审计不得提前按原值拒绝全部候选。
现在对实际 Health 检查 CanDieIfNotRaidMap，再逐只 Hurt scav/wolf 两个诊断 clone；
完整 DamageInfo、IsDead、受伤事件与死亡事件必须同时成立。SetHealth(0) 不等于死亡。
认证早于战斗 router 绑定，同步窗口内监听当前 Health 的实例事件，finally 退订；
不向赛季战斗结算写入事件。其他资格、候选数量和缓存签名门保留。
第三轮 F3 已越过静态拒绝，但真实击杀出现空引用：已安装的击杀提示死亡订阅直接读取伤害来源，
与原来的 null 来源不兼容。现在两只诊断 clone 互作伤害来源，拒绝 null/自身/主玩家，
避免认证触发玩家击杀提示；异常记录 stable key、队伍、事件状态和完整栈，监听仍在 finally 退订。
第四轮不再出现伤害异常或逐 key 拒绝，整体认证仍被口令矩阵门槛阻断，见下节。

**生产口令认证与缓存恢复（2026-09-02 第四轮，COMPAT）**：原 RecordEffectStatus 没有生产调用，
只有 steady 的自结算分量通过，所有 key 恒为 1 条可用口令，达不到冻结的 3 条。
新增 `ModeHCommandCertificationProbe`：双方移入审计后的擂台点位、激活 AI 并保持无敌，
用同一个生产 adapter 对可读且可还原字段跨至少 3 帧、累计 0.3 秒采样；每帧先读再重申，
只有双方一直保持、结束后原值回读一致才写逐 effect VerifiedBehavior。
目标/路径/技能 marker 尚无相应遥测，保留 ReportOnly，不使用 Validate 中的占位 true 认证它们。
采样结束、取消和异常均还原 adapter；认证 owner 退订、回收双角色。原受控伤害与死亡门仍执行。
测量前绑定三签名、逐 key 清旧证据；报告覆盖通用和招牌口令。缓存从 Passed 记录恢复已知合法
逐 effect 状态，不能直接信任聚合口令状态，恢复后重查门槛。日志包含逐 key 可用口令数、
记录/通过/公共口令总计与具体失败原因。独立模拟探针通过；首次认证、缓存及成功退出仍待实机。

**恢复壳**：`Show` 增加 `stopRewardAnimation` 回调形参，删掉了 `FindObjectsOfType<WishFountainRewardAnimationView>()` 全场扫描——那会把**原版许愿池**正在播放的奖励动画一并销毁；现在只终止本模式自己登记的实例。

**风险门**：`ModeHRuntimeGates` 现在区分「扫描因 I/O 失败」（可重试自愈）与「确有未结算真实资产事务」（要走恢复流程）。此前四个异常分支把两者混成一种，七个旧模式入口里 5 处显示同一句误导文案、ZombieMode 一处显示无关的「其他模式进行中」、另一处完全静默。现在统一走 `ResolveLegacyBlockedMessageKey()`：先自愈重试一次，再按真实成因取文案。这也落实了 `docs/contracts.md:157` 承诺却一直没实现的「I/O 异常提供重试」。

**新增 `tests/ModeHReachabilityGuard.py`**：只回答「这段代码会不会被执行」。断言四个 partial 各有恰好一个实现体、入口可达、`RequestSeasonWrite` 有调用方、入场意图有消费方、恢复壳有实例化、声明支持的地图五点位齐全。已反例验证三种事故形态都会红。

**2026-08-31 状态更新**：战斗驱动已经由 `ModeHRuntimeModule_CombatFlow` 完整接线：公开整备生成赔率、虚拟筹码预约、锁盘、分帧生成、对战 Tick、口令窗口、拍铃、接力、MatchSettling 单批结算、伤病/战痕 offer、幕间、转会与名人堂均有可达调用链。真实仓库押注仍有意保持关闭：`realStakeSelected=false`，journal 只承担历史风险扫描与恢复；在 escrow 重建、满仓返还和 ManualIntervention 完成实机矩阵前不创建新真实资产事务。

## 2026-08-29 第二次事故：调用点存在但转换表不放行

上一节接通了编排层，随后一批（看盘 / 赔率 / 锁盘 / 分帧生成校验）却踩了**同构但更隐蔽**的坑：这次调用方都在，调用却必然失败。四个 P0 全部是「运行时调用 vs 冻结表 / 关停闩锁」的断裂，编译与当时 33 条 guard 依然全绿。

**根因与修复**：

- **锁盘按钮永远无效**：新代码按错记的表写成 `LoadoutEditing -> LoadoutLocked`，而冻结表里锁盘只有 `OddsPreview -> LoadoutLocked` 一条边，且全仓没有任何调用转入 `OddsPreview`。玩家点锁盘只会得到一行 DevLog，而赔率页是 `ClaimModalInput` 的时停模态页、没有关闭按钮，只能靠游戏自身 ESC 菜单逃生。修复：主干改为 `MatchBrief -> LoadoutEditing -> OddsPreview -> LoadoutLocked -> MatchSpawning`，开页即推进到 `OddsPreview`（同一个页面既是配装也是看赔率），锁盘前再幂等补一跳兜底。**不改冻结表**。
- **回滚退看盘的转换非法**：`MatchSpawning` 在冻结表里没有直达 `MatchBrief` 的边。改为经 `Recovering` 过渡，由下面的恢复驱动落回同一场；这一支**刻意**用不消耗重试预算的 `RequestRecovering`——功能没写完不是技术故障，消耗预算会把赛季推进死路。
- **`Recovering` 是死态**：所有技术故障出口都转进 `Recovering`，却没有任何调用点以它为源转出去，`MaxAutomaticTechnicalRetriesPerMatch=2` 的预算永远走不到第二次。新增 `DriveRecovery`（在 `OnUpdateInternal` 里按 stateSequence 判重驱动，每次进入只尝试一次）+ `ResolveRecoveryResumeLifecycle`（战前战中一律回落**同一场** MatchBrief，早期与幕间状态回落自己）+ `RequestTechnicalRetry`（消耗预算 → 未超预算走 Recovering，超预算挂起）。**恢复驱动必须异步（下一帧）**：`EnsureMatchPlan` 是在页面组装里报故障的，同步回落会变成无限递归；同理，页面组装路径上的故障必须消耗重试预算，否则「回落 → 重建页面 → 再失败」会变成每帧一次的死循环。
- **关停闩锁永不复位**：`_commandsClosed` / `_shutdownCompleted` 只在 `OnAwake`（每进程一次）复位，一次会话只能玩一局；再次入场时船票已预扣、模块却对所有命令静默早返，玩家站在无人接管的原图上。新增 `BeginNewRunSession()`，在**意图匹配成功之后**复位闩锁与每局一次性字段（顺带清掉残留的 `_pendingContractMainId`）——放在意图匹配之后，才不会因为「路过一张受支持地图」就解除关停状态。

**同批补齐的可玩性缺口**：`AbortSetup` 现在给玩家一句归类文案 + 退票告知（新增 7 个 `Abort_*` key，由 `ResolveAbortMessageKey` 把内部 reasonId 归类）；入口被拒有 `ShowUnavailableNotice`；`GetReasonLocalizationKey` 改为白名单式 fail-safe，未注入的内部原因（如 `entry_exception:NullReference`）回落通用文案，不再可能显示 `*星号原文*`；船坞入口接通**恢复分支**（有待处理赛季记录时打开恢复壳而不是拒绝玩家，但有其它模式在跑时不接管）；换档/删档回调补 `ModeHRuntimeModule.NotifySlotRestored()` 重建内存 run owner——此前存档恢复只在 `OnAwake` 跑一次，从主菜单载入中断赛季时 recovery-only 闸立不起来，玩家可以直接开新赛季**静默覆盖**它；`RouteUiForLifecycle` 离开恢复通道时收起恢复壳；`EnsureMatchPlan` 缓存判据补上 `technicalRetrySequence`（只比 matchIndex 会让技术重试复用刚失败的同一份计划）；看盘页 `Label_Match` 改用 `Replace("{0}", …)`（此前直接拼接，页面显示「第 {0} 场 1 / 6」）；`ResetModeHStaticCaches` 补 `ModeHSaveFlushCoordinator.ShutdownSubscription()` 退订（4.6）。

**`ModeHReachabilityGuard` 增补两条断言**：① 两端都写死的 `TryTransition` 调用点，其 (from, to) 必须在冻结表内；② 恢复通道三态（Recovering / ErrorRecoveryPending / Suspended）一旦被进入，就必须有以它为源的转换调用点。只查恢复通道是因为普通玩法状态可由 `RequestRecovering` 这类**动态源**门面退出，静态归属不了、硬查会误报；而恢复通道是所有故障路径的终点，出口只能写死源状态——正好是出事的那条不变式。两条都做过反例验证（注入即红、还原即绿）。教训升级为一句话：**上一次是「构件在、没人调」，这一次是「有人调、调不通」；guard 断言「有调用方」还不够，必须断言「调用会成功」。**

## 2026-08-31 实战、结算与恢复收口

新 partial `ModeHRuntimeModule_CombatFlow.cs` 是实战与单批结算的唯一编排点；
`ModeHRuntimeModule_CombatProfiles.cs` 只承载选手查询、快照深拷贝和奖励记录查询辅助，
用于让主战斗文件保持在 1200 行预算内，不引入第二套流程。阵容、默认 kit、
口令、公开赔率与虚拟下注在锁盘前冻结；敌我双方经 `ModeHSpawnTransaction` 每帧至多生成一个，
提交后绑定 `ModeHEventRouter` 与 O(1) participant 字典。战斗帧只跑控制器、遥测和已缓存显示名。

胜负结算先写完整 report 与奖励 operation，再持久化；已有未归档 report 的恢复记录直接回
`Intermission`，不重放已结算战斗。技术失败则回滚 report/reward operation、预约筹码与战前快照，
再经 `ErrorRecoveryPending → Recovering` 显式出口进入同场重试；结算已经形成完整 report
后若物理落盘失败，释放运行时并挂起，等待恢复壳
继续归档，避免同一场重复发奖。

入口选择同步补上地图过滤与目标重绑：Mode H UI 只显示 `ModeHMapSupportRegistry.IsSupportedPair`
通过的配置；点击时更新 sceneName/sceneID 但不递增 generation。隔离清场新增角色保护白名单，
只回收明确敌对玩家的原图战斗单位，无法证明是敌人时 fail-closed 保留。

**2026-08-31 认证计划解析**：双租约与地图审计通过后，先按
`(gameBuildSignature, modBuildSignature, contentCatalogSignature, slotGeneration)` 查询认证缓存；
有效且通过的缓存重建生产注册表并直接创建 Drafting 赛季。没有有效缓存时从
`ModeHProfileRegistry.GetProductionStableKeys()` 取静态生产目录逐 key 认证，不再依赖首次进入尚为空的
`ModeHPresetRegistry.ProductionKeys`。认证成功即使缓存写入失败也只记可靠性警告，本局继续。

**2026-09-01 认证协程驱动修复（P0）**：`DriveCertification` 手工 `MoveNext()` 驱动
`ModeHProductionCertification.Run` 时**必须**把 `inner.Current` 透传
（`yield return inner.Current;`）。`Run` 内部是 `yield return CertifyKey(...)`——
yield 出子 `IEnumerator` 交给 Unity 协程调度器递归驱动。曾在这里写死 `yield return null`，
子协程被创建但一次都没 MoveNext：逐 key 的生成、阵营核对、受控击杀、
`RecordPassed`/`RecordRejected` 全部没执行，日志里每个 key 打出一条**空失败原因**的
「认证拒绝」，`_records` 恒空 → `passedStableKeys.Count = 0` → 必然撞
`MinProductionCandidateCount = 8` 门槛 → **Mode H 完全无法开局**（正式入场同一路径，
不只是 F3 验收）。正确参照就在隔壁 `_MatchFlow.DriveMatchSpawning`：
`while (inner.MoveNext()) yield return inner.Current;`。
由 `tests/ModeHCertificationCoroutineDriveGuard.py` 守卫。

## 2026-09-02 第五轮：成功入场消费与真实整备

兼容分类：COMPAT / WIRE+。第五轮首次生产认证与缓存复用已实机 PASS（12 个候选，通用可用口令 7 条）；这只关闭认证链，不代表六场赛季与真实押品全部通过。

`ModeHRuntimeModule_SceneFlow.CreateDraftingSeason` 在首份赛季写入并读回成功后调用 `ModeHEntry.CancelPendingEntry` 消费冻结入场意图及预扣票所有权。成功前保留退款凭据，防止后续单纯重访同一地图重开 H；Dev 强制清理也回收遗留意图。

`ModeHLoadoutKitApplicator` 的枪械弹药必须存入新造枪的 Inventory 与临时选手 Inventory，不能用只操作装备槽的 TryPlug。严格保持 kit 的冻结总量，按 MaxStackCount 拆分，每个新实例都进入 CreatedItems；不合并进旧堆，任何失败整批逆序回收。直接写 Inventory 不会失效官方 `_bulletCountCache`，因此缓存反射字段置 -1 后由公开 BulletCount getter 重算并回读；字段缺失明确拒绝，不伪装可开火。

F3 新 `MODE_H_STARTER_KITS` 在缓存认证后的 Drafting 租约内使用真实认证池与隔离生成桥，逐件检查全部 starter kit 的槽位、弹匣实际/可用数、库存总数与堆叠上限；异步迟到产物由请求 owner 回收。生产源码离线模拟 34 项通过。第六轮实机 `MODE_H_STARTER_KITS` 已 8/8 PASS，三枪弹匣可用数/总量分别 30/120、10/40、13/60；两次入场均 intent_cleared=True，后续重访未重开 H。该结果不代表完整 AI 比赛、六场赛季或真实押品恢复已通过（报告 `BossRushValidation_20260902_140735_794.log`）。

章节来源：`ModeH/ModeHRuntimeModule_SceneFlow.cs`、`ModeH/ModeHLoadoutKitApplicator.cs`、`DebugAndTools/F3GameplayValidationModeHKits.cs`。

## 2026-09-03 七日审核修复：押品回滚缺口、濒退制未接线、锁盘静默

兼容分类：COMPAT。三条都属于「编译绿、guard 绿、运行时静默出错」，与本模块前两次事故同源。

**押品：物品脱离仓库后的每一条失败出口都必须放回去。** `TryRemoveEscrow` 的末步
`TryAdvancePhase(EscrowRemovedDurable)` 内含落盘，`SavesSystem.IsSaving` 时必然失败
（`FlushBatch` 返回 `flush_deferred_is_saving`），此前该分支只回滚 phase 不回滚物品，
留下「账上没移除、仓库里没有」的错位：其后所有返还路径都被 `cancel_escrow_still_held`
挡死，重启后 `LoadPersisted` 清空内存 escrow 更会让它被静默归档成 `CancelledTerminal`。
现在该分支与 `TryComputeInventoryDigest` 失败分支完全对称地 `RollbackDetached`。

**取消前必须有 durable 证据。** `TryCancelWithoutRemoval` 原来只看内存 `_escrowItems`，
而那份内存态在切槽/重启时会被清空。新增 `VerifyEscrowStillInInventory`：逐项用
`CountOccurrences(semanticTreeDigest)` 与 Prepare 时记录的 `preCount` 比对，任一项现存
数量不足即 `EnterManualIntervention`。用逐项比对而非整仓 `inventoryPreDigest` 全等，
是因为后者对「玩家在基地挪了别的东西」也会误报，而恢复壳打开时距 Prepare 可能已隔一次重启。

**濒退制的另一半接线了。** owner 2026-08-17 / 08-18 两次裁决的「休息一场解除带伤、
按实际登场判定」此前完全没有实现：`ModeHCombatTelemetry.HasRested` 写好了零调用，
`injuryId` 与 `status` 都没有回到 `Available` 的路径，`Injury_Rested` / `Injury_Retired`
两个文案 key 零消费。后果是把带伤选手按在替补席毫无收益、伤病 debuff 与赔率惩罚整季不散。
现在 `BeginMatchSettlement` 在两次 `ResolveDownInjury` 之后对 starter/relay 两席各调一次
`ResolveRestRecovery`，结算页展示「完整休息」与「赛季退役」。休息名单只存运行时
`_restedProfileIds`，**不进任何持久化 DTO**——`ModeHCanonicalDigest` 按反射遍历全部公有
字段，给 DTO 加字段会让已存赛季 `VerifyDigest` 失败并进写屏障。

**锁盘失败不再静默。** `PrepareLockedMatch` 失败此前只写 `DevLog`，而它带
`[Conditional("BOSSRUSH_DEV")]`，正式构建里整个被剥离——玩家点锁盘毫无反应。
新增 `ResolveLockRejectReason` 按原因分档（押品类委托既有押品文案，另有
`LockReject_CommandUnavailable` / `LockReject_RosterMissing` / `LockReject_Generic`），
绝不把内部 reasonId 原文展示。这是继拍铃、押品选择之后修掉的第三处同类静默。

**摘要归一化清单补齐第二份入场名单。** `SetSemanticFields` 长期只登记
`ModeHMatchRosterDto.enteredProfileIds`，漏了 `ModeHMatchReportDto.entrantIds`；
两者都来自 HashSet 枚举，漏登记那一份的摘要就依赖写入顺序。两份现已都登记。

**行数预算**：本轮改动使 `ModeHRuntimeModule_CombatFlow.cs` 触及 1200 行硬上限，
结算呈现与休整路由已**逐字**提取至 `ModeHRuntimeModule_SettlementFlow.cs`（同一 partial 类，
行为不变），处理方式与 `Config.cs` / `ConfigModConfigKeys.cs` 一致。

新增/增补守卫：`ModeHStakeJournalGuard`（脱离后每条失败出口必须回滚、取消前必须核对
escrow 仍在仓库）、`ModeHStructureGuard`（休息解除必须接线且两席都做、锁盘失败必须有
ShowMessage、摘要清单字段必须在 DTO 中存在且两份名单都登记）。全部经反向验证：
逐条破坏不变式后 guard 确实转红。

章节来源：`ModeH/ModeHWarehouseStakeJournal.cs`、`ModeH/ModeHInjuryAndScarSystem.cs`、
`ModeH/ModeHRuntimeModule_CombatFlow.cs`、`ModeH/ModeHRuntimeModule_SettlementFlow.cs`、
`ModeH/ModeHRuntimeModule_MatchFlow.cs`、`ModeH/ModeHCanonicalDigest.cs`。
