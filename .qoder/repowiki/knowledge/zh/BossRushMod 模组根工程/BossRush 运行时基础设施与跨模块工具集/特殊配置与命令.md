模块入口由 `AlwaysOnRuntimeHooks.InitializeBootstrapRuntime` 调用 `Harmony.PatchAll()` 并注册 BaseHub/Combat/Death 三个 PatchGroup；随后通过 `InitializeAlwaysOnDeferredContent` 延迟初始化 EntityModelFactory 与 MapSpawnPointRegistry。修改刷怪/恢复/场景门控代码后需进行人工游戏内 smoke 测试（AGENTS.md 明确要求编译+guard 不足以证明正确性）。