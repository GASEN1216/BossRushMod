- 所有跨线程共享的状态变更通过 `ModeGRunState` 的 `TryAdvanceLifecycle` / `TryLockBattleResult` 方法加 `lock(_casLock)` 原子推进，禁止直接赋值 lifecyclePhase 或 battleResult。
- 每个子模块以独立单文件承载单一职责（如 DeathRouting、Telemetry、SpawnTransaction、RewardTransaction、CleanupController），并在类名前缀统一为 `ModeG`。
- 日志输出统一通过 `ModBehaviour.DevLog("[ModeG] ...")` 前缀打印，便于过滤 Mode G 相关调试信息。
- 可确定性生成的数据（波次计划、runId、runSeed）均基于 `ModeGDeterministicRandom` 的域盐（DomainSalts.Boss/Variant/Contract）派生，避免全局随机源；FNV-1a/SplitMix64 常量为冻结值，不得改动。
- 对外暴露的可变对象采用构造函数私有 + 静态 `Build` 工厂（如 `ModeGWavePlan.Build`），构造完成后字段标记 `readonly` 以保证 Active 阶段只读。
- 生命周期与战斗阶段使用枚举驱动的状态机（`ModeGLifecyclePhase`、`ModeGCombatPhase`），并通过 `CanAdvanceLifecycle` 白名单限制合法转换。
- fail-closed 原则贯穿关键路径：波次计划构建（池不足/互斥失败）、官方池抽取、奖励候选（GeneralBase <7 或 Premium <5）、生成 lease 失效、golden vectors 自检、署名 Boss 预检、展示资源缺包、存档 Store 异常（单向 StoreFaulted）均返回 null/false 并由调用方拒绝启动或静默降级，不做宽松回退。
- 事件订阅必须幂等：存档事件（OnCollectSaveData/OnSetFile/OnSaveDeleted）等全局事件带私有布尔防重复订阅并在清理路径退订；战斗中不写盘（写屏障），只在值变化的结算/官方收集/切档删除时落盘。
- 呈现层（`ModeGRecapPanel`、`ModeGHUD`、`ModeGInteractable`）全部新文本走 `L10n.T("BossRush_ModeG_*")` key，代码只持 key，文案统一在 `Localization/LocalizationInjector.cs` 注入；呈现失败必须防御式静默降级，不得拖崩宿主。
- 复玩记录层（总场次/胜利/最佳波次/宿敌图鉴里程碑）为纯荣誉展示，不参与任何数值计算，禁止反向影响难度或奖励。
