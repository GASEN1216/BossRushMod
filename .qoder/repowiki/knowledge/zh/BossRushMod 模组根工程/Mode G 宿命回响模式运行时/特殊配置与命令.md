入场条件：携带当前装备、BossRush 船票与宿命回响信物（TypeID 500057），且无营旗、无血猎收发器；展示资源缺包、存档 StoreFaulted、golden vectors 自检失败、当前过滤 Boss 池没有唯一官方 key、署名 Boss 资源不可用或当前场景不属于地图选择 UI 有效配置时，一律 fail-closed 拒绝启动并退还道具。2026-08-17 owner 已批准现有 `GetFilteredEnemyPresets()` 过滤 Boss 池和地图选择 UI 的全部有效地图用于 Mode G，当前 `IsProductionReady=true`；`AllowDevRawPngFallback=false`、`AllowDevTestEntry=false` 保持关闭。官方池仍排除托管三 Boss、空 key 和重复 preset 引用；1-5 个 key 时由 `ModeGWavePlan` 按 runSeed 从已有 key 确定性复制，6 个为完整编排目标。地图支持直接复用 `GetAllMapConfigs()`，要求非空 exact scene pair 与刷新点，preview 冻结玩家实际选择并已加载的当前地图。`CurrentVerificationRevision` 当前为 "2026-08-10.v1"。冻结常量：FNV-1a offset basis/prime、SplitMix64 增量、九波休整时长（普通 8s、第 3/6 波后 20s）、奖励分档（Resolve 0-2→6 件，3-5→7，6-8→8，9→9，10-11→10）均为 owner 冻结值。存档 key 冻结为 v1：`BossRush_ModeG_NemesisRecord_v1`（宿敌记录）、`BossRush_ModeG_Profile_v1`（个人记录），两者分别维护未知 schema 写屏障。2026-08-29 新增唯一一个运行时可调配置项 `BossRush_ModeGAbandonHotkey`（局内「放弃挑战」确认页快捷键，默认 K，取值对应 `KeyCode` 枚举，0 或负值视为禁用），接线在 `Config/ConfigModeG.cs`（与 `Config/Config.cs` 同一 partial class，拆开只为 `LargeFileBudgetGuard` 的 1200 行上限），ModConfig 下拉复用成就快捷键那份按键选项表。该键只负责打开时停确认页，不直接终局；放弃不返还船票与信物且清零契约连胜，确认页按 §3.1 强制披露这一代价。
