入场条件：裸装 + BossRush 船票 + 宿命回响信物（TypeID 500057），且无营旗、无血猎收发器；入场预检失败（含展示资源缺包、存档 StoreFaulted、golden vectors 自检失败、署名 Boss 预检失败）一律 fail-closed 拒绝启动并退还道具。可用性受 `ModeGAvailability` 发布闸控制：`IsProductionReady` 决定正式路牌是否显示（Phase 0A-4 全部通过并经 owner 签署后才允许置 true；当前版本已置 true，`AllowDevRawPngFallback`/`AllowDevTestEntry` 保持 false），`CurrentVerificationRevision`（当前 "2026-08-10.v1"）用于 eligibility registry 与地图支持注册表，游戏更新后须重新验证并递增。冻结常量：FNV-1a offset basis/prime、SplitMix64 增量、九波休整时长（普通 8s、第 3/6 波后 20s）、奖励分档（Resolve 0-2→6 件，3-5→7，6-8→8，9→9，10-11→10）均为 owner 冻结值，不得在普通变更中调整。存档 key 冻结为 v1：`BossRush_ModeG_NemesisRecord_v1`（宿敌记录）、`BossRush_ModeG_Profile_v1`（个人记录），独立于其他模式。
