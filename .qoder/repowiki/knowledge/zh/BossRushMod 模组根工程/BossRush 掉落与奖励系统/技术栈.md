Unity + C#；使用 Cysharp.Threading.Tasks (`UniTask`) 做异步等待；大量通过 `System.Reflection` 直接读写 `Duckov.Utilities.LootBoxLoader` 私有字段（randomCount、qualities、tags、excludeTags、randomPool、fixedItems）以绕过官方 API 限制；使用 `ItemAssetsCollection.Search` 与 `GetMetaData` 进行物品筛选，避免频繁 `InstantiateSync` 造成卡顿。