所有调试功能默认关闭，需在运行期设置 `DevModeEnabled = true`（部分功能还需 `HardcodedDevModeEnabled`）才会启用；F2~F12 各键分别对应物品生成器、交互调试、放置模式、背包检查、NPC 传送与 F3 作弊菜单。

2026-09-07：使用 `BOSSRUSH_DEV_BUILD=1` / `BOSSRUSH_NO_PAUSE=1` 跑 `compile_official.bat`；既有脚本会部署本机 Dev DLL。重启游戏后，在可丢弃测试档的基地打开 F3 → 验收测试 → 标记专用测试档 → 自动验收 + 完整待测清单。测试自动切图，运行中不切档；结束可“打开日志文件夹”或“复制日志路径”。
离线判读：`python tools/gameplay_coverage.py --log <日志> --analyze --expected-mvid <DLL的MVID>`。缺 SUMMARY/自动项、旧 DLL、失败记录不能算通过；WARN 与 `needs_log_review` 需结合日志复核，人工清单始终另验。
