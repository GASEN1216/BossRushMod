Unity + C#；基于 Duckov 模组框架的 `BossRushRuntimeModuleBase` 生命周期钩子；UI 混合使用 Unity GUI（OnGUI 窗口）与 Unity UI（Canvas/ScrollRect/Button 动态创建）；异步任务使用 Cysharp UniTask；日志通过 `DevLog` 条件编译（`[Conditional("BOSSRUSH_DEV")]`）。