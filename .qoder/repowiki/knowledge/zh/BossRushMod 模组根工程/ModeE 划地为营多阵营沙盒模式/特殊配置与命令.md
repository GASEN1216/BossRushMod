营旗物品需在基地主场景（`Base_SceneV2`）通过 `FactionFlagConfig.InjectIntoShops` 注入到所有 `StockShop`；刷怪消耗品通过 `RespawnItemConfig.ConfigureItem` 动态挂载 `UsageUtilities` 与 `RespawnItemUsage` 组件并设置隐藏字段以接入原版 `CA_UseItem` 流程。