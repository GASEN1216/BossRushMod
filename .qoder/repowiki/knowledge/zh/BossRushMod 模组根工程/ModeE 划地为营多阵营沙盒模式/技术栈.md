Unity + C#；使用 HarmonyLib 对 `Health.Hurt`、`StockShop`、`StockShopView` 等方法进行 Prefix/Transpiler/Postfix 补丁；通过 `Cysharp.Threading.Tasks` 的 `UniTask` 处理异步购买/出售；借助 `ItemFactory.RegisterConfigurator` 注册自定义物品并在 `Base_SceneV2` 场景向 `StockShop` 注入条目。