# 天空岛生产复核与交付前置

2026-09-11，`COMPAT`。本页补充 [天空岛剧情与持久化](天空岛剧情与持久化.md)，记录全项目生产审核中的独立天空岛检查。代码基线覆盖最近四批内容（`510309d`、`d36822d`、`1d665d9`、`b0ee07f`、`faac921`）；以实际生产调用与官方反编译源码为证据。没有启动游戏，也没有访问或修改玩家存档。

## 已确认并修复

`CR-2026-09-11-004`，P1 / COMPAT：原 `SkyIslandWorldStory.GrantKeepsakes` 先 `RecordNote(Keepsake_*)`，随后 `SkyIslandItems.TryGive` 才查询 prefab 和实例化。缺资源或实例化失败会永久记为已发；资源恢复后 `SkyIslandItemRules.Due` 仍拒绝补发。晴岚航徽和噬风之核已有回码头、服务半价与减风功能，因此会造成实际玩法奖励永久缺失。

现在 `TryGive(typeId, toStorage, recordGrant)` 先准备有效实例，再执行记账回调，最后转移到原目的地。准备失败不调用回调；回调拒绝或抛异常时清掉临时实例；真实转移开始后的异常仍保留已记台账，避免不确定的转移被重复结算。没有增加存档字段，也没有把死亡、卖掉或以前已经标记领取的物品自动补发。无法从历史台账中区分旧版误记与玩家主动处置，需要人工证据后另行处理。

`CR-2026-09-11-005`，P2 / COMPAT：原 `SkyIslandGnats.TakeSpawn` 在当前存档已只读时仍允许扣一把云苔纤维捧蛙卵，`ReleaseSpawn` 才拒绝记录。已知失败的交付仍能接取，玩家会白耗材料与往返时间。现在取蛙卵时先检查 `story.CanWrite`，显示同一份 `SaveStatus`，然后才允许扣材料；正常夜间接取与交付仍走原规则。

## 可玩闭环证据

| 内容 | 已核对的玩家路径与用途 | 仍需实机确认 |
| --- | --- | --- |
| 主线与撤离 | 东西航标各绑定两组清场；双航标打开钟庭和航标广场撤离；码头始终可撤；折翎和钟守的战胜、和解前置有明确替代路径 | 战斗体出现、门碰撞、实际撤离、失败返航 |
| 四处谜题与支线 | `ReadPoint` → 谜题三步 → 原 `RecordSearch` / 剧情动作；答错提示并停在当前步骤，不永久锁死；瞭台先清守卫 | 提示可理解性、谜题面板实际输入、支线节奏 |
| 来信、手记、名册 | 每趟一封、未收可重来；第一封带罗盘；后续来信对应岛上行动；结局后名册按分支回应 | 12 次出击的收集节奏是否让目标玩家愿意继续 |
| 材料与合成 | 30 处采集点、11 条配方、三处工台；居民与装置各有入口；材料被耗材、风晶灯和复蛙消耗；配方前置不依赖其自身产物 | 满背包落地、可见性、按键/手柄操作、是否形成有趣取舍 |
| 岛上的灯与纪念品 | 晴岚风晶来自碎片；七盏灯提供局部避风与完成奖励；航徽回码头、半价；噬风之核减风；护符对噬风伤害有消费点 | 付出与回报感受、灯的辨识、奖励实际到达 |
| 云蚋与复蛙 | 烟、风、驱风香、风灯、纱笠、蒲扇、灭蚊灯与药膏进入真实规则；蛙卵夜间接取、白天可交、三份持久完成 | 像素目标是否看得见/打得中、躲闪是否可读、夜间支线是否有趣 |

上述证据证明代码存在完整的行动与结果链路，不代表玩家体验已获验证。新增物品有消费点也不自动等于值得重复游玩；每趟一封信、复蛙往返、完成后重游的吸引力应由实际体验记录决定，本次没有把主观偏好判为缺陷或擅自改数值。

## 验证与边界

两项修复后，`SkyIslandContentPackGuard`、`SkyIslandMosquitoGuard` 和 `python tools/run_runtime_regressions.py --filter SkyIslandStory` 初次均通过。共享工作区另有并行天空岛修改，因此最终全量结果由父级审核在合并后的文件上统一重跑，不能把初次通过套用到后来的全部改动。

上述两个 guard 在本代理交接前再次通过；5 条内存反向探针分别把记账移到实例准备前、删掉记账门、把记账移到真实转移后、把调用方记账委托改为空操作、删掉蛙卵取件前的写屏障，5 / 5 均在预期断言上转红。探针只替换 guard 的内存输入，生产文件没有写入，前后 SHA-256 一致；记录在 `Build/production-review-20260911/sky_island_guard_probes.json`。

独立故障注入原样提取生产 `TryGive` / `TakeSpawn` 方法，用内存物品和存档替身执行 26 条断言：缺 prefab、实例化抛异常/返回空/返回错 ID、记账回调缺失/拒绝/异常、背包与仓库成功转移、转移异常保留台账，以及只读/正常/重复/白天/已完成的蛙卵接取。该探针不运行整个 Unity 类，也不证明官方转移实现或 ES3 I/O。日志位于 `Build/production-review-20260911/sky-island-delivery/result.json`；生产编译由父级统一执行。

引用：`Integration/SkyIsland/SkyIslandItems.cs`、`DebugAndTools/SkyIsland/SkyIslandWorldStory.cs`、`SkyIslandItemRules.cs`、`SkyIslandGnats.cs`、`SkyIslandStoryService.cs`、`SkyIslandStoryRules.cs`、`SkyIslandFieldcraftRules.cs`、`SkyIslandPuzzles.cs`、`SkyIslandLetters.cs`、`tests/SkyIslandContentPackGuard.py`、`tests/SkyIslandMosquitoGuard.py`、`tests/fixtures/SkyIslandStory/`。官方物品使用口径对照 `鸭科夫源码/TeamSoda.Duckov.Core/CA_UseItem.cs` 与 `鸭科夫源码/ItemStatsSystem/UsageUtilities.cs`。

## 2026-09-14 岩体生产链（COMPAT）

天空岛地表以下的岩体由 `tools/sky_island_geology.py` 生成，`generate_sky_island.island_shell` 只作接线。原生参考是 `ArtSource/SkyIsland/Production20260914/References/E_geology_native.png`。实际造型采用保留顶圈的主壳、偏心六面基岩断口与宽 / 长 / 窄三块扶壁。每组扶壁由三个宽浅岩段、窄倒角和错位断肩形成，下端向主岩收进，避免细长支脚的比例。早期预览的连续深浅环带已移除；层理不再靠绕整岛一圈的暗条表达。不同岛使用确定的剖面和岩层方向，新增顶点均低于可玩地表。旧的未封底壳与按高度放大的 `cliff_chunk_*` 不再参与近景岩体。

每岛有一个闭合主壳 `VIS_<island>_Geology` 和三块 `VIS_<island>_Buttress<role>_Geology`。扶壁与主壳只在岛侧局部咬合，属于明确登记的 intentional intersection；各块均有顶底盖，没有复制内部整个岛壳。独立射线检查要求每块扶壁既有主壳内的顶点，也有主壳外的顶点，防止完全埋入或浮离。所有对象均须纳入 `sky_island_geometry.json` 的 `visualMeshes`。

材质优先使用 `GeologySand / GeologyLight / GeologyDeep` 三个专属槽；未注册时逐槽回落 `Rock / RockLight / RockDeep`。专属砂岩使用 32 米世界周期，旧槽 fallback 保留自己的尺度。侧面纹理 V 沿 Unity 世界高度保持同一相位，仅接近水平的断口改用 XZ 投影，避免斜面跨过 45° 时突然旋成顶投影；不使用悬崖碎件图集。Unity 作者构建器实际遍历 `renderer.sharedMaterials`，按每槽名称查 geometry 元数据，不从对象名推断材质。根藤采样真实新岩面截线；`cliff_dressing` 保留旧循环随机数消耗，后续地面树石散布不能漂移。

`tests/SkyIslandGeologyPropertyTest.py` 执行 12 个真实主壳、36 个闭合且局部相交的扶壁、3 个平移/缩放域和 6 个破坏探针。封闭边、边方向、正体积、非退化、精确顶圈与地表上限必须同时成立。本轮 Blender FBX 导出再导入后，48 个岩体对象位置焊接仍闭合，世界 UV 无零面积三角面，三个材质槽保留；岩体共 13,200 个三角面。完整 Unity 场景中的最终画面与运行时性能由总体生产验证记录，不以局部 Blender 预览替代。

## 图集模型的接缝修复（COMPAT）

`tools/sky_island_surface_repair.py` 是独立 Blender 候选工具，不自动修改作者源件或主生成器。带图集 GLB 经常按 UV / 法线拆开同位置顶点；直接对这种网格执行 COLLAPSE，会分别移动接缝两边，产生真正的透底裂口。工具从原 GLB 重新归一化，在 0.000001 米范围内接合重合位置，再沿用原面数预算减面。UV 保存在面角，接合前后逐项校验面角的几何位置与图集坐标关系，最终仍按既有逐顶点 UV 契约导出。

首批候选为 `cave_rock / rock_b / glow_crystal`，来源、原 JSON 与候选 SHA、原件未修改证明、正反 3/4 画面及固定视幅对照保存在 `Build/sky-production-20260914/surface_repair/`。主体缺口减少不等于所有边界都该封闭：贴地底口、原生叶片与相互咬合的晶簇单独审查，工具不自动填这些面。原 `meta.bounds` 作为既有生成与碰撞声明保留，候选实际顶点包络另报，不能把声明未变写成模型每个点未变。使用 `--output`、`--report` 指定独立文件；工具拒绝覆盖原模型路径。采用候选、重导世界和 Unity 验证仍由主生产流程统一执行。

`--contain-placement-bounds` 对超出原范围的轴做最小平移，只有实际跨度大于旧区间才收缩；已在区间内的轴保持原样，不撑满旧减面碎片产生的异常极值。超过 3% 的缩放直接拒绝。候选保留 `meta.bounds`，另记 `meta.actualBounds` 与 `surfaceRepair.placementFit`（适配前包络、缩放、平移），要求每个实际点落在原声明内。最终前三件采用 `surface_repair/contract/` 的约束版。`great_tree` 原来已保留全部 8,831 面，原 GLB 与当前模型拓扑一致；单纯接缝恢复没有实际改进，审图结论为 KEEP，冠层体积属于另一个造型问题。

同日屋面复核另外采用了 `tea_house / village_house_a` 的原 GLB 接缝修复。两件原导入过程均精确重演旧 JSON 的 v / f / UV 后才制作候选；继续使用原图集和原面预算，候选各 2,999 面。茶铺无需净空变换，民居仅 X 平移 0.0003 米；两件旧 `meta.bounds` 逐项保持，实际顶点全在其内。正背整体与屋面近景共 24 张，集成前后 SHA、原 GLB SHA 与回退 JSON 位于 `Build/sky-production-20260914/surface_repair/roof/`。父级审图采纳后已复制两份作者源文件，未改其它模型登记；两源 SHA 回读分别为 `4c329c33…61e00d`、`08be1e52…10f3ae`，并已参与完整世界重生成。最终元数据的茶铺批次为 2,999 面，民居 A 两实例合批为 5,998 面。

两屋面及长凳调整后的最终 FBX SHA 为 `de36df8f87ebde6597f73ae4bad6c210113463f34381f50c3efa58d626abd58f`。`round03/geology-final-v2/` 对实际最终文件只读验证：48 岩体 / 13,200 面全部闭合，3 瀑布 / 6 批次保留整件法线，最大角差 0.106°；与封存原 FBX 的 284 个导航 / 碰撞 / 地面 / 标记对象逐项完全相同。JSON 保护检查 41 项通过，布局字节不变、146 个碰撞盒与 82 个元数据标记逐值相同。此处为 L2 文件与 Blender 验证，Unity 审图与性能由总体生产记录。

## 瀑布流水分面与法线（COMPAT）

`tools/sky_island_surface_materials.py` 只对 `waterfall` 取原图集内部样点，把明确青蓝流水放入 `TripoCascadeWater`，其余石壁、苔藓和混合泡沫边缘放入 `TripoCascadeStone`。两组复用原图集和源 UV，几何位置与三角形不变。`crystal_fountain` 的顶部实体晶体也使用青蓝色，颜色分类会误认为流水，因此不支持喷泉分面，保留原静态 `TripoCrystalFountain`。

分面的水网格是开口表面，不能对两个组分别重算法线。先在每个实际实例的世界坐标上，按原未拆分整件的 `create_object` 路径取得面序与每个面角的平滑法线；通过 `addmesh` 的可选 `corner_normals` 传到最终对象并显式设置 Blender custom split normals，跳过该组的独立 recalc。源件有开口和薄片，旧 bmesh 的定向选择会受摆放影响，不能把局部缓存法线只旋转后当作该实例的旧 whole 结果。其它普通几何保留原处理。显式法线与面角数量不匹配、源有退化面、同组混入未保留法线的几何均拒绝生成，不能静默丢弃法线。

Environment / Water 的双面着色使用 `SV_IsFrontFace` 修正法线，面序和法线同时翻转 180° 不能单独作为可见缺陷。验证要比较修正后的角差，并通过实际 FBX 导出再导入检查法线传递、位置及 UV，而不只检查 Python 数组。证据与正背原图集 / 分类 mask 位于 `Build/sky-production-20260914/surface_materials/`；真实流水高光和完整场景由 Unity 总体审图确认。

第三轮独立导出在原坐标与 yaw=37.25°、1.15 倍缩放、世界平移两个实例上实测：每件 3,722 面和 UV 完全对应，面序与平滑标志不变，生成位置误差为 0；FBX 往返位置最大误差 0.000038 米，法线相对原 whole 最大角差 0.106°，其中 FBX 传递本身额外误差小于 0.0033°。这不是逐位相同的法线承诺，保留的显式法线有小角度误差；旧拆组出现的 >5° 残余已归零。实际报告为 `surface_materials/round03/retained_normals_validation.json`。

## 2026-09-14 实体植被、可视道路净空与石凳落位

分类 `COMPAT / SCHEMA+`：`COMPAT` 指离线模型与布景调整，`SCHEMA+` 仅指 `sky_island_tripo.json` 的可选导出元数据，**不涉及玩家存档**。实现归属为 `tools/sky_island_botany.py` 与 `tools/sky_island_tripo_props.py`，未增加运行时逐帧组件。

独立程序库覆盖 `tree_green / tree_blossom / tree_gold / tree_olive / cherry_tree / bush_a / bush_b / cliff_shrub_cap / coral_clump / fern_clump / stone_bench` 共 11 种。原 11 份规范化 JSON 与图集保留，World 的统一 `_stamp` 先使用新库。实体叶冠按种类区分主簇、从属簇和枝干，使用已有材质调色板，替代旧图集黑边与破裂薄皮；石凳为带倒角的座板、支撑和两脚。管轴采用连续传递截面，消除近竖直 / 急弯处突然翻轴造成的针腰。box / crown 的源面序已修正；第一轮生产 `create_object` 原本会重算法线，不能把源负体积误报为已经发布的倒法线。

原语义摆放与锚点两个分支一度绕过 `_stamp`，新 stone_bench / cherry_tree 因而只在库中存在。现两分支统一走 `_stamp`，最终 World 中逐顶点匹配 4 张石凳、4 棵粉花树，误差阈值 0.0003 m，匹配原有记录锚点的偏差小于 0.015 m（导出位置只保留两位小数）。11 种库覆盖中有 10 种实际放置，`cliff_shrub_cap` 当前没有调用实例；不得声称它已在场景替换。`crystal_fountain` 同样没有当前实例，但它不属于 botany 覆盖库。

`path_clear` 复用 `PAVING_TRACKS` 与 `sky_island_dressing.distance_to_segment`，阈值为道路半宽 + 0.9 m + 实际植被半径。灌木取完整 XZ 包络；树只取离地 2.2 m 以下部分，避免把高处树冠当作地面阻挡。命中的纯装饰返回 0 个新增面，但仍报告为已处理，不能再回落旧模型或 primitive。若匹配已有登记碰撞的树，只记录 `review_registered_collision` 并保留外观，避免留下无形碰撞。最终 126 条记录全部为 `omit_decorative_plant`，没有登记碰撞待复核项；记录含名称、Unity 落点、实际半径、道路距离、半宽、边距与动作，位于导出元数据的 `vegetationPathClearance`。A 出生主路中央和北桥头已通过当前 World 焦点图核对，周围摊位、灯、花草仍保留。

B 南凳原中心约 (-4.01262, 5, -109.79367)，与摊位侧箱的 Limestone 部分有 45 对三角交叉，纳入 Chalk 支撑和腿后为 **50 对**。按 0.25 m 步长、32 方位近邻搜索，取得同时通过严格 `PlantingSpace.free` 和周围实际道具包络的近邻点；生产仅对该语义锚点应用确定偏移 (-2.451963201, 0, -0.487725805)。最终中心约 (-6.46458, 5, -110.28140)，旧 yaw -119.8° 保持；完整重跑 114 条摆放，仅该一条位置变化，RNG 状态不变，摊位与 layout 未移动。

实际最终 World 的完整凳体与摊位、箱桶、花箱、路灯分别做 BVH 三角相交，全部为 0；严格 `PlantingSpace.free` 通过，半径 1.954656 m，扣除道路半宽与 0.9 m 边距后剩余约 6.10748 m。该条 `placements[].adjustment` 记录原位置、偏移、半径、净空通过和保向信息，属于可选导出元数据扩展。最终 146 个 collisionBoxes、82 个 markers 与备份逐值相同，layout 字节相同；导航 metadata 仍为 3870 / 4048，计数相同不等于顶点数组逐值证明。

证据：[最终实际模型匹配与 BVH / 净空](../../../../../Build/sky-production-20260914/round03/blender/final_focus_validation.json)、[修复前后及 114 条摆放差异](../../../../../Build/sky-production-20260914/round02/blender/bench_fix_validation.json)、[本轮源契约与 126 条剔除记录](../../../../../Build/sky-production-20260914/round03/blender/source_contracts.json)。对应最终 FBX SHA `de36df8f…abd58f`，Blender SHA `0de153c3…5c233f`。这是 L2 源件、实际几何与离线渲染证据；Unity 性能复核由总体生产另行记录，用户延期的真人游戏实测未完成。

## 2026-09-14 生成结果稳定性、网格精度与云层（COMPAT）

`tools/sky_island_tripo_props.py` 用 `stable_seed` 的 CRC32 固定随机种子，不再用随 Python 进程变化的字符串 `hash` 决定障碍物朝向。`ArtSource/SkyIsland/model_orientations.json` 封存由原始 World 反解的 29 个障碍物和 35 个锚点朝向；缺省项使用确定种子。绘制和 `collision_fit` 共用 `obstacle_yaw`，避免生成外观与碰撞盒分别转向。两次独立 `PYTHONHASHSEED` 进程原先出现 21 个碰撞 / 29 个外观差异，修复后均为 0。最终 146 个盒与 82 个标记逐值恢复基线；最终 FBX 的 284 个导航、地面、碰撞和标记对象也逐项相同。固定朝向文件是生产输入，不是可丢弃的审查缓存。

`tools/sky_island_mesh_hygiene.py` 在 float32 的最终网格精度下折叠相邻重复角点、剔除零面积面。UV 岛不焊接，明确的正反双面花叶保留。初版按 double 检测仍有 443 个最终写入后共线的细花叶三角，float32 检测消除了这些空面；最终 Blender 检查为 0。自定义面角法线的模型要求输入已经清洁，发现删除或折叠会直接拒绝，不静默错配法线。

近、中、远云分别使用独立形态权重与材质，最后按真实顶点限制云顶：-82 / -100 / -215 米。近云以宽云筏为主，远云缩小并降低到天空蓝色带，避免侧视形成高过地标的大白墙。云仍是离线融合后的不透明网格，沿用原来的 GPU 微漂移与关闭投影设置，没有加入实时体积步进或逐帧 CPU 融合。原生 F 砂岩 PNG 经作者材质绑定进入 Unity；FBX 本身的材质名与 metadata 配对，不能声称 MixRGB 的图片节点已嵌入 FBX。

三轮生产与验证已完成，最终作者 FBX 为 `de36df8f…abd58f`，本地出击资源包为 `67081298…b9636`。全资产处置、39 项相关守卫、Unity 11 相机、PlayMode 物理验证与隔离编辑器性能记录由根级 `SKY_ISLAND_PRODUCTION_AUDIT.md` 汇总。当前真人游戏验证由用户明确延期，不能把编辑器帧时间换算成已通过的游戏帧率。
