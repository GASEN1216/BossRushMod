# 程序化低模替换清单

> **本文有两轮。第一轮（2026-09-09）已执行完毕，见下方「第一轮」；要接着做的看这里。**

---

# 第二轮清单（2026-09-10，实机验收之后）

## 执行状态（2026-09-10 15:44 进包并部署，包 `261bc723`）

回来的 17 件里 16 件已接线进包、1 件待拍板；另有 5 件因素材图主体画错，需要重新出模。**尚未实机。**

| 件 | 状态 | 场景实例 | 单件面数 | 合计面数 |
| --- | --- | ---: | ---: | ---: |
| `cliff_chunk_a` / `b` / `c` | ✅ 已进包 | 142 / 177 / 174 | 160 | 78,880 |
| `cliff_shrub_cap` | ✅ 已进包 | 167 | 140 | 23,380 |
| `bush_a` / `bush_b` | ✅ 已进包（`bush_c` 未回，717 丛暂由两件分摊） | 376 / 341 | 220 | 157,740 |
| `rock_b` | ✅ 已进包（`rock_a` 未回，219 块全用它） | 219 | 180 | 39,420 |
| `fern_clump` / `coral_clump` | ✅ 已进包（花园与村落绿篱的花草位；`flower_patch` 未回，暂由两件分摊） | 159 / 189 | 220 / 259 | 83,931 |
| `distant_islet_a` / `b` / `c` | ✅ 已进包 | 4 / 4 / 4 | 1,476 / 1,298 / 1,289 | 16,252 |
| `mushroom_cluster` | ✅ 已进包 | 9 | 899 | 8,091 |
| `glow_crystal` | ✅ 已进包 | 40 | 500 | 20,000 |
| `brass_lamp_post` | ✅ 已进包（灯头仍保留程序化 `Glow` 发光球） | 29 | 600 | 17,400 |
| `waterfall` | ✅ 已进包 | 3 | 3,722 | 11,166 |
| `brass_railing_module` | ⏸ 已导入、**未接线**：环岛护栏现在是木栏，换铜栏杆等于改画风，待 owner 定 | 0 | 800 | 0 |
| `bush_c` · `rock_a` · `flower_patch` · `lavender_clump` · `cliff_vine` | ⏳ 上一版素材图主体画成了建筑，Tripo 出的是民居/水井/拱门，已退回 `face_500/_rejected/`；素材图已重出并逐张审过，等重新建模 | — | 120–260 | — |

未回的 5 件在生成器里自动走回退：灌木和碎石由已有变体分摊，散布花丛与垂藤保持程序化。
所以现在就能进包；模型回来后重跑流水线即可替换，不用改代码。

**实测对比**（上一版包 `fb12e540` → 本版 `261bc723`）：

| 指标 | 上一版 | 本版 | 变化 |
| --- | ---: | ---: | ---: |
| Unity 实测可见三角面（`GEOMETRY_PASS visualTriangles`，不含云） | 1,515,740 | 1,817,552 | **+301,812（+19.9%）** |
| Blender FBX 统计 | 1,604,946 | 1,858,588 | +253,642（+15.8%） |
| 其中 Tripo 模型（Blender 统计） | 468,213（29%） | 955,635（**51%**） | |
| 渲染器 / 材质 / 贴图 | 769 / 96 / 73 | 787 / 112 / 89 | +18 / +16 / +16 |
| 导航 | 4037 顶点 | 4037 顶点 | 未变（`SkyIslandNavigation` 顶点数与索引数均一致） |
| 碰撞盒 / MeshCollider | 161 / 28 | 161 / 28 | 未变 |
| 玩法标记 | Search 20 · POI 13 · EnemySpawn 12 · Lamp 16 | 同左 | 未变 |
| 包体（字节） | 98,794,570 | 125,375,187 | **+26.6 MB（+26.9%）** |

两套三角面统计口径不同，**以 Unity 实测为准**。预算已按实测收紧过一次（未收紧时 Unity 实测 1,966,951，+29.8%）：
岛缘石块 240→160、林冠 200→140、灌木 300→220、碎石 250→180、发光水晶 900→500、铜灯柱 1000→600。

**下一批要盯的预算**：`cliff_vine` 要摆 855 份（岛缘 501 + 悬空花园/崖面/拱门约 354）。
按 120 面算就是约 10 万面，而岛缘那 501 条程序化垂藤现在只占约 2 万面（见下表 B-3）。
这一件回来后全岛至少还要涨 6 万面以上，到时候要么把单件压到 60 面左右，要么减少垂藤条数。

---

第一轮的 54 个 Tripo 件已全部进包并实机可见。**现状实测**（`sky_island_geometry.json`）：

| 指标 | 数值 |
| --- | ---: |
| 可见三角面合计 | **1,604,946** |
| 其中 Tripo 模型 | 468,213（**29%**） |
| 其中仍为程序化 | 1,136,733（**71%**） |
| 渲染器 | 769 |
| 导航 | 4037 顶点 / 4215 三角（上限 **4095**，余量 58） |
| 碰撞盒 | 161（88 障碍 + 73 附加件） |

## A. 已有模型、只差接线 —— **不用你出图**

这几处是第一轮漏接的，Tripo 件都已经在 `ArtSource/SkyIsland/tripo/` 里了：

| 对象 | 数量 | 现状 | 可用的现成件 |
| --- | ---: | --- | --- |
| `B_GardenTree01`–`04` 风铃集花园树 | 4 | `tree()` 程序化，6×10×6 | `tree_blossom` / `tree_green` |
| `D_GreatRootTree` 悬根巨树 | 1 | `tree()`，22×36×22 | `great_tree` |
| `D_RootTree02` 根树 | 1 | `tree()`，14×26×14 | `great_tree`（缩放） |
| 悬根林 `area_landmarks` D 岛 | 5 | `tree(tx,y,tz,2.2)` 硬编码 | `tree_olive` |

合计 **11 棵树**。`landscape_scatter` 的 149 棵散布树第一轮已经换过了——
屏幕上还像程序化的那些，多半是下面 B-4 的**灌木球**，不是树。

## B. 需要新模型 —— 请按这张表出图

数量与面数都是实测。「现状面数」是这一类目前吃掉的三角面，替换后的预算按它来定。

| # | 对象 | 数量 | 现状面数 | 现在长什么样 | 建议件名（变体数） |
| --- | --- | ---: | ---: | --- | --- |
| B-1 | **岛缘石块** ← 你问的「岛周围那些块」 | **493** | 39,440 | `sphere(8段5环, smooth=False)` 的刻面椭球，挂在每岛外轮廓下方 | `cliff_chunk_a/b/c`（3） |
| B-2 | 岛缘林冠 | 167 | 10,688 | `sphere(8段4环)` 扁球，压在石块顶上 | `cliff_shrub_cap`（1–2） |
| B-3 | 岛缘垂藤 | 501 | 20,040 | 3 段 `ribbon` 折线 | `cliff_vine`（2） |
| B-4 | **灌木球** ← 画面里最像程序化的东西 | **≈504** | **249,624** | `sphere(12段6环)` 的三轴扰动椭球，占散布点的 2/3 | `bush_a/b/c`（3） |
| B-5 | **远景悬浮岛** ← 你问的「天上那些块」 | **12** | 7,584 | 每座仅 632 面：`Forest`+`Limestone`+`RockLight`+`Grass`+`Rock`+`RockDeep` 六层薄片 | `distant_islet_a/b/c`（3） |
| B-6 | 地被花草 | 数千 | 163,706 | `Flower` 70,536 / `Lavender` 55,098 / `Coral` 20,198 / `Fern` 11,008 / `LilyWhite` 6,864 | `flower_patch` / `lavender_clump` / `fern_clump`（3） |
| B-7 | 蘑菇 · 水晶 · 发光物 | 数百 | 67,872 | `Chalk` 16,304 / `Glow` 15,780 / `PearlGlow` 14,048 / `CrystalLavender` 10,320 / `StarGlow` 6,860 / `CrystalTeal` 4,560 | `mushroom_cluster` / `glow_crystal`（2–3） |
| B-8 | 铜栏杆与灯柱 | 数百 | 98,155 | `Brass` 56,017 + `BrassLight` 42,138，`beam`/`torus`/`lathe` 拼的 | `brass_railing_module` / `brass_lamp`（2） |
| B-9 | 散落岩石 | 219 | 35,000 | `sphere(8段4环, smooth=False)` | `rock_a/b/c`（3） |
| B-10 | 瀑布 | 3 | 1,248 | 9 排四边形 + 4 个云球 | `waterfall`（1） |

**合计可替换面数 ≈ 693,000（占程序化部分的 61%）。**

优先级建议：**B-4 灌木球 > B-1 岛缘石块 > B-5 远景岛 > B-6 花草 > 其余**。
B-4 一项就占 25 万面，而且它遍布每个岛的边缘，是「粗制滥造」感的最大来源；
B-5 面数极小但**每一帧都在天上占着视野**，性价比最高。

## C. 不能换 —— 换了会坏功能

| 对象 | 现状面数 | 为什么不能换 |
| --- | ---: | --- |
| 岛体壳与地表 `island_shell` / `terrain_and_boundary` | ~60,000 | 它**本身就是可行走面**，提供 27 个 ground MeshCollider，也是 A\* 导航网格的来源 |
| 路径铺装 `make_paths` | ~15,000 | 同为可行走面，参与导航三角化 |
| 桥面与栈道 `bridge_details`（15 座） | ~80,000 | 导航关键路径。**导航 4037/4095，余量只剩 58 顶点**，任何顶点数变化都可能直接超限 |
| 云海 `world_clouds`（含远景雾底） | ~90,000 | 依赖 `SkyIslandCloud.shader` 的漂移、三档空气透视和 `_CloudLayer` 远景雾底；换成普通网格昼夜天色就没了 |
| 生活道具 `Life_*` | ~40,000 | 由 `sky_island_settlement` 摆放，已是独立一套模型流程 |

## D. 出图与接入要求（沿用第一轮的口径）

- **素材图必须均匀柔光、无投影、纯净浅灰背景、单体居中、四分之三视角**——强方向光的阴影会被烘进
  albedo，出模后拆不掉（详见 `docs/制作教程/天空岛_Tripo3D建模接入与画风对齐教程.md`）。
- 色相锁死原版暖琥珀 **H18–40**，草地 **H78–89**；冷色只用在水泥/实验室类材质上。
- 面数按**实例数**倒推，不是按单件观感：主世界烘成单个 FBX、**没有实例化**，
  摆 504 个灌木就是 504 份完整三角面。B-4 单件建议 ≤ 300 面，B-1 ≤ 250 面，B-5 单件可给 1500 面。
- 新件放 `ArtSource/SkyIsland/tripo/<name>.json`，在 `tools/sky_island_tripo_props.py` 里登记后
  由 `MODEL_TEXTURES` 自动建材质，**零 C# 改动**。
- **导航一个顶点都不能加**：所有新件按装饰接入，走 `PlantingSpace.free()` 避让。
- 碰撞由 `COLLISION_POLICY` 决定，与外观解耦；换外观不需要动碰撞。

---

# 第一轮清单（2026-09-09，已执行）

2026-09-09。目标：用 Tripo3D 模型替换观感较差的程序化几何。下面的数量、尺寸、面数全部从
`sky_island_layout.json` 与 `sky_island_geometry.json` 实测统计，不是估计。

当轮场景：**1,355,134 可见三角面 / 753 渲染器 / 导航 4037 顶点（上限 4095）**。

## 一、不能替换（换了会坏功能）

| 对象 | 生成处 | 为什么不能换 |
| --- | --- | --- |
| 岛体壳与地表 | `island_shell` / `terrain_and_boundary` | 它本身就是**可行走面**，提供 27 个 ground MeshCollider，并且是 A\* 导航网格的来源 |
| 路径铺装 | `make_paths` | 同为可行走面，参与导航三角化 |
| 桥面 | `bridge_details` 与桥体 | 导航关键路径。**导航顶点 4037/4095，余量只剩 58**，任何顶点数变化都可能直接超限 |
| 云海 | `world_clouds` | 依赖 `SkyIslandCloud.shader` 的漂移、三档空气透视与 `_CloudLayer` 远景雾底；换成普通网格就没有昼夜天色了 |

## 二、可以替换：碰撞与视觉是解耦的

这是本清单成立的前提。`generate_sky_island.py` 里碰撞来自
`collision_box(obs['id'], obs['center'], obs['size'])`——**读的是 layout 登记的中心与尺寸**；
而 `house()`、`tree()` 之类只负责画外观。因此只要 Tripo 模型按登记 footprint 缩放，
换掉外观不影响碰撞、不影响导航。

### 建议用 Tripo 替换（视觉主体）

| 优先级 | 对象 | 数量 | 现状 | 说明 |
| --- | --- | ---: | --- | --- |
| ★★★ | 程序化树 | **106** | `tree()`：4 个 10×5 段不平滑球 + 圆柱树干，约 550 面 | 画面里最大的低模来源，棱角分明的绿球 |
| ★★★ | 建筑 | **14** | `house()` 盒体 + 瓦顶 | 现在紧挨着 Tripo 民居，反差最刺眼，见下方尺寸表 |
| ★★ | 地标装置 | **20** | 归航钟、观星镜、风标、鸭子雕像、风铃架、纪念碑、亭柱等 | 单个体量大、位置显眼，一次性件适合给高面数 |
| ★★ | 掩体 cover | **18** | 简单盒体 | 战斗掩体，玩家会长时间贴着看 |
| ★ | 刻面石块 | **219** | `sphere(8段4环, 不平滑)`，64 面 | 数量大，建议压到 400 面以内 |

### 建筑尺寸表（Tripo 模型按此比例出图，我会按 footprint 缩放）

| 类型 | 岛 | 占地 (米) | 高 |
| --- | --- | --- | ---: |
| `temple` 镜水寺 | F | 43.0 × 23.0 | 12.0 |
| `dome` 工坊穹顶 | G | 32.0 × 28.0 | 18.0 |
| `barn` 谷仓 | C | 20.0 × 12.0 | 10.0 |
| `workshop_shed` 工坊棚 | G | 20.0 × 14.0 | 8.0 |
| `dock_house` 码头屋 | A | 18.0 × 12.0 | 8.0 |
| `tea_house` 茶铺 | B | 16.0 × 12.0 | 10.0 |
| `mill` 水车坊 | C | 16.0 × 12.0 | 13.0 |
| `house` 民居 ×5 | B | 10–16 × 8–12 | 7–9 |
| `pavilion` 亭 | F | 14.0 × 14.0 | 9.0 |
| `post_house` 邮亭 | S2 | 12.0 × 10.0 | 8.0 |

## 三、不建议用 Tripo 的部分

**774 个小散件（灌木球 327 / 石块 219 / 花球 228）和 3,502 个 Kenney 自然素材。**

它们观感差的原因不是"模型简陋"，而是 **`smooth=False` 加上 8 段 4 环的低分段**——
棱角是着色方式造成的，不是面数不够。改 `sphere()` 的调用参数即可：

- 现在：`sphere(..., 8, 4, False)` = 64 面，硬边刻面
- 改成：`sphere(..., 12, 6, True)` = 144 面，平滑

774 个散件按此改动只增加约 **6.2 万面**，就能消掉绝大部分"刻面感"；
而换成 Tripo 模型要增加约 **26 万面**，还要额外 774 份贴图采样。**性价比差一个数量级。**

Kenney 自然素材（草、花、蘑菇）单个只有几十面且屏幕占比极小，换 Tripo 纯属浪费预算。

## 四、面数账

| 方案 | 净增面数 | 场景总面数 | 相对当前 |
| --- | ---: | ---: | ---: |
| 只换树 + 建筑 | +112,000 | 1,467,000 | +8% |
| 再加地标 + 掩体 | +130,000 | 1,485,000 | +10% |
| 再加石块 | +204,000 | 1,559,000 | +15% |
| 全部换（含灌木花球） | +380,000 | 1,736,000 | **+28%** |
| 散件改平滑（不用 Tripo） | +62,000 | 1,417,000 | +5% |

**建议走「树 + 建筑 + 地标 + 掩体用 Tripo，散件改平滑」**：合计约 +19 万面（+14%），
覆盖了几乎全部视觉冲击，而把 327 个灌木球换 Tripo 那 16 万面省了下来。

## 五、实现要点

替换与新增不同：新增只要 `addmesh` 就行，**替换需要抑制原来的程序化绘制**。
对建筑类是在 `generate_sky_island.py` 的障碍循环里，当该 `obs['id']` 有对应 Tripo 件时
跳过 `house(obs, index)` 改为 stamp Tripo 网格并按 `obs['size']` 缩放；
`collision_box()` 照常先调用，因此碰撞不变。树与散件同理，在各自的散布函数里分流。

按 footprint 缩放与目前的按高度缩放不同，`sky_island_tripo_import.py` 需要增加一个
"按 XZ 占地缩放、高度随比例"的模式，否则 43×23 米的寺庙会被按高度压成错误比例。
